openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: DK Option Engine
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: monitoring
    description: Operational monitoring — background-job health, database connectivity, request-error rate, and audit-log-derived alerting signals (Phase 6, Sprint 74)
  - name: scanner
    description: Daily options scanner
  - name: options
    description: Option chain data
  - name: portfolio
    description: Portfolio positions and Greeks
  - name: trades
    description: Trade management
  - name: backtest
    description: Strategy backtesting
  - name: scoring
    description: Ravish Score AI ranking
  - name: journal
    description: Trade journal
  - name: ai
    description: AI Options Assistant
  - name: settings
    description: User settings
  - name: performance
    description: Performance analytics
  - name: value
    description: Buffett-style value-investing research & education (SIMULATED, advisory only)
  - name: portfolio-construction
    description: Target-allocation portfolio construction for value investing (advisory/education only, never places an order — distinct from the options-income "portfolio" tag)
  - name: trading-journal
    description: Trading journal for Engine 2 (Institutional Trading Engine) positions — mirrors the options "journal" tag's shape, distinct table/records, advisory only, never places an order
  - name: trading-structure
    description: Engine 2 Market Structure analysis (support/resistance zones, trend classification) — SIMULATED-first, advisory only, never places an order
  - name: trading-multi-timeframe
    description: Engine 2 Multi-Timeframe trend confluence analysis — composes Market Structure across multiple timeframes, SIMULATED-first, advisory only, never places an order
  - name: trading-regime
    description: Engine 2 Market Regime analysis — composes trend (Multi-Timeframe), liquidity, and realized-volatility axes into a single regime read, SIMULATED-first, advisory only, never places an order
  - name: trading-probability
    description: Engine 2 Probability analysis — a driftless lognormal probability cone (±1σ/±2σ) and honest availability, composed on Market Regime's own resolved price/volatility, SIMULATED-first, advisory only, never places an order
  - name: trading-positions
    description: Engine 2 trading position ledger (open/closed positions, entry/stop/target) — instrument-agnostic, distinct from the options-income "trades" table, advisory only, never places an order
  - name: trading-risk
    description: Engine 2 portfolio risk analysis (position sizing, stop/target discipline, portfolio risk budget) over the calling user's own trading_positions, composed on Market Regime's own resolved price/volatility for touch-probability context, SIMULATED-first, advisory only, never places an order
  - name: trading-liquidity
    description: Engine 2 Liquidity / Order Flow analysis (volume profile, dollar-volume liquidity score, buy/sell pressure proxy) — a sibling engine to Market Structure, not built on top of it, SIMULATED-first, advisory only, never places an order
  - name: trading-coach
    description: Engine 2 AI Trade Coach — free-form Q&A grounded in Structure/Multi-Timeframe/Liquidity/Regime/Probability, the calling user's own portfolio Risk analysis, and recent Trading Journal reflections, SIMULATED-first, educational only, never places an order
  - name: trading-backtest
    description: Engine 2 Backtesting — a genuine walk-forward price-action backtest replaying SIMULATED candles through a named entry/exit rule set (trend-following, mean-reversion, structure-breakout), producing a real trade log and equity curve, SIMULATED-first, advisory only, never places an order
  - name: trading-trade-plans
    description: Institutional Trade Workspace (Phase 25) — real persistence for a human-authored pre-trade plan (direction, thesis, entry/stop/target, derived position size and risk/reward), advisory only, never places an order
  - name: trading-workspace-notes
    description: Institutional Trade Workspace (Phase 25) — lightweight per-symbol free-text notes, distinct from the deeper structured Trading Journal
  - name: trading-session
    description: Institutional Trade Workspace (Phase 25) — which named trading sessions are currently open plus today's realized session high/low, descriptive only, never predictive
  - name: broker
    description: Read-only Alpaca Paper Trading broker/account verification (account balances, open positions, open orders, connection health) — Paper Trading endpoint only, never places, modifies, or cancels an order

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # ─── Monitoring (Phase 6, Sprint 74) ────────────────────────────────────
  /monitoring/status:
    get:
      operationId: getMonitoringStatus
      tags: [monitoring]
      summary: Operational health status — database, background jobs, request-error rate, and active alerts
      responses:
        "200":
          description: Current monitoring status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoringStatus"

  /ops/market-data-validation:
    get:
      operationId: getMarketDataValidation
      tags: [ops]
      summary: Administrator-only cross-provider live market data validation report
      description: >-
        Consolidates the Options Engine's provider selection, Engine 1's
        per-provider fundamentals status, Engine 2's market data provider,
        and the US market clock/calendar into one report — staleness and
        missing-data are flagged in the context of whether the market is
        actually open right now. Requires the "admin" role.
      responses:
        "200":
          description: Live market data validation report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LiveMarketValidationReport"
        "401":
          description: Not authenticated
        "403":
          description: Authenticated but not an administrator

  # ─── Scanner ───────────────────────────────────────────────────────────────
  /scanner/results:
    get:
      operationId: getScannerResults
      tags: [scanner]
      summary: Get latest scanner results
      parameters:
        - name: strategy
          in: query
          schema:
            type: string
            enum: [iron_condor, iron_fly, calendar_spread, earnings]
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Scanner results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ScannerResult"

  /scanner/run:
    post:
      operationId: runScanner
      tags: [scanner]
      summary: Trigger a new scanner run
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScannerRunInput"
      responses:
        "200":
          description: Scanner started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScannerRunResponse"

  /scanner/top:
    get:
      operationId: getTopOpportunities
      tags: [scanner]
      summary: Get top-ranked trade opportunities across all strategies
      responses:
        "200":
          description: Top opportunities
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopOpportunities"

  /scanner/earnings:
    get:
      operationId: getEarningsScan
      tags: [scanner]
      summary: Scan for near-term earnings with elevated IV and recommended structures
      responses:
        "200":
          description: Earnings plays
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EarningsPlay"

  /events:
    get:
      operationId: getUpcomingEvents
      tags: [scanner]
      summary: Upcoming economic / market event calendar (earnings, FOMC, CPI, jobs, dividends)
      parameters:
        - name: horizonDays
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Upcoming events sorted by date
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EventRiskEvent"

  /market-data/health:
    get:
      operationId: getMarketDataHealth
      tags: [scanner]
      summary: Get options market-data provider status and last-scan health metrics
      responses:
        "200":
          description: Market data health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketDataHealth"

  # ─── Broker (Alpaca Paper Trading, read-only) ──────────────────────────────
  /broker/health:
    get:
      operationId: getBrokerHealth
      tags: [broker]
      summary: Get Alpaca Paper Trading broker connection/account health (read-only)
      description: >-
        Performs a live, authenticated read-only round trip to Alpaca's Paper
        Trading API (GET /v2/account, /v2/positions, /v2/orders) and reports
        connection status, account balances, and open position/order counts.
        Never places, modifies, or cancels an order. Always targets Alpaca's
        Paper Trading endpoint — there is no live-trading equivalent.
      responses:
        "200":
          description: Broker health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerHealth"

  /broker/orders:
    get:
      operationId: getBrokerOrders
      tags: [broker]
      summary: List all Alpaca Paper Trading orders (any status), read-only
      description: >-
        A thin, read-only pass-through to GET /v2/orders?status=all. Never
        places, modifies, or cancels an order. Bounded to Alpaca's own
        default page size (no further pagination this sprint).
      responses:
        "200":
          description: Broker orders
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerOrdersResult"

  /broker/orders/{orderId}:
    get:
      operationId: getBrokerOrder
      tags: [broker]
      summary: Get a single Alpaca Paper Trading order by id, read-only
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Broker order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerOrderResult"

  /broker/positions:
    get:
      operationId: getBrokerPositions
      tags: [broker]
      summary: List all Alpaca Paper Trading positions, read-only
      description: >-
        A thin, read-only pass-through to GET /v2/positions. Distinct from
        GET /broker/reconciliation's own positions array, which is a
        local-vs-broker comparison, not a plain display list — this route
        surfaces Alpaca's own position fields (average entry price, side,
        market value, unrealized P/L) directly. Never modifies or closes a
        position.
      responses:
        "200":
          description: Broker positions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerPositionsResult"

  /broker/reconciliation:
    get:
      operationId: getBrokerReconciliation
      tags: [broker]
      summary: Compare local trade/order records against Alpaca Paper Trading orders and positions, read-only
      description: >-
        Manual, on-demand only — never runs on a schedule or background
        timer. Read-only: identifies discrepancies (missing at broker,
        missing locally, status/quantity/symbol mismatches, open-position
        mismatches) without correcting, cancelling, or closing anything on
        either side.
      responses:
        "200":
          description: Reconciliation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReconciliationResult"

  /broker/reconciliation/reports:
    get:
      operationId: listReconciliationReports
      tags: [broker]
      summary: List the calling user's own persisted reconciliation report history, newest first
      responses:
        "200":
          description: Reconciliation report history
          content:
            application/json:
              schema:
                type: object
                required: [reports]
                properties:
                  reports:
                    type: array
                    items:
                      $ref: "#/components/schemas/ReconciliationReportSummary"
    post:
      operationId: createReconciliationReport
      tags: [broker]
      summary: Run a fresh reconciliation and persist a snapshot of it — explicit trigger only, never on a schedule
      responses:
        "201":
          description: The newly-persisted reconciliation report summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReconciliationReportSummary"

  /broker/reconciliation/reports/{id}:
    get:
      operationId: getReconciliationReport
      tags: [broker]
      summary: Get one persisted reconciliation report's full detail, by id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Reconciliation report detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReconciliationReportDetail"
        "404":
          description: No such report, or it does not belong to the calling user

  # ─── Options Chain ─────────────────────────────────────────────────────────
  /options/chain/{symbol}:
    get:
      operationId: getOptionChain
      tags: [options]
      summary: Get live option chain for a symbol
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Option chain data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptionChain"

  /options/universe:
    get:
      operationId: getUniverse
      tags: [options]
      summary: Get the list of tracked symbols with market data
      responses:
        "200":
          description: Universe symbols
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UniverseSymbol"

  /options/expirations/{symbol}:
    get:
      operationId: getExpirations
      tags: [options]
      summary: Get available expiration dates for a symbol
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Expiration dates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ExpirationDate"

  # ─── Portfolio ──────────────────────────────────────────────────────────────
  /portfolio/positions:
    get:
      operationId: getPortfolioPositions
      tags: [portfolio]
      summary: Get live Alpaca positions
      responses:
        "200":
          description: Portfolio positions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Position"

  /portfolio/greeks:
    get:
      operationId: getPortfolioGreeks
      tags: [portfolio]
      summary: Get aggregate portfolio Greeks
      responses:
        "200":
          description: Portfolio Greeks summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioGreeks"

  /portfolio/summary:
    get:
      operationId: getPortfolioSummary
      tags: [portfolio]
      summary: Get portfolio account summary
      responses:
        "200":
          description: Portfolio summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioSummary"

  /portfolio/risk:
    get:
      operationId: getRiskStatus
      tags: [portfolio]
      summary: Get portfolio risk status and limit compliance
      responses:
        "200":
          description: Risk status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskStatus"

  /portfolio/theta:
    get:
      operationId: getThetaIncome
      tags: [portfolio]
      summary: Get theta income across horizons with symbol/strategy breakdowns
      responses:
        "200":
          description: Theta income
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThetaIncome"

  # ─── Trades ─────────────────────────────────────────────────────────────────
  /trades:
    get:
      operationId: listTrades
      tags: [trades]
      summary: List all trades
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [open, closed, pending]
        - name: strategy
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
      responses:
        "200":
          description: List of trades
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Trade"
    post:
      operationId: createTrade
      tags: [trades]
      summary: Create and execute a trade
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradeInput"
      responses:
        "201":
          description: Trade created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trade"

  /trades/{id}:
    get:
      operationId: getTrade
      tags: [trades]
      summary: Get a single trade
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trade details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trade"
    delete:
      operationId: closeTrade
      tags: [trades]
      summary: Close / exit a trade
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trade closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trade"

  /trades/adjustments:
    get:
      operationId: listTradeAdjustments
      tags: [trades]
      summary: Adjustment recommendations for every open trade
      responses:
        "200":
          description: One recommendation per open position
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradeAdjustment"

  /trades/{id}/adjustment:
    get:
      operationId: getTradeAdjustment
      tags: [trades]
      summary: Adjustment recommendation for a single trade
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Adjustment recommendation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeAdjustment"

  /trades/{id}/monitor:
    get:
      operationId: getTradeMonitor
      tags: [trades]
      summary: Get real-time monitoring data for an open trade
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trade monitoring data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeMonitor"

  # ─── Execution (Phase 5: semi-automatic) ────────────────────────────────────
  /execution/preview:
    post:
      operationId: previewExecution
      tags: [execution]
      summary: Build option order legs, compute metrics, and run pre-trade risk validation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExecutionPreviewInput"
      responses:
        "200":
          description: Execution ticket with order legs, metrics, and validation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExecutionTicket"

  /execution/submit:
    post:
      operationId: submitExecution
      tags: [execution]
      summary: Submit a validated multi-leg order through the broker (semi-auto only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExecutionSubmitInput"
      responses:
        "201":
          description: Order submitted; pending trade and journal entry created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExecutionSubmitResult"

  /execution/adjustment/preview:
    post:
      operationId: previewAdjustmentExecution
      tags: [execution]
      summary: Build a ready-to-submit roll/convert ticket for an open position's recommendation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdjustmentPreviewInput"
      responses:
        "200":
          description: Execution ticket for the recommended roll/convert structure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExecutionTicket"

  /execution/adjustment/submit:
    post:
      operationId: submitAdjustmentExecution
      tags: [execution]
      summary: Close the source position and open the recommended roll/convert structure (semi-auto only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdjustmentSubmitInput"
      responses:
        "201":
          description: Source position closed; rolled/converted trade and journal entry created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdjustmentSubmitResult"

  # ─── Order Preview & Risk Simulator (Paper Trading dry-run) ─────────────────
  /execution/order-preview:
    post:
      operationId: previewOrder
      tags: [execution]
      summary: >-
        Read-only dry-run order preview: never routes to a broker, never
        creates an order, never mutates local state. Always returns 200 —
        an unresolvable/invalid input honestly sets available:false and
        populates inputIssues rather than fabricating a partial ticket.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderPreviewInput"
      responses:
        "200":
          description: Order preview result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderPreviewResult"

  # ─── Position Sizing & Portfolio Impact Calculator (Paper Trading) ──────────
  /execution/position-sizing:
    post:
      operationId: previewPositionSizing
      tags: [execution]
      summary: >-
        Read-only position-sizing and portfolio-impact analysis, extending
        the Order Preview: never routes to a broker, never creates an
        order, never mutates local state. Always returns 200 — an
        unresolvable/invalid input honestly reflects that in the nested
        preview.available field rather than fabricating figures.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PositionSizingInput"
      responses:
        "200":
          description: Position sizing & portfolio impact analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PositionSizingResult"

  # ─── Trade Adjustment & Roll/Convert Preview Simulator (Paper Trading) ──────
  /execution/adjustment/preview-simulator:
    post:
      operationId: previewTradeAdjustment
      tags: [execution]
      summary: >-
        Read-only dry-run adjustment (roll/convert/close & replace)
        preview over an existing open position: never routes to a broker,
        never creates or closes an order, never mutates local state.
        Always returns 200 — a missing position or unavailable intent
        honestly sets available:false rather than fabricating a preview.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradeAdjustmentPreviewInput"
      responses:
        "200":
          description: Trade adjustment preview result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradeAdjustmentPreviewResult"

  # ─── Portfolio Stress Test & Scenario Simulator (Paper Trading) ─────────────
  /execution/stress-test:
    post:
      operationId: runPortfolioStressTest
      tags: [execution]
      summary: >-
        Read-only What-If stress test applying hypothetical underlying
        price, implied-volatility, and time-decay shocks to the user's own
        current open portfolio: never routes to a broker, never creates or
        modifies an order or position, never mutates local state. Always
        returns 200 — an empty portfolio honestly returns zeroed-out
        figures rather than fabricating exposure.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PortfolioStressTestInput"
      responses:
        "200":
          description: Portfolio stress test result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioStressTestResult"

  # ─── Earnings & Event Risk Portfolio Overlay (Paper Trading) ────────────────
  /portfolio/event-risk:
    get:
      operationId: getPortfolioEventRisk
      tags: [execution]
      summary: >-
        Read-only overlay of upcoming earnings/dividend/macro event risk
        across the user's own current open portfolio, reusing
        eventRisk.ts's existing getEventRiskForSymbol() unmodified: never
        routes to a broker, never creates or modifies an order or
        position, never mutates local state. Always returns 200 — an
        empty portfolio honestly returns zeroed-out summary figures
        rather than fabricating exposure.
      responses:
        "200":
          description: Portfolio event risk overlay result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioEventRiskResult"

  # ─── Correlation & Concentration Risk Overlay (Paper Trading) ───────────────
  /portfolio/concentration:
    get:
      operationId: getPortfolioConcentration
      tags: [execution]
      summary: >-
        Read-only concentration, directional-exposure, net-Greeks, and
        correlation-cluster overlay across the user's own current open
        portfolio: never routes to a broker, never creates or modifies
        an order or position, never mutates local state. Always returns
        200 — an empty portfolio honestly returns zeroed-out figures
        rather than fabricating exposure.
      responses:
        "200":
          description: Portfolio correlation & concentration result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioConcentrationResult"

  # ─── Portfolio Risk Dashboard & Health Score (Paper Trading) ────────────────
  /portfolio/dashboard:
    get:
      operationId: getPortfolioDashboard
      tags: [execution]
      summary: >-
        Read-only executive dashboard unifying Position Sizing, the
        Portfolio Stress Test, the Earnings & Event Risk Portfolio
        Overlay, and the Correlation & Concentration Risk Overlay into
        one Portfolio Health Score and executive summary: never routes
        to a broker, never creates or modifies an order or position,
        never mutates local state. Always returns 200 — an empty
        portfolio honestly returns a healthy-by-default score rather
        than fabricating risk.
      responses:
        "200":
          description: Portfolio dashboard result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioDashboardResult"

  # ─── Institutional Intelligence Engine (Phase 8, Sprint 1) ──────────────────
  /intelligence:
    get:
      operationId: getInstitutionalIntelligence
      tags: [execution]
      summary: >-
        Read-only, deterministic Institutional Intelligence result:
        composes the existing Portfolio Dashboard and theta-income
        calculations into an Executive Summary, Observations (with
        Explanations), a Health Overview, a Timeline of new/resolved/
        persistent observations, and Learning Links. This is NOT an LLM
        integration, NOT a chatbot, and NOT a statistical prediction
        engine — every field is either a direct pass-through of an
        already-computed value or a deterministic, disclosed rule
        (a threshold comparison, a template lookup, a set diff). No
        trade recommendation or execution suggestion is ever generated.
        Never routes to a broker, never creates or modifies an order or
        position, never mutates local state beyond an at-most-once-per-
        calendar-day intelligence snapshot row. Always returns 200 — an
        empty portfolio honestly returns a healthy-by-default result
        rather than fabricating observations.
      responses:
        "200":
          description: Institutional Intelligence result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalIntelligenceResult"

  # ─── AI Portfolio Analyst (Phase 8, Sprint 3) ───────────────────────────────
  # Executive portfolio briefing layer. PURE COMPOSITION over the
  # Institutional Intelligence Engine, Portfolio Dashboard, Portfolio
  # Event Risk overlay, and Theta Income — zero new pricing/risk/scoring
  # calculations. Not an LLM, not a chatbot, not predictive AI, not
  # financial advice, not trade recommendations — every sentence is a
  # deterministic, disclosed template gated by an already-computed
  # threshold. Never routes to a broker, never creates or modifies an
  # order or position, never mutates local state beyond the same
  # at-most-once-per-calendar-day intelligence snapshot row /intelligence
  # itself already writes.
  /portfolio-analyst:
    get:
      operationId: getPortfolioAnalyst
      tags: [execution]
      summary: >-
        Read-only, deterministic AI Portfolio Analyst result: the
        executive briefing layer composing the Institutional Intelligence
        Engine, Portfolio Dashboard, Portfolio Event Risk, and Theta
        Income into an Executive Briefing, Portfolio Snapshot, Health/
        Risk/Income/Greeks/Event/Learning Summaries, a Portfolio Timeline
        (Yesterday/Today/This Week), and Institutional Insights. Always
        returns 200 — an empty portfolio honestly returns a
        healthy-by-default result rather than fabricating observations.
      responses:
        "200":
          description: AI Portfolio Analyst result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioAnalystResult"

  # ─── AI Trade Journal (Phase 8, Sprint 4) ───────────────────────────────────
  # A deterministic behavioural analysis and trade review system. PURE
  # COMPOSITION over already-existing modules — not a chatbot, not an AI
  # trading signal engine, not financial advice, not portfolio
  # management. Every score/pattern is deterministic and traceable to an
  # existing calculation or a real, stored trade field. Never routes to a
  # broker, never creates or modifies an order or position, never mutates
  # the trades/journal/settings tables — every function is read-only.
  /trade-journal:
    get:
      operationId: getAITradeJournal
      tags: [execution]
      summary: >-
        Read-only, deterministic AI Trade Journal result: analyses every
        completed Paper Trading trade into a Trade Review (strategy,
        holding period, P/L, Greeks at entry/exit, Event Risk at entry,
        position size, Decision Quality tags), cross-trade Behaviour
        Analysis patterns, a Discipline Score, Learning Recommendations
        (education only, never a trade recommendation), and a
        chronological Journal Timeline. Always returns 200 — a user with
        no closed trades honestly returns empty arrays and a zero
        Discipline Score rather than fabricating history.
      responses:
        "200":
          description: AI Trade Journal result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AITradeJournalResult"

  /trade-journal/{tradeId}:
    get:
      operationId: getTradeReview
      tags: [execution]
      summary: >-
        A single closed trade's own Trade Review — the same object
        returned inside GET /trade-journal's own recentTrades array,
        resolvable directly by trade id. 404s for a trade that doesn't
        exist, isn't the caller's own, or isn't yet closed (a Trade
        Review only exists for a completed trade).
      parameters:
        - name: tradeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trade Review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalTradeReview"
        "404":
          description: Trade review not found

  # ─── Institutional Mentor (Phase 8, Sprint 5) ───────────────────────────────
  # PURE COMPOSITION over the Portfolio Dashboard, Correlation &
  # Concentration overlay, Portfolio Stress Test, and the AI Trade
  # Journal. Not an LLM, not a chatbot, not predictive AI, not financial
  # advice, not portfolio optimisation, not trade recommendations, not
  # execution logic. Never routes to a broker, never creates or modifies
  # an order or position, never mutates the trades/journal/settings
  # tables — every function is read-only, and this route triggers no
  # database write of any kind (unlike AI Portfolio Analyst, it does not
  # call buildInstitutionalIntelligence()).
  /institutional-mentor:
    get:
      operationId: getInstitutionalMentor
      tags: [execution]
      summary: >-
        Read-only, deterministic Institutional Mentor result: teaches the
        user how a professional portfolio manager would evaluate their
        own existing Paper Trading portfolio. Composes a 9-category
        Portfolio Scorecard (Capital Allocation, Risk Management,
        Diversification, Discipline, Income Generation, Position Sizing,
        Greeks Management, Event Preparation, Portfolio Health), a
        Professional Review, a Decision Review, and narrative Capital
        Allocation/Risk/Income/Behaviour reviews, each cross-linked to
        Learning Centre lessons, Strategy Academy pages, Glossary
        entries, and Explain Mode. Always returns 200 — an empty
        portfolio honestly returns a healthy-by-default result rather
        than fabricating observations.
      responses:
        "200":
          description: Institutional Mentor result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalMentorResult"

  # ─── AI Teacher & Learning Centre (Phase 8, Sprint 2) ───────────────────────
  # Educational only. Read-only. Deterministic — never an LLM call, never a
  # trade recommendation. Learning Progress (view/complete) is the ONLY
  # mutation this sprint introduces.
  /learning-centre/glossary:
    get:
      operationId: getGlossary
      tags: [execution]
      summary: >-
        Searchable, filterable glossary of options/portfolio/institutional
        terms. Deterministic, version-controlled content — never LLM-
        generated. Optional ?q= free-text search and ?category= filter.
      responses:
        "200":
          description: Glossary terms
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningGlossaryTerm"

  /learning-centre/paths:
    get:
      operationId: getLearningPaths
      tags: [execution]
      summary: All 8 structured Learning Paths (Foundations, Options Greeks,
        Volatility, Options Strategies, Portfolio, Performance,
        Institutional Thinking, Institutional Investing Engine), each with
        its own topics.
      responses:
        "200":
          description: All learning paths
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningPath"

  /learning-centre/paths/{pathKey}:
    get:
      operationId: getLearningPathByKey
      tags: [execution]
      summary: A single Learning Path by key.
      parameters:
        - name: pathKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: A single learning path
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningPath"
        "404":
          description: Unknown path key

  /learning-centre/strategy-academy:
    get:
      operationId: getStrategyAcademy
      tags: [execution]
      summary: >-
        All 8 Strategy Academy entries. For iron_condor/iron_fly/
        calendar_spread (strategies this platform's own scanner/execution
        engine actually builds) the paper example is a real, live worked
        example. For the other 5, the paper example is honestly disclosed
        as unavailable — never a fabricated live number for a strategy
        this engine doesn't trade.
      responses:
        "200":
          description: All strategy academy entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearningStrategyAcademyEntry"

  /learning-centre/strategy-academy/{strategy}:
    get:
      operationId: getStrategyAcademyEntryByKey
      tags: [execution]
      summary: A single Strategy Academy entry by strategy key.
      parameters:
        - name: strategy
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: A single strategy academy entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningStrategyAcademyEntry"
        "404":
          description: Unknown strategy key

  /learning-centre/portfolio-lesson:
    get:
      operationId: getPortfolioLesson
      tags: [execution]
      summary: >-
        Portfolio Learning Mode: uses the calling user's real, current
        Paper Trading portfolio as an educational example, bundling
        Explain Mode explanations for Portfolio Health, Buying Power,
        Delta, Theta, Concentration, and Event Risk. Never recommends a
        trade.
      responses:
        "200":
          description: Portfolio Learning Mode result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningPortfolioLesson"

  /learning-centre/progress:
    get:
      operationId: getLearningProgress
      tags: [execution]
      summary: >-
        Unified learning progress: lessons viewed/completed, learning
        path completion, and quiz progress for BOTH the Greeks quiz and
        the Value Investing quiz (reusing the same shared aggregation for
        each, never a duplicated calculation).
      responses:
        "200":
          description: Learning progress summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningProgressSummary"

  /learning-centre/progress/view:
    post:
      operationId: recordLearningItemViewed
      tags: [execution]
      summary: Record that the calling user viewed a lesson, glossary term,
        path, or strategy — the only mutation this sprint introduces.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LearningProgressAction"
      responses:
        "200":
          description: Recorded
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success:
                    type: boolean

  /learning-centre/progress/complete:
    post:
      operationId: recordLearningItemCompleted
      tags: [execution]
      summary: Record that the calling user completed a lesson, glossary
        term, path, or strategy.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LearningProgressAction"
      responses:
        "200":
          description: Recorded
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success:
                    type: boolean

  /learning-centre/simulate:
    post:
      operationId: runLearningSimulation
      tags: [execution]
      summary: >-
        Run a deterministic Interactive Education simulation (delta,
        theta, expected_move, payoff, or concentration). Always labeled
        Educational Simulation / Not Market Data / No Trade Recommendation
        in the response itself. Reuses this platform's own real
        Black-Scholes pricing function (bs()) for delta/theta, and the
        standard textbook payoff/HHI formulas for payoff/concentration —
        never randomness, never an LLM.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LearningSimulationInput"
      responses:
        "200":
          description: Simulation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearningSimulationResult"

  # ─── Full-Auto execution (Phase 6) ──────────────────────────────────────────
  /execution/auto/status:
    get:
      operationId: getAutoExecutionStatus
      tags: [execution]
      summary: Current full-auto engine state, guardrails, and today's stats
      responses:
        "200":
          description: Auto-execution status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoExecutionStatus"

  /execution/auto/log:
    get:
      operationId: getAutoExecutionLog
      tags: [execution]
      summary: Recent full-auto decision audit log
      responses:
        "200":
          description: Audit log entries (newest first)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AutoExecutionLogEntry"

  /execution/auto/adjust/log:
    get:
      operationId: getAutoAdjustmentLog
      tags: [execution]
      summary: Recent auto-ADJUSTMENT decision audit log (kind=adjust only)
      parameters:
        - name: decision
          in: query
          required: false
          schema:
            type: string
            enum: [executed, skipped, rejected, blocked]
        - name: symbol
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Adjustment audit log entries (newest first)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AutoExecutionLogEntry"

  /execution/auto/run:
    post:
      operationId: runAutoExecutionCycle
      tags: [execution]
      summary: Run one full-auto cycle now (no-op unless armed); returns decisions
      responses:
        "200":
          description: Cycle result with per-candidate decisions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoCycleResult"

  /execution/auto/adjust/run:
    post:
      operationId: runAutoAdjustmentCycle
      tags: [execution]
      summary: Run one auto-adjustment cycle now (no-op unless armed); de-risk only
      responses:
        "200":
          description: Adjustment cycle result with per-trade decisions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoAdjustCycleResult"

  # ─── Backtest ────────────────────────────────────────────────────────────────
  /backtest/run:
    post:
      operationId: runBacktest
      tags: [backtest]
      summary: Run a strategy backtest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BacktestInput"
      responses:
        "200":
          description: Backtest results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BacktestResult"

  /backtest/results:
    get:
      operationId: listBacktestResults
      tags: [backtest]
      summary: List saved backtest results
      responses:
        "200":
          description: Backtest results list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BacktestResult"

  # ─── Ravish Score ────────────────────────────────────────────────────────────
  /scoring/{symbol}:
    get:
      operationId: getRavishScore
      tags: [scoring]
      summary: Get Ravish Score for a symbol across all strategies
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Ravish Score breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RavishScore"

  /scoring/leaderboard:
    get:
      operationId: getScoringLeaderboard
      tags: [scoring]
      summary: Get top-scored opportunities across all symbols
      responses:
        "200":
          description: Leaderboard
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RavishScore"

  # ─── Journal ─────────────────────────────────────────────────────────────────
  /journal:
    get:
      operationId: listJournalEntries
      tags: [journal]
      summary: List journal entries
      responses:
        "200":
          description: Journal entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/JournalEntry"
    post:
      operationId: createJournalEntry
      tags: [journal]
      summary: Create a journal entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryInput"
      responses:
        "201":
          description: Journal entry created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"

  /journal/{id}:
    get:
      operationId: getJournalEntry
      tags: [journal]
      summary: Get a journal entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
    patch:
      operationId: updateJournalEntry
      tags: [journal]
      summary: Update a journal entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryUpdate"
      responses:
        "200":
          description: Updated journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"

  # ─── AI Assistant ─────────────────────────────────────────────────────────────
  /ai/chat:
    post:
      operationId: aiChat
      tags: [ai]
      summary: Send a message to the AI options assistant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiChatInput"
      responses:
        "200":
          description: AI response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiAnswer"

  /ai/messages:
    get:
      operationId: getAiMessages
      tags: [ai]
      summary: Get chat history
      responses:
        "200":
          description: Chat messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AiMessage"

  # ─── AI Trading Coach ─────────────────────────────────────────────────────────
  /coach/explain-trade:
    post:
      operationId: explainTrade
      tags: [coach]
      summary: Explain a scanner candidate or symbol+strategy (read-only, never executes)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExplainTradeInput"
      responses:
        "200":
          description: Deterministic explanation plus narrative
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExplainTradeResult"

  /coach/teach-greek:
    post:
      operationId: teachGreek
      tags: [coach]
      summary: Tutor one of the four Greeks with a live worked example
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TeachGreekInput"
      responses:
        "200":
          description: Greek lesson plus narrative
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeachGreekResult"

  /coach/learn/delta:
    get:
      operationId: getDeltaMasterclass
      tags: [coach]
      summary: Delta Masterclass content
      responses:
        "200":
          description: Learn content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearnContent"

  /coach/learn/greeks:
    get:
      operationId: getGreeksOverview
      tags: [coach]
      summary: Greeks overview content
      responses:
        "200":
          description: Learn content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LearnContent"

  /coach/quiz:
    post:
      operationId: generateQuiz
      tags: [coach]
      summary: Generate a Greeks/strategy/risk quiz (answers withheld)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuizInput"
      responses:
        "200":
          description: Generated quiz
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratedQuiz"

  /coach/quiz/grade:
    post:
      operationId: gradeQuiz
      tags: [coach]
      summary: Grade quiz answers server-side and record the attempt
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GradeQuizInput"
      responses:
        "200":
          description: Graded result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuizGradeResult"

  /coach/quiz/progress:
    get:
      operationId: getQuizProgress
      tags: [coach]
      summary: Recent quiz attempts, best score per topic, and overall stats
      responses:
        "200":
          description: Quiz progress
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuizProgress"

  /coach/journal-review:
    post:
      operationId: journalReview
      tags: [coach]
      summary: Coach a closed trade into a review and a lesson learned
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalReviewInput"
      responses:
        "200":
          description: Trade review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalReviewResult"

  /coach/lessons:
    get:
      operationId: getCoachLessons
      tags: [coach]
      summary: Recent saved coaching output
      responses:
        "200":
          description: Saved lessons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CoachLesson"

  # ─── Settings ─────────────────────────────────────────────────────────────────
  /settings:
    get:
      operationId: getSettings
      tags: [settings]
      summary: Get user settings
      responses:
        "200":
          description: User settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"
    patch:
      operationId: updateSettings
      tags: [settings]
      summary: Update user settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SettingsUpdate"
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"

  /fundamentals/provider-status:
    get:
      operationId: getFundamentalsProviderStatus
      tags: [settings]
      summary: Live fundamentals provider status (rate limits / outages)
      description: >-
        Most-recent health for each live fundamentals provider (Financial
        Modeling Prep, Alpha Vantage), so operators can see at a glance when a
        provider is rate-limited or unreachable instead of discovering it one
        symbol at a time.
      responses:
        "200":
          description: Per-provider status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FundamentalsProviderStatusResponse"

  # ─── Notifications (Platform, Phase 4 Sprint 56 — Alerts & Notifications)
  # In-app notification center, per the project owner's own kickoff
  # decision. Cross-engine: reuses Engine 1's watchlist target-crossing
  # check (Phase 2 Sprint 27) and Engine 2's Risk Management hard-cap
  # breach detection (Phase 3 Sprint 38/44) as trigger sources — zero new
  # detection logic. Ownership-scoped like every user-authored-resource
  # route since Sprint 7.
  /notifications:
    get:
      operationId: listNotifications
      tags: [notifications]
      summary: List the calling user's notifications, newest first
      responses:
        "200":
          description: Notifications
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlatformNotification"

  /notifications/check:
    post:
      operationId: checkNotifications
      tags: [notifications]
      summary: >-
        Evaluate the calling user's watchlist targets and risk caps on
        demand and persist any newly-triggered alerts (never a fabricated
        alert; a no-op honestly returns an empty array when nothing is
        newly triggered or alerts are disabled)
      responses:
        "200":
          description: Newly created notifications (may be empty)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlatformNotification"

  /notifications/{id}:
    patch:
      operationId: updateNotification
      tags: [notifications]
      summary: Mark a notification read or unread
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlatformNotificationUpdate"
      responses:
        "200":
          description: Updated notification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformNotification"
        "404":
          description: Notification not found

  # Phase 16 — Institutional Monitoring & Alerts Engine. A broader on-demand
  # check than /notifications/check: also runs Opportunity Alerts (a full
  # scan against every saved screen), deliberately excluded from the
  # automatic background tick per lib/monitoringEngine.ts's own disclosed
  # cost-control discipline.
  /monitoring-engine/check:
    post:
      operationId: fullMonitoringCheck
      tags: [notifications]
      summary: >-
        Evaluate every monitoring alert category on demand — watchlist
        targets, risk caps, symbol-level signal changes, portfolio drift/
        concentration, and Opportunity Alerts (saved-screen matches) — and
        persist any newly-triggered alerts (never fabricated; a no-op
        honestly returns an empty array when nothing is newly triggered or
        alerts are disabled)
      responses:
        "200":
          description: Newly created notifications (may be empty)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlatformNotification"

  /monitoring-engine/alert-notes:
    get:
      operationId: getAlertNotes
      tags: [notifications]
      summary: List the calling user's alert notes, newest first (optionally filtered by ?notificationId= or ?symbol=)
      parameters:
        - name: notificationId
          in: query
          required: false
          schema:
            type: integer
        - name: symbol
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Alert notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MonitoringAlertNote"
    post:
      operationId: addAlertNote
      tags: [notifications]
      summary: Add a note on an alert (free text, never AI-generated)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MonitoringAlertNoteCreate"
      responses:
        "200":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoringAlertNote"

  /monitoring-engine/alert-notes/{id}:
    patch:
      operationId: updateAlertNote
      tags: [notifications]
      summary: Update an alert note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MonitoringAlertNoteUpdate"
      responses:
        "200":
          description: Updated note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonitoringAlertNote"
        "404":
          description: Not found
    delete:
      operationId: deleteAlertNote
      tags: [notifications]
      summary: Delete an alert note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  # Phase 10 — Institutional Platform Polish & Control Center. Backs both
  # the Workspace System (named, saved widget layouts) and the Personal
  # Dashboard (pin/hide/reorder/resize widgets on the Institutional Home
  # page) — a workspace IS the currently-active widget config. Zero
  # trading, execution, pricing, portfolio, or risk calculations.
  # Ownership-scoped like /notifications above.
  /workspaces:
    get:
      operationId: listWorkspaces
      tags: [workspaces]
      summary: List the calling user's saved dashboard workspaces (lazily creates a "Default" one if none exist)
      responses:
        "200":
          description: Workspaces
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DashboardWorkspace"
    post:
      operationId: createWorkspace
      tags: [workspaces]
      summary: Save the current or a supplied widget layout as a new named workspace
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DashboardWorkspaceCreate"
      responses:
        "201":
          description: Created workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardWorkspace"
        "409":
          description: A workspace with that name already exists

  /workspaces/active:
    get:
      operationId: getActiveWorkspace
      tags: [workspaces]
      summary: Get the calling user's currently-active workspace (their Personal Dashboard layout)
      responses:
        "200":
          description: Active workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardWorkspace"

  /workspaces/{id}:
    patch:
      operationId: updateWorkspace
      tags: [workspaces]
      summary: Rename a workspace and/or save its widget layout (pin/hide/reorder/resize)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DashboardWorkspaceUpdate"
      responses:
        "200":
          description: Updated workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardWorkspace"
        "404":
          description: Workspace not found
        "409":
          description: A workspace with that name already exists
    delete:
      operationId: deleteWorkspace
      tags: [workspaces]
      summary: Delete a workspace (never the user's only remaining one)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "400":
          description: Cannot delete the only remaining workspace
        "404":
          description: Workspace not found

  /workspaces/{id}/duplicate:
    post:
      operationId: duplicateWorkspace
      tags: [workspaces]
      summary: Duplicate a workspace under a new name
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DashboardWorkspaceDuplicate"
      responses:
        "201":
          description: Duplicated workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardWorkspace"
        "404":
          description: Workspace not found
        "409":
          description: A workspace with that name already exists

  /workspaces/{id}/activate:
    post:
      operationId: activateWorkspace
      tags: [workspaces]
      summary: Switch to this workspace (deactivates every other one for this user)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Activated workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardWorkspace"
        "404":
          description: Workspace not found

  # Cross-Engine Daily Report (Phase 5, Sprint 68). On-demand only: computed
  # when requested, never scheduled/emailed/pushed. Pure composition of
  # already-shipped Engine 1/2/3 outputs — zero new engine calculations.
  # Ownership-scoped like /notifications above.
  /cross-engine-report:
    get:
      operationId: getCrossEngineDailyReport
      tags: [daily-report]
      summary: >-
        On-demand deterministic cross-engine daily report (Engine 1 macro +
        watchlist target crossings, Engine 2 trading risk, Engine 3 options
        income portfolio health) for the calling user — no LLM call
      responses:
        "200":
          description: Cross-engine daily report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CrossEngineDailyReport"

  /cross-engine-report/narrate:
    post:
      operationId: narrateCrossEngineDailyReport
      tags: [daily-report]
      summary: >-
        LLM-narrate the calling user's cross-engine daily report (read-only;
        never executes; SSE variant at .../narrate/stream is not modeled here)
      responses:
        "200":
          description: Narrated cross-engine daily report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NarrateCrossEngineDailyReportResult"

  # ─── Performance ──────────────────────────────────────────────────────────────
  /performance/analytics:
    get:
      operationId: getPerformanceAnalytics
      tags: [performance]
      summary: Get performance analytics and metrics
      parameters:
        - name: period
          in: query
          schema:
            type: string
            enum: [1m, 3m, 6m, 1y, all]
            default: "1y"
      responses:
        "200":
          description: Performance analytics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PerformanceAnalytics"

  /performance/equity-curve:
    get:
      operationId: getEquityCurve
      tags: [performance]
      summary: Get equity curve data points
      parameters:
        - name: period
          in: query
          schema:
            type: string
            enum: [1m, 3m, 6m, 1y, all]
            default: "1y"
      responses:
        "200":
          description: Equity curve
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EquityPoint"

  /performance/breakdown:
    get:
      operationId: getPerformanceBreakdown
      tags: [performance]
      summary: Get performance metrics broken down by a dimension
      parameters:
        - name: dimension
          in: query
          required: true
          schema:
            type: string
            enum: [strategy, ticker, duration, delta, ivrank]
        - name: period
          in: query
          schema:
            type: string
            enum: [1m, 3m, 6m, 1y, all]
            default: "all"
      responses:
        "200":
          description: Breakdown buckets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PerformanceBreakdownBucket"

  /portfolio/health:
    get:
      operationId: getPortfolioHealth
      tags: [portfolio]
      summary: Get the deterministic portfolio health score
      responses:
        "200":
          description: Portfolio health, exposure and risk-concentration scores
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioHealth"

  /briefing:
    get:
      operationId: getMarketBriefing
      tags: [portfolio]
      summary: Get the deterministic daily market briefing with AI narration
      responses:
        "200":
          description: Market briefing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketBriefingResponse"

  /reports:
    get:
      operationId: listDailyReports
      tags: [portfolio]
      summary: List persisted daily reports (newest first)
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 30
      responses:
        "200":
          description: Daily report history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DailyReportSummary"
    post:
      operationId: generateDailyReport
      tags: [portfolio]
      summary: Generate and persist a new daily report
      responses:
        "200":
          description: The generated report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyReport"
    delete:
      operationId: clearDailyReports
      tags: [portfolio]
      summary: Delete all persisted daily reports
      responses:
        "200":
          description: Reports cleared (returns the deleted rows so they can be restored)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClearReportsResult"

  /reports/restore:
    post:
      operationId: restoreDailyReports
      tags: [portfolio]
      summary: Restore previously cleared daily reports
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreReportsRequest"
      responses:
        "200":
          description: Reports restored
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RestoreReportsResult"

  /reports/compare-narration:
    post:
      operationId: narrateReportComparison
      tags: [portfolio]
      summary: AI-narrated plain-English summary of what changed between two reports
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompareReportsRequest"
      responses:
        "200":
          description: Narrated comparison (deltas remain deterministic on the client)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompareReportsResponse"
        "400":
          description: Invalid request
        "404":
          description: One or both reports not found

  /reports/{id}:
    get:
      operationId: getDailyReport
      tags: [portfolio]
      summary: Get a single persisted daily report
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Daily report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyReport"
    delete:
      operationId: deleteDailyReport
      tags: [portfolio]
      summary: Delete a persisted daily report
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Report deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteReportResult"

  # ─── Value Investing (Buffett-style research & education) ──────────────────────
  /stock-analyst/value-universe:
    get:
      operationId: getValueUniverse
      tags: [value]
      summary: Headline value ratings for all supported symbols (SIMULATED)
      parameters:
        - name: forceRefresh
          in: query
          required: false
          schema: { type: boolean }
          description: Bypass the short-lived live fundamentals cache and pull fresh data
      responses:
        "200":
          description: Value summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ValueSummary"

  # Phase 4, Sprint 55 — Macro/Regime Side-by-Side View. Engine 1's own
  # global, date-seeded macro/interest-rate regime proxy (lib/investingMacro.ts,
  # Phase 2 Sprint 26) previously had no dedicated route — only consumed
  # internally by the Tom Nash Engine. Global, not symbol-scoped: no path
  # parameter.
  /stock-analyst/macro:
    get:
      operationId: getMacroContext
      tags: [value]
      summary: Global, date-seeded macro/interest-rate regime context (SIMULATED, not symbol-specific)
      responses:
        "200":
          description: Macro context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValueMacroContext"

  /stock-analyst/value-research:
    post:
      operationId: generateValueResearch
      tags: [value]
      summary: Generate a value-investing research report with AI thesis (read-only; never executes)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValueResearchInput"
      responses:
        "200":
          description: Research report plus narrated thesis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValueResearchResult"
        "404":
          description: Unknown symbol

  /stock-analyst/value-research/ask:
    post:
      operationId: askValueResearch
      tags: [value]
      summary: Ask a free-form question about a researched symbol, grounded in the full assembled report (read-only; never executes; SSE variant at /value-research/ask/stream is not modeled here)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AskValueResearchInput"
      responses:
        "200":
          description: Grounded answer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AskValueResearchResult"
        "404":
          description: Unknown symbol

  # Phase 4, Sprint 61 — AI Investment Committee LLM-Narrated Synthesis.
  # Deliberately a separate, on-demand route (not folded into
  # synthesizeInvestmentCommittee()'s own eager output on /value/:symbol):
  # narration is an LLM call, and the eager report path stays deterministic-
  # only and fast, the same discipline every on-demand module since Sprint 19
  # follows.
  /stock-analyst/investment-committee/narrate:
    post:
      operationId: narrateInvestmentCommittee
      tags: [value]
      summary: LLM-narrate why the AI Investment Committee reached its consolidated verdict, grounded in the votes already computed on the report (read-only; never executes; SSE variant at /investment-committee/narrate/stream is not modeled here)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NarrateInvestmentCommitteeInput"
      responses:
        "200":
          description: Narrated synthesis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NarrateInvestmentCommitteeResult"
        "404":
          description: Unknown symbol

  /stock-analyst/value-history:
    get:
      operationId: getValueHistory
      tags: [value]
      summary: List persisted value-research runs
      responses:
        "200":
          description: History rows
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ValueHistoryRow"

  /stock-analyst/value-watchlist:
    get:
      operationId: getValueWatchlist
      tags: [value]
      summary: List value-investing watchlist items
      parameters:
        - name: checkTargets
          in: query
          required: false
          description: When "true", resolves a fresh price per row and computes priceTargetCrossed/marginOfSafetyTargetCrossed (opt-in — a heavier, on-demand check, never automatic).
          schema:
            type: string
            enum: ["true", "false"]
      responses:
        "200":
          description: Watchlist
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ValueWatchlistItem"
    post:
      operationId: addValueWatchlist
      tags: [value]
      summary: Add a symbol to the value watchlist (advisory only; never places an order)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValueWatchlistCreate"
      responses:
        "200":
          description: Created item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValueWatchlistItem"

  /stock-analyst/value-watchlist/{id}:
    patch:
      operationId: updateValueWatchlist
      tags: [value]
      summary: Update a watchlist item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValueWatchlistUpdate"
      responses:
        "200":
          description: Updated item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValueWatchlistItem"
        "404":
          description: Not found
    delete:
      operationId: deleteValueWatchlist
      tags: [value]
      summary: Remove a watchlist item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  /stock-analyst/research-notes/{symbol}:
    get:
      operationId: getResearchNotes
      tags: [value]
      summary: List the calling user's research notes for a symbol
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Research notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ResearchNoteItem"

  /stock-analyst/research-notes:
    get:
      operationId: getAllResearchNotes
      tags: [value]
      summary: List all of the calling user's research notes, across every symbol, newest first
      responses:
        "200":
          description: Research notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ResearchNoteItem"
    post:
      operationId: addResearchNote
      tags: [value]
      summary: Add a research note for a symbol (free text, never AI-generated)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResearchNoteCreate"
      responses:
        "200":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResearchNoteItem"

  /stock-analyst/research-notes/{id}:
    patch:
      operationId: updateResearchNote
      tags: [value]
      summary: Update a research note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResearchNoteUpdate"
      responses:
        "200":
          description: Updated note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResearchNoteItem"
        "404":
          description: Not found
    delete:
      operationId: deleteResearchNote
      tags: [value]
      summary: Delete a research note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  # ─── Institutional Investment Decision Engine (Phase 14) ──────────────────
  /stock-analyst/decision/{symbol}:
    get:
      operationId: getInstitutionalDecision
      tags: [value]
      summary: Deterministic Buy/Accumulate/Hold/Reduce/Sell/Avoid recommendation, checklist, and evidence for a symbol — pure composition over already-computed Business Quality/Competitive Advantage/Management Quality/Capital Allocation/Financial Strength/Valuation/Margin of Safety/Investment Committee/Tom Nash outputs. Optional ?portfolioId= supplies Portfolio Fit/Risk/Diversification context (undocumented query param — combining it with the path param in the formal spec triggers Orval's known duplicate-GetXParams-export collision, first disclosed at Sprint 40 for /trading/structure/:symbol's own ?interval=/?lookback=; the override is fully functional server-side, just outside the typed contract).
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Institutional decision analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalDecisionAnalysis"
        "404":
          description: Unknown symbol

  /stock-analyst/decision/{symbol}/snapshots:
    get:
      operationId: getDecisionSnapshots
      tags: [value]
      summary: List the calling user's saved decision snapshots for a symbol, newest first
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Decision snapshots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DecisionSnapshot"
    post:
      operationId: saveDecisionSnapshot
      tags: [value]
      summary: Save the current decision analysis for a symbol as a point-in-time snapshot (explicit save only, never automatic)
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Saved snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionSnapshot"
        "404":
          description: Unknown symbol

  /stock-analyst/decision/{symbol}/notes:
    get:
      operationId: getDecisionNotes
      tags: [value]
      summary: List the calling user's decision notes for a symbol
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Decision notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DecisionNote"
    post:
      operationId: addDecisionNote
      tags: [value]
      summary: Add a decision note for a symbol (free text, never AI-generated)
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecisionNoteCreate"
      responses:
        "200":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionNote"

  /stock-analyst/decision/notes/{id}:
    patch:
      operationId: updateDecisionNote
      tags: [value]
      summary: Update a decision note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecisionNoteUpdate"
      responses:
        "200":
          description: Updated note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecisionNote"
        "404":
          description: Not found
    delete:
      operationId: deleteDecisionNote
      tags: [value]
      summary: Delete a decision note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  # ─── Institutional Investment Committee Workbench (Phase 19) ─────────────
  /stock-analyst/decision/snapshots/recent:
    get:
      operationId: getRecentDecisionSnapshots
      tags: [value]
      summary: List the calling user's most recent saved decision snapshots across all symbols (newest first, capped at 20) — powers the Investment Committee Workbench's Committee Dashboard / Active Reviews. Reuses the same investing_decision_snapshots table as GET /decision/{symbol}/snapshots, symbol filter removed.
      responses:
        "200":
          description: Recent decision snapshots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DecisionSnapshot"

  /stock-analyst/investment-memo/{symbol}:
    get:
      operationId: getInvestmentMemo
      tags: [value]
      summary: Deterministic Investment Memo for a symbol — assembled entirely from already-computed Business Quality/Competitive Advantage/Financial Strength/Valuation/Margin of Safety/Decision Engine/Investment Committee outputs plus the user's own Research Notes and Monitoring alerts. No LLM, no new scoring, no price prediction. Optional ?portfolioId= (undocumented query param, same Orval-collision precedent as GET /decision/{symbol}) supplies Portfolio Impact context.
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Investment memo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentMemo"
        "404":
          description: Unknown symbol

  /opportunity-discovery/scan:
    post:
      operationId: scanOpportunities
      tags: [value]
      summary: Run a deterministic opportunity scan across the union of the Investing Engine's own known-symbol universes, apply the screener filters, rank by the Decision Engine's own synthesis score, and bucket into the 10 named opportunity categories — pure orchestration, zero new scoring.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpportunityScanRequest"
      responses:
        "200":
          description: Scan result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityScanResult"

  /opportunity-discovery/compare:
    get:
      operationId: compareOpportunitiesRoute
      tags: [value]
      summary: Compare a small, user-selected set of symbols side by side (?symbols=A,B,C) — highlights which symbol has the best already-computed value per dimension, no new scoring.
      parameters:
        - name: symbols
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Comparison result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunityComparisonResult"

  /opportunity-discovery/saved-screens:
    get:
      operationId: getSavedScreens
      tags: [value]
      summary: List the calling user's saved Screener filter sets, newest first
      responses:
        "200":
          description: Saved screens
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OpportunitySavedScreen"
    post:
      operationId: createSavedScreen
      tags: [value]
      summary: Save a named Screener filter set (criteria only — never persists scan results)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpportunitySavedScreenCreate"
      responses:
        "200":
          description: Created saved screen
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunitySavedScreen"

  /opportunity-discovery/saved-screens/{id}:
    patch:
      operationId: updateSavedScreen
      tags: [value]
      summary: Update a saved screen's name and/or filters
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpportunitySavedScreenUpdate"
      responses:
        "200":
          description: Updated saved screen
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpportunitySavedScreen"
        "404":
          description: Not found
    delete:
      operationId: deleteSavedScreen
      tags: [value]
      summary: Delete a saved screen
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  /stock-analyst/investment-thesis/{symbol}:
    get:
      operationId: getInvestmentThesis
      tags: [value]
      summary: Deterministic, template-based Investment Thesis for a symbol (no LLM, no price prediction)
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Investment thesis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentThesis"
        "404":
          description: Unknown symbol

  /stock-analyst/value-school:
    get:
      operationId: getValueLessons
      tags: [value]
      summary: Value Investing School lessons
      responses:
        "200":
          description: Lessons
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValueLessonsResponse"

  /stock-analyst/value-quiz:
    post:
      operationId: generateValueQuiz
      tags: [value]
      summary: Generate a value-investing quiz (answers withheld)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValueQuizInput"
      responses:
        "200":
          description: Generated quiz
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratedValueQuiz"

  /stock-analyst/value-quiz/grade:
    post:
      operationId: gradeValueQuiz
      tags: [value]
      summary: Grade value-quiz answers server-side and record the attempt
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GradeValueQuizInput"
      responses:
        "200":
          description: Graded result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValueQuizGradeResult"

  /stock-analyst/value/{symbol}:
    get:
      operationId: getValueReport
      tags: [value]
      summary: Full value-investing research report for a symbol (SIMULATED)
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Research report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValueResearchReport"
        "404":
          description: Unknown symbol

  /stock-analyst/financial-statements/{symbol}:
    get:
      operationId: getFinancialStatements
      tags: [value]
      summary: Full multi-year Income Statement / Balance Sheet / Cash Flow Statement for a symbol, fetched on demand
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Financial statements
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialStatements"
        "404":
          description: Unknown symbol

  /stock-analyst/industry-comparison/{symbol}:
    get:
      operationId: getIndustryComparison
      tags: [value]
      summary: Compares a company against a deterministic sector peer group, fetched on demand
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Industry comparison
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IndustryComparisonResult"
        "404":
          description: Unknown symbol

  # Phase 4, Sprint 60 — both routes below also accept an optional
  # ?documentType= query override (defaulting to "10-K" when omitted, so
  # every pre-Sprint-60 caller is unaffected), now also honoring "10-Q".
  # Deliberately left undocumented here rather than added as a formal query
  # parameter: documenting a path parameter and a query parameter together
  # on the same operation is a known Orval zod+split-types codegen
  # collision, first disclosed at Sprint 40 (GetTradingStructureParams) and
  # hit again at Sprint 45 — the same established workaround applies.
  /stock-analyst/filings/{symbol}:
    get:
      operationId: getFilingAnalysis
      tags: [value]
      summary: Fetches and extracts a company's most recent 10-K or 10-Q filing, fetched on demand
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Filing analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FilingAnalysis"
        "404":
          description: Unknown symbol

  /stock-analyst/management-quality/{symbol}:
    get:
      operationId: getManagementQualityAnalysis
      tags: [value]
      summary: Deterministic management-quality scoring reusing the Document Intelligence Engine and existing analyzers, fetched on demand
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Management quality analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagementQualityAnalysis"
        "404":
          description: Unknown symbol

  /stock-analyst/earnings/{symbol}:
    get:
      operationId: getEarningsIntelligence
      tags: [value]
      summary: Quarterly earnings actual-vs-estimate history and deterministic trend analysis, fetched on demand
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Earnings intelligence analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EarningsIntelligenceAnalysis"
        "404":
          description: Unknown symbol

  /portfolio-construction/portfolios:
    get:
      operationId: getPortfolios
      tags: [portfolio-construction]
      summary: List the caller's target-allocation portfolios
      responses:
        "200":
          description: Portfolios
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConstructionPortfolioSummary"
    post:
      operationId: createPortfolio
      tags: [portfolio-construction]
      summary: Create a new target-allocation portfolio
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConstructionPortfolioCreate"
      responses:
        "200":
          description: Created portfolio
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioSummary"

  /portfolio-construction/portfolios/{id}:
    get:
      operationId: getPortfolio
      tags: [portfolio-construction]
      summary: Get one portfolio's holdings and computed allocation (resolves a live/simulated price per holding)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Portfolio detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioDetail"
        "404":
          description: Not found
    patch:
      operationId: updatePortfolio
      tags: [portfolio-construction]
      summary: Rename or update a portfolio's description
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConstructionPortfolioUpdate"
      responses:
        "200":
          description: Updated portfolio
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioSummary"
        "404":
          description: Not found
    delete:
      operationId: deletePortfolio
      tags: [portfolio-construction]
      summary: Delete a portfolio (cascades its own holdings)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  /portfolio-construction/portfolios/{id}/holdings:
    post:
      operationId: addHolding
      tags: [portfolio-construction]
      summary: Add a holding to a portfolio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConstructionHoldingCreate"
      responses:
        "200":
          description: Created holding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionHolding"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/holdings/{holdingId}:
    patch:
      operationId: updateHolding
      tags: [portfolio-construction]
      summary: Update a holding's target weight, shares, or notes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: holdingId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConstructionHoldingUpdate"
      responses:
        "200":
          description: Updated holding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionHolding"
        "404":
          description: Not found
    delete:
      operationId: deleteHolding
      tags: [portfolio-construction]
      summary: Remove a holding from a portfolio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: holdingId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  /portfolio-construction/portfolios/{id}/risk:
    get:
      operationId: getPortfolioRisk
      tags: [portfolio-construction]
      summary: Compute a portfolio's risk analysis (concentration, sector exposure, beta) fresh, on demand
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Risk analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioRiskAnalysis"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/risk/snapshots:
    get:
      operationId: getPortfolioRiskSnapshots
      tags: [portfolio-construction]
      summary: List previously saved risk snapshots for a portfolio, newest first
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Snapshot history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConstructionRiskSnapshot"
        "404":
          description: Portfolio not found
    post:
      operationId: saveRiskSnapshot
      tags: [portfolio-construction]
      summary: Compute the portfolio's risk analysis fresh and save it as a snapshot
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Saved snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionRiskSnapshot"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/intelligence:
    get:
      operationId: getPortfolioIntelligence
      tags: [portfolio-construction]
      summary: Compute the full Institutional Portfolio Manager analysis (quality, capital allocation, allocation breakdowns, extended risk, performance, income) on demand
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Portfolio intelligence analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioIntelligence"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/watchlist-comparison:
    get:
      operationId: getPortfolioWatchlistComparison
      tags: [portfolio-construction]
      summary: Compare this portfolio's holdings against the caller's Value Watchlist
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Watchlist vs. portfolio comparison
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionWatchlistComparison"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/snapshots:
    get:
      operationId: getPortfolioSnapshots
      tags: [portfolio-construction]
      summary: List previously saved composite (quality/risk/diversification) snapshots for a portfolio, newest first
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Snapshot history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConstructionPortfolioSnapshot"
        "404":
          description: Portfolio not found
    post:
      operationId: savePortfolioSnapshot
      tags: [portfolio-construction]
      summary: Compute the portfolio intelligence analysis fresh and save it as a composite snapshot
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Saved snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioSnapshot"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/notes:
    get:
      operationId: getPortfolioNotes
      tags: [portfolio-construction]
      summary: List free-text notes for a portfolio, newest first
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConstructionPortfolioNote"
        "404":
          description: Portfolio not found
    post:
      operationId: addPortfolioNote
      tags: [portfolio-construction]
      summary: Add a free-text note to a portfolio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConstructionPortfolioNoteCreate"
      responses:
        "200":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioNote"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/notes/{noteId}:
    patch:
      operationId: updatePortfolioNote
      tags: [portfolio-construction]
      summary: Update a portfolio note's text
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: noteId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConstructionPortfolioNoteUpdate"
      responses:
        "200":
          description: Updated note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConstructionPortfolioNote"
        "404":
          description: Not found
    delete:
      operationId: deletePortfolioNote
      tags: [portfolio-construction]
      summary: Delete a portfolio note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: noteId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteResult"

  /portfolio-construction/portfolios/{id}/optimisation:
    get:
      operationId: getPortfolioOptimisation
      tags: [portfolio-construction]
      summary: Compute Portfolio Optimisation (health, concentration, diversification, position quality ranking, upgrade/trim/exit candidates, capital allocation suggestions, replacement opportunities, cash deployment suggestions) on demand, reusing Portfolio Intelligence, the Decision Engine, and Opportunity Discovery
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Portfolio optimisation analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioOptimisationAnalysis"
        "404":
          description: Portfolio not found

  /portfolio-construction/portfolios/{id}/optimisation/reviews:
    get:
      operationId: getOptimisationReviews
      tags: [portfolio-construction]
      summary: List saved optimisation reviews for a portfolio, newest first
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Saved reviews
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OptimisationReview"
        "404":
          description: Portfolio not found
    post:
      operationId: addOptimisationReview
      tags: [portfolio-construction]
      summary: Save an optimisation review (upgrade/trim/exit/replace/note) for a symbol within a portfolio, with its evidence snapshot
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OptimisationReviewCreate"
      responses:
        "200":
          description: Saved review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptimisationReview"
        "404":
          description: Portfolio not found

  # ─── Institutional AI Coach (Phase 21) ──────────────────────────────────────
  /stock-analyst/coach/{coach}/{symbol}:
    get:
      operationId: getCoachExplanation
      tags: [value]
      summary: Deterministic Institutional AI Coach explanation for a symbol — one of 8 coaches (investment, portfolio, decision, valuation, risk, research, monitoring, committee), each explaining an already-computed engine output (Decision Engine, the 4 valuation models + Consolidated Margin of Safety, Business/Investment Quality + Competitive Advantage, Monitoring alerts, the Investment Committee). No LLM, no new scoring, never a new recommendation — pure orchestration and static educational copy over lib/investingCoach.ts. Optional ?portfolioId= (undocumented query param, same Orval-collision precedent as GET /decision/{symbol}) supplies Portfolio Coach context.
      parameters:
        - name: coach
          in: path
          required: true
          schema:
            type: string
            enum: [investment, portfolio, decision, valuation, risk, research, monitoring, committee]
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Coach explanation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoachExplanation"
        "400":
          description: Unknown coach type
        "404":
          description: Unknown symbol

  # ─── Trading Journal (Engine 2) ─────────────────────────────────────────────
  /trading/journal:
    get:
      operationId: listTradingJournalEntries
      tags: [trading-journal]
      summary: List trading journal entries, newest first
      responses:
        "200":
          description: Trading journal entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradingJournalEntry"
    post:
      operationId: createTradingJournalEntry
      tags: [trading-journal]
      summary: Create a trading journal entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingJournalEntryInput"
      responses:
        "201":
          description: Trading journal entry created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingJournalEntry"

  /trading/journal/{id}:
    get:
      operationId: getTradingJournalEntry
      tags: [trading-journal]
      summary: Get a trading journal entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trading journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingJournalEntry"
        "404":
          description: Trading journal entry not found
    patch:
      operationId: updateTradingJournalEntry
      tags: [trading-journal]
      summary: Update a trading journal entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingJournalEntryUpdate"
      responses:
        "200":
          description: Updated trading journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingJournalEntry"
        "404":
          description: Trading journal entry not found
    delete:
      operationId: deleteTradingJournalEntry
      tags: [trading-journal]
      summary: Delete a trading journal entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Trading journal entry deleted
        "404":
          description: Trading journal entry not found

  # ─── Market Structure (Engine 2, Phase 3 Sprint 40) ─────────────────────────
  # Deliberately documents the path parameter only. The route also accepts
  # optional ?interval=&lookback= query overrides (defaulting to "1D"/90),
  # validated and handled entirely server-side — kept out of this formal
  # parameter list because Orval's zod+split-types generator produces a
  # genuine duplicate-export collision (GetTradingStructureParams) the one
  # time this spec combines a path param with a query param, confirmed via
  # direct testing to be a toolchain limitation, not a spec error. This
  # mirrors the established precedent (Sprint 30's /value-research/stream)
  # of keeping a route's full surface outside strict OpenAPI modeling when
  # the generator can't cleanly represent it.
  /trading/structure/{symbol}:
    get:
      operationId: getTradingStructure
      tags: [trading-structure]
      summary: Market structure analysis (support/resistance zones, trend classification) for a symbol — SIMULATED-first, honestly labelled dataSource. Accepts optional ?interval= and ?lookback= query overrides, handled server-side only (not part of this typed contract).
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Market structure analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingStructureAnalysis"
        "404":
          description: Unknown or invalid symbol

  # ─── Structure Shift Timeline (Engine 2, Phase 26 — Institutional Market
  # Structure Workbench) ────────────────────────────────────────────────────
  # Deliberately documents the path parameter only, matching Sprint 40's own
  # /trading/structure/{symbol} precedent — the route also accepts optional
  # ?interval=&?lookback= query overrides, handled entirely server-side
  # (not part of this typed contract, same disclosed Orval collision reason).
  /trading/structure-timeline/{symbol}:
    get:
      operationId: getTradingStructureTimeline
      tags: [trading-structure]
      summary: A deterministic timeline of structure shift events (new higher high/low, new lower high/low, trend change, range entry/exit, support/resistance test) for a symbol — reuses the existing Market Structure scorer repeatedly over an expanding candle window, zero new scoring. Accepts optional ?interval= and ?lookback= query overrides, handled server-side only.
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Structure shift timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingStructureShiftTimeline"
        "404":
          description: Unknown or invalid symbol

  # ─── Multi-Timeframe (Engine 2, Phase 3 Sprint 41) ──────────────────────────
  # Deliberately documents the path parameter only, matching Sprint 40's own
  # precedent for /trading/structure/{symbol} — the route also accepts an
  # optional ?timeframes= override (Phase 26 — Institutional Market Structure
  # Workbench, a comma-separated subset of the 5 real timeframes), handled
  # entirely server-side, so the path+query Orval collision Sprint 40
  # disclosed doesn't reproduce here in the formal spec, but the same
  # documented-surface discipline is kept for consistency.
  /trading/multi-timeframe/{symbol}:
    get:
      operationId: getTradingMultiTimeframe
      tags: [trading-multi-timeframe]
      summary: Multi-timeframe trend confluence analysis for a symbol — composes the Market Structure scorer across multiple timeframes (15m/1h/1D by default; Phase 26 added an optional ?timeframes= override for a caller-chosen subset of the 5 real timeframes, handled server-side only), SIMULATED-first, honestly labelled dataSource
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Multi-timeframe confluence analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingMultiTimeframeAnalysis"
        "404":
          description: Unknown or invalid symbol

  # ─── Market Regime (Engine 2, Phase 3 Sprint 42) ────────────────────────────
  /trading/regime/{symbol}:
    get:
      operationId: getTradingRegime
      tags: [trading-regime]
      summary: Market regime analysis for a symbol — composes trend (Multi-Timeframe), liquidity, and realized-volatility axes into a single regime read, SIMULATED-first, honestly labelled dataSource
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Market regime analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingRegimeAnalysis"
        "404":
          description: Unknown or invalid symbol

  # ─── Probability (Engine 2, Phase 3 Sprint 43) ──────────────────────────────
  /trading/probability/{symbol}:
    get:
      operationId: getTradingProbability
      tags: [trading-probability]
      summary: Probability analysis for a symbol — a driftless lognormal probability cone (±1σ/±2σ) at multiple day horizons, composed on Market Regime's own resolved price/volatility, SIMULATED-first, honestly labelled dataSource
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Probability analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingProbabilityAnalysis"
        "404":
          description: Unknown or invalid symbol

  # ─── Trading Positions (Engine 2, Phase 3 Sprint 44) ────────────────────────
  /trading/positions:
    get:
      operationId: listTradingPositions
      tags: [trading-positions]
      summary: List the calling user's trading positions, newest first
      responses:
        "200":
          description: Trading positions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradingPosition"
    post:
      operationId: createTradingPosition
      tags: [trading-positions]
      summary: Create a trading position
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingPositionInput"
      responses:
        "201":
          description: Trading position created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingPosition"

  /trading/positions/{id}:
    get:
      operationId: getTradingPosition
      tags: [trading-positions]
      summary: Get a trading position
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trading position
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingPosition"
        "404":
          description: Trading position not found
    patch:
      operationId: updateTradingPosition
      tags: [trading-positions]
      summary: Update a trading position
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingPositionUpdate"
      responses:
        "200":
          description: Updated trading position
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingPosition"
        "404":
          description: Trading position not found
    delete:
      operationId: deleteTradingPosition
      tags: [trading-positions]
      summary: Delete a trading position
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Trading position deleted
        "404":
          description: Trading position not found

  # ─── Institutional Trade Workspace (Engine 2, Phase 25) ─────────────────────
  # Real persistence for the TradePlan domain concept (in-memory only since
  # Phase 24) — the Trade Workspace's own Trade Plan Panel and its "Save
  # Workspace" workflow step are the real UI consumer that triggers this.
  /trading/trade-plans:
    get:
      operationId: listTradingTradePlans
      tags: [trading-trade-plans]
      summary: List the calling user's trade plans, newest first
      responses:
        "200":
          description: Trade plans
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradingTradePlan"
    post:
      operationId: createTradingTradePlan
      tags: [trading-trade-plans]
      summary: Create a trade plan (status always starts at draft)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingTradePlanInput"
      responses:
        "201":
          description: Trade plan created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingTradePlan"

  /trading/trade-plans/{symbol}:
    get:
      operationId: listTradingTradePlansForSymbol
      tags: [trading-trade-plans]
      summary: List the calling user's trade plans for one symbol, newest first
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Trade plans for the symbol
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradingTradePlan"

  /trading/trade-plans/{id}:
    patch:
      operationId: updateTradingTradePlan
      tags: [trading-trade-plans]
      summary: Update a trade plan's thesis and/or status (status changes are validated against the allowed draft/active/closed/cancelled transition rules)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingTradePlanUpdate"
      responses:
        "200":
          description: Updated trade plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingTradePlan"
        "400":
          description: Invalid status transition
        "404":
          description: Trade plan not found
    delete:
      operationId: deleteTradingTradePlan
      tags: [trading-trade-plans]
      summary: Delete a trade plan
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Trade plan deleted
        "404":
          description: Trade plan not found

  /trading/workspace-notes:
    get:
      operationId: listTradingWorkspaceNotes
      tags: [trading-workspace-notes]
      summary: List the calling user's workspace notes, newest first
      responses:
        "200":
          description: Workspace notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradingWorkspaceNote"
    post:
      operationId: createTradingWorkspaceNote
      tags: [trading-workspace-notes]
      summary: Create a workspace note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingWorkspaceNoteInput"
      responses:
        "201":
          description: Workspace note created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingWorkspaceNote"

  /trading/workspace-notes/{symbol}:
    get:
      operationId: listTradingWorkspaceNotesForSymbol
      tags: [trading-workspace-notes]
      summary: List the calling user's workspace notes for one symbol, newest first
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Workspace notes for the symbol
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradingWorkspaceNote"

  /trading/workspace-notes/{id}:
    patch:
      operationId: updateTradingWorkspaceNote
      tags: [trading-workspace-notes]
      summary: Update a workspace note's text
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingWorkspaceNoteUpdate"
      responses:
        "200":
          description: Updated workspace note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingWorkspaceNote"
        "404":
          description: Workspace note not found
    delete:
      operationId: deleteTradingWorkspaceNote
      tags: [trading-workspace-notes]
      summary: Delete a workspace note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Workspace note deleted
        "404":
          description: Workspace note not found

  # Deliberately documents the path parameter only, mirroring /trading/structure/{symbol}'s
  # own established precedent — the route also accepts an optional ?asOf= override,
  # validated and handled entirely server-side, kept out of this formal contract.
  /trading/session/{symbol}:
    get:
      operationId: getTradingSession
      tags: [trading-session]
      summary: Which named trading sessions (Sydney/Tokyo/London/New York) are currently open, plus today's realized session high/low — descriptive only, never predictive
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Session data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingSessionData"
        "404":
          description: Unknown or invalid symbol

  # ─── Risk Management (Engine 2, Phase 3 Sprint 44) ──────────────────────────
  /trading/risk:
    get:
      operationId: getTradingRisk
      tags: [trading-risk]
      summary: Portfolio risk analysis over the calling user's own open trading positions — position sizing, stop/target discipline, and portfolio risk budget, SIMULATED-first, honestly reports insufficient data rather than fabricating a default account value or position
      responses:
        "200":
          description: Trading risk analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingRiskAnalysis"

  # Deliberately documents the path parameter only. The route also accepts
  # optional ?interval=&lookback= query overrides (defaulting to "1D"/90),
  # validated and handled entirely server-side — kept out of this formal
  # parameter list for the same reason routes/tradingStructure.ts's own
  # Sprint 40 path is: Orval's zod+split-types generator produces a genuine
  # duplicate-export collision when a path param and a query param are
  # documented together on the same operation.
  /trading/liquidity/{symbol}:
    get:
      operationId: getTradingLiquidity
      tags: [trading-liquidity]
      summary: Liquidity / Order Flow analysis for a symbol — volume profile, dollar-volume liquidity score, and a buy/sell pressure proxy derived from the candle series' own up/down closes, SIMULATED-first, honestly labelled dataSource
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Liquidity analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingLiquidityAnalysis"
        "404":
          description: Unknown or invalid symbol
        "400":
          description: Invalid interval or lookback query override

  # ─── AI Trade Coach (Engine 2, Phase 3 Sprint 47) ───────────────────────────
  # SSE variant at /trading/coach/ask/stream is not modeled here, matching
  # /stock-analyst/value-research/ask's own precedent — orval only models
  # single-shot JSON responses.
  /trading/coach/ask:
    post:
      operationId: askTradingCoach
      tags: [trading-coach]
      summary: Ask a free-form question about a symbol, grounded in Structure/Multi-Timeframe/Liquidity/Regime/Probability, the calling user's own portfolio Risk analysis, and recent Trading Journal reflections (read-only; never executes; SSE variant at /trading/coach/ask/stream is not modeled here)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingCoachAskInput"
      responses:
        "200":
          description: Grounded answer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingCoachAskResult"
        "404":
          description: Unknown or invalid symbol

  # ─── Backtesting (Engine 2, Phase 3 Sprint 49) ──────────────────────────────
  /trading/backtest/run:
    post:
      operationId: runTradingBacktest
      tags: [trading-backtest]
      summary: Run a genuine walk-forward price-action backtest for a symbol against a named strategy, persisting the result to the calling user's own history (read-only over historical candles; never places an order)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TradingRunBacktestInput"
      responses:
        "200":
          description: Backtest result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TradingBacktestResult"
        "404":
          description: Unknown or invalid symbol

  /trading/backtest/results:
    get:
      operationId: listTradingBacktestResults
      tags: [trading-backtest]
      summary: List the calling user's own persisted backtest results, newest first
      responses:
        "200":
          description: Backtest results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TradingBacktestResult"

  # ─── Options Engine-Native Backtesting (Phase 4, Sprint 58 — Route + UI,
  # conditional on Sprint 57's own Core output proving valuable). A genuine
  # bar-by-bar walk-forward simulation, distinct from the legacy
  # /backtest/run's fabricated-statistics generator (unmodified, left in
  # place as a parallel legacy path) — reuses Engine 2's MarketDataProvider
  # for the underlying spot price path and optionsMath.ts's own
  # getSnapshot() for IV, composed in lib/optionsBacktest.ts.
  /options-backtest/run:
    post:
      operationId: runOptionsBacktest
      tags: [options-backtest]
      summary: Run a genuine walk-forward options-strategy backtest for a symbol, persisting the result to the calling user's own history (read-only over historical candles and deterministic options pricing; never places an order)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OptionsRunBacktestInput"
      responses:
        "200":
          description: Backtest result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OptionsBacktestResult"
        "404":
          description: Unknown or invalid symbol

  /options-backtest/results:
    get:
      operationId: listOptionsBacktestResults
      tags: [options-backtest]
      summary: List the calling user's own persisted options backtest results, newest first
      responses:
        "200":
          description: Backtest results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OptionsBacktestResult"

  # ─── Institutional Reporting & Client Presentation Engine (Phase 22) —
  # pure composition layer over already-computed engines (ValueResearchReport,
  # InstitutionalDecisionAnalysis, InvestmentMemo, CoachExplanation,
  # PortfolioIntelligenceAnalysis, PortfolioOptimisationAnalysis,
  # OpportunityScanResult, platform_notifications, WatchlistTargetCheck,
  # CrossEngineDailyReport, LearningProgressSummary). No new valuation model,
  # scoring system, or investment recommendation — see
  # lib/institutionalReporting.ts's own header comment. ──────────────────────
  /reporting/types:
    get:
      operationId: getReportTypes
      tags: [reporting]
      summary: The 9 available Institutional Report types, their labels, descriptions, and whether each requires a symbol/portfolio
      responses:
        "200":
          description: Report type metadata
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReportTypeMeta"

  /reporting/investment-committee/{symbol}:
    get:
      operationId: getInvestmentCommitteeReport
      tags: [reporting]
      summary: Investment Committee Report — the Investment Committee's consolidated verdict, the Decision Engine's synthesis, evidence, and portfolio impact for a single security. Pure composition, zero new analysis. Optional ?portfolioId= (undocumented query param, same Orval-collision precedent as GET /decision/{symbol}) supplies portfolio context.
      parameters:
        - name: symbol
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Investment Committee Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"
        "404":
          description: Unknown symbol

  /reporting/company-research/{symbol}:
    get:
      operationId: getCompanyResearchReport
      tags: [reporting]
      summary: Single Company Research Report — the full institutional research picture for one security (Business Quality, Financial Strength, Valuation, Margin of Safety, Decision Engine, Investment Committee, Portfolio Impact, Evidence, Monitoring, Research Notes, the full Investment Memo, and optionally AI Coach explanations). Optional ?portfolioId=/?includeCoach= (undocumented query params, same Orval-collision precedent).
      parameters:
        - name: symbol
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Single Company Research Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"
        "404":
          description: Unknown symbol

  /reporting/portfolio-review/{portfolioId}:
    get:
      operationId: getPortfolioReviewReport
      tags: [reporting]
      summary: Portfolio Review Report — Portfolio Optimisation's own health, diversification, position-quality ranking, and upgrade/trim/exit candidates for a selected portfolio. Pure composition, zero new analysis.
      parameters:
        - name: portfolioId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Portfolio Review Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"
        "404":
          description: Portfolio not found

  /reporting/portfolio-health/{portfolioId}:
    get:
      operationId: getPortfolioHealthReport
      tags: [reporting]
      summary: Portfolio Health Report — Portfolio Intelligence's own quality/capital-allocation/diversification scores, allocation mix, risk, income, and performance for a selected portfolio. Pure composition, zero new analysis.
      parameters:
        - name: portfolioId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Portfolio Health Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"
        "404":
          description: Portfolio not found

  /reporting/watchlist:
    get:
      operationId: getWatchlistReport
      tags: [reporting]
      summary: Watchlist Report — every Watchlist item's own price/margin-of-safety target status, reused directly from computeWatchlistTargets().
      responses:
        "200":
          description: Watchlist Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"

  /reporting/opportunity-discovery:
    get:
      operationId: getOpportunityDiscoveryReport
      tags: [reporting]
      summary: Opportunity Discovery Report — the Opportunity Discovery scan's own buckets, reused directly.
      responses:
        "200":
          description: Opportunity Discovery Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"

  /reporting/monitoring-summary:
    get:
      operationId: getMonitoringSummaryReport
      tags: [reporting]
      summary: Monitoring Summary Report — the user's own recorded monitoring alerts across every symbol.
      responses:
        "200":
          description: Monitoring Summary Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"

  /reporting/ai-coach-summary:
    get:
      operationId: getAiCoachLearningSummaryReport
      tags: [reporting]
      summary: AI Coach Learning Summary — the Learning Centre's own progress tracker (lessons, glossary, strategies, coach explanations viewed, path completion, quiz performance).
      responses:
        "200":
          description: AI Coach Learning Summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"

  /reporting/executive-summary:
    get:
      operationId: getExecutiveSummaryReport
      tags: [reporting]
      summary: Executive Summary — the Cross-Engine Daily Report's own one-pager, reused directly.
      responses:
        "200":
          description: Executive Summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstitutionalReport"

  /reporting/reports:
    post:
      operationId: saveInstitutionalReport
      tags: [reporting]
      summary: Regenerate the given report type server-side and persist it to the calling user's own history (never trusts a client-supplied report body)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveInstitutionalReportInput"
      responses:
        "200":
          description: Saved report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedInstitutionalReport"
        "404":
          description: Could not generate this report
    get:
      operationId: listInstitutionalReports
      tags: [reporting]
      summary: List the calling user's own persisted Institutional Reports, newest first
      responses:
        "200":
          description: Report list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InstitutionalReportListItem"

  /reporting/reports/{id}:
    get:
      operationId: getSavedInstitutionalReport
      tags: [reporting]
      summary: Fetch one persisted Institutional Report by id (ownership-scoped)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Saved report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedInstitutionalReport"
        "404":
          description: Report not found
    delete:
      operationId: deleteInstitutionalReport
      tags: [reporting]
      summary: Delete one persisted Institutional Report by id (ownership-scoped)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success: { type: boolean }
        "404":
          description: Report not found

components:
  schemas:
    # ─── Value Investing (Buffett-style; all SIMULATED, advisory only) ────────────
    ValueFactorScore:
      type: object
      required: [label, score, detail]
      properties:
        label: { type: string }
        score: { type: number }
        detail: { type: string }

    ValueBusinessQuality:
      type: object
      required: [score, rating, factors, summary]
      properties:
        score: { type: number }
        rating:
          type: string
          enum: [Wonderful, Good, Average, Weak]
        factors:
          type: array
          items: { $ref: "#/components/schemas/ValueFactorScore" }
        summary: { type: string }

    ValueMoatSource:
      type: object
      required: [source, strength]
      properties:
        source: { type: string }
        strength: { type: number }

    ValueMoatAnalysis:
      type: object
      required: [rating, score, durabilityYears, sources, summary]
      properties:
        rating:
          type: string
          enum: [Wide, Medium, Narrow, None]
        score: { type: number }
        durabilityYears: { type: number }
        sources:
          type: array
          items: { $ref: "#/components/schemas/ValueMoatSource" }
        summary: { type: string }

    ValueCompetitiveDimensionScore:
      type: object
      required: [dimension, score, weight, detail]
      properties:
        dimension: { type: string }
        score: { type: number, nullable: true }
        weight: { type: number }
        detail: { type: string }
        reason: { type: string }

    ValueCompetitiveAdvantageAnalysis:
      type: object
      required: [score, dimensions, classification, strengths, weaknesses, confidenceLevel, confidenceExplanation, summary]
      properties:
        score: { type: number, nullable: true }
        dimensions:
          type: array
          items: { $ref: "#/components/schemas/ValueCompetitiveDimensionScore" }
        classification:
          type: string
          enum: [Wide, Medium, Narrow, None]
        strengths:
          type: array
          items: { type: string }
        weaknesses:
          type: array
          items: { type: string }
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation: { type: string }
        summary: { type: string }

    ValueFinancialStrength:
      type: object
      required: [rating, score, metrics, flags, summary]
      properties:
        rating:
          type: string
          enum: [Strong, Acceptable, Weak, Risky]
        score: { type: number }
        metrics:
          type: array
          items: { $ref: "#/components/schemas/ValueFactorScore" }
        flags:
          type: array
          items: { type: string }
        summary: { type: string }

    ValueFairValueMethod:
      type: object
      required: [method, fairValue, detail]
      properties:
        method: { type: string }
        fairValue: { type: number }
        detail: { type: string }

    ValueValuation:
      type: object
      required: [available, dataSource, price, summary]
      properties:
        available: { type: boolean }
        dataSource: { type: string }
        price: { type: number }
        summary: { type: string }
        fairValue: { type: number }
        fairValueLow: { type: number }
        fairValueHigh: { type: number }
        methods:
          type: array
          items: { $ref: "#/components/schemas/ValueFairValueMethod" }
        marginOfSafety: { type: number }
        marginOfSafetyLabel:
          type: string
          enum: [High, Medium, Low, None]
        rating:
          type: string
          enum: [Cheap, Fair, Expensive, Very Expensive]
        reason: { type: string }

    ValueGrahamValuation:
      type: object
      required: [available, price, summary]
      properties:
        available: { type: boolean }
        price: { type: number }
        summary: { type: string }
        grahamNumber: { type: ["number", "null"] }
        growthFormulaValue: { type: ["number", "null"] }
        fairValue: { type: number }
        methods:
          type: array
          items: { $ref: "#/components/schemas/ValueFairValueMethod" }
        marginOfSafety: { type: number }
        marginOfSafetyLabel:
          type: string
          enum: [High, Medium, Low, None]
        rating:
          type: string
          enum: [Cheap, Fair, Expensive, Very Expensive]
        reason: { type: string }

    ValueDcfValuation:
      type: object
      required: [available, price, discountRate, terminalGrowthRate, summary]
      properties:
        available: { type: boolean }
        price: { type: number }
        discountRate: { type: number }
        terminalGrowthRate: { type: number }
        summary: { type: string }
        projectionYears: { type: number }
        projectedFreeCashFlows:
          type: array
          items: { type: number }
        terminalValue: { type: number }
        fairValue: { type: number }
        methods:
          type: array
          items: { $ref: "#/components/schemas/ValueFairValueMethod" }
        marginOfSafety: { type: number }
        marginOfSafetyLabel:
          type: string
          enum: [High, Medium, Low, None]
        rating:
          type: string
          enum: [Cheap, Fair, Expensive, Very Expensive]
        confidenceLabel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation: { type: string }
        reason: { type: string }

    ValueBuffettValuation:
      type: object
      required: [available, price, requiredReturn, summary]
      properties:
        available: { type: boolean }
        price: { type: number }
        requiredReturn: { type: number }
        summary: { type: string }
        ownerEarnings: { type: number }
        fairValue: { type: number }
        methods:
          type: array
          items: { $ref: "#/components/schemas/ValueFairValueMethod" }
        marginOfSafety: { type: number }
        marginOfSafetyLabel:
          type: string
          enum: [High, Medium, Low, None]
        rating:
          type: string
          enum: [Cheap, Fair, Expensive, Very Expensive]
        reason: { type: string }

    ValueQualityMetricScore:
      type: object
      required: [metric, availability, weight, detail]
      properties:
        metric: { type: string }
        availability:
          type: string
          enum: [available, unavailable]
        score: { type: ["number", "null"] }
        weight: { type: number }
        detail: { type: string }
        reason: { type: string }

    ValueInvestmentQuality:
      type: object
      required: [metrics, strengths, weaknesses, confidenceLevel, confidenceExplanation, summary]
      properties:
        score: { type: ["number", "null"] }
        metrics:
          type: array
          items: { $ref: "#/components/schemas/ValueQualityMetricScore" }
        strengths:
          type: array
          items: { type: string }
        weaknesses:
          type: array
          items: { type: string }
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation: { type: string }
        summary: { type: string }

    ValueModelFairValue:
      type: object
      required: [model, fairValue]
      properties:
        model: { type: string }
        fairValue: { type: number }

    ValueConsolidatedMarginOfSafety:
      type: object
      required: [price, modelsConsidered, modelsAvailable, fairValues, agreement, summary]
      properties:
        price: { type: number }
        modelsConsidered: { type: number }
        modelsAvailable: { type: number }
        fairValues:
          type: array
          items: { $ref: "#/components/schemas/ValueModelFairValue" }
        minFairValue: { type: ["number", "null"] }
        maxFairValue: { type: ["number", "null"] }
        averageFairValue: { type: ["number", "null"] }
        averageMarginOfSafety: { type: ["number", "null"] }
        agreement:
          type: string
          enum: [unanimous, majority, split, insufficient-data]
        summary: { type: string }

    ValueTomNashPillarScore:
      type: object
      required: [label, detail]
      properties:
        label: { type: string }
        score: { type: ["number", "null"] }
        detail: { type: string }

    ValueSectorMacroContext:
      type: object
      required: [sector, industry, macroRegime, macroRegimeLabel, detail]
      properties:
        sector: { type: string, nullable: true }
        industry: { type: string, nullable: true }
        macroRegime:
          type: string
          enum: [rising_rates, falling_rates, stable_rates]
        macroRegimeLabel: { type: string }
        detail: { type: string }

    ValueRateSensitivityAnalysis:
      type: object
      required: [durationScore, classification, sensitivityLabel, detail]
      properties:
        durationScore: { type: number }
        classification:
          type: string
          enum: ["Long-Duration Growth", "Value / Short-Duration", Blend]
        sensitivityLabel: { type: string }
        detail: { type: string }

    ValueAiTechCycleAnalysis:
      type: object
      required: [score, label, detail]
      properties:
        score: { type: number }
        label:
          type: string
          enum: [High, Moderate, Low]
        detail: { type: string }

    ValueTomNashAnalysis:
      type: object
      required: [businessQuality, growth, capitalAllocation, financialStrength, valuation, sectorMacro, rateSensitivity, aiTechCycle, dataCompleteness, convictionScore, verdict, rationale, summary]
      properties:
        businessQuality: { $ref: "#/components/schemas/ValueTomNashPillarScore" }
        growth: { $ref: "#/components/schemas/ValueTomNashPillarScore" }
        capitalAllocation: { $ref: "#/components/schemas/ValueTomNashPillarScore" }
        financialStrength: { $ref: "#/components/schemas/ValueTomNashPillarScore" }
        valuation: { $ref: "#/components/schemas/ValueTomNashPillarScore" }
        sectorMacro: { $ref: "#/components/schemas/ValueSectorMacroContext" }
        rateSensitivity: { $ref: "#/components/schemas/ValueRateSensitivityAnalysis" }
        aiTechCycle: { $ref: "#/components/schemas/ValueAiTechCycleAnalysis" }
        dataCompleteness: { type: number }
        convictionScore: { type: number }
        verdict:
          type: string
          enum: [Buy, Hold, Wait]
        rationale:
          type: array
          items: { type: string }
        summary: { type: string }

    ValueCommitteeVote:
      type: object
      required: [analyst, verdict, confidence, rationale]
      properties:
        analyst:
          type: string
          enum: [Graham, Buffett, Tom Nash]
        verdict:
          type: string
          enum: [Buy, Hold, Wait]
        confidence: { type: number }
        rationale: { type: string }

    ValueInvestmentCommittee:
      type: object
      required: [votes, consolidatedVerdict, confidenceScore, agreement, reasoning, summary]
      properties:
        votes:
          type: array
          items: { $ref: "#/components/schemas/ValueCommitteeVote" }
        consolidatedVerdict:
          type: string
          enum: [Buy, Hold, Wait]
        confidenceScore: { type: number }
        agreement:
          type: string
          enum: [unanimous, majority, split, insufficient-data]
        reasoning:
          type: array
          items: { type: string }
        summary: { type: string }

    ValueRatioMetric:
      type: object
      required: [label, available, displayValue]
      properties:
        label: { type: string }
        value: { type: ["number", "null"] }
        displayValue: { type: string }
        available: { type: boolean }
        reason: { type: string }

    ValueRatioTrend:
      type: object
      required: [label, history]
      properties:
        label: { type: string }
        history:
          type: array
          items: { type: number }

    ValueFinancialRatios:
      type: object
      required: [valuation, profitability, liquidityAndLeverage, trends, summary]
      properties:
        valuation:
          type: array
          items: { $ref: "#/components/schemas/ValueRatioMetric" }
        profitability:
          type: array
          items: { $ref: "#/components/schemas/ValueRatioMetric" }
        liquidityAndLeverage:
          type: array
          items: { $ref: "#/components/schemas/ValueRatioMetric" }
        trends:
          type: array
          items: { $ref: "#/components/schemas/ValueRatioTrend" }
        summary: { type: string }

    ValueDecision:
      type: object
      required: [verdict, conviction, rationale, summary]
      properties:
        verdict:
          type: string
          enum: [LONG-TERM BUY, BUY ONLY ON PULLBACK, WATCHLIST, HOLD, TRIM, AVOID]
        conviction: { type: number }
        rationale:
          type: array
          items: { type: string }
        summary: { type: string }

    ValueStockVsOptions:
      type: object
      required: [verdict, stockCase, optionsCase, summary]
      properties:
        verdict:
          type: string
          enum: [Long-term stock, Options income, Both, Watchlist only, Avoid]
        ivRank: { type: ["number", "null"] }
        stockCase: { type: string }
        optionsCase: { type: string }
        summary: { type: string }

    ValueKeyMetric:
      type: object
      required: [label, value]
      properties:
        label: { type: string }
        value: { type: string }

    ValueRiskFlag:
      type: object
      required: [severity, text]
      properties:
        severity:
          type: string
          enum: [low, medium, high]
        text: { type: string }

    ValueReportSection:
      type: object
      required: [id, title, body]
      properties:
        id: { type: string }
        title: { type: string }
        body: { type: string }
        bullets:
          type: array
          items: { type: string }

    ValueFundamentalsFallback:
      type: object
      required: [attemptedProvider, reason, message]
      properties:
        attemptedProvider: { type: string }
        reason:
          type: string
          enum: [rate_limit, error, no_data]
        message: { type: string }

    ValueResearchReport:
      type: object
      required:
        [symbol, name, kind, asOf, fetchedAt, dataSource, simulated, price, sector, industry, businessQuality, investmentQuality, moat, competitiveAdvantage, financialStrength, financialRatios, valuation, grahamValuation, dcfValuation, buffettValuation, consolidatedMarginOfSafety, tomNash, investmentCommittee, decision, stockVsOptions, keyMetrics, risks, sections, disclaimer]
      properties:
        symbol: { type: string }
        name: { type: string }
        kind:
          type: string
          enum: [stock, etf]
        asOf: { type: string }
        fetchedAt: { type: string }
        dataSource: { type: string }
        simulated: { type: boolean }
        fallback: { $ref: "#/components/schemas/ValueFundamentalsFallback" }
        price: { type: number }
        sector: { type: string, nullable: true }
        industry: { type: string, nullable: true }
        businessQuality: { $ref: "#/components/schemas/ValueBusinessQuality" }
        investmentQuality: { $ref: "#/components/schemas/ValueInvestmentQuality" }
        moat: { $ref: "#/components/schemas/ValueMoatAnalysis" }
        competitiveAdvantage: { $ref: "#/components/schemas/ValueCompetitiveAdvantageAnalysis" }
        financialStrength: { $ref: "#/components/schemas/ValueFinancialStrength" }
        financialRatios: { $ref: "#/components/schemas/ValueFinancialRatios" }
        valuation: { $ref: "#/components/schemas/ValueValuation" }
        grahamValuation: { $ref: "#/components/schemas/ValueGrahamValuation" }
        dcfValuation: { $ref: "#/components/schemas/ValueDcfValuation" }
        buffettValuation: { $ref: "#/components/schemas/ValueBuffettValuation" }
        consolidatedMarginOfSafety: { $ref: "#/components/schemas/ValueConsolidatedMarginOfSafety" }
        tomNash: { $ref: "#/components/schemas/ValueTomNashAnalysis" }
        investmentCommittee: { $ref: "#/components/schemas/ValueInvestmentCommittee" }
        decision: { $ref: "#/components/schemas/ValueDecision" }
        stockVsOptions: { $ref: "#/components/schemas/ValueStockVsOptions" }
        keyMetrics:
          type: array
          items: { $ref: "#/components/schemas/ValueKeyMetric" }
        risks:
          type: array
          items: { $ref: "#/components/schemas/ValueRiskFlag" }
        sections:
          type: array
          items: { $ref: "#/components/schemas/ValueReportSection" }
        disclaimer: { type: string }

    IncomeStatementYear:
      type: object
      required: [year, revenue, costOfRevenue, grossProfit, operatingExpenses, operatingIncome, netIncome]
      properties:
        year: { type: string }
        revenue: { type: number }
        costOfRevenue: { type: number }
        grossProfit: { type: number }
        operatingExpenses: { type: number }
        operatingIncome: { type: number }
        netIncome: { type: number }

    BalanceSheetYear:
      type: object
      required: [year, totalAssets, totalLiabilities, totalEquity, currentAssets, currentLiabilities, inventory, cash]
      properties:
        year: { type: string }
        totalAssets: { type: number }
        totalLiabilities: { type: number }
        totalEquity: { type: number }
        currentAssets: { type: number }
        currentLiabilities: { type: number }
        inventory: { type: number }
        cash: { type: number }

    CashFlowYear:
      type: object
      required: [year, operatingCashFlow, capitalExpenditures, freeCashFlow, investingCashFlow, financingCashFlow]
      properties:
        year: { type: string }
        operatingCashFlow: { type: number }
        capitalExpenditures: { type: number }
        freeCashFlow: { type: number }
        investingCashFlow: { type: number }
        financingCashFlow: { type: number }

    FinancialStatements:
      type: object
      required: [symbol, name, dataSource, fetchedAt, incomeStatement, balanceSheet, cashFlow]
      properties:
        symbol: { type: string }
        name: { type: string }
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        fetchedAt: { type: string }
        incomeStatement:
          type: array
          items: { $ref: "#/components/schemas/IncomeStatementYear" }
        balanceSheet:
          type: array
          items: { $ref: "#/components/schemas/BalanceSheetYear" }
        cashFlow:
          type: array
          items: { $ref: "#/components/schemas/CashFlowYear" }

    QuarterlyEarningsRecord:
      type: object
      required: [fiscalQuarter, reportDate, epsActual, epsEstimate, epsSurprisePct, revenueActual, revenueEstimate, revenueSurprisePct]
      properties:
        fiscalQuarter: { type: string }
        reportDate: { type: string, nullable: true }
        epsActual: { type: number, nullable: true }
        epsEstimate: { type: number, nullable: true }
        epsSurprisePct: { type: number, nullable: true }
        revenueActual: { type: number, nullable: true }
        revenueEstimate: { type: number, nullable: true }
        revenueSurprisePct: { type: number, nullable: true }

    EarningsGrowthTrend:
      type: object
      required: [epsYoyGrowthPct, revenueYoyGrowthPct, direction, detail]
      properties:
        epsYoyGrowthPct: { type: number, nullable: true }
        revenueYoyGrowthPct: { type: number, nullable: true }
        direction:
          type: string
          enum: [accelerating, decelerating, stable, insufficient-data]
        detail: { type: string }

    EarningsSurpriseStreak:
      type: object
      required: [direction, count]
      properties:
        direction:
          type: string
          enum: [beat, miss, meet]
        count: { type: integer }

    EarningsIntelligenceAnalysis:
      type: object
      required: [symbol, dataSource, quarters, epsBeatRate, revenueBeatRate, epsSurpriseStreak, earningsGrowthTrend, consistencyScore, confidenceLevel, confidenceExplanation, summary]
      properties:
        symbol: { type: string }
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        quarters:
          type: array
          items: { $ref: "#/components/schemas/QuarterlyEarningsRecord" }
        epsBeatRate: { type: number, nullable: true }
        revenueBeatRate: { type: number, nullable: true }
        epsSurpriseStreak:
          allOf:
            - $ref: "#/components/schemas/EarningsSurpriseStreak"
          nullable: true
        earningsGrowthTrend:
          $ref: "#/components/schemas/EarningsGrowthTrend"
        consistencyScore: { type: number, nullable: true }
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation: { type: string }
        summary: { type: string }

    ComparisonMetric:
      type: object
      required: [key, label, direction, companyValue, peerMedian, peerCount, rank, totalRanked, percentile, available]
      properties:
        key: { type: string }
        label: { type: string }
        direction:
          type: string
          enum: [higher-better, lower-better, context-only]
        companyValue: { type: number, nullable: true }
        peerMedian: { type: number, nullable: true }
        peerCount: { type: integer }
        rank: { type: integer, nullable: true }
        totalRanked: { type: integer }
        percentile: { type: number, nullable: true }
        available: { type: boolean }
        reason: { type: string }

    IndustryPeerSummary:
      type: object
      required: [symbol, name, dataSource, fallback]
      properties:
        symbol: { type: string }
        name: { type: string }
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        fallback: { type: boolean }

    IndustryComparisonResult:
      type: object
      required:
        [symbol, name, sector, industry, dataSource, simulated, peerGroup, metrics, strengths, weaknesses, overallPercentile, competitivePosition, confidenceLevel, confidenceExplanation, summary]
      properties:
        symbol: { type: string }
        name: { type: string }
        sector: { type: string }
        industry: { type: string }
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        simulated: { type: boolean }
        peerGroup:
          type: array
          items: { $ref: "#/components/schemas/IndustryPeerSummary" }
        metrics:
          type: array
          items: { $ref: "#/components/schemas/ComparisonMetric" }
        strengths:
          type: array
          items: { type: string }
        weaknesses:
          type: array
          items: { type: string }
        overallPercentile: { type: number, nullable: true }
        competitivePosition:
          type: string
          enum: [Leader, "Above Average", Average, "Below Average", Laggard, "Insufficient Data"]
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation: { type: string }
        summary: { type: string }

    FilingDocumentSection:
      type: object
      required: [key, label, found, wordCount]
      properties:
        key: { type: string }
        label: { type: string }
        found: { type: boolean }
        rawText: { type: string, nullable: true }
        excerpt: { type: string, nullable: true }
        wordCount: { type: integer }
        reason: { type: string }

    FilingFinancialHighlight:
      type: object
      required: [label, value]
      properties:
        label: { type: string }
        value: { type: string }
        detail: { type: string }

    FilingAnalysis:
      type: object
      required:
        [symbol, name, documentType, filingDate, sourceUrl, accessionNumber, fetchedAt, documentAvailable, sections, executiveSummary, keyFinancialHighlights, confidenceLevel, confidenceExplanation, disclaimer]
      properties:
        symbol: { type: string }
        name: { type: string }
        documentType:
          type: string
          enum: ["10-K", "10-Q", earnings-transcript, investor-presentation, sustainability-report, management-commentary]
        filingDate: { type: string, nullable: true }
        sourceUrl: { type: string, nullable: true }
        accessionNumber: { type: string, nullable: true }
        fetchedAt: { type: string }
        documentAvailable: { type: boolean }
        documentUnavailableReason: { type: string }
        sections:
          type: array
          items: { $ref: "#/components/schemas/FilingDocumentSection" }
        executiveSummary: { type: string }
        keyFinancialHighlights:
          type: array
          items: { $ref: "#/components/schemas/FilingFinancialHighlight" }
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation: { type: string }
        disclaimer: { type: string }

    ManagementSourceSection:
      type: object
      required: [key, label]
      properties:
        key: { type: string }
        label: { type: string }
        excerpt: { type: string, nullable: true }
        sourceUrl: { type: string, nullable: true }

    ManagementQualityDimension:
      type: object
      required: [dimension, score, weight, detail]
      properties:
        dimension: { type: string }
        score: { type: number, nullable: true }
        weight: { type: number }
        detail: { type: string }
        reason: { type: string }
        sourceSection: { $ref: "#/components/schemas/ManagementSourceSection" }

    ManagementQualityAnalysis:
      type: object
      required: [symbol, score, dimensions, strengths, weaknesses, confidenceLevel, confidenceExplanation, summary, disclaimer]
      properties:
        symbol: { type: string }
        score: { type: number, nullable: true }
        dimensions:
          type: array
          items: { $ref: "#/components/schemas/ManagementQualityDimension" }
        strengths:
          type: array
          items: { type: string }
        weaknesses:
          type: array
          items: { type: string }
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation: { type: string }
        summary: { type: string }
        disclaimer: { type: string }

    ValueResearchInput:
      type: object
      required: [symbol]
      properties:
        symbol: { type: string }
        persist: { type: boolean }
        forceRefresh: { type: boolean }
        level:
          type: string
          enum: [beginner, advanced]

    ValueResearchResult:
      type: object
      required: [report, commentary, commentarySource]
      properties:
        report: { $ref: "#/components/schemas/ValueResearchReport" }
        commentary: { type: string }
        commentarySource:
          type: string
          enum: [llm, template]
        historyId: { type: integer }

    # ─── AI Investment Analyst free-form Q&A (Phase 2, Sprint 30; read-only,
    # advisory/education only, never executes) ────────────────────────────────
    AskValueResearchInput:
      type: object
      required: [symbol, question]
      properties:
        symbol: { type: string }
        question: { type: string }
        forceRefresh: { type: boolean }

    AskValueResearchResult:
      type: object
      required: [answer, answerSource]
      properties:
        answer: { type: string }
        answerSource:
          type: string
          enum: [llm, template]

    # ─── AI Investment Committee LLM-Narrated Synthesis (Phase 4, Sprint 61;
    # read-only, advisory/education only, never executes) ──────────────────────
    NarrateInvestmentCommitteeInput:
      type: object
      required: [symbol]
      properties:
        symbol: { type: string }
        forceRefresh: { type: boolean }

    NarrateInvestmentCommitteeResult:
      type: object
      required: [narrative, narrativeSource]
      properties:
        narrative: { type: string }
        narrativeSource:
          type: string
          enum: [llm, template]

    ValueMacroContext:
      type: object
      required: [asOf, regime, regimeLabel, rateTrendPct, dataSource, summary]
      properties:
        asOf: { type: string }
        regime:
          type: string
          enum: [rising_rates, falling_rates, stable_rates]
        regimeLabel: { type: string }
        rateTrendPct: { type: number }
        dataSource:
          type: string
          enum: [SIMULATED]
        summary: { type: string }

    ValueSummary:
      type: object
      required: [symbol, name, kind, price, businessQualityScore, businessQualityRating, moatRating, financialStrength, valuationRating, decision, stockInvestmentScore, optionsSuitabilityScore, useCase, suggestedAction, dataSource, fetchedAt, simulated]
      properties:
        symbol: { type: string }
        name: { type: string }
        kind:
          type: string
          enum: [stock, etf]
        price: { type: number }
        businessQualityScore: { type: number }
        businessQualityRating: { type: string }
        moatRating: { type: string }
        financialStrength: { type: string }
        valuationRating: { type: string }
        marginOfSafety: { type: ["number", "null"] }
        decision: { type: string }
        stockInvestmentScore: { type: number }
        optionsSuitabilityScore: { type: number }
        useCase:
          type: string
          enum: [Stock, Options, Both, Watchlist, Avoid]
        suggestedAction: { type: string }
        dataSource: { type: string }
        fetchedAt: { type: string }
        simulated: { type: boolean }

    ValueHistoryRow:
      type: object
      required: [id, symbol, analysisDate, businessQualityScore, businessQualityRating, moatRating, financialStrength, valuationRating, marginOfSafety, valueInvestorDecision, stockVsOptionsDecision, dataSource, createdAt]
      properties:
        id: { type: integer }
        symbol: { type: string }
        analysisDate: { type: string }
        businessQualityScore: { type: number }
        businessQualityRating: { type: string }
        moatRating: { type: string }
        financialStrength: { type: string }
        valuationRating: { type: string }
        marginOfSafety: { type: string }
        valueInvestorDecision: { type: string }
        stockVsOptionsDecision: { type: string }
        dataSource: { type: string }
        createdAt: { type: string }

    ValueWatchlistItem:
      type: object
      required: [id, symbol, category, marginOfSafetyTarget, reason, currentDecision, createdAt, updatedAt, currentPrice, priceTargetCrossed, marginOfSafetyTargetCrossed]
      properties:
        id: { type: integer }
        symbol: { type: string }
        category: { type: string }
        fairValueEstimate: { type: ["number", "null"] }
        desiredBuyPrice: { type: ["number", "null"] }
        marginOfSafetyTarget: { type: number }
        reason: { type: string }
        currentDecision: { type: string }
        lastResearchedAt: { type: ["string", "null"] }
        createdAt: { type: string }
        updatedAt: { type: string }
        # Phase 2, Sprint 27 — always present, always null unless the GET was
        # called with ?checkTargets=true (never fabricated, never stale-silent).
        currentPrice: { type: ["number", "null"] }
        priceTargetCrossed: { type: ["boolean", "null"] }
        marginOfSafetyTargetCrossed: { type: ["boolean", "null"] }

    ValueWatchlistCreate:
      type: object
      required: [symbol]
      properties:
        symbol: { type: string }
        category: { type: string }
        fairValueEstimate: { type: ["number", "null"] }
        desiredBuyPrice: { type: ["number", "null"] }
        marginOfSafetyTarget: { type: number }
        reason: { type: string }
        currentDecision: { type: string }

    ValueWatchlistUpdate:
      type: object
      properties:
        category: { type: string }
        fairValueEstimate: { type: ["number", "null"] }
        desiredBuyPrice: { type: ["number", "null"] }
        marginOfSafetyTarget: { type: number }
        reason: { type: string }
        currentDecision: { type: string }

    # ─── Research Notes (Phase 12) — free-text, per-user, per-symbol notes.
    # Never AI-generated, never tied to a watchlist row by foreign key. ────
    ResearchNoteItem:
      type: object
      required: [id, symbol, note, createdAt, updatedAt]
      properties:
        id: { type: integer }
        symbol: { type: string }
        note: { type: string }
        createdAt: { type: string }
        updatedAt: { type: string }

    ResearchNoteCreate:
      type: object
      required: [symbol, note]
      properties:
        symbol: { type: string }
        note: { type: string }

    ResearchNoteUpdate:
      type: object
      required: [note]
      properties:
        note: { type: string }

    # ─── Investment Thesis Generator (Phase 12) — deterministic, template-
    # based composition over an already-built ValueResearchReport. No LLM,
    # no new scoring, no price prediction. ──────────────────────────────────
    InvestmentThesisSection:
      type: object
      required: [heading, paragraphs]
      properties:
        heading: { type: string }
        paragraphs:
          type: array
          items: { type: string }

    InvestmentThesis:
      type: object
      required: [symbol, name, asOf, dataSource, generatedAt, overview, sections, supportingPoints, riskFactors, disclaimer]
      properties:
        symbol: { type: string }
        name: { type: string }
        asOf: { type: string }
        dataSource: { type: string, enum: [SIMULATED, LIVE] }
        generatedAt: { type: string }
        overview: { type: string }
        sections:
          type: array
          items:
            $ref: "#/components/schemas/InvestmentThesisSection"
        supportingPoints:
          type: array
          items: { type: string }
        riskFactors:
          type: array
          items: { type: string }
        disclaimer: { type: string }

    # ─── Institutional Investment Committee Workbench (Phase 19) ─────────────
    InvestmentMemoSection:
      type: object
      required: [heading, paragraphs]
      properties:
        heading: { type: string }
        paragraphs:
          type: array
          items: { type: string }

    InvestmentMemo:
      type: object
      required: [symbol, name, asOf, dataSource, generatedAt, recommendation, confidence, overview, sections, disclaimer]
      properties:
        symbol: { type: string }
        name: { type: string }
        asOf: { type: string }
        dataSource: { type: string, enum: [SIMULATED, LIVE] }
        generatedAt: { type: string }
        recommendation: { type: string, enum: [Buy, Accumulate, Hold, Reduce, Sell, Avoid] }
        confidence: { type: number }
        overview: { type: string }
        sections:
          type: array
          items:
            $ref: "#/components/schemas/InvestmentMemoSection"
        disclaimer: { type: string }

    # ─── Institutional AI Coach (Phase 21) — pure educational orchestration
    # layer over already-computed engines; no new scoring, no new
    # recommendation. See lib/investingCoach.ts's own header comment. ────────
    CoachEvidenceItem:
      type: object
      required: [label, detail, source]
      properties:
        label: { type: string }
        detail: { type: string }
        source: { type: string }

    CoachExplanation:
      type: object
      required:
        - coach
        - coachLabel
        - symbol
        - headline
        - whyThisExists
        - metricsUsed
        - supportingEvidence
        - risksReducingConfidence
        - strengthsIncreasingConfidence
        - howToInterpret
        - commonMistakes
        - institutionalPerspective
        - relatedGlossaryKeys
        - calculationSources
        - disclaimer
      properties:
        coach:
          type: string
          enum: [investment, portfolio, decision, valuation, risk, research, monitoring, committee]
        coachLabel: { type: string }
        symbol: { type: string }
        headline: { type: string }
        whyThisExists: { type: string }
        metricsUsed:
          type: array
          items: { $ref: "#/components/schemas/CoachEvidenceItem" }
        supportingEvidence:
          type: array
          items: { $ref: "#/components/schemas/CoachEvidenceItem" }
        risksReducingConfidence:
          type: array
          items: { type: string }
        strengthsIncreasingConfidence:
          type: array
          items: { type: string }
        howToInterpret:
          type: array
          items: { type: string }
        commonMistakes:
          type: array
          items: { type: string }
        institutionalPerspective: { type: string }
        relatedGlossaryKeys:
          type: array
          items: { type: string }
        calculationSources:
          type: array
          items: { type: string }
        disclaimer: { type: string }

    # ─── Portfolio Construction (Phase 2, Sprint 28; advisory/education only,
    # never places an order) ───────────────────────────────────────────────
    ConstructionPortfolioSummary:
      type: object
      required: [id, name, description, holdingsCount, createdAt, updatedAt]
      properties:
        id: { type: integer }
        name: { type: string }
        description: { type: string }
        holdingsCount: { type: integer }
        createdAt: { type: string }
        updatedAt: { type: string }

    ConstructionPortfolioCreate:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }

    ConstructionPortfolioUpdate:
      type: object
      properties:
        name: { type: string }
        description: { type: string }

    ConstructionHolding:
      type: object
      required: [id, portfolioId, symbol, targetWeightPct, shares, avgCostBasis, notes, createdAt, updatedAt]
      properties:
        id: { type: integer }
        portfolioId: { type: integer }
        symbol: { type: string }
        targetWeightPct: { type: number }
        shares: { type: ["number", "null"] }
        avgCostBasis: { type: ["number", "null"] }
        notes: { type: string }
        createdAt: { type: string }
        updatedAt: { type: string }

    ConstructionHoldingCreate:
      type: object
      required: [symbol]
      properties:
        symbol: { type: string }
        targetWeightPct: { type: number }
        shares: { type: ["number", "null"] }
        avgCostBasis: { type: ["number", "null"] }
        notes: { type: string }

    ConstructionHoldingUpdate:
      type: object
      properties:
        targetWeightPct: { type: number }
        shares: { type: ["number", "null"] }
        avgCostBasis: { type: ["number", "null"] }
        notes: { type: string }

    ConstructionPortfolioHoldingAllocation:
      type: object
      required: [id, symbol, targetWeightPct, shares, avgCostBasis, notes, currentPrice, marketValue, actualWeightPct, driftPct, rebalanceAction, sector, industry, beta, marketCap]
      properties:
        id: { type: integer }
        symbol: { type: string }
        targetWeightPct: { type: number }
        shares: { type: ["number", "null"] }
        avgCostBasis: { type: ["number", "null"] }
        notes: { type: string }
        currentPrice: { type: ["number", "null"] }
        marketValue: { type: ["number", "null"] }
        actualWeightPct: { type: ["number", "null"] }
        driftPct: { type: ["number", "null"] }
        rebalanceAction:
          type: string
          enum: [buy, sell, hold, unknown]
        sector: { type: ["string", "null"] }
        industry: { type: ["string", "null"] }
        beta: { type: ["number", "null"] }
        marketCap: { type: ["number", "null"] }

    ConstructionPortfolioAllocationResult:
      type: object
      required: [holdings, totalMarketValue, totalTargetWeightPct, targetWeightSumWarning, unresolvedSymbols, summary]
      properties:
        holdings:
          type: array
          items: { $ref: "#/components/schemas/ConstructionPortfolioHoldingAllocation" }
        totalMarketValue: { type: ["number", "null"] }
        totalTargetWeightPct: { type: number }
        targetWeightSumWarning: { type: ["string", "null"] }
        unresolvedSymbols:
          type: array
          items: { type: string }
        summary: { type: string }

    ConstructionPortfolioDetail:
      type: object
      required: [id, name, description, createdAt, updatedAt, allocation]
      properties:
        id: { type: integer }
        name: { type: string }
        description: { type: string }
        createdAt: { type: string }
        updatedAt: { type: string }
        allocation:
          $ref: "#/components/schemas/ConstructionPortfolioAllocationResult"

    # ─── Portfolio Risk Analysis (Phase 2, Sprint 29; advisory/education only,
    # architecture modeled on the Options Engine's portfolioHealth.ts) ───────
    ConstructionRiskComponent:
      type: object
      required: [key, label, score, weight, detail]
      properties:
        key: { type: string }
        label: { type: string }
        score: { type: ["number", "null"] }
        weight: { type: number }
        detail: { type: string }

    ConstructionConcentrationRisk:
      type: object
      required: [score, label, detail, largestSymbol, largestSymbolWeightPct, capBreached]
      properties:
        score: { type: ["number", "null"] }
        label: { type: string }
        detail: { type: string }
        largestSymbol: { type: ["string", "null"] }
        largestSymbolWeightPct: { type: ["number", "null"] }
        capBreached: { type: boolean }

    ConstructionSectorExposureRow:
      type: object
      required: [sector, marketValue, weightPct]
      properties:
        sector: { type: string }
        marketValue: { type: number }
        weightPct: { type: number }

    ConstructionSectorExposureRisk:
      type: object
      required: [score, label, detail, largestSector, largestSectorWeightPct, capBreached, breakdown, unclassifiedWeightPct]
      properties:
        score: { type: ["number", "null"] }
        label: { type: string }
        detail: { type: string }
        largestSector: { type: ["string", "null"] }
        largestSectorWeightPct: { type: ["number", "null"] }
        capBreached: { type: boolean }
        breakdown:
          type: array
          items: { $ref: "#/components/schemas/ConstructionSectorExposureRow" }
        unclassifiedWeightPct: { type: ["number", "null"] }

    ConstructionBetaEstimateRisk:
      type: object
      required: [score, label, detail, portfolioBeta, coveragePct]
      properties:
        score: { type: ["number", "null"] }
        label: { type: string }
        detail: { type: string }
        portfolioBeta: { type: ["number", "null"] }
        coveragePct: { type: ["number", "null"] }

    ConstructionPortfolioRiskAnalysis:
      type: object
      required: [overall, concentration, sectorExposure, betaEstimate, components, totalMarketValue, unresolvedSymbols]
      properties:
        overall:
          type: object
          required: [score, label, detail]
          properties:
            score: { type: ["number", "null"] }
            label: { type: string }
            detail: { type: string }
        concentration:
          $ref: "#/components/schemas/ConstructionConcentrationRisk"
        sectorExposure:
          $ref: "#/components/schemas/ConstructionSectorExposureRisk"
        betaEstimate:
          $ref: "#/components/schemas/ConstructionBetaEstimateRisk"
        components:
          type: array
          items: { $ref: "#/components/schemas/ConstructionRiskComponent" }
        totalMarketValue: { type: ["number", "null"] }
        unresolvedSymbols:
          type: array
          items: { type: string }

    ConstructionRiskSnapshot:
      type: object
      required: [id, portfolioId, overallScore, analysis, createdAt]
      properties:
        id: { type: integer }
        portfolioId: { type: integer }
        overallScore: { type: ["number", "null"] }
        analysis:
          $ref: "#/components/schemas/ConstructionPortfolioRiskAnalysis"
        createdAt: { type: string }

    # ─── Institutional Portfolio Manager (Phase 13; on-demand composition over
    # every already-shipped Institutional Investing Engine module — advisory/
    # education only, never places an order) ─────────────────────────────────
    ConstructionScoreCard:
      type: object
      required: [score, label, detail]
      properties:
        score: { type: ["number", "null"] }
        label: { type: string }
        detail: { type: string }

    ConstructionAllocationSlice:
      type: object
      required: [label, weightPct]
      properties:
        label: { type: string }
        weightPct: { type: number }

    ConstructionUnavailableDimension:
      type: object
      required: [available, reason]
      properties:
        available: { type: boolean, enum: [false] }
        reason: { type: string }

    ConstructionHoldingIntelligence:
      type: object
      required:
        [
          symbol, weightPct, qualityScore, capitalAllocationScore, growthScore, valuationRating,
          committeeVerdict, marketCapBand, shares, avgCostBasis, currentPrice, costBasisValue,
          marketValue, unrealizedPnl, unrealizedPnlPct, dividendYield, dividendPerShare,
          estAnnualDividendIncome, suggestedShareDelta,
        ]
      properties:
        symbol: { type: string }
        weightPct: { type: ["number", "null"] }
        qualityScore: { type: ["number", "null"] }
        capitalAllocationScore: { type: ["number", "null"] }
        growthScore: { type: ["number", "null"] }
        valuationRating: { type: ["string", "null"] }
        committeeVerdict: { type: ["string", "null"] }
        marketCapBand: { type: ["string", "null"] }
        shares: { type: ["number", "null"] }
        avgCostBasis: { type: ["number", "null"] }
        currentPrice: { type: ["number", "null"] }
        costBasisValue: { type: ["number", "null"] }
        marketValue: { type: ["number", "null"] }
        unrealizedPnl: { type: ["number", "null"] }
        unrealizedPnlPct: { type: ["number", "null"] }
        dividendYield: { type: ["number", "null"] }
        dividendPerShare: { type: ["number", "null"] }
        estAnnualDividendIncome: { type: ["number", "null"] }
        suggestedShareDelta: { type: ["number", "null"] }

    ConstructionPortfolioIntelligence:
      type: object
      required:
        [
          qualityScore, capitalAllocationScore, diversificationScore, weightedMetrics, allocation,
          risk, income, performance, holdings, unresolvedSymbols, summary,
        ]
      properties:
        qualityScore: { $ref: "#/components/schemas/ConstructionScoreCard" }
        capitalAllocationScore: { $ref: "#/components/schemas/ConstructionScoreCard" }
        diversificationScore: { $ref: "#/components/schemas/ConstructionScoreCard" }
        weightedMetrics:
          type: object
          required: [roic, roe, grossMargin, operatingMargin, fcfYield, dividendYield, debtToEquity]
          properties:
            roic: { type: ["number", "null"] }
            roe: { type: ["number", "null"] }
            grossMargin: { type: ["number", "null"] }
            operatingMargin: { type: ["number", "null"] }
            fcfYield: { type: ["number", "null"] }
            dividendYield: { type: ["number", "null"] }
            debtToEquity: { type: ["number", "null"] }
        allocation:
          type: object
          required:
            [
              bySector, byIndustry, byMarketCapBand, byCountry, byCurrency, growthValueMix,
              qualityMix, largestPositionPct, top10ExposurePct, cashAllocationPct, cashAllocationNote,
            ]
          properties:
            bySector:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            byIndustry:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            byMarketCapBand:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            byCountry: { $ref: "#/components/schemas/ConstructionUnavailableDimension" }
            byCurrency: { $ref: "#/components/schemas/ConstructionUnavailableDimension" }
            growthValueMix:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            qualityMix:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            largestPositionPct: { type: ["number", "null"] }
            top10ExposurePct: { type: ["number", "null"] }
            cashAllocationPct: { type: ["number", "null"] }
            cashAllocationNote: { type: string }
        risk:
          type: object
          required:
            [
              overall, concentration, sectorExposure, cyclicality, cashRisk, dividendDependence,
              leverageExposure, qualityDrift, portfolioStability,
            ]
          properties:
            overall:
              type: object
              required: [score, label, detail]
              properties:
                score: { type: ["number", "null"] }
                label: { type: string }
                detail: { type: string }
            concentration: { $ref: "#/components/schemas/ConstructionConcentrationRisk" }
            sectorExposure: { $ref: "#/components/schemas/ConstructionSectorExposureRisk" }
            cyclicality: { $ref: "#/components/schemas/ConstructionBetaEstimateRisk" }
            cashRisk: { $ref: "#/components/schemas/ConstructionScoreCard" }
            dividendDependence: { $ref: "#/components/schemas/ConstructionScoreCard" }
            leverageExposure: { $ref: "#/components/schemas/ConstructionScoreCard" }
            qualityDrift: { $ref: "#/components/schemas/ConstructionScoreCard" }
            portfolioStability: { $ref: "#/components/schemas/ConstructionScoreCard" }
        income:
          type: object
          required: [portfolioDividendYield, estAnnualDividendIncome]
          properties:
            portfolioDividendYield: { type: ["number", "null"] }
            estAnnualDividendIncome: { type: ["number", "null"] }
        performance:
          type: object
          required: [totalCostBasisValue, totalMarketValue, totalUnrealizedPnl, totalUnrealizedPnlPct, holdingsWithoutCostBasis]
          properties:
            totalCostBasisValue: { type: ["number", "null"] }
            totalMarketValue: { type: ["number", "null"] }
            totalUnrealizedPnl: { type: ["number", "null"] }
            totalUnrealizedPnlPct: { type: ["number", "null"] }
            holdingsWithoutCostBasis:
              type: array
              items: { type: string }
        holdings:
          type: array
          items: { $ref: "#/components/schemas/ConstructionHoldingIntelligence" }
        unresolvedSymbols:
          type: array
          items: { type: string }
        summary: { type: string }

    ConstructionWatchlistComparison:
      type: object
      required: [inBoth, onlyInWatchlist, onlyInPortfolio, summary]
      properties:
        inBoth:
          type: array
          items: { type: string }
        onlyInWatchlist:
          type: array
          items: { type: string }
        onlyInPortfolio:
          type: array
          items: { type: string }
        summary: { type: string }

    ConstructionPortfolioSnapshot:
      type: object
      required: [id, portfolioId, qualityScore, riskScore, diversificationScore, totalMarketValue, holdingsCount, analysis, createdAt]
      properties:
        id: { type: integer }
        portfolioId: { type: integer }
        qualityScore: { type: ["number", "null"] }
        riskScore: { type: ["number", "null"] }
        diversificationScore: { type: ["number", "null"] }
        totalMarketValue: { type: ["number", "null"] }
        holdingsCount: { type: integer }
        analysis: { $ref: "#/components/schemas/ConstructionPortfolioIntelligence" }
        createdAt: { type: string }

    ConstructionPortfolioNote:
      type: object
      required: [id, portfolioId, note, createdAt, updatedAt]
      properties:
        id: { type: integer }
        portfolioId: { type: integer }
        note: { type: string }
        createdAt: { type: string }
        updatedAt: { type: string }

    ConstructionPortfolioNoteCreate:
      type: object
      required: [note]
      properties:
        note: { type: string }

    ConstructionPortfolioNoteUpdate:
      type: object
      required: [note]
      properties:
        note: { type: string }

    # ─── Institutional Portfolio Optimisation Engine (Phase 18) — pure
    # composition over already-computed Portfolio Intelligence/Decision
    # Engine/Opportunity Discovery outputs. Zero new scoring, zero LLM,
    # never a price prediction or return forecast. ─────────────────────────
    OptimisationEvidenceMetric:
      type: object
      required: [label, value]
      properties:
        label: { type: string }
        value: { type: string }

    OptimisationEvidence:
      type: object
      required:
        [
          metrics, decisionEngineRecommendation, investmentCommitteeRecommendation, rankExplanation,
          portfolioImpact, riskImpact, diversificationImpact,
        ]
      properties:
        metrics:
          type: array
          items: { $ref: "#/components/schemas/OptimisationEvidenceMetric" }
        decisionEngineRecommendation: { type: string }
        investmentCommitteeRecommendation: { type: string }
        rankExplanation: { type: string }
        portfolioImpact: { type: string }
        riskImpact: { type: string }
        diversificationImpact: { type: string }

    OptimisationPositionRow:
      type: object
      required:
        [
          symbol, name, sector, weightPct, qualityScore, valuationRating, investmentCommitteeVerdict,
          decisionRecommendation, rankScore, action, actionReason,
        ]
      properties:
        symbol: { type: string }
        name: { type: string }
        sector: { type: ["string", "null"] }
        weightPct: { type: ["number", "null"] }
        qualityScore: { type: ["number", "null"] }
        valuationRating: { type: string }
        investmentCommitteeVerdict: { type: string }
        decisionRecommendation: { type: string }
        rankScore: { type: number }
        action: { type: string, enum: [exit, trim, upgrade, core] }
        actionReason: { type: string }

    OptimisationCandidate:
      type: object
      required: [symbol, name, sector, weightPct, action, reason, evidence]
      properties:
        symbol: { type: string }
        name: { type: string }
        sector: { type: ["string", "null"] }
        weightPct: { type: ["number", "null"] }
        action: { type: string, enum: [exit, trim, upgrade, core] }
        reason: { type: string }
        evidence: { $ref: "#/components/schemas/OptimisationEvidence" }

    OptimisationReplacementOpportunity:
      type: object
      required: [forSymbol, symbol, name, sector, evidence]
      properties:
        forSymbol: { type: ["string", "null"] }
        symbol: { type: string }
        name: { type: string }
        sector: { type: ["string", "null"] }
        evidence: { $ref: "#/components/schemas/OptimisationEvidence" }

    OptimisationCapitalAllocationSuggestion:
      type: object
      required: [action, detail]
      properties:
        action: { type: string }
        detail: { type: string }

    PortfolioOptimisationAnalysis:
      type: object
      required:
        [
          portfolioId, health, concentration, diversification, positionQualityRanking, upgradeCandidates,
          trimCandidates, exitCandidates, capitalAllocationSuggestions, replacementOpportunities,
          cashDeploymentSuggestions, summary, disclaimer,
        ]
      properties:
        portfolioId: { type: integer }
        health:
          type: object
          required:
            [
              qualityScore, qualityLabel, capitalAllocationScore, diversificationScore, diversificationLabel,
              overallRiskScore, overallRiskLabel, summary,
            ]
          properties:
            qualityScore: { type: ["number", "null"] }
            qualityLabel: { type: string }
            capitalAllocationScore: { type: ["number", "null"] }
            diversificationScore: { type: ["number", "null"] }
            diversificationLabel: { type: string }
            overallRiskScore: { type: ["number", "null"] }
            overallRiskLabel: { type: string }
            summary: { type: string }
        concentration: { $ref: "#/components/schemas/ConstructionConcentrationRisk" }
        diversification:
          type: object
          required: [bySector, byIndustry, growthValueMix, qualityMix, largestPositionPct, top10ExposurePct]
          properties:
            bySector:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            byIndustry:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            growthValueMix:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            qualityMix:
              type: array
              items: { $ref: "#/components/schemas/ConstructionAllocationSlice" }
            largestPositionPct: { type: ["number", "null"] }
            top10ExposurePct: { type: ["number", "null"] }
        positionQualityRanking:
          type: array
          items: { $ref: "#/components/schemas/OptimisationPositionRow" }
        upgradeCandidates:
          type: array
          items: { $ref: "#/components/schemas/OptimisationCandidate" }
        trimCandidates:
          type: array
          items: { $ref: "#/components/schemas/OptimisationCandidate" }
        exitCandidates:
          type: array
          items: { $ref: "#/components/schemas/OptimisationCandidate" }
        capitalAllocationSuggestions:
          type: array
          items: { $ref: "#/components/schemas/OptimisationCapitalAllocationSuggestion" }
        replacementOpportunities:
          type: array
          items: { $ref: "#/components/schemas/OptimisationReplacementOpportunity" }
        cashDeploymentSuggestions:
          type: array
          items: { $ref: "#/components/schemas/OptimisationReplacementOpportunity" }
        summary: { type: string }
        disclaimer: { type: string }

    OptimisationReview:
      type: object
      required: [id, portfolioId, symbol, action, note, createdAt]
      properties:
        id: { type: integer }
        portfolioId: { type: integer }
        symbol: { type: ["string", "null"] }
        action: { type: string }
        note: { type: string }
        evidence:
          type: object
          additionalProperties: true
          description: A snapshot of the evidence shown when this review was saved
        createdAt: { type: string }

    OptimisationReviewCreate:
      type: object
      required: [action, note]
      properties:
        symbol: { type: ["string", "null"] }
        action: { type: string }
        note: { type: string }
        evidence:
          type: object
          additionalProperties: true
          description: A snapshot of the evidence shown when this review was saved

    # ─── Institutional Investment Decision Engine (Phase 14) — pure
    # composition over already-computed ValueResearchReport/Management
    # Quality/Portfolio Intelligence outputs. Zero new scoring, zero LLM. ────
    DecisionChecklistItem:
      type: object
      required: [id, label, status, explanation]
      properties:
        id: { type: string }
        label: { type: string }
        status: { type: string, enum: [pass, warning, fail, unavailable] }
        explanation: { type: string }

    DecisionEvidenceItem:
      type: object
      required: [label, detail]
      properties:
        label: { type: string }
        detail: { type: string }

    DecisionManagementQuality:
      type: object
      required: [available, score]
      properties:
        available: { type: boolean }
        score: { type: ["number", "null"] }
        reason: { type: string }

    DecisionPortfolioFit:
      type: object
      required: [available]
      properties:
        available: { type: boolean }
        reason: { type: string }
        portfolioId: { type: integer }
        alreadyHeld: { type: boolean }
        currentWeightPct: { type: ["number", "null"] }
        sectorExposurePct: { type: ["number", "null"] }

    InstitutionalDecisionAnalysis:
      type: object
      required:
        - symbol
        - asOf
        - kind
        - price
        - recommendation
        - confidence
        - summary
        - explanation
        - drivers
        - risks
        - supportingEvidence
        - contradictingEvidence
        - checklist
        - strengths
        - weaknesses
        - catalysts
        - thingsToMonitor
        - whyBuy
        - whyWait
        - whySell
        - managementQuality
        - portfolioFit
        - riskChecklistItem
        - diversificationChecklistItem
        - disclaimer
      properties:
        symbol: { type: string }
        asOf: { type: string }
        kind: { type: string }
        price: { type: number }
        recommendation: { type: string, enum: [Buy, Accumulate, Hold, Reduce, Sell, Avoid] }
        confidence: { type: number }
        summary: { type: string }
        explanation: { type: string }
        drivers: { type: array, items: { type: string } }
        risks: { type: array, items: { type: string } }
        supportingEvidence:
          type: array
          items: { $ref: "#/components/schemas/DecisionEvidenceItem" }
        contradictingEvidence:
          type: array
          items: { $ref: "#/components/schemas/DecisionEvidenceItem" }
        checklist:
          type: array
          items: { $ref: "#/components/schemas/DecisionChecklistItem" }
        strengths: { type: array, items: { type: string } }
        weaknesses: { type: array, items: { type: string } }
        catalysts: { type: array, items: { type: string } }
        thingsToMonitor: { type: array, items: { type: string } }
        whyBuy: { type: array, items: { type: string } }
        whyWait: { type: array, items: { type: string } }
        whySell: { type: array, items: { type: string } }
        managementQuality: { $ref: "#/components/schemas/DecisionManagementQuality" }
        portfolioFit: { $ref: "#/components/schemas/DecisionPortfolioFit" }
        riskChecklistItem: { $ref: "#/components/schemas/DecisionChecklistItem" }
        diversificationChecklistItem: { $ref: "#/components/schemas/DecisionChecklistItem" }
        disclaimer: { type: string }

    DecisionSnapshot:
      type: object
      required: [id, symbol, recommendation, confidence, analysis, createdAt]
      properties:
        id: { type: integer }
        symbol: { type: string }
        recommendation: { type: string }
        confidence: { type: number }
        analysis: { $ref: "#/components/schemas/InstitutionalDecisionAnalysis" }
        createdAt: { type: string }

    DecisionNote:
      type: object
      required: [id, symbol, note, createdAt, updatedAt]
      properties:
        id: { type: integer }
        symbol: { type: string }
        note: { type: string }
        createdAt: { type: string }
        updatedAt: { type: string }

    DecisionNoteCreate:
      type: object
      required: [note]
      properties:
        note: { type: string }

    DecisionNoteUpdate:
      type: object
      required: [note]
      properties:
        note: { type: string }

    # Phase 15 — Institutional Opportunity Discovery Engine. Every field below
    # is either a direct reuse of an already-computed ValueResearchReport field
    # or the Decision Engine's own already-computed synthesis
    # score/recommendation (lib/decisionEngine.ts, Phase 14) — this module adds
    # no new valuation model, quality score, or financial ratio.
    OpportunityRow:
      type: object
      required:
        - symbol
        - name
        - kind
        - price
        - sector
        - industry
        - businessQualityScore
        - businessQualityRating
        - investmentQualityScore
        - moatRating
        - moatScore
        - competitiveAdvantageScore
        - financialStrengthRating
        - financialStrengthScore
        - valuationRating
        - marginOfSafety
        - marketCap
        - revenueGrowth5y
        - roic
        - roe
        - debtToEquity
        - fcfMargin
        - dividendYield
        - investmentCommitteeVerdict
        - investmentCommitteeConfidence
        - tomNashConvictionScore
        - tomNashVerdict
        - decisionRecommendation
        - rankScore
        - rankExplanation
        - dataSource
        - fetchedAt
        - simulated
      properties:
        symbol: { type: string }
        name: { type: string }
        kind: { type: string }
        price: { type: number }
        sector: { type: ["string", "null"] }
        industry: { type: ["string", "null"] }
        businessQualityScore: { type: number }
        businessQualityRating: { type: string }
        investmentQualityScore: { type: ["number", "null"] }
        moatRating: { type: string, enum: [Wide, Medium, Narrow, None] }
        moatScore: { type: number }
        competitiveAdvantageScore: { type: ["number", "null"] }
        financialStrengthRating: { type: string, enum: [Strong, Acceptable, Weak, Risky] }
        financialStrengthScore: { type: number }
        valuationRating: { type: string, enum: [Cheap, Fair, Expensive, "Very Expensive", Unavailable] }
        marginOfSafety: { type: ["number", "null"] }
        marketCap: { type: ["number", "null"] }
        revenueGrowth5y: { type: number }
        roic: { type: number }
        roe: { type: number }
        debtToEquity: { type: number }
        fcfMargin: { type: number }
        dividendYield: { type: number }
        investmentCommitteeVerdict: { type: string, enum: [Buy, Hold, Wait] }
        investmentCommitteeConfidence: { type: number }
        tomNashConvictionScore: { type: number }
        tomNashVerdict: { type: string, enum: [Buy, Hold, Wait] }
        decisionRecommendation: { type: string, enum: [Buy, Accumulate, Hold, Reduce, Sell, Avoid] }
        rankScore: { type: number }
        rankExplanation: { type: string }
        dataSource: { type: string }
        fetchedAt: { type: string }
        simulated: { type: boolean }

    OpportunityBucket:
      type: object
      required: [category, label, rule, rows]
      properties:
        category:
          type: string
          enum:
            [
              top-opportunities,
              undervalued,
              high-quality,
              wide-moat,
              dividend,
              growth,
              deep-value,
              turnaround,
              watchlist-candidates,
              portfolio-upgrade-candidates,
            ]
        label: { type: string }
        rule: { type: string }
        rows:
          type: array
          items: { $ref: "#/components/schemas/OpportunityRow" }

    OpportunityScreenerFilters:
      type: object
      properties:
        country: { type: string, description: "Accepted but never applied — no provider surfaces a country field anywhere; supplying this always adds \"country\" to the response's unavailableFilters." }
        sector: { type: string }
        industry: { type: string }
        minMarketCap: { type: number }
        maxMarketCap: { type: number }
        minRevenueGrowth: { type: number }
        minRoic: { type: number }
        minRoe: { type: number }
        maxDebtToEquity: { type: number }
        minFcfMargin: { type: number }
        minDividendYield: { type: number }
        valuationRatings:
          type: array
          items: { type: string, enum: [Cheap, Fair, Expensive, "Very Expensive", Unavailable] }
        minMarginOfSafety: { type: number }
        minBusinessQualityScore: { type: number }
        investmentCommitteeVerdicts:
          type: array
          items: { type: string, enum: [Buy, Hold, Wait] }
        decisionRecommendations:
          type: array
          items: { type: string, enum: [Buy, Accumulate, Hold, Reduce, Sell, Avoid] }
        minTomNashScore: { type: number }

    OpportunityScanRequest:
      type: object
      properties:
        symbols:
          type: array
          items: { type: string }
        filters: { $ref: "#/components/schemas/OpportunityScreenerFilters" }
        forceRefresh: { type: boolean }
        watchlistAware: { type: boolean, description: "When true, resolves the calling user's own Watchlist symbols to populate the Watchlist Candidates bucket." }
        portfolioId: { type: integer, description: "When supplied (and owned by the calling user), resolves that portfolio's own held symbols to populate the Portfolio Upgrade Candidates bucket." }

    OpportunityScanResult:
      type: object
      required: [universeSize, unresolvedSymbols, scannedAt, unavailableFilters, totalBeforeFilter, rows, buckets]
      properties:
        universeSize: { type: integer }
        unresolvedSymbols:
          type: array
          items: { type: string }
        scannedAt: { type: string }
        unavailableFilters:
          type: array
          items: { type: string }
        totalBeforeFilter: { type: integer }
        rows:
          type: array
          items: { $ref: "#/components/schemas/OpportunityRow" }
        buckets:
          type: array
          items: { $ref: "#/components/schemas/OpportunityBucket" }

    OpportunityComparisonResult:
      type: object
      required: [rows, bestBy]
      properties:
        rows:
          type: array
          items: { $ref: "#/components/schemas/OpportunityRow" }
        bestBy:
          type: object
          additionalProperties: { type: string }

    OpportunitySavedScreen:
      type: object
      required: [id, name, filters, createdAt, updatedAt]
      properties:
        id: { type: integer }
        name: { type: string }
        filters: { $ref: "#/components/schemas/OpportunityScreenerFilters" }
        createdAt: { type: string }
        updatedAt: { type: string }

    OpportunitySavedScreenCreate:
      type: object
      required: [name, filters]
      properties:
        name: { type: string }
        filters: { $ref: "#/components/schemas/OpportunityScreenerFilters" }

    OpportunitySavedScreenUpdate:
      type: object
      properties:
        name: { type: string }
        filters: { $ref: "#/components/schemas/OpportunityScreenerFilters" }

    ValueLesson:
      type: object
      required: [id, title, level, summary, body, takeaway]
      properties:
        id: { type: string }
        title: { type: string }
        level:
          type: string
          enum: [beginner, intermediate, advanced]
        summary: { type: string }
        body:
          type: array
          items: { type: string }
        takeaway: { type: string }

    ValueLessonsResponse:
      type: object
      required: [lessons, disclaimer]
      properties:
        lessons:
          type: array
          items: { $ref: "#/components/schemas/ValueLesson" }
        disclaimer: { type: string }

    ValueQuizInput:
      type: object
      properties:
        topic:
          type: string
          enum: [moats, valuation, philosophy, mixed]
        count: { type: integer }

    ValueQuizQuestion:
      type: object
      required: [id, prompt, options]
      properties:
        id: { type: string }
        prompt: { type: string }
        options:
          type: array
          items: { type: string }

    GeneratedValueQuiz:
      type: object
      required: [quizId, topic, questions, disclaimer]
      properties:
        quizId: { type: string }
        topic: { type: string }
        questions:
          type: array
          items: { $ref: "#/components/schemas/ValueQuizQuestion" }
        disclaimer: { type: string }

    GradeValueQuizInput:
      type: object
      required: [quizId, answers]
      properties:
        quizId: { type: string }
        answers:
          type: array
          items: { type: integer }

    ValueQuizGradeItem:
      type: object
      required: [id, prompt, yourAnswer, correctAnswer, correct, explanation]
      properties:
        id: { type: string }
        prompt: { type: string }
        yourAnswer: { type: integer }
        correctAnswer: { type: integer }
        correct: { type: boolean }
        explanation: { type: string }

    ValueQuizGradeResult:
      type: object
      required: [topic, score, total, percent, message, results, disclaimer]
      properties:
        topic: { type: string }
        score: { type: integer }
        total: { type: integer }
        percent: { type: number }
        message: { type: string }
        results:
          type: array
          items: { $ref: "#/components/schemas/ValueQuizGradeItem" }
        disclaimer: { type: string }

    DeleteResult:
      type: object
      required: [success]
      properties:
        success: { type: boolean }

    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    # ─── Monitoring (Phase 6, Sprint 74) ────────────────────────────────────
    MonitoringDatabaseHealth:
      type: object
      required: [connected, latencyMs, error]
      properties:
        connected: { type: boolean }
        latencyMs: { type: number, nullable: true }
        error: { type: string, nullable: true }

    MonitoringJobHealth:
      type: object
      required: [job, lastRunAt, lastDurationMs, lastStatus, lastError, consecutiveFailures, totalRuns, totalFailures]
      properties:
        job: { type: string }
        lastRunAt: { type: string, format: date-time, nullable: true }
        lastDurationMs: { type: number, nullable: true }
        lastStatus: { type: string, enum: [ok, error, never_run] }
        lastError: { type: string, nullable: true }
        consecutiveFailures: { type: integer }
        totalRuns: { type: integer }
        totalFailures: { type: integer }

    MonitoringRequestMetrics:
      type: object
      required: [total, byStatusClass]
      properties:
        total: { type: integer }
        byStatusClass:
          type: object
          required: ["2xx", "3xx", "4xx", "5xx", other]
          properties:
            "2xx": { type: integer }
            "3xx": { type: integer }
            "4xx": { type: integer }
            "5xx": { type: integer }
            other: { type: integer }

    MonitoringAuditSignals:
      type: object
      required: [guardrailBlocksLastHour, authFailuresLastHour, computedAt]
      properties:
        guardrailBlocksLastHour: { type: integer }
        authFailuresLastHour: { type: integer }
        computedAt:
          type: string
          format: date-time
          nullable: true
          description: When these two counts were last computed by the periodic monitoring tick — null before the server's first tick (every 5 minutes) since these are deliberately not queried on the live request path.

    MonitoringAlert:
      type: object
      required: [category, severity, message]
      properties:
        category: { type: string }
        severity: { type: string, enum: [warning, critical] }
        message: { type: string }

    MarketDataSourceStatus:
      type: object
      required: [engine, source, connected, keyPresent, lastSuccessAt, staleMinutes, stale, missingData, message]
      properties:
        engine: { type: string, enum: [options, investing, trading] }
        source: { type: string }
        connected: { type: boolean }
        keyPresent: { type: ["boolean", "null"] }
        lastSuccessAt: { type: ["string", "null"] }
        staleMinutes: { type: ["number", "null"] }
        stale: { type: boolean }
        missingData: { type: boolean }
        message: { type: string }

    MarketClockStatus:
      type: object
      required: [source, isOpen, currentTimeEt, nextOpen, nextClose, reason]
      properties:
        source: { type: string, enum: [alpaca, static_approximation] }
        isOpen: { type: boolean }
        currentTimeEt: { type: string }
        nextOpen: { type: ["string", "null"] }
        nextClose: { type: ["string", "null"] }
        reason: { type: string }

    LiveMarketValidationReport:
      type: object
      required:
        [
          generatedAt,
          marketClock,
          optionsEngine,
          investingEngine,
          tradingEngine,
          conflictingProviderDetection,
          overallStale,
          overallMissingData,
        ]
      properties:
        generatedAt: { type: string }
        marketClock:
          $ref: "#/components/schemas/MarketClockStatus"
        optionsEngine:
          $ref: "#/components/schemas/MarketDataSourceStatus"
        investingEngine:
          type: array
          items:
            $ref: "#/components/schemas/MarketDataSourceStatus"
        tradingEngine:
          $ref: "#/components/schemas/MarketDataSourceStatus"
        conflictingProviderDetection:
          type: object
          required: [applicable, reason]
          properties:
            applicable: { type: boolean, enum: [false] }
            reason: { type: string }
        overallStale: { type: boolean }
        overallMissingData: { type: boolean }

    MonitoringStatus:
      type: object
      required: [status, timestamp, database, jobs, requestMetrics, auditSignals, alerts]
      properties:
        status: { type: string, enum: [ok, degraded] }
        timestamp: { type: string, format: date-time }
        database:
          $ref: "#/components/schemas/MonitoringDatabaseHealth"
        jobs:
          type: array
          items:
            $ref: "#/components/schemas/MonitoringJobHealth"
        requestMetrics:
          $ref: "#/components/schemas/MonitoringRequestMetrics"
        auditSignals:
          $ref: "#/components/schemas/MonitoringAuditSignals"
        alerts:
          type: array
          items:
            $ref: "#/components/schemas/MonitoringAlert"

    # ─── Scanner ─────────────────────────────────────────────────────────────
    ScannerResult:
      type: object
      required: [id, symbol, strategy, maxProfit, maxLoss, pop, ev, theta, ravishScore, ivRank, createdAt]
      properties:
        id:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
          enum: [iron_condor, iron_fly, calendar_spread, earnings]
        expiration:
          type: ["string", "null"]
        daysToExpiry:
          type: ["integer", "null"]
        shortPutStrike:
          type: ["number", "null"]
        shortCallStrike:
          type: ["number", "null"]
        longPutStrike:
          type: ["number", "null"]
        longCallStrike:
          type: ["number", "null"]
        credit:
          type: ["number", "null"]
        maxProfit:
          type: number
        maxLoss:
          type: number
        pop:
          type: number
          description: Probability of profit (0-100)
        ev:
          type: number
          description: Expected value
        theta:
          type: number
          description: Daily theta income
        vega:
          type: ["number", "null"]
        ivRank:
          type: number
        ravishScore:
          type: number
        ravishTier:
          type: string
          enum: [elite, high_conviction, good, ignore]
        returnOnCapital:
          type: ["number", "null"]
        eventRiskLevel:
          type: string
          enum: [none, low, medium, high]
          description: Event Risk Filter level for this candidate's window
        eventRiskPenalty:
          type: number
          description: Ravish points subtracted from the score due to event risk
        eventRiskEvents:
          type: array
          items:
            $ref: "#/components/schemas/EventRiskEvent"
        status:
          type: string
          enum: [active, executed, ignored]
        createdAt:
          type: string

    EventRiskEvent:
      type: object
      required: [type, label, date, daysAway, impact, scope]
      properties:
        type:
          type: string
          enum: [earnings, fomc, cpi, jobs, economic, news, dividend]
        label:
          type: string
        date:
          type: string
          description: Event date (YYYY-MM-DD)
        daysAway:
          type: integer
        impact:
          type: string
          enum: [low, medium, high]
        scope:
          type: string
          enum: [market, symbol]
        symbol:
          type: ["string", "null"]

    ScannerRunInput:
      type: object
      properties:
        symbols:
          type: array
          items:
            type: string
        strategies:
          type: array
          items:
            type: string

    ScannerRunResponse:
      type: object
      required: [status, message]
      properties:
        status:
          type: string
        message:
          type: string
        resultsCount:
          type: ["integer", "null"]

    TopOpportunities:
      type: object
      required: [ironCondors, ironFlys, calendarSpreads, earnings, lastScanned]
      properties:
        ironCondors:
          type: array
          items:
            $ref: "#/components/schemas/ScannerResult"
        ironFlys:
          type: array
          items:
            $ref: "#/components/schemas/ScannerResult"
        calendarSpreads:
          type: array
          items:
            $ref: "#/components/schemas/ScannerResult"
        earnings:
          type: array
          items:
            $ref: "#/components/schemas/ScannerResult"
        lastScanned:
          type: ["string", "null"]

    # ─── Options ──────────────────────────────────────────────────────────────
    OptionContract:
      type: object
      required: [strike, type, expiration, bid, ask, delta, theta, vega, gamma, iv, openInterest]
      properties:
        strike:
          type: number
        type:
          type: string
          enum: [call, put]
        expiration:
          type: string
        bid:
          type: number
        ask:
          type: number
        mid:
          type: number
        delta:
          type: number
        theta:
          type: number
        vega:
          type: number
        gamma:
          type: number
        iv:
          type: number
        openInterest:
          type: integer
        volume:
          type: ["integer", "null"]

    OptionChain:
      type: object
      required: [symbol, underlyingPrice, ivRank, expiration, calls, puts]
      properties:
        symbol:
          type: string
        underlyingPrice:
          type: number
        ivRank:
          type: number
        iv30:
          type: ["number", "null"]
        earningsDate:
          type: ["string", "null"]
        expiration:
          type: string
        calls:
          type: array
          items:
            $ref: "#/components/schemas/OptionContract"
        puts:
          type: array
          items:
            $ref: "#/components/schemas/OptionContract"

    UniverseSymbol:
      type: object
      required: [symbol, price, change, changePercent, ivRank, iv30, earningsDate]
      properties:
        symbol:
          type: string
        name:
          type: string
        price:
          type: number
        change:
          type: number
        changePercent:
          type: number
        ivRank:
          type: number
        iv30:
          type: number
        earningsDate:
          type: ["string", "null"]
        volume:
          type: ["integer", "null"]

    ExpirationDate:
      type: object
      required: [date, daysToExpiry]
      properties:
        date:
          type: string
        daysToExpiry:
          type: integer

    # ─── Portfolio ─────────────────────────────────────────────────────────────
    Position:
      type: object
      required: [id, symbol, strategy, legs, openDate, credit, currentValue, unrealizedPnl, delta, theta, vega, gamma, status]
      properties:
        id:
          type: string
        symbol:
          type: string
        strategy:
          type: string
        legs:
          type: array
          items:
            $ref: "#/components/schemas/PositionLeg"
        openDate:
          type: string
        expiration:
          type: ["string", "null"]
        credit:
          type: number
        currentValue:
          type: number
        unrealizedPnl:
          type: number
        unrealizedPnlPercent:
          type: number
        delta:
          type: number
        theta:
          type: number
        vega:
          type: number
        gamma:
          type: number
        status:
          type: string
          enum: [open, closed, expired]

    PositionLeg:
      type: object
      required: [symbol, side, quantity, strike, type, expiration]
      properties:
        symbol:
          type: string
        side:
          type: string
          enum: [long, short]
        quantity:
          type: integer
        strike:
          type: number
        type:
          type: string
          enum: [call, put]
        expiration:
          type: string
        openPrice:
          type: ["number", "null"]
        currentPrice:
          type: ["number", "null"]

    PortfolioGreeks:
      type: object
      required: [totalDelta, totalTheta, totalVega, totalGamma, deltaStatus, dailyThetaIncome, totalPositions]
      properties:
        totalDelta:
          type: number
        totalTheta:
          type: number
        totalVega:
          type: number
        totalGamma:
          type: number
        deltaStatus:
          type: string
          enum: [neutral, bullish, bearish]
        deltaNeutralScore:
          type: number
          description: 0-100 score for how delta neutral the portfolio is
        directionalExposure:
          type: number
          description: Net directional exposure in delta-dollars (positive = bullish)
        exposureLabel:
          type: string
          description: Human-readable bullish/bearish/neutral exposure summary
        dailyThetaIncome:
          type: number
        monthlyThetaProjection:
          type: number
        totalPositions:
          type: integer
        recommendations:
          type: array
          items:
            type: string

    PortfolioSummary:
      type: object
      required: [accountValue, cashBalance, buyingPower, totalPnl, dayPnl, openPositions]
      properties:
        accountValue:
          type: number
        cashBalance:
          type: number
        buyingPower:
          type: number
        totalPnl:
          type: number
        totalPnlPercent:
          type: number
        dayPnl:
          type: number
        dayPnlPercent:
          type: number
        openPositions:
          type: integer
        maxRiskUsed:
          type: number
          description: Percentage of account at risk

    RiskStatus:
      type: object
      required: [accountValue, maxRiskPerTradePct, maxPortfolioRiskPct, portfolioRiskDollars, portfolioRiskUsedPct, withinLimits, openTrades, stopLossMultiplier, profitTargets, checks, violations, perTrade]
      properties:
        accountValue:
          type: number
        maxRiskPerTradePct:
          type: number
        maxPortfolioRiskPct:
          type: number
        portfolioRiskDollars:
          type: number
        portfolioRiskUsedPct:
          type: number
        withinLimits:
          type: boolean
        openTrades:
          type: integer
        stopLossMultiplier:
          type: number
        profitTargets:
          type: array
          items:
            type: number
        checks:
          type: array
          items:
            $ref: "#/components/schemas/RiskCheck"
        violations:
          type: array
          items:
            type: string
        perTrade:
          type: array
          items:
            $ref: "#/components/schemas/RiskPerTrade"

    RiskCheck:
      type: object
      required: [label, passed, detail]
      properties:
        label:
          type: string
        passed:
          type: boolean
        detail:
          type: string

    RiskPerTrade:
      type: object
      required: [id, symbol, strategy, riskDollars, riskPct, withinLimit]
      properties:
        id:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        riskDollars:
          type: number
        riskPct:
          type: number
        withinLimit:
          type: boolean

    # ─── Trades ────────────────────────────────────────────────────────────────
    Trade:
      type: object
      required: [id, symbol, strategy, status, legs, openDate, credit, maxProfit, maxLoss, pop, ev, theta, ravishScore]
      properties:
        id:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
          enum: [iron_condor, iron_fly, calendar_spread, earnings]
        status:
          type: string
          enum: [pending, open, closed, expired]
        executionMode:
          type: string
          enum: [manual, semi_auto, full_auto]
        legs:
          type: array
          items:
            $ref: "#/components/schemas/TradeLeg"
        openDate:
          type: string
        closeDate:
          type: ["string", "null"]
        expiration:
          type: ["string", "null"]
        credit:
          type: number
        maxProfit:
          type: number
        maxLoss:
          type: number
        currentPnl:
          type: ["number", "null"]
        currentPnlPercent:
          type: ["number", "null"]
        pop:
          type: number
        ev:
          type: number
        theta:
          type: number
        ravishScore:
          type: number
        exitReason:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        alpacaOrderId:
          type: ["string", "null"]
          description: >-
            The broker order id returned at submission time, or a
            "mock-<uuid>" id when no broker credentials were configured at
            submission (this trade was never actually sent to Alpaca).
            Added in the Trade History sprint — previously stored but not
            exposed via this API.

    TradeLeg:
      type: object
      required: [side, quantity, strike, optionType, expiration, openPrice]
      properties:
        side:
          type: string
          enum: [buy, sell]
        quantity:
          type: integer
        strike:
          type: number
        optionType:
          type: string
          enum: [call, put]
        expiration:
          type: string
        openPrice:
          type: number
        closePrice:
          type: ["number", "null"]

    TradeInput:
      type: object
      required: [symbol, strategy, legs, executionMode]
      properties:
        symbol:
          type: string
        strategy:
          type: string
          enum: [iron_condor, iron_fly, calendar_spread, earnings]
        executionMode:
          type: string
          enum: [manual, semi_auto, full_auto]
        legs:
          type: array
          items:
            $ref: "#/components/schemas/TradeLeg"
        notes:
          type: string
        scannerResultId:
          type: ["integer", "null"]

    TradeMonitor:
      type: object
      required: [tradeId, currentPnl, currentPnlPercent, profitTarget50, profitTarget75, profitTarget90, stopLoss, daysToExpiry, delta, theta]
      properties:
        tradeId:
          type: integer
        currentPnl:
          type: number
        currentPnlPercent:
          type: number
        profitTarget50:
          type: number
        profitTarget75:
          type: number
        profitTarget90:
          type: number
        stopLoss:
          type: number
        daysToExpiry:
          type: integer
        delta:
          type: number
        theta:
          type: number
        alerts:
          type: array
          items:
            type: string

    # ─── Trade Adjustment Engine (Task #20) ──────────────────────────────────────
    TradeAdjustmentSignal:
      type: object
      required: [key, label, triggered, value, threshold, detail]
      properties:
        key:
          type: string
        label:
          type: string
        triggered:
          type: boolean
        value:
          type: number
        threshold:
          type: number
        detail:
          type: string

    TradeAdjustmentGreeks:
      type: object
      required: [delta, gamma, theta, vega]
      properties:
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number

    TradeAdjustment:
      type: object
      required:
        [tradeId, symbol, symbolName, strategy, strategyLabel, status, action, actionLabel, severity, rationale, signals, triggeredSignals, autoActionable, underlyingPrice, daysToExpiry, currentPnl, currentPnlPercent, maxLoss, maxProfit, entryPop, currentPop, threatenedSide, greeks, assignmentRisk, isCredit, disclaimer]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        symbolName:
          type: string
        strategy:
          type: string
        strategyLabel:
          type: string
        status:
          type: string
        action:
          type: string
          enum: [hold, close_for_profit, close_for_loss, roll_threatened, roll_untested, convert, reduce_risk, do_nothing]
        actionLabel:
          type: string
        severity:
          type: string
          enum: [none, info, warning, critical]
        rationale:
          type: string
        signals:
          type: array
          items:
            $ref: "#/components/schemas/TradeAdjustmentSignal"
        triggeredSignals:
          type: array
          items:
            type: string
        autoActionable:
          type: boolean
        underlyingPrice:
          type: number
        daysToExpiry:
          type: integer
        currentPnl:
          type: number
        currentPnlPercent:
          type: number
        maxLoss:
          type: number
        maxProfit:
          type: number
        entryPop:
          type: number
        currentPop:
          type: number
        nearestShortStrike:
          type: ["number", "null"]
        distanceToShortPct:
          type: ["number", "null"]
        threatenedSide:
          type: ["string", "null"]
          enum: [call, put, null]
        greeks:
          $ref: "#/components/schemas/TradeAdjustmentGreeks"
        assignmentRisk:
          type: string
        isCredit:
          type: boolean
        disclaimer:
          type: string

    # ─── Execution (Phase 5) ─────────────────────────────────────────────────────
    ExecutionPreviewInput:
      type: object
      required: [quantity]
      properties:
        scannerResultId:
          type: ["integer", "null"]
        symbol:
          type: ["string", "null"]
        strategy:
          type: ["string", "null"]
          enum: [iron_condor, iron_fly, calendar_spread, earnings, null]
        quantity:
          type: integer
          default: 1

    ExecutionSubmitInput:
      type: object
      required: [quantity, confirm]
      properties:
        scannerResultId:
          type: ["integer", "null"]
        symbol:
          type: ["string", "null"]
        strategy:
          type: ["string", "null"]
          enum: [iron_condor, iron_fly, calendar_spread, earnings, null]
        quantity:
          type: integer
          default: 1
        confirm:
          type: boolean

    ExecutionOrderLeg:
      type: object
      required: [occSymbol, side, optionType, strike, expiration, ratioQty, positionIntent, price]
      properties:
        occSymbol:
          type: string
        side:
          type: string
          enum: [buy, sell]
        optionType:
          type: string
          enum: [call, put]
        strike:
          type: number
        expiration:
          type: string
        ratioQty:
          type: integer
        positionIntent:
          type: string
          enum: [buy_to_open, sell_to_open]
        price:
          type: number

    PreTradeCheck:
      type: object
      required: [label, passed, detail]
      properties:
        label:
          type: string
        passed:
          type: boolean
        detail:
          type: string

    PreTradeValidation:
      type: object
      required: [valid, checks, violations, warnings, riskDollars, riskPct, portfolioRiskBeforePct, portfolioRiskAfterPct]
      properties:
        valid:
          type: boolean
        checks:
          type: array
          items:
            $ref: "#/components/schemas/PreTradeCheck"
        violations:
          type: array
          items:
            type: string
        warnings:
          type: array
          items:
            type: string
        riskDollars:
          type: number
        riskPct:
          type: number
        portfolioRiskBeforePct:
          type: number
        portfolioRiskAfterPct:
          type: number

    ExecutionTicket:
      type: object
      required:
        [symbol, strategy, expiration, daysToExpiry, quantity, legs, netCredit, isCredit, maxProfit, maxLoss, pop, ev, ravishScore, ravishTier, returnOnCapital, buyingPowerRequired, accountValue, riskPct, portfolioRiskBeforePct, portfolioRiskAfterPct, executionMode, canSubmit, validation, warnings]
      properties:
        symbol:
          type: string
        strategy:
          type: string
          enum: [iron_condor, iron_fly, calendar_spread, earnings]
        expiration:
          type: string
        daysToExpiry:
          type: integer
        quantity:
          type: integer
        legs:
          type: array
          items:
            $ref: "#/components/schemas/ExecutionOrderLeg"
        netCredit:
          type: number
        isCredit:
          type: boolean
        maxProfit:
          type: number
        maxLoss:
          type: number
        pop:
          type: number
        ev:
          type: number
        ravishScore:
          type: number
        ravishTier:
          type: string
        returnOnCapital:
          type: number
        buyingPowerRequired:
          type: number
        accountValue:
          type: number
        riskPct:
          type: number
        portfolioRiskBeforePct:
          type: number
        portfolioRiskAfterPct:
          type: number
        executionMode:
          type: string
          enum: [manual, semi_auto, full_auto]
        canSubmit:
          type: boolean
        validation:
          $ref: "#/components/schemas/PreTradeValidation"
        warnings:
          type: array
          items:
            type: string
        adjustment:
          oneOf:
            - $ref: "#/components/schemas/AdjustmentTicketContext"
            - type: "null"

    AdjustmentSourceLeg:
      type: object
      required: [side, optionType, strike, expiration, quantity]
      properties:
        side:
          type: string
          enum: [buy, sell]
        optionType:
          type: string
          enum: [call, put]
        strike:
          type: number
        expiration:
          type: string
        quantity:
          type: integer

    AdjustmentSourcePosition:
      type: object
      required:
        [tradeId, symbol, strategy, strategyLabel, expiration, daysToExpiry, legs, credit, maxProfit, maxLoss, pop, costToClose, currentPnl, isCredit]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        strategyLabel:
          type: string
        expiration:
          type: ["string", "null"]
        daysToExpiry:
          type: integer
        legs:
          type: array
          items:
            $ref: "#/components/schemas/AdjustmentSourceLeg"
        credit:
          type: number
        maxProfit:
          type: number
        maxLoss:
          type: number
        pop:
          type: number
        costToClose:
          type: number
        currentPnl:
          type: number
        isCredit:
          type: boolean

    AdjustmentTicketContext:
      type: object
      required: [sourceTradeId, action, actionLabel, kind, fromStrategy, toStrategy, closeDescription, rationale, source, netCashflow]
      properties:
        sourceTradeId:
          type: integer
        action:
          type: string
          enum: [roll_threatened, roll_untested, convert]
        actionLabel:
          type: string
        kind:
          type: string
          enum: [roll, convert]
        fromStrategy:
          type: string
        toStrategy:
          type: string
        closeDescription:
          type: string
        rationale:
          type: string
        source:
          $ref: "#/components/schemas/AdjustmentSourcePosition"
        netCashflow:
          type: number

    AdjustmentPreviewInput:
      type: object
      required: [tradeId]
      properties:
        tradeId:
          type: integer
        quantity:
          type: ["integer", "null"]

    AdjustmentSubmitInput:
      type: object
      required: [tradeId, confirm]
      properties:
        tradeId:
          type: integer
        quantity:
          type: ["integer", "null"]
        confirm:
          type: boolean

    AdjustmentSubmitResult:
      type: object
      required: [orderId, status, broker, tradeId, journalId, message, ticket, closedTradeId, closedReason]
      properties:
        orderId:
          type: string
        status:
          type: string
        broker:
          type: string
          enum: [alpaca, mock]
        tradeId:
          type: integer
        journalId:
          type: integer
        message:
          type: string
        ticket:
          $ref: "#/components/schemas/ExecutionTicket"
        closedTradeId:
          type: integer
        closedReason:
          type: string

    ExecutionSubmitResult:
      type: object
      required: [orderId, status, broker, tradeId, journalId, message, ticket]
      properties:
        orderId:
          type: string
        status:
          type: string
        broker:
          type: string
          enum: [alpaca, mock]
        tradeId:
          type: integer
        journalId:
          type: integer
        message:
          type: string
        ticket:
          $ref: "#/components/schemas/ExecutionTicket"

    # ─── Order Preview & Risk Simulator (Paper Trading dry-run) ─────────────────
    # Read-only pre-trade simulation: never contacts a broker execution
    # endpoint, never creates an order, never mutates local state. Reuses
    # execution.ts's own unmodified previewOptionOrder()/canonicalQuote()
    # for its numeric figures.
    OrderPreviewInput:
      type: object
      properties:
        symbol:
          type: ["string", "null"]
        strategy:
          type: ["string", "null"]
          enum: [iron_condor, iron_fly, calendar_spread, earnings, null]
        quantity:
          type: ["integer", "null"]

    OrderPreviewInputIssue:
      type: object
      required: [field, code, message]
      properties:
        field:
          type: string
          enum: [symbol, strategy, quantity]
        code:
          type: string
        message:
          type: string

    OrderPreviewValidationItem:
      type: object
      required: [code, label, status, detail]
      properties:
        code:
          type: string
        label:
          type: string
        status:
          type: string
          enum: [ok, warning, blocked]
        detail:
          type: string

    OrderPreviewTicket:
      allOf:
        - $ref: "#/components/schemas/ExecutionTicket"
        - type: object
          required: [entryPricePerSpread, notionalValue, marginImpact, riskRewardRatio]
          properties:
            entryPricePerSpread:
              type: number
            notionalValue:
              type: number
            marginImpact:
              type: number
            riskRewardRatio:
              type: ["number", "null"]

    OrderPreviewResult:
      type: object
      required:
        [
          available,
          inputIssues,
          ticket,
          preTradeChecklist,
          credentialsConfigured,
          brokerConnected,
          lastBrokerCheckAt,
          accountValue,
          generatedAt,
        ]
      properties:
        available:
          type: boolean
        inputIssues:
          type: array
          items:
            $ref: "#/components/schemas/OrderPreviewInputIssue"
        ticket:
          oneOf:
            - $ref: "#/components/schemas/OrderPreviewTicket"
            - type: "null"
        preTradeChecklist:
          type: array
          items:
            $ref: "#/components/schemas/OrderPreviewValidationItem"
        credentialsConfigured:
          type: boolean
        brokerConnected:
          type: ["boolean", "null"]
        lastBrokerCheckAt:
          type: ["string", "null"]
        accountValue:
          type: number
        generatedAt:
          type: string

    # ─── Position Sizing & Portfolio Impact Calculator (Paper Trading) ──────────
    # Extends OrderPreviewResult above with position-sizing figures, a
    # before/after portfolio-impact snapshot, a risk-warnings list, and a
    # scenario comparison — read-only, never routes to a broker.
    PositionSizingInput:
      allOf:
        - $ref: "#/components/schemas/OrderPreviewInput"
        - type: object
          properties:
            customQuantity:
              type: ["integer", "null"]

    BreakEvenPrice:
      type: object
      required: [label, price]
      properties:
        label:
          type: string
        price:
          type: number

    PositionSizingFigures:
      type: object
      required:
        [
          recommendedQuantity,
          positionSizePctOfPortfolio,
          buyingPowerUtilizationPct,
          capitalAtRisk,
          maxTheoreticalLoss,
          maxTheoreticalGain,
          breakEvens,
          breakEvenUnavailableReason,
          riskRewardRatio,
          concentrationBeforePct,
          concentrationAfterPct,
        ]
      properties:
        recommendedQuantity:
          type: ["integer", "null"]
        positionSizePctOfPortfolio:
          type: ["number", "null"]
        buyingPowerUtilizationPct:
          type: ["number", "null"]
        capitalAtRisk:
          type: ["number", "null"]
        maxTheoreticalLoss:
          type: ["number", "null"]
        maxTheoreticalGain:
          type: ["number", "null"]
        breakEvens:
          type: array
          items:
            $ref: "#/components/schemas/BreakEvenPrice"
        breakEvenUnavailableReason:
          type: ["string", "null"]
        riskRewardRatio:
          type: ["number", "null"]
        concentrationBeforePct:
          type: ["number", "null"]
        concentrationAfterPct:
          type: ["number", "null"]

    SymbolExposure:
      type: object
      required: [symbol, riskDollars, pctOfAccount]
      properties:
        symbol:
          type: string
        riskDollars:
          type: number
        pctOfAccount:
          type: number

    PortfolioGreeksSnapshot:
      type: object
      required: [delta, gamma, theta, vega]
      properties:
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number

    PortfolioSnapshot:
      type: object
      required:
        [
          openPositionsCount,
          totalRiskDollars,
          totalRiskPct,
          exposureBySymbol,
          longExposureDollars,
          shortExposureDollars,
          greeks,
        ]
      properties:
        openPositionsCount:
          type: integer
        totalRiskDollars:
          type: number
        totalRiskPct:
          type: number
        exposureBySymbol:
          type: array
          items:
            $ref: "#/components/schemas/SymbolExposure"
        longExposureDollars:
          type: number
        shortExposureDollars:
          type: number
        greeks:
          $ref: "#/components/schemas/PortfolioGreeksSnapshot"

    SectorExposure:
      type: object
      required: [available, reason]
      properties:
        available:
          type: boolean
          enum: [false]
        reason:
          type: string

    PortfolioImpact:
      type: object
      required: [current, hypothetical, sectorExposure, deltaImpact, thetaImpact, gammaImpact, vegaImpact]
      properties:
        current:
          $ref: "#/components/schemas/PortfolioSnapshot"
        hypothetical:
          oneOf:
            - $ref: "#/components/schemas/PortfolioSnapshot"
            - type: "null"
        sectorExposure:
          $ref: "#/components/schemas/SectorExposure"
        deltaImpact:
          type: ["number", "null"]
        thetaImpact:
          type: ["number", "null"]
        gammaImpact:
          type: ["number", "null"]
        vegaImpact:
          type: ["number", "null"]

    PositionSizingWarning:
      type: object
      required: [code, label, status, detail]
      properties:
        code:
          type: string
        label:
          type: string
        status:
          type: string
          enum: [ok, warning, blocked]
        detail:
          type: string

    ScenarioResult:
      type: object
      required:
        [
          label,
          quantity,
          available,
          unavailableReason,
          capitalAtRisk,
          buyingPowerRequired,
          buyingPowerUtilizationPct,
          concentrationAfterPct,
          positionSizePctOfPortfolio,
        ]
      properties:
        label:
          type: string
        quantity:
          type: integer
        available:
          type: boolean
        unavailableReason:
          type: ["string", "null"]
        capitalAtRisk:
          type: ["number", "null"]
        buyingPowerRequired:
          type: ["number", "null"]
        buyingPowerUtilizationPct:
          type: ["number", "null"]
        concentrationAfterPct:
          type: ["number", "null"]
        positionSizePctOfPortfolio:
          type: ["number", "null"]

    PositionSizingResult:
      type: object
      required: [preview, positionSizing, portfolioImpact, riskWarnings, scenarios, generatedAt]
      properties:
        preview:
          $ref: "#/components/schemas/OrderPreviewResult"
        positionSizing:
          oneOf:
            - $ref: "#/components/schemas/PositionSizingFigures"
            - type: "null"
        portfolioImpact:
          $ref: "#/components/schemas/PortfolioImpact"
        riskWarnings:
          type: array
          items:
            $ref: "#/components/schemas/PositionSizingWarning"
        scenarios:
          type: array
          items:
            $ref: "#/components/schemas/ScenarioResult"
        generatedAt:
          type: string

    # ─── Trade Adjustment & Roll/Convert Preview Simulator (Paper Trading) ──────
    # Read-only dry-run over an EXISTING open position: never contacts a
    # broker execution endpoint, never creates or closes an order. Reuses
    # execution.ts's own buildAdjustmentTicket()/previewAdjustmentOrder()/
    # previewOptionOrder() for its numeric figures.
    AdjustmentIntent:
      type: string
      enum: [roll_forward, roll_out, roll_up, roll_down, roll_out_up, roll_out_down, convert, close_replace]

    TradeAdjustmentPreviewInput:
      type: object
      properties:
        tradeId:
          type: ["integer", "null"]
        intent:
          oneOf:
            - $ref: "#/components/schemas/AdjustmentIntent"
            - type: "null"
        quantity:
          type: ["integer", "null"]

    TradeAdjustmentInputIssue:
      type: object
      required: [field, code, message]
      properties:
        field:
          type: string
          enum: [tradeId, intent, quantity]
        code:
          type: string
        message:
          type: string

    TradeAdjustmentWarning:
      type: object
      required: [code, label, status, detail]
      properties:
        code:
          type: string
        label:
          type: string
        status:
          type: string
          enum: [ok, warning, blocked]
        detail:
          type: string

    MetricComparison:
      type: object
      required: [code, label, before, after, change, direction]
      properties:
        code:
          type: string
        label:
          type: string
        before:
          type: ["number", "null"]
        after:
          type: ["number", "null"]
        change:
          type: ["number", "null"]
        direction:
          type: string
          enum: [improved, worse, neutral, unknown]

    TradeAdjustmentPreviewResult:
      type: object
      required:
        [
          available,
          inputIssues,
          intent,
          intentLabel,
          intentAvailable,
          intentUnavailableReason,
          existingPosition,
          proposedPosition,
          netCashflow,
          greeksBefore,
          greeksAfter,
          breakEvenBefore,
          breakEvenBeforeUnavailableReason,
          breakEvenAfter,
          breakEvenAfterUnavailableReason,
          portfolioExposureBefore,
          portfolioExposureAfter,
          comparisons,
          riskWarnings,
          credentialsConfigured,
          brokerConnected,
          lastBrokerCheckAt,
          accountValue,
          generatedAt,
        ]
      properties:
        available:
          type: boolean
        inputIssues:
          type: array
          items:
            $ref: "#/components/schemas/TradeAdjustmentInputIssue"
        intent:
          oneOf:
            - $ref: "#/components/schemas/AdjustmentIntent"
            - type: "null"
        intentLabel:
          type: ["string", "null"]
        intentAvailable:
          type: boolean
        intentUnavailableReason:
          type: ["string", "null"]
        existingPosition:
          oneOf:
            - $ref: "#/components/schemas/AdjustmentSourcePosition"
            - type: "null"
        proposedPosition:
          oneOf:
            - $ref: "#/components/schemas/ExecutionTicket"
            - type: "null"
        netCashflow:
          type: ["number", "null"]
        greeksBefore:
          oneOf:
            - $ref: "#/components/schemas/PortfolioGreeksSnapshot"
            - type: "null"
        greeksAfter:
          oneOf:
            - $ref: "#/components/schemas/PortfolioGreeksSnapshot"
            - type: "null"
        breakEvenBefore:
          type: array
          items:
            $ref: "#/components/schemas/BreakEvenPrice"
        breakEvenBeforeUnavailableReason:
          type: ["string", "null"]
        breakEvenAfter:
          type: array
          items:
            $ref: "#/components/schemas/BreakEvenPrice"
        breakEvenAfterUnavailableReason:
          type: ["string", "null"]
        portfolioExposureBefore:
          $ref: "#/components/schemas/PortfolioSnapshot"
        portfolioExposureAfter:
          oneOf:
            - $ref: "#/components/schemas/PortfolioSnapshot"
            - type: "null"
        comparisons:
          type: array
          items:
            $ref: "#/components/schemas/MetricComparison"
        riskWarnings:
          type: array
          items:
            $ref: "#/components/schemas/TradeAdjustmentWarning"
        credentialsConfigured:
          type: boolean
        brokerConnected:
          type: ["boolean", "null"]
        lastBrokerCheckAt:
          type: ["string", "null"]
        accountValue:
          type: number
        generatedAt:
          type: string

    # ─── Full-Auto execution (Phase 6) ───────────────────────────────────────────
    AutoExecutionStatus:
      type: object
      required: [executionMode, autoExecuteEnabled, armed, guardrails, today, blockReason]
      properties:
        executionMode:
          type: string
          enum: [manual, semi_auto, full_auto]
        autoExecuteEnabled:
          type: boolean
        armed:
          type: boolean
        guardrails:
          type: object
          required: [maxTradesPerDay, maxConcurrentPositions, minRavishScore, maxDailyLossPct, quantityPerTrade]
          properties:
            maxTradesPerDay:
              type: integer
            maxConcurrentPositions:
              type: integer
            minRavishScore:
              type: number
            maxDailyLossPct:
              type: number
            quantityPerTrade:
              type: integer
        today:
          type: object
          required: [tradesToday, concurrentPositions, dailyRealizedPnl, accountValue, remainingCapacity, dailyLossLimit]
          properties:
            tradesToday:
              type: integer
            concurrentPositions:
              type: integer
            dailyRealizedPnl:
              type: number
            accountValue:
              type: number
            remainingCapacity:
              type: integer
            dailyLossLimit:
              type: number
        blockReason:
          type: ["string", "null"]

    AutoDecision:
      type: object
      required: [symbol, strategy, decision, reason, ravishScore, tradeId, orderId]
      properties:
        symbol:
          type: ["string", "null"]
        strategy:
          type: ["string", "null"]
        decision:
          type: string
          enum: [executed, skipped, rejected, blocked]
        reason:
          type: string
        ravishScore:
          type: number
        tradeId:
          type: ["integer", "null"]
        orderId:
          type: ["string", "null"]

    AutoCycleResult:
      type: object
      required: [runId, ranAt, blocked, blockReason, scanned, executed, skipped, rejected, decisions]
      properties:
        runId:
          type: string
        ranAt:
          type: string
        blocked:
          type: boolean
        blockReason:
          type: ["string", "null"]
        scanned:
          type: integer
        executed:
          type: integer
        skipped:
          type: integer
        rejected:
          type: integer
        decisions:
          type: array
          items:
            $ref: "#/components/schemas/AutoDecision"

    # ─── Auto-Adjustment (Task #20) ──────────────────────────────────────────────
    AutoAdjustDecision:
      type: object
      required: [symbol, strategy, decision, action, reason, tradeId, realizedPnl]
      properties:
        symbol:
          type: ["string", "null"]
        strategy:
          type: ["string", "null"]
        decision:
          type: string
          enum: [executed, skipped, blocked]
        action:
          type: string
        reason:
          type: string
        tradeId:
          type: ["integer", "null"]
        realizedPnl:
          type: ["number", "null"]

    AutoAdjustCycleResult:
      type: object
      required: [runId, ranAt, blocked, blockReason, scanned, executed, skipped, decisions]
      properties:
        runId:
          type: string
        ranAt:
          type: string
        blocked:
          type: boolean
        blockReason:
          type: ["string", "null"]
        scanned:
          type: integer
        executed:
          type: integer
        skipped:
          type: integer
        decisions:
          type: array
          items:
            $ref: "#/components/schemas/AutoAdjustDecision"

    AutoExecutionLogEntry:
      type: object
      required: [id, runId, kind, decision, reason, symbol, strategy, ravishScore, tradeId, alpacaOrderId, createdAt]
      properties:
        id:
          type: integer
        runId:
          type: string
        kind:
          type: string
          enum: [open, adjust]
        decision:
          type: string
          enum: [executed, skipped, rejected, blocked]
        reason:
          type: string
        symbol:
          type: ["string", "null"]
        strategy:
          type: ["string", "null"]
        ravishScore:
          type: number
        tradeId:
          type: ["integer", "null"]
        alpacaOrderId:
          type: ["string", "null"]
        createdAt:
          type: string

    # ─── Backtest ──────────────────────────────────────────────────────────────
    BacktestInput:
      type: object
      required: [symbol, strategy, period]
      properties:
        symbol:
          type: string
        strategy:
          type: string
          enum: [iron_condor, iron_fly, calendar_spread]
        period:
          type: string
          enum: [1y, 2y, 5y]
        shortDelta:
          type: number
          default: 0.20
        wingWidth:
          type: ["integer", "null"]
        daysToExpiry:
          type: integer
          default: 45
        profitTarget:
          type: number
          default: 0.50
        stopLoss:
          type: number
          default: 2.0

    BacktestResult:
      type: object
      required: [id, symbol, strategy, period, winRate, avgWin, avgLoss, expectancy, maxDrawdown, sharpeRatio, sortinoRatio, totalTrades, totalReturn, equityCurve, createdAt]
      properties:
        id:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        period:
          type: string
        winRate:
          type: number
        avgWin:
          type: number
        avgLoss:
          type: number
        expectancy:
          type: number
        maxDrawdown:
          type: number
        sharpeRatio:
          type: number
        sortinoRatio:
          type: number
        totalTrades:
          type: integer
        totalReturn:
          type: number
        evPerTrade:
          type: number
        evMonthly:
          type: number
        evAnnualized:
          type: number
        equityCurve:
          type: array
          items:
            $ref: "#/components/schemas/EquityPoint"
        createdAt:
          type: string

    EquityPoint:
      type: object
      required: [date, value]
      properties:
        date:
          type: string
        value:
          type: number
        drawdown:
          type: ["number", "null"]

    # ─── Ravish Score ──────────────────────────────────────────────────────────
    RavishScore:
      type: object
      required: [symbol, strategy, totalScore, tier, popScore, evScore, thetaScore, winRateScore, liquidityScore, ivRankScore, recommendation]
      properties:
        symbol:
          type: string
        strategy:
          type: string
        totalScore:
          type: number
        tier:
          type: string
          enum: [elite, high_conviction, good, ignore]
        popScore:
          type: number
          description: POP component (30% weight)
        evScore:
          type: number
          description: EV component (25% weight)
        thetaScore:
          type: number
          description: Theta efficiency component (15% weight)
        winRateScore:
          type: number
          description: Historical win rate component (15% weight)
        liquidityScore:
          type: number
          description: Liquidity component (10% weight)
        ivRankScore:
          type: number
          description: IV Rank component (5% weight)
        pop:
          type: number
        ev:
          type: number
        theta:
          type: number
        ivRank:
          type: number
        recommendation:
          type: string

    # ─── Journal ───────────────────────────────────────────────────────────────
    JournalEntry:
      type: object
      required: [id, title, content, mood, createdAt]
      properties:
        id:
          type: integer
        tradeId:
          type: ["integer", "null"]
        title:
          type: string
        content:
          type: string
        mood:
          type: string
          enum: [confident, neutral, cautious, frustrated, excited]
        lessonLearned:
          type: ["string", "null"]
        thesis:
          type: ["string", "null"]
          description: The case for taking the trade — distinct from the general-purpose `content` notes field.
        entryReasoning:
          type: ["string", "null"]
          description: What specifically triggered entry.
        tags:
          type: array
          items:
            type: string
        strategy:
          type: ["string", "null"]
        entryCredit:
          type: ["number", "null"]
        maxProfit:
          type: ["number", "null"]
        maxLoss:
          type: ["number", "null"]
        ev:
          type: ["number", "null"]
        pop:
          type: ["number", "null"]
        ravishScore:
          type: ["number", "null"]
        exitReason:
          type: ["string", "null"]
        realizedPnl:
          type: ["number", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string

    JournalEntryInput:
      type: object
      required: [title, content, mood]
      properties:
        tradeId:
          type: ["integer", "null"]
        title:
          type: string
        content:
          type: string
        mood:
          type: string
          enum: [confident, neutral, cautious, frustrated, excited]
        lessonLearned:
          type: string
        thesis:
          type: string
        entryReasoning:
          type: string
        tags:
          type: array
          items:
            type: string
        strategy:
          type: ["string", "null"]
        entryCredit:
          type: ["number", "null"]
        maxProfit:
          type: ["number", "null"]
        maxLoss:
          type: ["number", "null"]
        ev:
          type: ["number", "null"]
        pop:
          type: ["number", "null"]
        ravishScore:
          type: ["number", "null"]
        exitReason:
          type: ["string", "null"]
        realizedPnl:
          type: ["number", "null"]

    JournalEntryUpdate:
      type: object
      properties:
        title:
          type: string
        content:
          type: string
        mood:
          type: string
          enum: [confident, neutral, cautious, frustrated, excited]
        lessonLearned:
          type: string
        thesis:
          type: string
        entryReasoning:
          type: string
        tags:
          type: array
          items:
            type: string

    # ─── AI ────────────────────────────────────────────────────────────────────
    AiChatInput:
      type: object
      required: [message]
      properties:
        message:
          type: string
        context:
          type: string
          description: Optional context like current page or symbol
        mode:
          type: string
          enum: [explain_trade, teach_greeks, risk_coach, strategy_coach, quiz, value_research]
          description: >-
            Optional explicit coach mode. When set, the assistant routes
            deterministically into that mode instead of relying on keyword
            auto-detect. Omit for auto-detect.
        level:
          type: string
          enum: [beginner, advanced]
          description: >-
            Optional explanation depth for narrated answers. Beginner defines
            jargon and keeps it simple; advanced is concise and precise.

    AiAnswer:
      type: object
      required: [id, message, createdAt]
      properties:
        id:
          type: integer
        message:
          type: string
        data:
          type: object
          additionalProperties: true
          description: Structured data if the AI returned actionable results
        createdAt:
          type: string

    AiMessage:
      type: object
      required: [id, role, message, createdAt]
      properties:
        id:
          type: integer
        role:
          type: string
          enum: [user, assistant]
        message:
          type: string
        createdAt:
          type: string

    # ─── AI Trading Coach ────────────────────────────────────────────────────────
    ExplainTradeInput:
      type: object
      properties:
        scannerResultId:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
          enum: [iron_condor, iron_fly, calendar_spread, earnings]

    CoachLeg:
      type: object
      required: [side, optionType, strike, expiration, quantity, openPrice]
      properties:
        side:
          type: string
          enum: [buy, sell]
        optionType:
          type: string
          enum: [call, put]
        strike:
          type: number
        expiration:
          type: string
        quantity:
          type: integer
        openPrice:
          type: number

    CoachGreeks:
      type: object
      required: [delta, gamma, theta, vega]
      properties:
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number

    TradeExplanation:
      type: object
      required:
        [symbol, symbolName, strategy, strategyLabel, underlyingPrice, expiration, daysToExpiry, quantity, legs, credit, isCredit, maxProfit, maxLoss, pop, ev, returnOnCapital, profitZone, greeks, greeksPlain, ravishScore, ravishTier, ravishBreakdown, ivRank, assignmentRisk, keyPoints, rejected, disclaimer]
      properties:
        symbol: { type: string }
        symbolName: { type: string }
        strategy: { type: string }
        strategyLabel: { type: string }
        underlyingPrice: { type: number }
        expiration: { type: string }
        daysToExpiry: { type: integer }
        quantity: { type: integer }
        legs:
          type: array
          items:
            $ref: "#/components/schemas/CoachLeg"
        credit: { type: number }
        isCredit: { type: boolean }
        maxProfit: { type: number }
        maxLoss: { type: number }
        pop: { type: number }
        ev: { type: number }
        returnOnCapital: { type: number }
        breakevenLow:
          type: number
          nullable: true
        breakevenHigh:
          type: number
          nullable: true
        profitZone: { type: string }
        greeks:
          $ref: "#/components/schemas/CoachGreeks"
        greeksPlain:
          type: object
          required: [delta, theta, vega, gamma]
          properties:
            delta: { type: string }
            theta: { type: string }
            vega: { type: string }
            gamma: { type: string }
        ravishScore: { type: number }
        ravishTier: { type: string }
        ravishBreakdown:
          type: object
          required: [popScore, evScore, thetaScore, winRateScore, liquidityScore, ivRankScore]
          properties:
            popScore: { type: number }
            evScore: { type: number }
            thetaScore: { type: number }
            winRateScore: { type: number }
            liquidityScore: { type: number }
            ivRankScore: { type: number }
        ivRank: { type: number }
        assignmentRisk: { type: string }
        keyPoints:
          type: array
          items: { type: string }
        rejected: { type: boolean }
        rejectReason:
          type: string
          nullable: true
        disclaimer: { type: string }

    ExplainTradeResult:
      type: object
      required: [explanation, narrative, source, llmAvailable]
      properties:
        id:
          type: integer
        explanation:
          $ref: "#/components/schemas/TradeExplanation"
        narrative: { type: string }
        source:
          type: string
          enum: [llm, template]
        llmAvailable: { type: boolean }

    TeachGreekInput:
      type: object
      required: [greek]
      properties:
        greek:
          type: string
          enum: [delta, theta, gamma, vega]
        symbol:
          type: string

    GreekLesson:
      type: object
      required: [greek, title, symbol, oneLiner, definition, forPremiumSellers, example, exampleValue, keyPoints, disclaimer]
      properties:
        greek:
          type: string
          enum: [delta, theta, gamma, vega]
        title: { type: string }
        symbol: { type: string }
        oneLiner: { type: string }
        definition: { type: string }
        forPremiumSellers: { type: string }
        example: { type: string }
        exampleValue: { type: number }
        keyPoints:
          type: array
          items: { type: string }
        disclaimer: { type: string }

    TeachGreekResult:
      type: object
      required: [lesson, narrative, source, llmAvailable]
      properties:
        lesson:
          $ref: "#/components/schemas/GreekLesson"
        narrative: { type: string }
        source:
          type: string
          enum: [llm, template]
        llmAvailable: { type: boolean }

    LearnSection:
      type: object
      required: [heading, body]
      properties:
        heading: { type: string }
        body: { type: string }

    LearnQuizQuestion:
      type: object
      required: [prompt, options, correctIndex, explanation]
      properties:
        prompt: { type: string }
        options:
          type: array
          items: { type: string }
        correctIndex: { type: integer }
        explanation: { type: string }

    LearnContent:
      type: object
      required: [slug, title, subtitle, sections, keyPoints, disclaimer]
      properties:
        slug: { type: string }
        title: { type: string }
        subtitle: { type: string }
        sections:
          type: array
          items:
            $ref: "#/components/schemas/LearnSection"
        workedExamples:
          type: array
          description: Optional worked examples with live deterministic numbers.
          items:
            $ref: "#/components/schemas/LearnSection"
        quiz:
          type: array
          description: Optional inline mini-quiz checks for the lesson.
          items:
            $ref: "#/components/schemas/LearnQuizQuestion"
        keyPoints:
          type: array
          items: { type: string }
        disclaimer: { type: string }

    QuizInput:
      type: object
      properties:
        topic:
          type: string
          enum: [greeks, strategy, risk, mixed]
        count:
          type: integer

    QuizQuestion:
      type: object
      required: [id, prompt, options]
      properties:
        id: { type: string }
        prompt: { type: string }
        options:
          type: array
          items: { type: string }

    GeneratedQuiz:
      type: object
      required: [quizId, topic, questions]
      properties:
        quizId: { type: string }
        topic: { type: string }
        questions:
          type: array
          items:
            $ref: "#/components/schemas/QuizQuestion"

    GradeQuizInput:
      type: object
      required: [quizId, answers]
      properties:
        quizId: { type: string }
        answers:
          type: array
          items: { type: integer }

    QuizGradeItem:
      type: object
      required: [id, prompt, yourAnswer, correctAnswer, correct, explanation]
      properties:
        id: { type: string }
        prompt: { type: string }
        yourAnswer: { type: integer }
        correctAnswer: { type: integer }
        correct: { type: boolean }
        explanation: { type: string }

    QuizGradeResult:
      type: object
      required: [topic, score, total, percent, message, results, disclaimer]
      properties:
        topic: { type: string }
        score: { type: integer }
        total: { type: integer }
        percent: { type: number }
        message: { type: string }
        results:
          type: array
          items:
            $ref: "#/components/schemas/QuizGradeItem"
        disclaimer: { type: string }

    QuizProgressAttempt:
      type: object
      required: [id, topic, score, total, percent, createdAt]
      properties:
        id: { type: integer }
        topic: { type: string }
        score: { type: integer }
        total: { type: integer }
        percent: { type: number }
        createdAt: { type: string }

    QuizProgressTopicBest:
      type: object
      required: [topic, bestPercent, attempts]
      properties:
        topic: { type: string }
        bestPercent: { type: number }
        attempts: { type: integer }

    QuizProgress:
      type: object
      required:
        [
          attempts,
          bestByTopic,
          totalAttempts,
          averagePercent,
          streak,
          improvement,
          firstPercent,
          latestPercent,
        ]
      properties:
        attempts:
          type: array
          items:
            $ref: "#/components/schemas/QuizProgressAttempt"
        bestByTopic:
          type: array
          items:
            $ref: "#/components/schemas/QuizProgressTopicBest"
        totalAttempts: { type: integer }
        averagePercent: { type: number }
        streak:
          type: integer
          description: Consecutive calendar days (UTC) with at least one quiz attempt, counting back from today/yesterday. 0 if the streak is broken.
        improvement:
          type: number
          description: Percentage-point change from the earliest attempt to the most recent attempt (latestPercent - firstPercent). Can be negative.
        firstPercent:
          type: number
          description: Score percent of the learner's earliest recorded attempt.
        latestPercent:
          type: number
          description: Score percent of the learner's most recent attempt.

    JournalReviewInput:
      type: object
      required: [tradeId]
      properties:
        tradeId:
          type: integer

    JournalReviewResult:
      type: object
      required: [tradeId, symbol, strategy, review, lessonLearned, source, llmAvailable]
      properties:
        tradeId: { type: integer }
        symbol: { type: string }
        strategy: { type: string }
        review: { type: string }
        lessonLearned: { type: string }
        source:
          type: string
          enum: [llm, template]
        llmAvailable: { type: boolean }

    CoachLesson:
      type: object
      required: [id, kind, topic, title, content, source, createdAt]
      properties:
        id: { type: integer }
        kind: { type: string }
        topic: { type: string }
        title: { type: string }
        content: { type: string }
        source: { type: string }
        createdAt: { type: string }

    # ─── Notifications (Platform, Phase 4 Sprint 56) ────────────────────────────
    PlatformNotification:
      type: object
      required: [id, type, title, message, dataSource, relatedSymbol, isRead, severity, previousValue, currentValue, evidence, recommendedAction, createdAt]
      properties:
        id: { type: integer }
        type:
          type: string
          enum:
            [
              watchlist_target_crossed,
              risk_cap_breached,
              decision_change,
              valuation_change,
              quality_change,
              committee_change,
              tomnash_change,
              financial_deterioration,
              dividend_change,
              earnings_alert,
              portfolio_drift,
              sector_concentration_breach,
              position_sizing_breach,
              opportunity_match,
            ]
        title: { type: string }
        message: { type: string }
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        relatedSymbol: { type: ["string", "null"] }
        isRead: { type: boolean }
        severity:
          type: string
          enum: [info, warning, critical]
        previousValue: { type: ["string", "null"] }
        currentValue: { type: ["string", "null"] }
        evidence: { type: array, items: { type: string } }
        recommendedAction: { type: ["string", "null"] }
        createdAt: { type: string }

    PlatformNotificationUpdate:
      type: object
      required: [isRead]
      properties:
        isRead: { type: boolean }

    # Phase 16 — Institutional Monitoring & Alerts Engine.
    MonitoringAlertNote:
      type: object
      required: [id, notificationId, symbol, note, createdAt, updatedAt]
      properties:
        id: { type: integer }
        notificationId: { type: ["integer", "null"] }
        symbol: { type: ["string", "null"] }
        note: { type: string }
        createdAt: { type: string }
        updatedAt: { type: string }

    MonitoringAlertNoteCreate:
      type: object
      required: [note]
      properties:
        notificationId: { type: ["integer", "null"] }
        symbol: { type: ["string", "null"] }
        note: { type: string }

    MonitoringAlertNoteUpdate:
      type: object
      required: [note]
      properties:
        note: { type: string }

    # ─── Dashboard Workspaces (Phase 10) ────────────────────────────────────
    WorkspaceWidgetConfigEntry:
      type: object
      required: [id, visible, size, order]
      properties:
        id: { type: string }
        visible: { type: boolean }
        size:
          type: string
          enum: [normal, compact]
        order: { type: integer }

    DashboardWorkspace:
      type: object
      required: [id, name, isDefault, isActive, widgetConfig, createdAt, updatedAt]
      properties:
        id: { type: integer }
        name: { type: string }
        isDefault: { type: boolean }
        isActive: { type: boolean }
        widgetConfig:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceWidgetConfigEntry"
        createdAt: { type: string }
        updatedAt: { type: string }

    DashboardWorkspaceCreate:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 60 }
        widgetConfig:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceWidgetConfigEntry"

    DashboardWorkspaceUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 60 }
        widgetConfig:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceWidgetConfigEntry"

    DashboardWorkspaceDuplicate:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 60 }

    # ─── Cross-Engine Daily Report (Phase 5, Sprint 68) ────────────────────────
    ReportWatchlistCrossing:
      type: object
      required: [symbol, currentPrice, priceTargetCrossed, marginOfSafetyTargetCrossed]
      properties:
        symbol: { type: string }
        currentPrice: { type: ["number", "null"] }
        priceTargetCrossed: { type: ["boolean", "null"] }
        marginOfSafetyTargetCrossed: { type: ["boolean", "null"] }

    ReportEngine1Summary:
      type: object
      required: [macro, watchlistTotalItems, watchlistCrossings]
      properties:
        macro:
          $ref: "#/components/schemas/ValueMacroContext"
        watchlistTotalItems: { type: integer }
        watchlistCrossings:
          type: array
          items:
            $ref: "#/components/schemas/ReportWatchlistCrossing"

    ReportEngine2Summary:
      type: object
      required: [risk]
      properties:
        risk:
          $ref: "#/components/schemas/TradingRiskAnalysis"

    ReportEngine3Summary:
      type: object
      required:
        [
          healthScore,
          healthLabel,
          openPositions,
          totalUnrealizedPnl,
          attentionCount,
          criticalCount,
          topOpportunitySymbol,
          topOpportunityRavishScore,
        ]
      properties:
        healthScore: { type: number }
        healthLabel: { type: string }
        openPositions: { type: integer }
        totalUnrealizedPnl: { type: number }
        attentionCount: { type: integer }
        criticalCount: { type: integer }
        topOpportunitySymbol: { type: ["string", "null"] }
        topOpportunityRavishScore: { type: ["number", "null"] }

    CrossEngineDailyReport:
      type: object
      required: [date, generatedAt, engine1, engine2, engine3, summary, disclaimer]
      properties:
        date: { type: string }
        generatedAt: { type: string }
        engine1:
          $ref: "#/components/schemas/ReportEngine1Summary"
        engine2:
          $ref: "#/components/schemas/ReportEngine2Summary"
        engine3:
          $ref: "#/components/schemas/ReportEngine3Summary"
        summary: { type: string }
        disclaimer: { type: string }

    NarrateCrossEngineDailyReportResult:
      type: object
      required: [narrative, narrativeSource]
      properties:
        narrative: { type: string }
        narrativeSource:
          type: string
          enum: [llm, template]

    # ─── Settings ──────────────────────────────────────────────────────────────
    Settings:
      type: object
      required: [executionMode, maxRiskPerTrade, maxPortfolioRisk, profitTarget50, profitTarget75, profitTarget90, stopLossMultiplier, alpacaConnected]
      properties:
        executionMode:
          type: string
          enum: [manual, semi_auto, full_auto]
        maxRiskPerTrade:
          type: number
          description: Max risk per trade as percentage of account (default 1%)
        maxPortfolioRisk:
          type: number
          description: Max portfolio risk as percentage (default 10%)
        profitTarget50:
          type: number
        profitTarget75:
          type: number
        profitTarget90:
          type: number
        stopLossMultiplier:
          type: number
          description: Stop loss as multiple of credit received (default 2x)
        alpacaConnected:
          type: boolean
          description: Whether the most recent Alpaca Paper Trading broker health check (GET /broker/health) authenticated successfully — computed, never client-settable, and honestly false until a check has actually been performed
        alpacaApiKey:
          type: ["string", "null"]
        scannerMode:
          type: string
          enum: [mock, live]
          description: Scanner data mode — mock (simulated) or live (provider chains)
        marketDataProvider:
          type: string
          enum: [mock, alpaca, polygon]
          description: Preferred options-data provider when in live mode
        defaultDte:
          type: integer
          description: Default days to expiry (default 45)
        shortDelta:
          type: number
          description: Default short delta (default 0.20)
        minIvRank:
          type: number
          description: Minimum IV rank to consider trades
        autoExecuteEnabled:
          type: boolean
          description: Full-auto master switch / kill switch (default false)
        autoMaxTradesPerDay:
          type: integer
        autoMaxConcurrentPositions:
          type: integer
        autoMinRavishScore:
          type: number
        autoMaxDailyLossPct:
          type: number
        autoQuantityPerTrade:
          type: integer
        adjDeltaDriftTrigger:
          type: number
          description: Net |delta| at which a position is flagged for adjustment
        adjPopDropTrigger:
          type: number
          description: POP drop (points vs entry) that flags a position
        adjShortStrikeProximityPct:
          type: number
          description: Spot-to-short-strike distance (%) considered "tested"
        adjIvExpansionTrigger:
          type: number
          description: IV expansion (% vs entry IV) that flags a position
        adjDteTrigger:
          type: integer
          description: DTE at/under which gamma-management kicks in
        autoAdjustEnabled:
          type: boolean
          description: Auto-adjustment master switch (close/de-risk only)
        eventRiskEnabled:
          type: boolean
          description: Event Risk Filter master switch
        eventRiskBlockEarningsShortPremium:
          type: boolean
          description: Block opening short-premium trades before earnings
        eventRiskAutoBlockHigh:
          type: boolean
          description: Block AutoPilot auto-execution when event risk is high
        fundamentalsProvider:
          type: string
          enum: [simulated, alpha_vantage, financial_modeling_prep]
          description: Value-investing fundamentals provider (only "simulated" is wired today)
        fundamentalsConnected:
          type: boolean
          description: Whether a live fundamentals provider is configured (always false today)
        fundamentalsLastFetchedAt:
          type: ["string", "null"]
          description: ISO timestamp of the most recent successful live fundamentals fetch (null if a live provider has never been reached this process)
        fundamentalsStalenessHours:
          type: integer
          description: How old (in hours) live fundamentals may be before the UI flags them as stale and nudges a refresh (defaults to 24)
        fundamentalsAutoRefresh:
          type: boolean
          description: Auto re-fetch live fundamentals once data crosses the staleness threshold (no effect for simulated data)
        investingRiskFreeRate:
          type: number
          description: Risk-free rate assumption for the Investing Engine's valuation models (default 0.045, matches the options engine's RISK_FREE constant)
        investingDefaultDiscountRate:
          type: number
          description: Default discount-rate assumption for the Investing Engine's DCF/Buffett valuation models (default 0.09)
        investingFilingsProvider:
          type: string
          description: Filings data source for Annual Report Analysis (only "edgar" is wired today)
        tradingDataProvider:
          type: string
          description: Institutional Trading Engine market-data provider (only "simulated" is wired today)
        tradingDataConnected:
          type: boolean
          description: Whether a live trading market-data provider is configured (always false today)
        tradingAccountValue:
          type: ["number", "null"]
          description: Account value used to size Engine 2 (trading) position risk — distinct from Engine 3's options-derived account value; null until the user sets it
        alertsEnabled:
          type: boolean
          description: Whether the in-app notification center generates alerts for this user (watchlist target-crossing, risk hard-cap breach); default true

    FundamentalsProviderStatus:
      type: object
      required: [provider, label, selected, keyPresent, state, message]
      properties:
        provider:
          type: string
          description: Live provider id (financial_modeling_prep, alpha_vantage)
        label:
          type: string
          description: Human-readable provider name
        selected:
          type: boolean
          description: Whether this provider is the one selected in Settings
        keyPresent:
          type: boolean
          description: Whether the provider's API key is configured in the environment
        state:
          type: string
          enum: [ok, rate_limited, unreachable, no_data, not_configured, idle]
          description: >-
            Derived health. ok = responding; rate_limited = temporary, recovers
            on its own; unreachable = misconfigured/down; no_data = key works but
            no data; not_configured = no API key; idle = configured, not yet queried
        message:
          type: string
          description: Operator-facing explanation of the current state
        lastSuccessAt:
          type: ["string", "null"]
          description: ISO timestamp of the most recent successful live fetch
        lastFallbackAt:
          type: ["string", "null"]
          description: ISO timestamp of the most recent fallback to simulated data
        lastFallbackReason:
          type: ["string", "null"]
          enum: [rate_limit, error, no_data, null]
          description: Reason for the most recent fallback

    FundamentalsProviderStatusResponse:
      type: object
      required: [statuses]
      properties:
        statuses:
          type: array
          items: { $ref: "#/components/schemas/FundamentalsProviderStatus" }

    SettingsUpdate:
      type: object
      properties:
        executionMode:
          type: string
          enum: [manual, semi_auto, full_auto]
        maxRiskPerTrade:
          type: number
        maxPortfolioRisk:
          type: number
        profitTarget50:
          type: number
        profitTarget75:
          type: number
        profitTarget90:
          type: number
        stopLossMultiplier:
          type: number
        alpacaApiKey:
          type: string
        scannerMode:
          type: string
          enum: [mock, live]
        marketDataProvider:
          type: string
          enum: [mock, alpaca, polygon]
        defaultDte:
          type: integer
        shortDelta:
          type: number
        minIvRank:
          type: number
        autoExecuteEnabled:
          type: boolean
        autoMaxTradesPerDay:
          type: integer
        autoMaxConcurrentPositions:
          type: integer
        autoMinRavishScore:
          type: number
        autoMaxDailyLossPct:
          type: number
        autoQuantityPerTrade:
          type: integer
        adjDeltaDriftTrigger:
          type: number
        adjPopDropTrigger:
          type: number
        adjShortStrikeProximityPct:
          type: number
        adjIvExpansionTrigger:
          type: number
        adjDteTrigger:
          type: integer
        autoAdjustEnabled:
          type: boolean
        eventRiskEnabled:
          type: boolean
        eventRiskBlockEarningsShortPremium:
          type: boolean
        eventRiskAutoBlockHigh:
          type: boolean
        fundamentalsProvider:
          type: string
          enum: [simulated, alpha_vantage, financial_modeling_prep]
        fundamentalsStalenessHours:
          type: integer
        fundamentalsAutoRefresh:
          type: boolean
        investingRiskFreeRate:
          type: number
        investingDefaultDiscountRate:
          type: number
        investingFilingsProvider:
          type: string
        tradingDataProvider:
          type: string
        tradingAccountValue:
          type: number
        alertsEnabled:
          type: boolean

    # ─── Earnings & Market Data ──────────────────────────────────────────────────
    EarningsPlay:
      type: object
      required: [symbol, name, price, daysToEarnings, iv, ivRank, expectedMovePct, expectedMoveDollars, historicalMovePct, ivCrushPotentialPct, recommendedStrategy, rationale]
      properties:
        symbol:
          type: string
        name:
          type: string
        price:
          type: number
        earningsDate:
          type: ["string", "null"]
        daysToEarnings:
          type: integer
        iv:
          type: number
        ivRank:
          type: number
        expectedMovePct:
          type: number
        expectedMoveDollars:
          type: number
        historicalMovePct:
          type: number
        ivCrushPotentialPct:
          type: number
        recommendedStrategy:
          type: string
          enum: [iron_fly, iron_condor, calendar_spread]
        rationale:
          type: string

    ThetaBreakdown:
      type: object
      required: [key, theta]
      properties:
        key:
          type: string
        theta:
          type: number

    ThetaIncome:
      type: object
      required: [daily, weekly, monthly, annualized, bySymbol, byStrategy]
      properties:
        daily:
          type: number
        weekly:
          type: number
        monthly:
          type: number
        annualized:
          type: number
        bySymbol:
          type: array
          items:
            $ref: "#/components/schemas/ThetaBreakdown"
        byStrategy:
          type: array
          items:
            $ref: "#/components/schemas/ThetaBreakdown"

    MarketDataHealth:
      type: object
      required: [provider, requestedProvider, mode, connected, reason, symbolsScanned, contractsScanned, rejectedByLiquidity, rejectedByRisk, positiveEvFound]
      properties:
        provider:
          type: string
          enum: [mock, alpaca, polygon]
        requestedProvider:
          type: string
          enum: [mock, alpaca, polygon]
        mode:
          type: string
          enum: [mock, live]
        connected:
          type: boolean
        reason:
          type: string
        lastChainUpdate:
          type: ["string", "null"]
        symbolsScanned:
          type: integer
        contractsScanned:
          type: integer
        rejectedByLiquidity:
          type: integer
        rejectedByRisk:
          type: integer
        positiveEvFound:
          type: integer
        lastScannedAt:
          type: ["string", "null"]

    BrokerHealth:
      type: object
      required: [connected, authenticationSuccessful, reason, checkedAt]
      properties:
        connected:
          type: boolean
          description: True only when Alpaca's own account endpoint was reached and authenticated successfully this check.
        authenticationSuccessful:
          type: boolean
          description: True only when the configured credentials were accepted by Alpaca — currently always equal to `connected`, kept as a distinct field since a future check may reach Alpaca but fail some other way.
        accountStatus:
          type: ["string", "null"]
          description: Alpaca's own account status string (e.g. ACTIVE), null when not connected.
        buyingPower:
          type: ["number", "null"]
        cashBalance:
          type: ["number", "null"]
        portfolioValue:
          type: ["number", "null"]
        openPositionsCount:
          type: ["integer", "null"]
        openOrdersCount:
          type: ["integer", "null"]
        lastSuccessfulCheckAt:
          type: ["string", "null"]
          description: Timestamp of the most recent check that authenticated successfully — never updated by a failed check, so a broken connection never appears "recently successful."
        reason:
          type: string
          description: Human-readable explanation of the current status (never connected/not connected/authentication failed).
        checkedAt:
          type: string
          description: Timestamp of this specific check.

    NormalizedOrderStatus:
      type: string
      enum: [new, accepted, pending, partially_filled, filled, cancelled, rejected, expired, unknown]
      description: This platform's own normalized order-lifecycle bucket, mapped from Alpaca's larger raw status vocabulary — see lib/providers/alpacaOrderLifecycle.ts.

    BrokerOrder:
      type: object
      required: [id, symbol, side, qty, type, status, normalizedStatus, filledQty, filledAvgPrice, submittedAt]
      properties:
        id:
          type: string
        symbol:
          type: string
        side:
          type: string
        qty:
          type: number
        type:
          type: string
        status:
          type: string
          description: Alpaca's own raw status string, unmodified.
        normalizedStatus:
          $ref: "#/components/schemas/NormalizedOrderStatus"
        filledQty:
          type: number
        filledAvgPrice:
          type: ["number", "null"]
        submittedAt:
          type: ["string", "null"]

    BrokerOrdersResult:
      type: object
      required: [available, unavailableReason, orders, checkedAt]
      properties:
        available:
          type: boolean
        unavailableReason:
          type: ["string", "null"]
        orders:
          type: array
          items:
            $ref: "#/components/schemas/BrokerOrder"
        checkedAt:
          type: string

    BrokerOrderResult:
      type: object
      required: [available, unavailableReason, order, checkedAt]
      properties:
        available:
          type: boolean
        unavailableReason:
          type: ["string", "null"]
        order:
          oneOf:
            - $ref: "#/components/schemas/BrokerOrder"
            - type: "null"
        checkedAt:
          type: string

    BrokerPosition:
      type: object
      required: [symbol, qty, side, marketValue, avgEntryPrice, unrealizedPl]
      properties:
        symbol:
          type: string
        qty:
          type: number
        side:
          type: string
        marketValue:
          type: number
        avgEntryPrice:
          type: number
        unrealizedPl:
          type: number

    BrokerPositionsResult:
      type: object
      required: [available, unavailableReason, positions, checkedAt]
      properties:
        available:
          type: boolean
        unavailableReason:
          type: ["string", "null"]
        positions:
          type: array
          items:
            $ref: "#/components/schemas/BrokerPosition"
        checkedAt:
          type: string

    NormalizedLocalStatus:
      type: string
      enum: [pending, open, closed, unknown]
      description: The local trades table's own coarse status vocabulary, normalized for comparison against NormalizedOrderStatus.

    ReconciliationIssueType:
      type: string
      enum: [missing_at_broker, missing_locally, status_mismatch, quantity_mismatch, symbol_mismatch]

    OrderReconciliationEntry:
      type: object
      required:
        [
          tradeId,
          alpacaOrderId,
          localSymbol,
          brokerSymbol,
          localStatus,
          brokerStatus,
          brokerRawStatus,
          localQuantity,
          brokerQuantity,
          filledQuantity,
          averageFillPrice,
          issues,
        ]
      properties:
        tradeId:
          type: ["integer", "null"]
        alpacaOrderId:
          type: ["string", "null"]
        localSymbol:
          type: ["string", "null"]
        brokerSymbol:
          type: ["string", "null"]
        localStatus:
          oneOf:
            - $ref: "#/components/schemas/NormalizedLocalStatus"
            - type: "null"
        brokerStatus:
          oneOf:
            - $ref: "#/components/schemas/NormalizedOrderStatus"
            - type: "null"
        brokerRawStatus:
          type: ["string", "null"]
        localQuantity:
          type: ["number", "null"]
        brokerQuantity:
          type: ["number", "null"]
        filledQuantity:
          type: ["number", "null"]
        averageFillPrice:
          type: ["number", "null"]
        issues:
          type: array
          items:
            $ref: "#/components/schemas/ReconciliationIssueType"

    PositionReconciliationEntry:
      type: object
      required: [occSymbol, tradeId, localQuantity, brokerQuantity, mismatch, detail]
      properties:
        occSymbol:
          type: string
        tradeId:
          type: ["integer", "null"]
        localQuantity:
          type: ["number", "null"]
        brokerQuantity:
          type: ["number", "null"]
        mismatch:
          type: boolean
        detail:
          type: string

    ReconciliationResult:
      type: object
      required:
        [
          available,
          unavailableReason,
          generatedAt,
          localOrdersConsidered,
          brokerOrdersConsidered,
          orders,
          positions,
          issueCount,
          fullyReconciled,
        ]
      properties:
        available:
          type: boolean
          description: False when credentials are missing or Alpaca could not be reached/authenticated — never a fabricated reconciled result in that case.
        unavailableReason:
          type: ["string", "null"]
        generatedAt:
          type: string
          description: When this reconciliation was computed — the manual, on-demand "last reconciliation time."
        localOrdersConsidered:
          type: integer
          description: Count of local, trackable (non-mock, non-closed) trade records compared.
        brokerOrdersConsidered:
          type: integer
        orders:
          type: array
          items:
            $ref: "#/components/schemas/OrderReconciliationEntry"
        positions:
          type: array
          items:
            $ref: "#/components/schemas/PositionReconciliationEntry"
        issueCount:
          type: integer
        fullyReconciled:
          type: boolean
          description: True only when available and zero issues were found across every order and position entry.

    ReconciliationReportSummary:
      type: object
      required:
        [
          id,
          generatedAt,
          available,
          unavailableReason,
          localOrdersConsidered,
          brokerOrdersConsidered,
          issueCount,
          fullyReconciled,
          createdAt,
        ]
      properties:
        id:
          type: integer
        generatedAt:
          type: string
        available:
          type: boolean
        unavailableReason:
          type: ["string", "null"]
        localOrdersConsidered:
          type: integer
        brokerOrdersConsidered:
          type: integer
        issueCount:
          type: integer
        fullyReconciled:
          type: boolean
        createdAt:
          type: string
          description: When this snapshot was persisted (distinct from generatedAt, the moment the comparison itself ran).

    ReconciliationReportDetail:
      allOf:
        - $ref: "#/components/schemas/ReconciliationReportSummary"
        - type: object
          required: [detail]
          properties:
            detail:
              $ref: "#/components/schemas/ReconciliationResult"

    # ─── Performance ───────────────────────────────────────────────────────────
    PerformanceAnalytics:
      type: object
      required:
        [
          winRate,
          avgWin,
          avgLoss,
          expectancy,
          totalTrades,
          totalReturn,
          totalCapitalDeployed,
          returnOnCapital,
          profitFactor,
          maxDrawdown,
          sharpeRatio,
          sortinoRatio,
          avgHoldingDays,
          thetaCollected,
          totalCommission,
          totalSlippage,
          commissionImpactPct,
          actualPop,
          expectedPop,
          bestStrategy,
          worstStrategy,
          bestTickers,
          worstTickers,
          monthlyReturns,
          strategyBreakdown,
        ]
      properties:
        winRate:
          type: number
        avgWin:
          type: number
        avgLoss:
          type: number
        expectancy:
          type: number
        totalTrades:
          type: integer
        totalReturn:
          type: number
        totalCapitalDeployed:
          type: number
        returnOnCapital:
          type: number
        profitFactor:
          type: number
        maxDrawdown:
          type: number
        sharpeRatio:
          type: number
        sortinoRatio:
          type: number
        avgHoldingDays:
          type: number
        thetaCollected:
          type: number
        totalCommission:
          type: number
        totalSlippage:
          type: number
        commissionImpactPct:
          type: number
        actualPop:
          type: number
        expectedPop:
          type: number
        bestStrategy:
          type: string
        worstStrategy:
          type: string
        bestTickers:
          type: array
          items:
            $ref: "#/components/schemas/TickerStat"
        worstTickers:
          type: array
          items:
            $ref: "#/components/schemas/TickerStat"
        monthlyReturns:
          type: array
          items:
            $ref: "#/components/schemas/MonthlyReturn"
        strategyBreakdown:
          type: array
          items:
            $ref: "#/components/schemas/PerformanceBreakdownBucket"

    TickerStat:
      type: object
      required: [symbol, totalTrades, winRate, totalReturn]
      properties:
        symbol:
          type: string
        totalTrades:
          type: integer
        winRate:
          type: number
        totalReturn:
          type: number

    PerformanceBreakdownBucket:
      type: object
      required:
        [
          key,
          label,
          totalTrades,
          winRate,
          totalReturn,
          avgReturn,
          profitFactor,
          expectancy,
          returnOnCapital,
          actualPop,
          expectedPop,
          avgHoldingDays,
          thetaCollected,
        ]
      properties:
        key:
          type: string
        label:
          type: string
        totalTrades:
          type: integer
        winRate:
          type: number
        totalReturn:
          type: number
        avgReturn:
          type: number
        profitFactor:
          type: number
        expectancy:
          type: number
        returnOnCapital:
          type: number
        actualPop:
          type: number
        expectedPop:
          type: number
        avgHoldingDays:
          type: number
        thetaCollected:
          type: number

    MonthlyReturn:
      type: object
      required: [month, return, trades]
      properties:
        month:
          type: string
        return:
          type: number
        trades:
          type: integer

    StrategyStats:
      type: object
      required: [strategy, winRate, totalTrades, avgReturn]
      properties:
        strategy:
          type: string
        winRate:
          type: number
        totalTrades:
          type: integer
        avgReturn:
          type: number
        totalReturn:
          type: number

    ScoreCard:
      type: object
      required: [score, label, detail]
      properties:
        score:
          type: integer
        label:
          type: string
        detail:
          type: string

    PortfolioHealthComponent:
      type: object
      required: [key, label, score, weight, detail]
      properties:
        key:
          type: string
        label:
          type: string
        score:
          type: integer
        weight:
          type: number
        detail:
          type: string

    ThreatCounts:
      type: object
      required: [green, yellow, red]
      properties:
        green:
          type: integer
        yellow:
          type: integer
        red:
          type: integer

    PortfolioHealth:
      type: object
      required:
        [health, exposure, riskConcentration, components, netDelta, netTheta, netVega, netGamma, largestSymbolPct, threatCounts]
      properties:
        health:
          $ref: "#/components/schemas/ScoreCard"
        exposure:
          $ref: "#/components/schemas/ScoreCard"
        riskConcentration:
          $ref: "#/components/schemas/ScoreCard"
        components:
          type: array
          items:
            $ref: "#/components/schemas/PortfolioHealthComponent"
        netDelta:
          type: number
        netTheta:
          type: number
        netVega:
          type: number
        netGamma:
          type: number
        largestSymbol:
          type: string
          nullable: true
        largestSymbolPct:
          type: number
        threatCounts:
          $ref: "#/components/schemas/ThreatCounts"

    MarketBriefingEvent:
      type: object
      required: [type, label, date, daysAway, impact]
      properties:
        type:
          type: string
        label:
          type: string
        date:
          type: string
        daysAway:
          type: integer
        impact:
          type: string
          enum: [low, medium, high]
        symbol:
          type: string
          nullable: true

    MarketBriefing:
      type: object
      required:
        [date, regime, regimeLabel, syntheticVix, vixLabel, avgIvRank, breadth, ivEnvironment, headline, drivers, catalysts]
      properties:
        date:
          type: string
        regime:
          type: string
          enum: [risk_on, neutral, risk_off]
        regimeLabel:
          type: string
        syntheticVix:
          type: number
        vixLabel:
          type: string
        avgIvRank:
          type: integer
        breadth:
          type: integer
        ivEnvironment:
          type: string
        headline:
          type: string
        drivers:
          type: array
          items:
            type: string
        catalysts:
          type: array
          items:
            $ref: "#/components/schemas/MarketBriefingEvent"

    MarketBriefingResponse:
      type: object
      required: [briefing, narration, narrationSource]
      properties:
        briefing:
          $ref: "#/components/schemas/MarketBriefing"
        narration:
          type: string
        narrationSource:
          type: string
          enum: [llm, template]

    ReportSummary:
      type: object
      required:
        [accountValue, openPositions, totalUnrealizedPnl, netDelta, netTheta, netVega, netGamma, portfolioRiskUsedPct, maxPortfolioRiskPct, withinLimits, thetaDaily, thetaMonthly]
      properties:
        accountValue:
          type: number
        openPositions:
          type: integer
        totalUnrealizedPnl:
          type: number
        netDelta:
          type: number
        netTheta:
          type: number
        netVega:
          type: number
        netGamma:
          type: number
        portfolioRiskUsedPct:
          type: number
        maxPortfolioRiskPct:
          type: number
        withinLimits:
          type: boolean
        thetaDaily:
          type: number
        thetaMonthly:
          type: number

    PositionThreat:
      type: object
      required:
        [tradeId, symbol, symbolName, strategy, strategyLabel, threatLevel, severity, action, actionLabel, currentPnl, currentPnlPercent, currentPop, daysToExpiry, rationale]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        symbolName:
          type: string
        strategy:
          type: string
        strategyLabel:
          type: string
        threatLevel:
          type: string
          enum: [green, yellow, red]
        severity:
          type: string
        action:
          type: string
        actionLabel:
          type: string
        currentPnl:
          type: number
        currentPnlPercent:
          type: number
        currentPop:
          type: number
        daysToExpiry:
          type: integer
        rationale:
          type: string

    ReportOpportunity:
      type: object
      required: [symbol, strategy, ravishScore, ravishTier, pop, ev, theta, eventRiskLevel]
      properties:
        symbol:
          type: string
        strategy:
          type: string
        ravishScore:
          type: integer
        ravishTier:
          type: string
        pop:
          type: number
        ev:
          type: number
        theta:
          type: number
        eventRiskLevel:
          type: string

    TradeToAvoid:
      type: object
      required: [symbol, symbolName, reason, level]
      properties:
        symbol:
          type: string
        symbolName:
          type: string
        reason:
          type: string
        level:
          type: string
        earningsInDays:
          type: integer
          nullable: true

    DailyReport:
      type: object
      required:
        [date, generatedAt, health, summary, market, positions, opportunities, tradesToAvoid, disclaimer]
      properties:
        id:
          type: integer
        date:
          type: string
        generatedAt:
          type: string
        briefing:
          type: string
        briefingSource:
          type: string
        health:
          $ref: "#/components/schemas/PortfolioHealth"
        summary:
          $ref: "#/components/schemas/ReportSummary"
        market:
          $ref: "#/components/schemas/MarketBriefing"
        positions:
          type: array
          items:
            $ref: "#/components/schemas/PositionThreat"
        opportunities:
          type: array
          items:
            $ref: "#/components/schemas/ReportOpportunity"
        tradesToAvoid:
          type: array
          items:
            $ref: "#/components/schemas/TradeToAvoid"
        disclaimer:
          type: string

    CompareReportsRequest:
      type: object
      required: [fromId, toId]
      properties:
        fromId:
          type: integer
        toId:
          type: integer

    CompareReportsResponse:
      type: object
      required: [narration, narrationSource]
      properties:
        narration:
          type: string
        narrationSource:
          type: string
          enum: [llm, template]

    DailyReportSummary:
      type: object
      required:
        [id, reportDate, healthScore, healthLabel, exposureScore, riskScore, openPositions, redCount, yellowCount, netTheta, createdAt]
      properties:
        id:
          type: integer
        reportDate:
          type: string
        healthScore:
          type: integer
        healthLabel:
          type: string
        exposureScore:
          type: integer
        riskScore:
          type: integer
        openPositions:
          type: integer
        redCount:
          type: integer
        yellowCount:
          type: integer
        netTheta:
          type: number
        briefing:
          type: string
        createdAt:
          type: string

    DeleteReportResult:
      type: object
      required: [id, deleted]
      properties:
        id:
          type: integer
        deleted:
          type: boolean

    RestorableReport:
      type: object
      required:
        [reportDate, healthScore, healthLabel, exposureScore, riskScore, openPositions, redCount, yellowCount, netTheta, briefing, briefingSource, payload, createdAt]
      properties:
        reportDate:
          type: string
        healthScore:
          type: integer
        healthLabel:
          type: string
        exposureScore:
          type: integer
        riskScore:
          type: integer
        openPositions:
          type: integer
        redCount:
          type: integer
        yellowCount:
          type: integer
        netTheta:
          type: number
        briefing:
          type: string
        briefingSource:
          type: string
        payload:
          type: object
          additionalProperties: true
        createdAt:
          type: string

    ClearReportsResult:
      type: object
      required: [deleted, reports]
      properties:
        deleted:
          type: integer
        reports:
          type: array
          items:
            $ref: "#/components/schemas/RestorableReport"

    RestoreReportsRequest:
      type: object
      required: [reports]
      properties:
        reports:
          type: array
          items:
            $ref: "#/components/schemas/RestorableReport"

    RestoreReportsResult:
      type: object
      required: [restored]
      properties:
        restored:
          type: integer

    # ─── Trading Journal (Engine 2, Phase 3 Sprint 39) ────────────────────────
    TradingJournalEntry:
      type: object
      required: [id, title, content, mood, tags, createdAt, updatedAt]
      properties:
        id:
          type: integer
        tradingPositionId:
          type: ["integer", "null"]
        title:
          type: string
        content:
          type: string
        mood:
          type: string
          enum: [confident, neutral, cautious, frustrated, excited]
        lessonLearned:
          type: ["string", "null"]
        tags:
          type: array
          items:
            type: string
        setupType:
          type: ["string", "null"]
        entryPrice:
          type: ["number", "null"]
        exitPrice:
          type: ["number", "null"]
        rMultiple:
          type: ["number", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string

    TradingJournalEntryInput:
      type: object
      required: [title, content, mood]
      properties:
        tradingPositionId:
          type: ["integer", "null"]
        title:
          type: string
        content:
          type: string
        mood:
          type: string
          enum: [confident, neutral, cautious, frustrated, excited]
        lessonLearned:
          type: string
        tags:
          type: array
          items:
            type: string
        setupType:
          type: string
        entryPrice:
          type: number
        exitPrice:
          type: number
        rMultiple:
          type: number

    TradingJournalEntryUpdate:
      type: object
      properties:
        tradingPositionId:
          type: ["integer", "null"]
        title:
          type: string
        content:
          type: string
        mood:
          type: string
          enum: [confident, neutral, cautious, frustrated, excited]
        lessonLearned:
          type: string
        tags:
          type: array
          items:
            type: string
        setupType:
          type: string
        entryPrice:
          type: number
        exitPrice:
          type: number
        rMultiple:
          type: number

    # ─── Market Structure (Engine 2, Phase 3 Sprint 40) ───────────────────────
    TradingTimeframe:
      type: string
      enum: ["1m", "5m", "15m", "1h", "1D"]

    TradingSwingPoint:
      type: object
      required: [time, price, kind]
      properties:
        time:
          type: string
        price:
          type: number
        kind:
          type: string
          enum: [high, low]

    TradingSupportResistanceZone:
      type: object
      required: [price, kind, strength]
      properties:
        price:
          type: number
        kind:
          type: string
          enum: [support, resistance]
        strength:
          type: integer

    # ─── Structure Shift Timeline (Engine 2, Phase 26 — Institutional Market
    # Structure Workbench) — reuses TradingMarketStructure's own scorer
    # repeatedly, zero new scoring.
    TradingStructureShiftEvent:
      type: object
      required: [time, type, label, price, detail]
      properties:
        time:
          type: string
        type:
          type: string
          enum:
            [
              higher_high,
              higher_low,
              lower_high,
              lower_low,
              trend_change,
              range_entry,
              range_exit,
              support_test,
              resistance_test,
            ]
        label:
          type: string
        price:
          type: number
        detail:
          type: string

    TradingStructureShiftTimeline:
      type: object
      required: [symbol, interval, dataSource, candleCount, events, summary]
      properties:
        symbol:
          type: string
        interval:
          $ref: "#/components/schemas/TradingTimeframe"
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        candleCount:
          type: integer
        events:
          type: array
          items:
            $ref: "#/components/schemas/TradingStructureShiftEvent"
        summary:
          type: string

    TradingStructureAnalysis:
      type: object
      required:
        [
          symbol,
          interval,
          dataSource,
          candleCount,
          currentPrice,
          trend,
          trendDetail,
          swingPoints,
          zones,
          confidenceLevel,
          confidenceExplanation,
          summary,
        ]
      properties:
        symbol:
          type: string
        interval:
          $ref: "#/components/schemas/TradingTimeframe"
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        candleCount:
          type: integer
        currentPrice:
          type: number
        trend:
          type: string
          enum: [uptrend, downtrend, range]
        trendDetail:
          type: string
        swingPoints:
          type: array
          items:
            $ref: "#/components/schemas/TradingSwingPoint"
        zones:
          type: array
          items:
            $ref: "#/components/schemas/TradingSupportResistanceZone"
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation:
          type: string
        summary:
          type: string

    # ─── Multi-Timeframe (Engine 2, Phase 3 Sprint 41) ────────────────────────
    TradingTimeframeStructure:
      type: object
      required: [interval, structure]
      properties:
        interval:
          $ref: "#/components/schemas/TradingTimeframe"
        structure:
          $ref: "#/components/schemas/TradingStructureAnalysis"

    TradingMultiTimeframeAnalysis:
      type: object
      required:
        [
          symbol,
          dataSource,
          timeframes,
          trendAgreement,
          dominantTrend,
          confluenceScore,
          confidenceLevel,
          confidenceExplanation,
          summary,
        ]
      properties:
        symbol:
          type: string
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        timeframes:
          type: array
          items:
            $ref: "#/components/schemas/TradingTimeframeStructure"
        trendAgreement:
          type: string
          enum: [unanimous, majority, split, insufficient-data]
        dominantTrend:
          type: ["string", "null"]
          enum: [uptrend, downtrend, range, null]
        confluenceScore:
          type: ["number", "null"]
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation:
          type: string
        summary:
          type: string

    # ─── Market Regime (Engine 2, Phase 3 Sprint 42) ──────────────────────────
    # Deliberately a projection of TradingRegimeAnalysis's own fields — the
    # full nested multiTimeframe/liquidity sub-analyses are not part of this
    # documented contract (see routes/tradingRegime.ts's own disclosed scope
    # comment for why): Structure/Multi-Timeframe already have their own
    # cards on the same page, and Liquidity's own dedicated schema doesn't
    # exist yet (its Route+UI remains backlogged).
    TradingRegimeAnalysis:
      type: object
      required:
        [
          symbol,
          dataSource,
          regimeLabel,
          trendRegime,
          trendAgreement,
          volatilityRegime,
          volatilityAnnualizedPct,
          volatilityExplanation,
          liquidityRegime,
          confidenceLevel,
          confidenceExplanation,
          summary,
        ]
      properties:
        symbol:
          type: string
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        regimeLabel:
          type: string
          enum:
            [
              trending-bullish,
              trending-bearish,
              range-bound,
              volatile-choppy,
              quiet-consolidation,
            ]
        trendRegime:
          type: ["string", "null"]
          enum: [uptrend, downtrend, range, null]
        trendAgreement:
          type: string
          enum: [unanimous, majority, split, insufficient-data]
        volatilityRegime:
          type: string
          enum: [high, normal, low]
        volatilityAnnualizedPct:
          type: ["number", "null"]
        volatilityExplanation:
          type: string
        liquidityRegime:
          type: string
          enum: [High, Moderate, Low]
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation:
          type: string
        summary:
          type: string

    # ─── Probability (Engine 2, Phase 3 Sprint 43) ────────────────────────────
    TradingProbabilityConeLevel:
      type: object
      required: [daysAhead, low1Sigma, high1Sigma, low2Sigma, high2Sigma]
      properties:
        daysAhead:
          type: number
        low1Sigma:
          type: number
        high1Sigma:
          type: number
        low2Sigma:
          type: number
        high2Sigma:
          type: number

    # Deliberately a projection of ProbabilityAnalysis's own fields — the
    # full nested `regime: TradingRegimeAnalysis` sub-analysis is not part
    # of this documented contract (see routes/tradingProbability.ts's own
    # disclosed scope comment for why): the Market Regime card (Sprint 42)
    # already surfaces that detail on the same page.
    TradingProbabilityAnalysis:
      type: object
      required:
        [
          symbol,
          dataSource,
          currentPrice,
          volatilityAnnualizedPct,
          available,
          unavailableReason,
          cone,
          confidenceLevel,
          confidenceExplanation,
          summary,
        ]
      properties:
        symbol:
          type: string
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        currentPrice:
          type: number
        volatilityAnnualizedPct:
          type: ["number", "null"]
        available:
          type: boolean
        unavailableReason:
          type: ["string", "null"]
        cone:
          type: array
          items:
            $ref: "#/components/schemas/TradingProbabilityConeLevel"
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation:
          type: string
        summary:
          type: string

    TradingPosition:
      type: object
      required: [id, symbol, instrumentType, side, status, quantity, entryPrice, entryDate, notes, createdAt, updatedAt]
      properties:
        id:
          type: integer
        symbol:
          type: string
        instrumentType:
          type: string
        side:
          type: string
          enum: [long, short]
        status:
          type: string
          enum: [open, closed]
        quantity:
          type: number
        entryPrice:
          type: number
        entryDate:
          type: string
        exitPrice:
          type: ["number", "null"]
        exitDate:
          type: ["string", "null"]
        stopPrice:
          type: ["number", "null"]
        targetPrice:
          type: ["number", "null"]
        notes:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    TradingPositionInput:
      type: object
      required: [symbol, quantity, entryPrice]
      properties:
        symbol:
          type: string
        instrumentType:
          type: string
        side:
          type: string
          enum: [long, short]
        status:
          type: string
          enum: [open, closed]
        quantity:
          type: number
        entryPrice:
          type: number
        exitPrice:
          type: number
        stopPrice:
          type: number
        targetPrice:
          type: number
        notes:
          type: string

    TradingPositionUpdate:
      type: object
      properties:
        symbol:
          type: string
        instrumentType:
          type: string
        side:
          type: string
          enum: [long, short]
        status:
          type: string
          enum: [open, closed]
        quantity:
          type: number
        entryPrice:
          type: number
        exitPrice:
          type: number
        stopPrice:
          type: number
        targetPrice:
          type: number
        notes:
          type: string

    TradingTradePlanRisk:
      type: object
      required: [accountRiskPct, entryPrice, stopPrice, targetPrice, positionSize, riskRewardRatio]
      properties:
        accountRiskPct:
          type: number
        entryPrice:
          type: number
        stopPrice:
          type: number
        targetPrice:
          type: number
        positionSize:
          type: ["number", "null"]
        riskRewardRatio:
          type: ["number", "null"]

    TradingTradePlan:
      type: object
      required: [id, symbol, direction, status, thesis, risk, createdAt, updatedAt]
      properties:
        id:
          type: integer
        symbol:
          type: string
        direction:
          type: string
          enum: [long, short]
        status:
          type: string
          enum: [draft, active, closed, cancelled]
        thesis:
          type: string
        risk:
          $ref: "#/components/schemas/TradingTradePlanRisk"
        createdAt:
          type: string
        updatedAt:
          type: string

    TradingTradePlanInput:
      type: object
      required: [symbol, direction, thesis, accountRiskPct, entryPrice, stopPrice, targetPrice]
      properties:
        symbol:
          type: string
        direction:
          type: string
          enum: [long, short]
        thesis:
          type: string
        accountRiskPct:
          type: number
        entryPrice:
          type: number
        stopPrice:
          type: number
        targetPrice:
          type: number
        accountValue:
          type: ["number", "null"]
          description: The user's own current account value, used only to derive positionSize — honestly null (never fabricated) when not supplied

    TradingTradePlanUpdate:
      type: object
      properties:
        thesis:
          type: string
        status:
          type: string
          enum: [draft, active, closed, cancelled]

    TradingWorkspaceNote:
      type: object
      required: [id, symbol, note, createdAt, updatedAt]
      properties:
        id:
          type: integer
        symbol:
          type: string
        note:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    TradingWorkspaceNoteInput:
      type: object
      required: [symbol, note]
      properties:
        symbol:
          type: string
        note:
          type: string

    TradingWorkspaceNoteUpdate:
      type: object
      required: [note]
      properties:
        note:
          type: string

    TradingSessionData:
      type: object
      required: [symbol, asOf, activeSessions, sessionHigh, sessionLow]
      properties:
        symbol:
          type: string
        asOf:
          type: string
        activeSessions:
          type: array
          items:
            type: string
            enum: [sydney, tokyo, london, new_york]
        sessionHigh:
          type: ["number", "null"]
        sessionLow:
          type: ["number", "null"]

    TradingRiskScoreCard:
      type: object
      required: [score, label, detail]
      properties:
        score:
          type: ["number", "null"]
        label:
          type: string
        detail:
          type: string

    TradingRiskComponent:
      type: object
      required: [key, label, score, weight, detail]
      properties:
        key:
          type: string
        label:
          type: string
        score:
          type: ["number", "null"]
        weight:
          type: number
        detail:
          type: string

    TradingPositionSizingRisk:
      allOf:
        - $ref: "#/components/schemas/TradingRiskScoreCard"
        - type: object
          required: [largestPositionSymbol, largestPositionRiskPct, capBreached, unpricedSymbols]
          properties:
            largestPositionSymbol:
              type: ["string", "null"]
            largestPositionRiskPct:
              type: ["number", "null"]
            capBreached:
              type: boolean
            unpricedSymbols:
              type: array
              items:
                type: string

    TradingStopDisciplineRisk:
      allOf:
        - $ref: "#/components/schemas/TradingRiskScoreCard"
        - type: object
          required:
            [
              openPositionsCount,
              positionsWithStop,
              positionsWithTarget,
              positionsFullyPlanned,
              missingStopSymbols,
              missingTargetSymbols,
            ]
          properties:
            openPositionsCount:
              type: integer
            positionsWithStop:
              type: integer
            positionsWithTarget:
              type: integer
            positionsFullyPlanned:
              type: integer
            missingStopSymbols:
              type: array
              items:
                type: string
            missingTargetSymbols:
              type: array
              items:
                type: string

    TradingPerPositionBudget:
      type: object
      required: [id, symbol, riskDollars, riskPct, withinLimit]
      properties:
        id:
          type: integer
        symbol:
          type: string
        riskDollars:
          type: ["number", "null"]
        riskPct:
          type: ["number", "null"]
        withinLimit:
          type: ["boolean", "null"]

    TradingPortfolioRiskBudget:
      allOf:
        - $ref: "#/components/schemas/TradingRiskScoreCard"
        - type: object
          required: [accountValue, totalRiskDollars, totalRiskUsedPct, capBreached, perPosition]
          properties:
            accountValue:
              type: ["number", "null"]
            totalRiskDollars:
              type: ["number", "null"]
            totalRiskUsedPct:
              type: ["number", "null"]
            capBreached:
              type: boolean
            perPosition:
              type: array
              items:
                $ref: "#/components/schemas/TradingPerPositionBudget"

    TradingPositionProbabilityContext:
      type: object
      required: [positionId, symbol, daysAhead, regimeLabel, stopTouchProbability, targetTouchProbability]
      properties:
        positionId:
          type: integer
        symbol:
          type: string
        daysAhead:
          type: number
        regimeLabel:
          type: ["string", "null"]
          enum:
            [
              trending-bullish,
              trending-bearish,
              range-bound,
              volatile-choppy,
              quiet-consolidation,
              null,
            ]
        stopTouchProbability:
          type: ["number", "null"]
        targetTouchProbability:
          type: ["number", "null"]

    TradingRiskAnalysis:
      type: object
      required:
        [
          overall,
          positionSizing,
          stopDiscipline,
          portfolioBudget,
          components,
          accountValue,
          openPositionsCount,
          positionContexts,
        ]
      properties:
        overall:
          $ref: "#/components/schemas/TradingRiskScoreCard"
        positionSizing:
          $ref: "#/components/schemas/TradingPositionSizingRisk"
        stopDiscipline:
          $ref: "#/components/schemas/TradingStopDisciplineRisk"
        portfolioBudget:
          $ref: "#/components/schemas/TradingPortfolioRiskBudget"
        components:
          type: array
          items:
            $ref: "#/components/schemas/TradingRiskComponent"
        accountValue:
          type: ["number", "null"]
        openPositionsCount:
          type: integer
        positionContexts:
          type: array
          items:
            $ref: "#/components/schemas/TradingPositionProbabilityContext"

    TradingVolumeProfileLevel:
      type: object
      required: [price, volume, pctOfTotal]
      properties:
        price:
          type: number
        volume:
          type: number
        pctOfTotal:
          type: number

    TradingBuySellPressure:
      type: object
      required: [buyPct, sellPct, direction]
      properties:
        buyPct:
          type: number
        sellPct:
          type: number
        direction:
          type: string
          enum: [buying, selling, neutral]

    TradingLiquidityAnalysis:
      type: object
      required:
        [
          symbol,
          interval,
          dataSource,
          candleCount,
          currentPrice,
          volumeProfile,
          avgDollarVolume,
          liquidityScore,
          liquidityBand,
          buySellPressure,
          confidenceLevel,
          confidenceExplanation,
          summary,
        ]
      properties:
        symbol:
          type: string
        interval:
          $ref: "#/components/schemas/TradingTimeframe"
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        candleCount:
          type: integer
        currentPrice:
          type: number
        volumeProfile:
          type: array
          items:
            $ref: "#/components/schemas/TradingVolumeProfileLevel"
        avgDollarVolume:
          type: number
        liquidityScore:
          type: number
        liquidityBand:
          type: string
          enum: [High, Moderate, Low]
        buySellPressure:
          $ref: "#/components/schemas/TradingBuySellPressure"
        confidenceLevel:
          type: string
          enum: [High, Moderate, Low]
        confidenceExplanation:
          type: string
        summary:
          type: string

    TradingCoachAskInput:
      type: object
      required: [symbol, question]
      properties:
        symbol:
          type: string
        question:
          type: string

    TradingCoachAskResult:
      type: object
      required: [answer, answerSource]
      properties:
        answer:
          type: string
        answerSource:
          type: string
          enum: [llm, template]

    TradingBacktestStrategy:
      type: string
      enum: [trend-following, mean-reversion, structure-breakout]

    TradingRunBacktestInput:
      type: object
      required: [symbol, strategy]
      properties:
        symbol:
          type: string
        strategy:
          $ref: "#/components/schemas/TradingBacktestStrategy"
        interval:
          $ref: "#/components/schemas/TradingTimeframe"
        lookback:
          type: integer
        stopLossPct:
          type: number
        targetPct:
          type: number

    TradingBacktestTrade:
      type: object
      required: [entryDate, entryPrice, exitDate, exitPrice, exitReason, pnlPct, rMultiple]
      properties:
        entryDate:
          type: string
        entryPrice:
          type: number
        exitDate:
          type: string
        exitPrice:
          type: number
        exitReason:
          type: string
          enum: [stop, target, trend-flip, time-limit, end-of-period]
        pnlPct:
          type: number
        rMultiple:
          type: number

    TradingBacktestEquityPoint:
      type: object
      required: [date, value, drawdownPct]
      properties:
        date:
          type: string
        value:
          type: number
        drawdownPct:
          type: number

    TradingBacktestResult:
      type: object
      required:
        - symbol
        - strategy
        - interval
        - dataSource
        - candleCount
        - available
        - unavailableReason
        - trades
        - totalTrades
        - winRate
        - avgR
        - totalReturnPct
        - maxDrawdownPct
        - sharpeRatio
        - equityCurve
        - summary
      properties:
        id:
          type: integer
        createdAt:
          type: string
        symbol:
          type: string
        strategy:
          $ref: "#/components/schemas/TradingBacktestStrategy"
        interval:
          $ref: "#/components/schemas/TradingTimeframe"
        dataSource:
          type: string
          enum: [SIMULATED, LIVE]
        candleCount:
          type: integer
        available:
          type: boolean
        unavailableReason:
          type: string
          nullable: true
        trades:
          type: array
          items:
            $ref: "#/components/schemas/TradingBacktestTrade"
        totalTrades:
          type: integer
        winRate:
          type: number
          nullable: true
        avgR:
          type: number
          nullable: true
        totalReturnPct:
          type: number
          nullable: true
        maxDrawdownPct:
          type: number
          nullable: true
        sharpeRatio:
          type: number
          nullable: true
        equityCurve:
          type: array
          items:
            $ref: "#/components/schemas/TradingBacktestEquityPoint"
        summary:
          type: string

    # ─── Options Engine-Native Backtesting (Phase 4, Sprint 58) ─────────────────
    OptionsBacktestStrategy:
      type: string
      enum: [iron_condor]

    OptionsRunBacktestInput:
      type: object
      required: [symbol, strategy]
      properties:
        symbol:
          type: string
        strategy:
          $ref: "#/components/schemas/OptionsBacktestStrategy"
        lookback:
          type: integer
        entryDte:
          type: integer
        shortDelta:
          type: number
        profitTargetPct:
          type: number
        stopLossMultiple:
          type: number
        dteExitTrigger:
          type: integer

    OptionsBacktestTrade:
      type: object
      required: [entryDate, expirationDate, entryCredit, exitDate, exitDebit, exitReason, pnl, maxLoss, rMultiple, daysHeld]
      properties:
        entryDate:
          type: string
        expirationDate:
          type: string
        entryCredit:
          type: number
        exitDate:
          type: string
        exitDebit:
          type: number
        exitReason:
          type: string
          enum: [profit-target, stop-loss, dte-trigger, expiration, end-of-period]
        pnl:
          type: number
        maxLoss:
          type: number
        rMultiple:
          type: number
        daysHeld:
          type: integer

    OptionsBacktestEquityPoint:
      type: object
      required: [date, value, drawdownPct]
      properties:
        date:
          type: string
        value:
          type: number
        drawdownPct:
          type: number

    OptionsBacktestResult:
      type: object
      required:
        - symbol
        - strategy
        - underlyingDataSource
        - optionsDataSource
        - candleCount
        - available
        - unavailableReason
        - trades
        - totalTrades
        - winRate
        - avgR
        - totalReturnPct
        - maxDrawdownPct
        - sharpeRatio
        - equityCurve
        - summary
      properties:
        id:
          type: integer
        createdAt:
          type: string
        symbol:
          type: string
        strategy:
          $ref: "#/components/schemas/OptionsBacktestStrategy"
        underlyingDataSource:
          type: string
          enum: [SIMULATED, LIVE]
        optionsDataSource:
          type: string
          enum: [SIMULATED]
        candleCount:
          type: integer
        available:
          type: boolean
        unavailableReason:
          type: string
          nullable: true
        trades:
          type: array
          items:
            $ref: "#/components/schemas/OptionsBacktestTrade"
        totalTrades:
          type: integer
        winRate:
          type: number
          nullable: true
        avgR:
          type: number
          nullable: true
        totalReturnPct:
          type: number
          nullable: true
        maxDrawdownPct:
          type: number
          nullable: true
        sharpeRatio:
          type: number
          nullable: true
        equityCurve:
          type: array
          items:
            $ref: "#/components/schemas/OptionsBacktestEquityPoint"
        summary:
          type: string

    # ─── Portfolio Stress Test & Scenario Simulator (Paper Trading) ─────────────
    # Read-only What-If simulator over the user's OWN current open
    # portfolio: applies hypothetical underlying price / implied-volatility
    # / time-decay shocks and reprices every open position via
    # optionsMath.ts's own unmodified bs(). Never contacts a broker
    # execution endpoint, never creates or modifies an order or position.
    ScenarioShockInput:
      type: object
      properties:
        label:
          type: ["string", "null"]
        priceShockPct:
          type: ["number", "null"]
        ivShockPct:
          type: ["number", "null"]
        timeDecayDays:
          type: ["number", "null"]

    PortfolioStressTestInput:
      type: object
      properties:
        scenarios:
          type: ["array", "null"]
          items:
            $ref: "#/components/schemas/ScenarioShockInput"

    StressTestInputIssue:
      type: object
      required: [index, field, code, message]
      properties:
        index:
          type: ["integer", "null"]
        field:
          type: string
        code:
          type: string
        message:
          type: string

    Shock:
      type: object
      required: [priceShockPct, ivShockPct, timeDecayDays]
      properties:
        priceShockPct:
          type: number
        ivShockPct:
          type: number
        timeDecayDays:
          type: number

    PositionGreeksLike:
      type: object
      required: [delta, gamma, theta, vega]
      properties:
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number

    PositionScenarioResult:
      type: object
      required: [tradeId, symbol, strategy, greeks, costToClose, unrealizedPnl, unrealizedPnlPercent]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        greeks:
          $ref: "#/components/schemas/PositionGreeksLike"
        costToClose:
          type: number
        unrealizedPnl:
          type: number
        unrealizedPnlPercent:
          type: number

    MarkExposureBySymbol:
      type: object
      required: [symbol, markValue, pctOfAccount]
      properties:
        symbol:
          type: string
        markValue:
          type: number
        pctOfAccount:
          type: number

    MarkExposureByStrategy:
      type: object
      required: [strategy, markValue, pctOfAccount]
      properties:
        strategy:
          type: string
        markValue:
          type: number
        pctOfAccount:
          type: number

    ScenarioEvaluation:
      type: object
      required:
        [
          portfolioValue,
          totalUnrealizedPnl,
          greeks,
          exposureBySymbol,
          exposureByStrategy,
          totalRiskDollars,
          totalRiskPct,
          buyingPower,
          positions,
        ]
      properties:
        portfolioValue:
          type: number
        totalUnrealizedPnl:
          type: number
        greeks:
          $ref: "#/components/schemas/PortfolioGreeksSnapshot"
        exposureBySymbol:
          type: array
          items:
            $ref: "#/components/schemas/MarkExposureBySymbol"
        exposureByStrategy:
          type: array
          items:
            $ref: "#/components/schemas/MarkExposureByStrategy"
        totalRiskDollars:
          type: number
        totalRiskPct:
          type: number
        buyingPower:
          type: number
        positions:
          type: array
          items:
            $ref: "#/components/schemas/PositionScenarioResult"

    RiskThresholdBreach:
      type: object
      required: [tradeId, symbol, lossDollars, lossPctOfAccount, thresholdPct]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        lossDollars:
          type: number
        lossPctOfAccount:
          type: number
        thresholdPct:
          type: number

    ConcentrationChange:
      type: object
      required: [symbol, beforePct, afterPct, changePts]
      properties:
        symbol:
          type: string
        beforePct:
          type: number
        afterPct:
          type: number
        changePts:
          type: number

    LargestPositionImpact:
      type: object
      required: [tradeId, symbol, strategy, pnlImpact]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        pnlImpact:
          type: number

    ScenarioComparisonEntry:
      type: object
      required:
        [
          label,
          shock,
          after,
          portfolioValueImpact,
          unrealizedPnlImpact,
          buyingPowerImpactDollars,
          deltaChange,
          gammaChange,
          thetaChange,
          vegaChange,
          largestLosingPosition,
          largestGainingPosition,
          positionsBreachingThreshold,
          concentrationChanges,
          drawdownPct,
          riskScoreAfter,
        ]
      properties:
        label:
          type: string
        shock:
          $ref: "#/components/schemas/Shock"
        after:
          $ref: "#/components/schemas/ScenarioEvaluation"
        portfolioValueImpact:
          type: number
        unrealizedPnlImpact:
          type: number
        buyingPowerImpactDollars:
          type: number
        deltaChange:
          type: number
        gammaChange:
          type: number
        thetaChange:
          type: number
        vegaChange:
          type: number
        largestLosingPosition:
          oneOf:
            - $ref: "#/components/schemas/LargestPositionImpact"
            - type: "null"
        largestGainingPosition:
          oneOf:
            - $ref: "#/components/schemas/LargestPositionImpact"
            - type: "null"
        positionsBreachingThreshold:
          type: array
          items:
            $ref: "#/components/schemas/RiskThresholdBreach"
        concentrationChanges:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationChange"
        drawdownPct:
          type: number
        riskScoreAfter:
          type: number

    PortfolioStressTestResult:
      type: object
      required:
        [
          available,
          inputIssues,
          accountValue,
          credentialsConfigured,
          brokerConnected,
          lastBrokerCheckAt,
          sectorExposure,
          base,
          riskScoreBefore,
          scenarios,
          generatedAt,
        ]
      properties:
        available:
          type: boolean
        inputIssues:
          type: array
          items:
            $ref: "#/components/schemas/StressTestInputIssue"
        accountValue:
          type: number
        credentialsConfigured:
          type: boolean
        brokerConnected:
          type: ["boolean", "null"]
        lastBrokerCheckAt:
          type: ["string", "null"]
        sectorExposure:
          $ref: "#/components/schemas/SectorExposure"
        base:
          $ref: "#/components/schemas/ScenarioEvaluation"
        riskScoreBefore:
          type: number
        scenarios:
          type: array
          items:
            $ref: "#/components/schemas/ScenarioComparisonEntry"
        generatedAt:
          type: string

    # ─── Earnings & Event Risk Portfolio Overlay (Paper Trading) ────────────────
    # Read-only overlay reusing eventRisk.ts's own existing
    # getEventRiskForSymbol()/EventRiskEvent unmodified. Never contacts a
    # broker execution endpoint, never creates or modifies an order or
    # position.
    PortfolioEventRiskPrimaryEvent:
      oneOf:
        - $ref: "#/components/schemas/EventRiskEvent"
        - type: "null"

    PositionEventRisk:
      type: object
      required:
        [
          tradeId,
          symbol,
          strategy,
          quantity,
          portfolioWeightPct,
          expiration,
          eventStatus,
          primaryEvent,
          events,
          riskLevel,
          riskGuidance,
          riskGuidanceLabel,
          confidence,
          eventSource,
          lastUpdated,
        ]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        quantity:
          type: integer
        portfolioWeightPct:
          type: number
        expiration:
          type: ["string", "null"]
        eventStatus:
          type: string
          enum: [has_events, no_events, expiration_unknown]
        primaryEvent:
          $ref: "#/components/schemas/PortfolioEventRiskPrimaryEvent"
        events:
          type: array
          items:
            $ref: "#/components/schemas/EventRiskEvent"
        riskLevel:
          type: string
          enum: [none, low, medium, high]
        riskGuidance:
          type: string
          enum: [monitor, consider_review, consider_adjustment, no_immediate_event_risk]
        riskGuidanceLabel:
          type: string
        confidence:
          type: ["string", "null"]
          enum: [scheduled, simulated_estimate, null]
        eventSource:
          type: string
          enum: [SIMULATED]
        lastUpdated:
          type: string

    HighestRiskPosition:
      type: object
      required: [tradeId, symbol, riskLevel]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        riskLevel:
          type: string
          enum: [none, low, medium, high]

    PortfolioEventRiskSummary:
      type: object
      required:
        [
          totalPositions,
          positionsWithEvents,
          positionsWithoutEvents,
          highRiskCount,
          within1Day,
          within3Days,
          within7Days,
          within14Days,
          aggregateExposurePct,
          highestRiskPosition,
        ]
      properties:
        totalPositions:
          type: integer
        positionsWithEvents:
          type: integer
        positionsWithoutEvents:
          type: integer
        highRiskCount:
          type: integer
        within1Day:
          type: integer
        within3Days:
          type: integer
        within7Days:
          type: integer
        within14Days:
          type: integer
        aggregateExposurePct:
          type: number
        highestRiskPosition:
          oneOf:
            - $ref: "#/components/schemas/HighestRiskPosition"
            - type: "null"

    UnsupportedEventCategory:
      type: object
      required: [category, label, reason]
      properties:
        category:
          type: string
        label:
          type: string
        reason:
          type: string

    PortfolioEventRiskResult:
      type: object
      required:
        [
          positions,
          summary,
          accountValue,
          credentialsConfigured,
          brokerConnected,
          lastBrokerCheckAt,
          eventRiskEnabled,
          unsupportedEventCategories,
          generatedAt,
        ]
      properties:
        positions:
          type: array
          items:
            $ref: "#/components/schemas/PositionEventRisk"
        summary:
          $ref: "#/components/schemas/PortfolioEventRiskSummary"
        accountValue:
          type: number
        credentialsConfigured:
          type: boolean
        brokerConnected:
          type: ["boolean", "null"]
        lastBrokerCheckAt:
          type: ["string", "null"]
        eventRiskEnabled:
          type: boolean
        unsupportedEventCategories:
          type: array
          items:
            $ref: "#/components/schemas/UnsupportedEventCategory"
        generatedAt:
          type: string

    # ─── Correlation & Concentration Risk Overlay (Paper Trading) ───────────────
    # Read-only overlay reusing lib/positionSizing.ts's own existing
    # currentOpenTrades()/buildSnapshot() and serverState.ts's own existing
    # computeTradeGreeks() unmodified. Never contacts a broker execution
    # endpoint, never creates or modifies an order or position. Every
    # "correlation" here is a categorical cluster (grouping over already-
    # known traits), never a fabricated statistical correlation
    # coefficient.
    ConcentrationBucket:
      type: object
      required: [key, label, positionCount, weightPct]
      properties:
        key:
          type: string
        label:
          type: string
        positionCount:
          type: integer
        weightPct:
          type: number

    ConcentrationBreakdown:
      type: object
      required: [dimension, buckets, concentrationScore, largestBucket]
      properties:
        dimension:
          type: string
        buckets:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"
        concentrationScore:
          type: number
        largestBucket:
          oneOf:
            - $ref: "#/components/schemas/ConcentrationBucket"
            - type: "null"

    LongShortExposure:
      type: object
      required: [longExposureDollars, shortExposureDollars, longPct, shortPct]
      properties:
        longExposureDollars:
          type: number
        shortExposureDollars:
          type: number
        longPct:
          type: number
        shortPct:
          type: number

    CallPutExposure:
      type: object
      required: [callNotional, putNotional, callPct, putPct]
      properties:
        callNotional:
          type: number
        putNotional:
          type: number
        callPct:
          type: number
        putPct:
          type: number

    PositionGreeksContribution:
      type: object
      required: [tradeId, symbol, strategy, delta, gamma, theta, vega, deltaSharePct]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number
        deltaSharePct:
          type: number

    ClusterGroup:
      type: object
      required: [dimension, key, label, tradeIds, positionCount]
      properties:
        dimension:
          type: string
        key:
          type: string
        label:
          type: string
        tradeIds:
          type: array
          items:
            type: integer
        positionCount:
          type: integer

    DimensionScoreRef:
      type: object
      required: [dimension, label, concentrationScore]
      properties:
        dimension:
          type: string
        label:
          type: string
        concentrationScore:
          type: number

    PortfolioSummaryHighlights:
      type: object
      required:
        [
          largestConcentration,
          highestDirectionalExposure,
          highestGreeksContributor,
          mostDiversifiedArea,
          leastDiversifiedArea,
          concentrationScore,
          diversificationScore,
          portfolioHealthLabel,
        ]
      properties:
        largestConcentration:
          oneOf:
            - type: object
              required: [dimension, bucket]
              properties:
                dimension:
                  type: string
                bucket:
                  $ref: "#/components/schemas/ConcentrationBucket"
            - type: "null"
        highestDirectionalExposure:
          oneOf:
            - type: object
              required: [direction, exposureDollars, pct]
              properties:
                direction:
                  type: string
                  enum: [long, short]
                exposureDollars:
                  type: number
                pct:
                  type: number
            - type: "null"
        highestGreeksContributor:
          oneOf:
            - type: object
              required: [tradeId, symbol, delta]
              properties:
                tradeId:
                  type: integer
                symbol:
                  type: string
                delta:
                  type: number
            - type: "null"
        mostDiversifiedArea:
          oneOf:
            - $ref: "#/components/schemas/DimensionScoreRef"
            - type: "null"
        leastDiversifiedArea:
          oneOf:
            - $ref: "#/components/schemas/DimensionScoreRef"
            - type: "null"
        concentrationScore:
          type: number
        diversificationScore:
          type: number
        portfolioHealthLabel:
          type: string

    RiskGuidanceAdvisory:
      type: object
      required: [code, label, detail]
      properties:
        code:
          type: string
        label:
          type: string
        detail:
          type: string

    ConcentrationRiskGuidance:
      type: object
      required: [code, label, advisories]
      properties:
        code:
          type: string
          enum: [well_diversified, moderate_concentration, high_concentration, review_exposure]
        label:
          type: string
        advisories:
          type: array
          items:
            $ref: "#/components/schemas/RiskGuidanceAdvisory"

    PortfolioConcentrationBreakdowns:
      type: object
      required: [symbol, underlying, sector, strategy, expiration, assetClass, directionalBias]
      properties:
        symbol:
          $ref: "#/components/schemas/ConcentrationBreakdown"
        underlying:
          $ref: "#/components/schemas/ConcentrationBreakdown"
        sector:
          $ref: "#/components/schemas/ConcentrationBreakdown"
        strategy:
          $ref: "#/components/schemas/ConcentrationBreakdown"
        expiration:
          $ref: "#/components/schemas/ConcentrationBreakdown"
        assetClass:
          $ref: "#/components/schemas/ConcentrationBreakdown"
        directionalBias:
          $ref: "#/components/schemas/ConcentrationBreakdown"

    PortfolioConcentrationResult:
      type: object
      required:
        [
          totalPositions,
          totalPortfolioValue,
          accountValue,
          netGreeks,
          netBeta,
          netBetaUnavailableReason,
          netDirectionalExposure,
          breakdowns,
          longShort,
          callPut,
          greeksContributions,
          clusters,
          summary,
          riskGuidance,
          credentialsConfigured,
          brokerConnected,
          lastBrokerCheckAt,
          sectorDataSource,
          generatedAt,
        ]
      properties:
        totalPositions:
          type: integer
        totalPortfolioValue:
          type: number
        accountValue:
          type: number
        netGreeks:
          $ref: "#/components/schemas/PortfolioGreeksSnapshot"
        netBeta:
          type: "null"
        netBetaUnavailableReason:
          type: string
        netDirectionalExposure:
          type: object
          required: [longExposureDollars, shortExposureDollars, netExposureDollars, netBiasLabel]
          properties:
            longExposureDollars:
              type: number
            shortExposureDollars:
              type: number
            netExposureDollars:
              type: number
            netBiasLabel:
              type: string
              enum: [net_long, net_short, balanced]
        breakdowns:
          $ref: "#/components/schemas/PortfolioConcentrationBreakdowns"
        longShort:
          $ref: "#/components/schemas/LongShortExposure"
        callPut:
          $ref: "#/components/schemas/CallPutExposure"
        greeksContributions:
          type: array
          items:
            $ref: "#/components/schemas/PositionGreeksContribution"
        clusters:
          type: array
          items:
            $ref: "#/components/schemas/ClusterGroup"
        summary:
          $ref: "#/components/schemas/PortfolioSummaryHighlights"
        riskGuidance:
          $ref: "#/components/schemas/ConcentrationRiskGuidance"
        credentialsConfigured:
          type: boolean
        brokerConnected:
          type: ["boolean", "null"]
        lastBrokerCheckAt:
          type: ["string", "null"]
        sectorDataSource:
          type: string
          enum: [KNOWN_UNIVERSE_METADATA]
        generatedAt:
          type: string

    # ─── Portfolio Risk Dashboard & Health Score (Paper Trading) ────────────────
    # Read-only executive dashboard composing the 3 already-existing
    # overlays (Portfolio Stress Test, Earnings & Event Risk, Correlation
    # & Concentration) plus lib/positionSizing.ts's own existing
    # currentOpenTrades()/buildSnapshot() — never a new pricing/provider
    # call, never a fabricated statistical model. Every Health Score
    # factor is a direct projection of an already-computed figure from
    # one of those 3 overlays.
    DashboardOverallRiskRating:
      type: object
      required: [code, label]
      properties:
        code:
          type: string
          enum: [healthy, moderate_risk, elevated_risk, high_risk]
        label:
          type: string

    DashboardHealthScoreFactor:
      type: object
      required: [code, label, score, sourceModule, detail]
      properties:
        code:
          type: string
        label:
          type: string
        score:
          type: number
        sourceModule:
          type: string
        detail:
          type: string

    DashboardRiskContributor:
      type: object
      required: [tradeId, symbol, delta, deltaSharePct]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        delta:
          type: number
        deltaSharePct:
          type: number

    DashboardWidgetSummary:
      type: object
      required: [code, label, headline, detail, linkHref]
      properties:
        code:
          type: string
        label:
          type: string
        headline:
          type: string
        detail:
          type: string
        linkHref:
          type: string

    DashboardGuidanceAdvisory:
      type: object
      required: [code, label, detail]
      properties:
        code:
          type: string
          enum:
            [
              healthy_portfolio,
              moderate_risk,
              elevated_risk,
              high_risk,
              elevated_concentration,
              elevated_event_risk,
              diversification_recommended,
              review_large_positions,
            ]
        label:
          type: string
        detail:
          type: string

    DashboardStressTestSummaryEntry:
      type: object
      required: [label, portfolioValueImpact, riskScoreAfter]
      properties:
        label:
          type: string
        portfolioValueImpact:
          type: number
        riskScoreAfter:
          type: number

    PortfolioDashboardResult:
      type: object
      required:
        [
          portfolioValue,
          buyingPower,
          totalRiskDollars,
          totalRiskPct,
          healthScore,
          overallRiskRating,
          paperTradingMode,
          credentialsConfigured,
          brokerConnected,
          lastBrokerCheckAt,
          lastPortfolioUpdate,
          openPositionsCount,
          healthFactors,
          netGreeks,
          netBeta,
          netBetaUnavailableReason,
          largestPosition,
          largestRiskContributor,
          highestEventRisk,
          highestConcentration,
          highestDirectionalExposure,
          widgets,
          allocationBySymbol,
          allocationBySector,
          allocationByStrategy,
          expirationDistribution,
          eventTimelineSummary,
          stressTestSummary,
          guidance,
          generatedAt,
        ]
      properties:
        portfolioValue:
          type: number
        buyingPower:
          type: number
        totalRiskDollars:
          type: number
        totalRiskPct:
          type: number
        healthScore:
          type: number
        overallRiskRating:
          $ref: "#/components/schemas/DashboardOverallRiskRating"
        paperTradingMode:
          type: boolean
          enum: [true]
        credentialsConfigured:
          type: boolean
        brokerConnected:
          type: ["boolean", "null"]
        lastBrokerCheckAt:
          type: ["string", "null"]
        lastPortfolioUpdate:
          type: string
        openPositionsCount:
          type: integer
        healthFactors:
          type: array
          items:
            $ref: "#/components/schemas/DashboardHealthScoreFactor"
        netGreeks:
          $ref: "#/components/schemas/PortfolioGreeksSnapshot"
        netBeta:
          type: "null"
        netBetaUnavailableReason:
          type: string
        largestPosition:
          oneOf:
            - $ref: "#/components/schemas/SymbolExposure"
            - type: "null"
        largestRiskContributor:
          oneOf:
            - $ref: "#/components/schemas/DashboardRiskContributor"
            - type: "null"
        highestEventRisk:
          oneOf:
            - $ref: "#/components/schemas/HighestRiskPosition"
            - type: "null"
        highestConcentration:
          oneOf:
            - type: object
              required: [dimension, bucket]
              properties:
                dimension:
                  type: string
                bucket:
                  $ref: "#/components/schemas/ConcentrationBucket"
            - type: "null"
        highestDirectionalExposure:
          oneOf:
            - type: object
              required: [direction, exposureDollars, pct]
              properties:
                direction:
                  type: string
                  enum: [long, short]
                exposureDollars:
                  type: number
                pct:
                  type: number
            - type: "null"
        widgets:
          type: array
          items:
            $ref: "#/components/schemas/DashboardWidgetSummary"
        allocationBySymbol:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"
        allocationBySector:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"
        allocationByStrategy:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"
        expirationDistribution:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"
        eventTimelineSummary:
          $ref: "#/components/schemas/PortfolioEventRiskSummary"
        stressTestSummary:
          type: array
          items:
            $ref: "#/components/schemas/DashboardStressTestSummaryEntry"
        guidance:
          type: array
          items:
            $ref: "#/components/schemas/DashboardGuidanceAdvisory"
        generatedAt:
          type: string

    # ─── Institutional Intelligence Engine (Phase 8, Sprint 1) ────────────────
    IntelligenceObservationMetric:
      type: object
      required: [label, value]
      properties:
        label:
          type: string
        value:
          type: string

    IntelligenceLearningLink:
      type: object
      required: [label, href, comingSoon]
      properties:
        label:
          type: string
        href:
          type: ["string", "null"]
        comingSoon:
          type: boolean

    IntelligenceObservation:
      type: object
      required:
        [
          code,
          category,
          severity,
          title,
          explanation,
          supportingMetrics,
          sourceModule,
          timestamp,
          confidence,
          confidenceReason,
          learningLinks,
        ]
      properties:
        code:
          type: string
        category:
          type: string
        severity:
          type: string
          enum: [info, positive, watch, elevated]
        title:
          type: string
        explanation:
          type: string
        supportingMetrics:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceObservationMetric"
        sourceModule:
          type: string
        timestamp:
          type: string
        confidence:
          type: string
          enum: [high, moderate]
        confidenceReason:
          type: string
        learningLinks:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceLearningLink"

    IntelligenceHealthDriver:
      type: object
      required: [code, label, score, detail]
      properties:
        code:
          type: string
        label:
          type: string
        score:
          type: number
        detail:
          type: string

    IntelligenceBrokerHealthSummary:
      type: object
      required: [credentialsConfigured, connected, label]
      properties:
        credentialsConfigured:
          type: boolean
        connected:
          type: ["boolean", "null"]
        label:
          type: string

    IntelligenceHealthOverview:
      type: object
      required:
        [
          overallHealthScore,
          overallRiskRating,
          healthTrend,
          healthTrendDetail,
          healthDrivers,
          brokerHealth,
          healthSummary,
        ]
      properties:
        overallHealthScore:
          type: number
        overallRiskRating:
          $ref: "#/components/schemas/DashboardOverallRiskRating"
        healthTrend:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        healthTrendDetail:
          type: string
        healthDrivers:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceHealthDriver"
        brokerHealth:
          $ref: "#/components/schemas/IntelligenceBrokerHealthSummary"
        healthSummary:
          type: string

    IntelligenceDailySummary:
      type: object
      required: [headline, bullets, generatedAt]
      properties:
        headline:
          type: string
        bullets:
          type: array
          items:
            type: string
        generatedAt:
          type: string

    IntelligenceTimelineEntry:
      type: object
      required: [code, label, category, status]
      properties:
        code:
          type: string
        label:
          type: string
        category:
          type: string
        status:
          type: string
          enum: [new, resolved, persistent]

    IntelligenceTimelineChange:
      type: object
      required: [label, direction, detail]
      properties:
        label:
          type: string
        direction:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        detail:
          type: string

    IntelligenceRiskRatingChange:
      type: object
      required: [from, to]
      properties:
        from:
          type: string
        to:
          type: string

    IntelligenceTimeline:
      type: object
      required: [entries, healthChange, riskRatingChange, incomeChange, asOf, comparedTo]
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceTimelineEntry"
        healthChange:
          oneOf:
            - $ref: "#/components/schemas/IntelligenceTimelineChange"
            - type: "null"
        riskRatingChange:
          oneOf:
            - $ref: "#/components/schemas/IntelligenceRiskRatingChange"
            - type: "null"
        incomeChange:
          oneOf:
            - $ref: "#/components/schemas/IntelligenceTimelineChange"
            - type: "null"
        asOf:
          type: string
        comparedTo:
          type: ["string", "null"]

    InstitutionalIntelligenceResult:
      type: object
      required:
        [
          paperTradingMode,
          deterministicAnalysis,
          executiveSummary,
          observations,
          highestPriority,
          health,
          timeline,
          learningLinks,
          portfolioInsights,
          incomeInsights,
          riskInsights,
          generatedAt,
        ]
      properties:
        paperTradingMode:
          type: boolean
          enum: [true]
        deterministicAnalysis:
          type: boolean
          enum: [true]
        executiveSummary:
          $ref: "#/components/schemas/IntelligenceDailySummary"
        observations:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceObservation"
        highestPriority:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceObservation"
        health:
          $ref: "#/components/schemas/IntelligenceHealthOverview"
        timeline:
          $ref: "#/components/schemas/IntelligenceTimeline"
        learningLinks:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceLearningLink"
        portfolioInsights:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceObservation"
        incomeInsights:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceObservation"
        riskInsights:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceObservation"
        generatedAt:
          type: string

    # ─── AI Teacher & Learning Centre (Phase 8, Sprint 2) ──────────────────────
    LearningGlossaryTerm:
      type: object
      required: [key, term, category, definition, relatedTermKeys, relatedLessonKeys]
      properties:
        key:
          type: string
        term:
          type: string
        category:
          type: string
          enum: [foundations, greeks, volatility, strategies, portfolio, performance, institutional, value-investing]
        definition:
          type: string
        relatedTermKeys:
          type: array
          items:
            type: string
        relatedLessonKeys:
          type: array
          items:
            type: string

    LearningTopic:
      type: object
      required: [key, title, summary, body, whyItMatters, externalHref, relatedGlossaryKeys, estimatedMinutes]
      properties:
        key:
          type: string
        title:
          type: string
        summary:
          type: string
        body:
          type: array
          items:
            type: string
        whyItMatters:
          type: string
        externalHref:
          type: ["string", "null"]
        relatedGlossaryKeys:
          type: array
          items:
            type: string
        estimatedMinutes:
          type: number

    LearningPath:
      type: object
      required: [key, title, description, glossaryCategory, topics]
      properties:
        key:
          type: string
        title:
          type: string
        description:
          type: string
        glossaryCategory:
          type: string
          enum: [foundations, greeks, volatility, strategies, portfolio, performance, institutional, value-investing]
        topics:
          type: array
          items:
            $ref: "#/components/schemas/LearningTopic"

    LearningStrategyGreeks:
      type: object
      required: [delta, gamma, theta, vega]
      properties:
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number

    LearningStrategyPaperExample:
      type: object
      required: [available, unavailableReason, symbol, detail, greeks]
      properties:
        available:
          type: boolean
        unavailableReason:
          type: ["string", "null"]
        symbol:
          type: ["string", "null"]
        detail:
          type: ["string", "null"]
        greeks:
          oneOf:
            - $ref: "#/components/schemas/LearningStrategyGreeks"
            - type: "null"

    LearningStrategyAcademyEntry:
      type: object
      required:
        [
          key,
          label,
          builtByThisEngine,
          construction,
          idealMarket,
          maxProfit,
          maxLoss,
          greeksProfile,
          timeDecay,
          volatilityBehavior,
          assignmentRisk,
          commonMistakes,
          institutionalPerspective,
          paperExample,
        ]
      properties:
        key:
          type: string
        label:
          type: string
        builtByThisEngine:
          type: boolean
        construction:
          type: string
        idealMarket:
          type: string
        maxProfit:
          type: string
        maxLoss:
          type: string
        greeksProfile:
          type: string
        timeDecay:
          type: string
        volatilityBehavior:
          type: string
        assignmentRisk:
          type: string
        commonMistakes:
          type: array
          items:
            type: string
        institutionalPerspective:
          type: string
        paperExample:
          $ref: "#/components/schemas/LearningStrategyPaperExample"

    LearningQuizProgressAttempt:
      type: object
      required: [id, topic, score, total, percent, createdAt]
      properties:
        id:
          type: ["integer", "null"]
        topic:
          type: string
        score:
          type: number
        total:
          type: number
        percent:
          type: number
        createdAt:
          type: string

    LearningQuizProgressTopicBest:
      type: object
      required: [topic, bestPercent, attempts]
      properties:
        topic:
          type: string
        bestPercent:
          type: number
        attempts:
          type: number

    LearningQuizProgressSummary:
      type: object
      required: [attempts, bestByTopic, totalAttempts, averagePercent, streak, improvement, firstPercent, latestPercent]
      properties:
        attempts:
          type: array
          items:
            $ref: "#/components/schemas/LearningQuizProgressAttempt"
        bestByTopic:
          type: array
          items:
            $ref: "#/components/schemas/LearningQuizProgressTopicBest"
        totalAttempts:
          type: number
        averagePercent:
          type: number
        streak:
          type: number
        improvement:
          type: number
        firstPercent:
          type: number
        latestPercent:
          type: number

    LearningPathCompletion:
      type: object
      required: [pathKey, title, topicsTotal, topicsCompleted, percentComplete]
      properties:
        pathKey:
          type: string
        title:
          type: string
        topicsTotal:
          type: number
        topicsCompleted:
          type: number
        percentComplete:
          type: number

    LearningHistoryEntry:
      type: object
      required: [itemType, itemKey, viewedAt, completedAt]
      properties:
        itemType:
          type: string
          enum: [lesson, glossary, path, strategy, coach]
        itemKey:
          type: string
        viewedAt:
          type: string
        completedAt:
          type: ["string", "null"]

    LearningProgressSummary:
      type: object
      required:
        [
          lessonsViewed,
          lessonsCompleted,
          glossaryTermsViewed,
          strategiesViewed,
          coachesViewed,
          pathCompletion,
          greeksQuiz,
          valueQuiz,
          recentHistory,
          completedLessonKeys,
          completedGlossaryKeys,
          completedStrategyKeys,
          completedCoachKeys,
        ]
      properties:
        lessonsViewed:
          type: number
        lessonsCompleted:
          type: number
        glossaryTermsViewed:
          type: number
        strategiesViewed:
          type: number
        coachesViewed:
          type: number
        pathCompletion:
          type: array
          items:
            $ref: "#/components/schemas/LearningPathCompletion"
        greeksQuiz:
          $ref: "#/components/schemas/LearningQuizProgressSummary"
        valueQuiz:
          $ref: "#/components/schemas/LearningQuizProgressSummary"
        recentHistory:
          type: array
          items:
            $ref: "#/components/schemas/LearningHistoryEntry"
        completedLessonKeys:
          type: array
          items:
            type: string
        completedGlossaryKeys:
          type: array
          items:
            type: string
        completedStrategyKeys:
          type: array
          items:
            type: string
        completedCoachKeys:
          type: array
          items:
            type: string

    LearningProgressAction:
      type: object
      required: [itemType, itemKey]
      properties:
        itemType:
          type: string
          enum: [lesson, glossary, path, strategy, coach]
        itemKey:
          type: string

    LearningSimulationInput:
      type: object
      required: [type]
      properties:
        type:
          type: string
          enum: [delta, theta, expected_move, payoff, concentration]
        strike:
          type: number
        iv:
          type: number
        dte:
          type: number
        price:
          type: number
        strategy:
          type: string
          enum: [covered_call, cash_secured_put, iron_condor]
        stockCostBasis:
          type: number
        callStrike:
          type: number
        callPremium:
          type: number
        putStrike:
          type: number
        putPremium:
          type: number
        longPutStrike:
          type: number
        longCallStrike:
          type: number
        netCredit:
          type: number
        weights:
          type: array
          items:
            type: number

    LearningSimulationPoint:
      type: object
      required: [x, y]
      properties:
        x:
          type: number
        y:
          type: number

    LearningSimulationResult:
      type: object
      required: [type, label, xLabel, yLabel, points, summary, educationalSimulation, notMarketData, noTradeRecommendation]
      properties:
        type:
          type: string
          enum: [delta, theta, expected_move, payoff, concentration]
        label:
          type: string
        xLabel:
          type: string
        yLabel:
          type: string
        points:
          type: array
          items:
            $ref: "#/components/schemas/LearningSimulationPoint"
        summary:
          type: string
        educationalSimulation:
          type: boolean
          enum: [true]
        notMarketData:
          type: boolean
          enum: [true]
        noTradeRecommendation:
          type: boolean
          enum: [true]

    LearningMetricExplanation:
      type: object
      required: [code, label, currentValue, plainEnglish, sourceCalculation, whyItMatters, relatedLessonHref, relatedGlossaryKeys, reusedObservation]
      properties:
        code:
          type: string
        label:
          type: string
        currentValue:
          type: string
        plainEnglish:
          type: string
        sourceCalculation:
          type: string
        whyItMatters:
          type: string
        relatedLessonHref:
          type: ["string", "null"]
        relatedGlossaryKeys:
          type: array
          items:
            type: string
        reusedObservation:
          type: boolean

    LearningPortfolioLesson:
      type: object
      required: [items, generatedAt]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/LearningMetricExplanation"
        generatedAt:
          type: string

    # ─── AI Portfolio Analyst (Phase 8, Sprint 3) ──────────────────────────────
    # Pure composition over the Institutional Intelligence Engine,
    # Portfolio Dashboard, Portfolio Event Risk, and Theta Income — every
    # schema below reuses an existing Dashboard*/Intelligence*/Theta*
    # schema via $ref wherever the underlying shape genuinely matches.
    # "Analyst" prefix avoids a real naming collision with the
    # pre-existing PortfolioSnapshot schema (Position Sizing & Portfolio
    # Impact, an unrelated, differently-shaped schema).
    AnalystPortfolioSnapshot:
      type: object
      required:
        [
          healthScore,
          overallRiskRating,
          buyingPower,
          openPositionsCount,
          monthlyTheta,
          dailyTheta,
          totalRiskDollars,
          totalRiskPct,
          generatedAt,
        ]
      properties:
        healthScore:
          type: number
        overallRiskRating:
          $ref: "#/components/schemas/DashboardOverallRiskRating"
        buyingPower:
          type: number
        openPositionsCount:
          type: integer
        monthlyTheta:
          type: number
        dailyTheta:
          type: number
        totalRiskDollars:
          type: number
        totalRiskPct:
          type: number
        generatedAt:
          type: string

    AnalystHealthSummary:
      type: object
      required: [overallHealthScore, overallRiskRating, trend, trendDetail, strengths, weaknesses, drivers, summary]
      properties:
        overallHealthScore:
          type: number
        overallRiskRating:
          $ref: "#/components/schemas/DashboardOverallRiskRating"
        trend:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        trendDetail:
          type: string
        strengths:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceHealthDriver"
        weaknesses:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceHealthDriver"
        drivers:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceHealthDriver"
        summary:
          type: string

    AnalystRiskSummary:
      type: object
      required: [highestRisk, riskTrend, largestExposure, diversificationScore, worstStressScenario, guidance]
      properties:
        highestRisk:
          type: string
        riskTrend:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        largestExposure:
          type: string
        diversificationScore:
          type: ["number", "null"]
        worstStressScenario:
          oneOf:
            - $ref: "#/components/schemas/DashboardStressTestSummaryEntry"
            - type: "null"
        guidance:
          type: array
          items:
            $ref: "#/components/schemas/DashboardGuidanceAdvisory"

    AnalystIncomeSummary:
      type: object
      required: [monthlyTheta, weeklyTheta, dailyTheta, annualizedTheta, incomeHealth, incomeHealthDetail, bySymbol, byStrategy]
      properties:
        monthlyTheta:
          type: number
        weeklyTheta:
          type: number
        dailyTheta:
          type: number
        annualizedTheta:
          type: number
        incomeHealth:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        incomeHealthDetail:
          type: string
        bySymbol:
          type: array
          items:
            $ref: "#/components/schemas/ThetaBreakdown"
        byStrategy:
          type: array
          items:
            $ref: "#/components/schemas/ThetaBreakdown"

    AnalystGreeksSummary:
      type: object
      required: [delta, gamma, theta, vega, largestContributor, deltaTrend, deltaTrendDetail, educationalLinks]
      properties:
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number
        largestContributor:
          oneOf:
            - $ref: "#/components/schemas/DashboardRiskContributor"
            - type: "null"
        deltaTrend:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        deltaTrendDetail:
          type: string
        educationalLinks:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceLearningLink"

    AnalystEventSummary:
      type: object
      required: [upcomingEvents, highestRiskEvent, safePositionsCount, atRiskPositionsCount, expirationClusters]
      properties:
        upcomingEvents:
          $ref: "#/components/schemas/PortfolioEventRiskSummary"
        highestRiskEvent:
          oneOf:
            - $ref: "#/components/schemas/HighestRiskPosition"
            - type: "null"
        safePositionsCount:
          type: integer
        atRiskPositionsCount:
          type: integer
        expirationClusters:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"

    AnalystLearningCrossLink:
      type: object
      required: [category, lessonHref, lessonTitle, glossaryHref, glossaryTerm, strategyHref, strategyLabel]
      properties:
        category:
          type: string
        lessonHref:
          type: ["string", "null"]
        lessonTitle:
          type: ["string", "null"]
        glossaryHref:
          type: ["string", "null"]
        glossaryTerm:
          type: ["string", "null"]
        strategyHref:
          type: ["string", "null"]
        strategyLabel:
          type: ["string", "null"]

    AnalystLearningSummary:
      type: object
      required: [health, risk, income, greeks, event]
      properties:
        health:
          $ref: "#/components/schemas/AnalystLearningCrossLink"
        risk:
          $ref: "#/components/schemas/AnalystLearningCrossLink"
        income:
          $ref: "#/components/schemas/AnalystLearningCrossLink"
        greeks:
          $ref: "#/components/schemas/AnalystLearningCrossLink"
        event:
          $ref: "#/components/schemas/AnalystLearningCrossLink"

    AnalystWeeklyHealthSummary:
      type: object
      required: [daysRecorded, healthScoreMin, healthScoreMax, trend]
      properties:
        daysRecorded:
          type: integer
        healthScoreMin:
          type: ["number", "null"]
        healthScoreMax:
          type: ["number", "null"]
        trend:
          type: string
          enum: [improving, declining, stable, insufficient_history]

    AnalystTimelineSection:
      type: object
      required: [asOf, comparedTo, newIssues, resolvedIssues, persistentIssues, healthChange, incomeChange, thisWeek]
      properties:
        asOf:
          type: string
        comparedTo:
          type: ["string", "null"]
        newIssues:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceTimelineEntry"
        resolvedIssues:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceTimelineEntry"
        persistentIssues:
          type: array
          items:
            $ref: "#/components/schemas/IntelligenceTimelineEntry"
        healthChange:
          oneOf:
            - $ref: "#/components/schemas/IntelligenceTimelineChange"
            - type: "null"
        incomeChange:
          oneOf:
            - $ref: "#/components/schemas/IntelligenceTimelineChange"
            - type: "null"
        thisWeek:
          $ref: "#/components/schemas/AnalystWeeklyHealthSummary"

    AnalystInstitutionalInsight:
      type: object
      required: [text, category]
      properties:
        text:
          type: string
        category:
          type: string

    PortfolioAnalystResult:
      type: object
      required:
        [
          paperTradingMode,
          deterministicAnalysis,
          executiveBriefing,
          snapshot,
          healthSummary,
          riskSummary,
          incomeSummary,
          greeksSummary,
          eventSummary,
          learningSummary,
          timeline,
          institutionalInsights,
          generatedAt,
        ]
      properties:
        paperTradingMode:
          type: boolean
          enum: [true]
        deterministicAnalysis:
          type: boolean
          enum: [true]
        executiveBriefing:
          $ref: "#/components/schemas/IntelligenceDailySummary"
        snapshot:
          $ref: "#/components/schemas/AnalystPortfolioSnapshot"
        healthSummary:
          $ref: "#/components/schemas/AnalystHealthSummary"
        riskSummary:
          $ref: "#/components/schemas/AnalystRiskSummary"
        incomeSummary:
          $ref: "#/components/schemas/AnalystIncomeSummary"
        greeksSummary:
          $ref: "#/components/schemas/AnalystGreeksSummary"
        eventSummary:
          $ref: "#/components/schemas/AnalystEventSummary"
        learningSummary:
          $ref: "#/components/schemas/AnalystLearningSummary"
        timeline:
          $ref: "#/components/schemas/AnalystTimelineSection"
        institutionalInsights:
          type: array
          items:
            $ref: "#/components/schemas/AnalystInstitutionalInsight"
        generatedAt:
          type: string

    # ─── AI Trade Journal (Phase 8, Sprint 4) ──────────────────────────────────
    # "Journal" prefix avoids any collision with the pre-existing, unrelated
    # JournalEntry/JournalEntryInput/JournalEntryUpdate schemas (the free-text,
    # user-authored journal) and Engine 2's own Trading-prefixed journal schemas.
    JournalGreeksSnapshot:
      type: object
      required: [delta, gamma, theta, vega]
      properties:
        delta:
          type: number
        gamma:
          type: number
        theta:
          type: number
        vega:
          type: number

    JournalEventRiskAtEntry:
      type: object
      required: [level, events]
      properties:
        level:
          type: string
          enum: [none, low, medium, high]
        events:
          type: array
          items:
            $ref: "#/components/schemas/EventRiskEvent"

    JournalDecisionQualityTag:
      type: object
      required: [code, label, detail, severity, ruleReference]
      properties:
        code:
          type: string
          enum:
            [
              sizing_respected,
              sizing_exceeded,
              exit_stop_loss_rule,
              exit_profit_target_rule,
              exit_manual,
              winner_let_run,
              winner_closed_early,
              loss_capped_appropriately,
              loss_ran_beyond_plan,
              held_through_earnings,
            ]
        label:
          type: string
        detail:
          type: string
        severity:
          type: string
          enum: [positive, info, watch]
        ruleReference:
          type: string

    JournalLinkedEntry:
      type: object
      required: [id, title, content]
      properties:
        id:
          type: integer
        title:
          type: string
        content:
          type: string

    JournalTradeReview:
      type: object
      required:
        [
          tradeId,
          symbol,
          strategy,
          openDate,
          closeDate,
          holdingPeriodDays,
          credit,
          maxProfit,
          maxLoss,
          realizedPnl,
          realizedPnlPercent,
          positionSizeContracts,
          positionSizePctOfAccount,
          greeksAtEntry,
          greeksAtExit,
          eventRiskAtEntry,
          exitReason,
          decisionQuality,
          linkedJournalEntry,
        ]
      properties:
        tradeId:
          type: integer
        symbol:
          type: string
        strategy:
          type: string
        openDate:
          type: string
        closeDate:
          type: ["string", "null"]
        holdingPeriodDays:
          type: number
        credit:
          type: number
        maxProfit:
          type: number
        maxLoss:
          type: number
        realizedPnl:
          type: ["number", "null"]
        realizedPnlPercent:
          type: ["number", "null"]
        positionSizeContracts:
          type: integer
        positionSizePctOfAccount:
          type: number
        greeksAtEntry:
          $ref: "#/components/schemas/JournalGreeksSnapshot"
        greeksAtExit:
          oneOf:
            - $ref: "#/components/schemas/JournalGreeksSnapshot"
            - type: "null"
        eventRiskAtEntry:
          $ref: "#/components/schemas/JournalEventRiskAtEntry"
        exitReason:
          type: ["string", "null"]
        decisionQuality:
          type: array
          items:
            $ref: "#/components/schemas/JournalDecisionQualityTag"
        linkedJournalEntry:
          oneOf:
            - $ref: "#/components/schemas/JournalLinkedEntry"
            - type: "null"

    JournalBehaviorPattern:
      type: object
      required: [code, label, detail, severity, tradeCount]
      properties:
        code:
          type: string
        label:
          type: string
        detail:
          type: string
        severity:
          type: string
          enum: [positive, watch, elevated]
        tradeCount:
          type: integer

    JournalBehaviorTrend:
      type: object
      required: [direction, detail, asOfTradeId, asOfDate]
      properties:
        direction:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        detail:
          type: string
        asOfTradeId:
          type: integer
        asOfDate:
          type: string

    JournalLearningCrossLink:
      type: object
      required: [category, lessonHref, lessonTitle, glossaryHref, glossaryTerm, strategyHref, strategyLabel]
      properties:
        category:
          type: string
        lessonHref:
          type: ["string", "null"]
        lessonTitle:
          type: ["string", "null"]
        glossaryHref:
          type: ["string", "null"]
        glossaryTerm:
          type: ["string", "null"]
        strategyHref:
          type: ["string", "null"]
        strategyLabel:
          type: ["string", "null"]

    JournalTimelineEvent:
      type: object
      required: [type, label, timestamp, tradeId]
      properties:
        type:
          type: string
          enum: [trade_opened, trade_closed, learning_completed, behaviour_change]
        label:
          type: string
        timestamp:
          type: string
        tradeId:
          type: ["integer", "null"]

    JournalDecisionQualitySummary:
      type: object
      required: [sizingRespectedRatePct, ruleBasedExitRatePct, averageDisciplineScore]
      properties:
        sizingRespectedRatePct:
          type: number
        ruleBasedExitRatePct:
          type: number
        averageDisciplineScore:
          type: number

    AITradeJournalResult:
      type: object
      required:
        [
          paperTradingMode,
          deterministicAnalysis,
          educationalOnly,
          totalClosedTrades,
          recentTrades,
          behaviorPatterns,
          behaviorTrend,
          disciplineScore,
          decisionQualitySummary,
          strengths,
          areasToImprove,
          learningRecommendations,
          timeline,
          generatedAt,
        ]
      properties:
        paperTradingMode:
          type: boolean
          enum: [true]
        deterministicAnalysis:
          type: boolean
          enum: [true]
        educationalOnly:
          type: boolean
          enum: [true]
        totalClosedTrades:
          type: integer
        recentTrades:
          type: array
          items:
            $ref: "#/components/schemas/JournalTradeReview"
        behaviorPatterns:
          type: array
          items:
            $ref: "#/components/schemas/JournalBehaviorPattern"
        behaviorTrend:
          oneOf:
            - $ref: "#/components/schemas/JournalBehaviorTrend"
            - type: "null"
        disciplineScore:
          type: number
        decisionQualitySummary:
          $ref: "#/components/schemas/JournalDecisionQualitySummary"
        strengths:
          type: array
          items:
            $ref: "#/components/schemas/JournalBehaviorPattern"
        areasToImprove:
          type: array
          items:
            $ref: "#/components/schemas/JournalBehaviorPattern"
        learningRecommendations:
          type: array
          items:
            $ref: "#/components/schemas/JournalLearningCrossLink"
        timeline:
          type: array
          items:
            $ref: "#/components/schemas/JournalTimelineEvent"
        generatedAt:
          type: string

    # ─── Institutional Mentor (Phase 8, Sprint 5) ──────────────────────────────
    # "Mentor" prefix avoids any collision with the pre-existing Analyst-
    # and Journal-prefixed schemas from the same Phase 8 sprint family.
    MentorGrade:
      type: string
      enum: [Excellent, Good, Fair, Poor]

    MentorScorecardEntry:
      type: object
      required: [category, label, score, grade, sourceModule, why]
      properties:
        category:
          type: string
          enum:
            [
              capital_allocation,
              risk_management,
              diversification,
              discipline,
              income_generation,
              position_sizing,
              greeks_management,
              event_preparation,
              portfolio_health,
            ]
        label:
          type: string
        score:
          type: number
        grade:
          $ref: "#/components/schemas/MentorGrade"
        sourceModule:
          type: string
        why:
          type: string

    MentorProfessionalObservation:
      type: object
      required: [text, category, sourceModule]
      properties:
        text:
          type: string
        category:
          type: string
        sourceModule:
          type: string

    MentorDecisionReviewItem:
      type: object
      required: [code, text, status, sourceModule, detail]
      properties:
        code:
          type: string
        text:
          type: string
        status:
          type: string
          enum: [followed, exceeded, improved, declined, neutral]
        sourceModule:
          type: string
        detail:
          type: string

    MentorCapitalAllocationReview:
      type: object
      required:
        [
          capitalEfficiencyScore,
          capitalEfficiencyGrade,
          allocationByStrategy,
          positionDistribution,
          cashUtilizationPct,
          buyingPower,
          portfolioValue,
          summary,
        ]
      properties:
        capitalEfficiencyScore:
          type: number
        capitalEfficiencyGrade:
          $ref: "#/components/schemas/MentorGrade"
        allocationByStrategy:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"
        positionDistribution:
          type: array
          items:
            $ref: "#/components/schemas/ConcentrationBucket"
        cashUtilizationPct:
          type: number
        buyingPower:
          type: number
        portfolioValue:
          type: number
        summary:
          type: string

    MentorRiskReview:
      type: object
      required:
        [
          largestPortfolioRisk,
          primaryContributor,
          riskTrend,
          riskTrendDetail,
          worstStressScenario,
          highestConcentration,
          highestEventRisk,
          guidance,
          summary,
        ]
      properties:
        largestPortfolioRisk:
          type: string
        primaryContributor:
          type: string
        riskTrend:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        riskTrendDetail:
          type: string
        worstStressScenario:
          oneOf:
            - type: object
              required: [label, portfolioValueImpact, riskScoreAfter]
              properties:
                label:
                  type: string
                portfolioValueImpact:
                  type: number
                riskScoreAfter:
                  type: number
            - type: "null"
        highestConcentration:
          oneOf:
            - type: object
              required: [dimension, bucket]
              properties:
                dimension:
                  type: string
                bucket:
                  $ref: "#/components/schemas/ConcentrationBucket"
            - type: "null"
        highestEventRisk:
          oneOf:
            - $ref: "#/components/schemas/HighestRiskPosition"
            - type: "null"
        guidance:
          type: array
          items:
            $ref: "#/components/schemas/DashboardGuidanceAdvisory"
        summary:
          type: string

    MentorIncomeReview:
      type: object
      required:
        [
          monthlyTheta,
          weeklyTheta,
          dailyTheta,
          annualizedTheta,
          incomeTrend,
          incomeTrendDetail,
          incomeSourceCount,
          bySymbol,
          byStrategy,
          summary,
        ]
      properties:
        monthlyTheta:
          type: number
        weeklyTheta:
          type: number
        dailyTheta:
          type: number
        annualizedTheta:
          type: number
        incomeTrend:
          type: string
          enum: [improving, declining, stable, insufficient_history]
        incomeTrendDetail:
          type: string
        incomeSourceCount:
          type: integer
        bySymbol:
          type: array
          items:
            $ref: "#/components/schemas/ThetaBreakdown"
        byStrategy:
          type: array
          items:
            $ref: "#/components/schemas/ThetaBreakdown"
        summary:
          type: string

    MentorBehaviourReview:
      type: object
      required:
        [
          disciplineScore,
          decisionQualitySummary,
          behaviorPatterns,
          behaviorTrend,
          strengths,
          areasToImprove,
          totalClosedTrades,
          summary,
        ]
      properties:
        disciplineScore:
          type: number
        decisionQualitySummary:
          $ref: "#/components/schemas/JournalDecisionQualitySummary"
        behaviorPatterns:
          type: array
          items:
            $ref: "#/components/schemas/JournalBehaviorPattern"
        behaviorTrend:
          oneOf:
            - $ref: "#/components/schemas/JournalBehaviorTrend"
            - type: "null"
        strengths:
          type: array
          items:
            $ref: "#/components/schemas/JournalBehaviorPattern"
        areasToImprove:
          type: array
          items:
            $ref: "#/components/schemas/JournalBehaviorPattern"
        totalClosedTrades:
          type: integer
        summary:
          type: string

    MentorLearningCrossLink:
      type: object
      required: [category, lessonHref, lessonTitle, glossaryHref, glossaryTerm, strategyHref, strategyLabel, explainModeHref]
      properties:
        category:
          type: string
        lessonHref:
          type: ["string", "null"]
        lessonTitle:
          type: ["string", "null"]
        glossaryHref:
          type: ["string", "null"]
        glossaryTerm:
          type: ["string", "null"]
        strategyHref:
          type: ["string", "null"]
        strategyLabel:
          type: ["string", "null"]
        explainModeHref:
          type: ["string", "null"]

    MentorLearningSummary:
      type: object
      required: [scorecard, capitalAllocation, risk, income, diversification, greeksManagement, eventPreparation, behaviour]
      properties:
        scorecard:
          $ref: "#/components/schemas/MentorLearningCrossLink"
        capitalAllocation:
          $ref: "#/components/schemas/MentorLearningCrossLink"
        risk:
          $ref: "#/components/schemas/MentorLearningCrossLink"
        income:
          $ref: "#/components/schemas/MentorLearningCrossLink"
        diversification:
          $ref: "#/components/schemas/MentorLearningCrossLink"
        greeksManagement:
          $ref: "#/components/schemas/MentorLearningCrossLink"
        eventPreparation:
          $ref: "#/components/schemas/MentorLearningCrossLink"
        behaviour:
          $ref: "#/components/schemas/MentorLearningCrossLink"

    # Phase 12 — a plain, ownership-scoped read of the user's own Engine 1
    # Value Watchlist (value_watchlist), reviewed by the Institutional
    # Mentor. Introduces no new scoring — currentDecision/marginOfSafetyTarget
    # are already-stored fields on the watchlist row itself.
    MentorWatchlistReviewItem:
      type: object
      required: [symbol, category, currentDecision, marginOfSafetyTarget, reason, lastResearchedAt]
      properties:
        symbol: { type: string }
        category: { type: string }
        currentDecision: { type: string }
        marginOfSafetyTarget: { type: number }
        reason: { type: string }
        lastResearchedAt: { type: ["string", "null"] }

    MentorWatchlistReview:
      type: object
      required: [itemCount, items, summary]
      properties:
        itemCount: { type: integer }
        items:
          type: array
          items:
            $ref: "#/components/schemas/MentorWatchlistReviewItem"
        summary: { type: string }

    # Phase 13 — a plain, ownership-scoped read of the user's own Engine 1
    # target-allocation portfolios (investing_portfolios/investing_holdings),
    # reviewed by the Institutional Mentor. Introduces no new scoring —
    # portfolio/holding counts only, never lib/portfolioIntelligence.ts's own
    # on-demand analyzer composition.
    MentorPortfolioReview:
      type: object
      required: [portfolioCount, totalHoldingsCount, summary]
      properties:
        portfolioCount: { type: integer }
        totalHoldingsCount: { type: integer }
        summary: { type: string }

    # Phase 14 — a plain, ownership-scoped count of the user's own saved
    # Decision Engine snapshots/notes, reviewed by the Institutional Mentor.
    # Introduces no new scoring — never a call into lib/decisionEngine.ts's
    # own per-symbol composition, which stays on-demand behind its own route.
    MentorDecisionEngineReview:
      type: object
      required: [snapshotCount, noteCount, distinctSymbolCount, summary]
      properties:
        snapshotCount: { type: integer }
        noteCount: { type: integer }
        distinctSymbolCount: { type: integer }
        summary: { type: string }

    # Phase 15 — a plain, ownership-scoped count of the user's own saved
    # Screener filter sets, reviewed by the Institutional Mentor. Introduces
    # no new scanning/ranking — never a call into lib/opportunityDiscovery.ts's
    # own scan orchestration, which stays on-demand behind its own page.
    MentorOpportunityDiscoveryReview:
      type: object
      required: [savedScreenCount, summary]
      properties:
        savedScreenCount: { type: integer }
        summary: { type: string }

    InstitutionalMentorResult:
      type: object
      required:
        [
          paperTradingMode,
          deterministicAnalysis,
          educationalOnly,
          scorecard,
          professionalReview,
          decisionReview,
          capitalAllocationReview,
          riskReview,
          incomeReview,
          behaviourReview,
          learningSummary,
          watchlistReview,
          portfolioReview,
          decisionEngineReview,
          opportunityDiscoveryReview,
          generatedAt,
        ]
      properties:
        paperTradingMode:
          type: boolean
          enum: [true]
        deterministicAnalysis:
          type: boolean
          enum: [true]
        educationalOnly:
          type: boolean
          enum: [true]
        scorecard:
          type: array
          items:
            $ref: "#/components/schemas/MentorScorecardEntry"
        professionalReview:
          type: array
          items:
            $ref: "#/components/schemas/MentorProfessionalObservation"
        decisionReview:
          type: array
          items:
            $ref: "#/components/schemas/MentorDecisionReviewItem"
        capitalAllocationReview:
          $ref: "#/components/schemas/MentorCapitalAllocationReview"
        riskReview:
          $ref: "#/components/schemas/MentorRiskReview"
        incomeReview:
          $ref: "#/components/schemas/MentorIncomeReview"
        behaviourReview:
          $ref: "#/components/schemas/MentorBehaviourReview"
        learningSummary:
          $ref: "#/components/schemas/MentorLearningSummary"
        watchlistReview:
          $ref: "#/components/schemas/MentorWatchlistReview"
        portfolioReview:
          $ref: "#/components/schemas/MentorPortfolioReview"
        decisionEngineReview:
          $ref: "#/components/schemas/MentorDecisionEngineReview"
        opportunityDiscoveryReview:
          $ref: "#/components/schemas/MentorOpportunityDiscoveryReview"
        generatedAt:
          type: string

    # ─── Institutional Reporting & Client Presentation Engine (Phase 22) —
    # pure composition layer over already-computed engines; no new valuation
    # model, scoring system, or investment recommendation. See
    # lib/institutionalReporting.ts's own header comment. ────────────────────
    ReportTypeMeta:
      type: object
      required: [reportType, label, description, requiresSymbol, requiresPortfolio]
      properties:
        reportType:
          type: string
          enum: [investment-committee, company-research, portfolio-review, portfolio-health, watchlist, opportunity-discovery, monitoring-summary, ai-coach-summary, executive-summary]
        label: { type: string }
        description: { type: string }
        requiresSymbol: { type: boolean }
        requiresPortfolio: { type: boolean }

    InstitutionalReport:
      type: object
      required: [reportType, title, subtitle, symbol, portfolioId, generatedAt, dataSource, sections, disclaimer]
      properties:
        reportType:
          type: string
          enum: [investment-committee, company-research, portfolio-review, portfolio-health, watchlist, opportunity-discovery, monitoring-summary, ai-coach-summary, executive-summary]
        title: { type: string }
        subtitle: { type: string }
        symbol: { type: string, nullable: true }
        portfolioId: { type: integer, nullable: true }
        generatedAt: { type: string }
        dataSource: { type: string }
        sections:
          type: array
          items: { $ref: "#/components/schemas/ValueReportSection" }
        disclaimer: { type: string }

    SaveInstitutionalReportInput:
      type: object
      required: [reportType]
      properties:
        reportType:
          type: string
          enum: [investment-committee, company-research, portfolio-review, portfolio-health, watchlist, opportunity-discovery, monitoring-summary, ai-coach-summary, executive-summary]
        symbol: { type: string }
        portfolioId: { type: integer }

    SavedInstitutionalReport:
      type: object
      required: [id, reportType, title, symbol, portfolioId, dataSource, createdAt, report]
      properties:
        id: { type: integer }
        reportType: { type: string }
        title: { type: string }
        symbol: { type: string, nullable: true }
        portfolioId: { type: integer, nullable: true }
        dataSource: { type: string }
        createdAt: { type: string }
        report:
          $ref: "#/components/schemas/InstitutionalReport"

    InstitutionalReportListItem:
      type: object
      required: [id, reportType, title, symbol, portfolioId, dataSource, createdAt]
      properties:
        id: { type: integer }
        reportType: { type: string }
        title: { type: string }
        symbol: { type: string, nullable: true }
        portfolioId: { type: integer, nullable: true }
        dataSource: { type: string }
        createdAt: { type: string }
