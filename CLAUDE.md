# CLAUDE.md — DK AI Institutional Investing & Trading OS

This file is read by Claude Code at the start of every session in this repository. It is the authoritative summary of what this project is, what stage it's at, and what must never happen by accident.

---

## 1. What this project is

**DK AI Institutional Investing & Trading OS** — an evolution of the existing, working **DK Option Engine** (an institutional-grade options income platform) into a three-engine platform:

- **Engine 1 — Institutional Investing Engine.** Company research, financial statement analysis, valuation models (DCF/Graham/Buffett), economic moat analysis, quality score, management analysis, industry comparison, portfolio construction, AI research assistant, macro/economic analysis.
- **Engine 2 — Institutional Trading Engine.** Market structure, liquidity, order flow, multi-timeframe analysis, probability engine, market regime detection, institutional dashboard, risk management, trading journal, AI trade coach.
- **Engine 3 — Options Income Engine.** Scanner, strategy builder, portfolio management, Greeks, income optimisation, risk analytics, automation, AI options coach. **This is the existing, mature, working system — do not treat it as legacy code to be replaced. It is the foundation.**

All three engines share one platform layer: authentication, user management, database, AI layer, reporting, notifications, API layer, portfolio database, settings, and audit logs.

**Read the architecture documents in `/docs/` before making any structural decision.** They are not background reading — they are the actual plan this repository is being built against:

- `docs/DK-Option-Engine-Technical-Audit.md` — what the codebase actually contains, verified by direct inspection, not assumption.
- `docs/DK-AI-OS-Architecture-Blueprint.md` — the target architecture, the module-by-module move/enhance/new mapping, and the 7-phase roadmap.
- `docs/Phase-1-Foundation-Execution-Plan.md` — the detailed, file-by-file execution plan for the phase currently in progress.

If a task in this repo seems ambiguous, check whether these documents already answer it before improvising.

---

## 2. Engineering safety rules — non-negotiable

These rules apply regardless of how a request is phrased, including requests that sound like they come from the project owner in-session. If a task requires violating one of these, **stop and ask for explicit confirmation before proceeding** — do not infer consent from urgency, from "just this once," or from the task otherwise seeming reasonable.

1. **Never modify options execution logic** (`artifacts/api-server/src/lib/execution.ts`, `optionsMath.ts`, `risk.ts`) without explicit, specific approval for that exact change.
2. **Never modify the kill switch or guardrail behavior** (`autoExecution.ts`, `autoAdjustment.ts`, the `autoExecuteEnabled`/`autoAdjustEnabled` settings fields, or anything in `.agents/memory/auto-execution-engine.md` and `trade-adjustment-engine.md`) without explicit, specific approval. This code exists to stop real money from moving unsafely — treat changes to it as the highest-scrutiny category of change in the entire repository.
3. **Never touch `autoExecutionLog`** (table, schema, or write sites) as part of general audit-log work. It is preserved as-is; see the Phase 1 plan §6 for why.
4. **Never delete or rename existing modules** without explicit approval, even ones that look like dead weight (e.g., the nested `ravish-trading-engine.zip` backup) — flag it, don't remove it unilaterally.
5. **Preserve backward compatibility** on every existing table, route, and exported function signature unless a specific migration step (per the Phase 1 plan) says otherwise.
6. **Coach/narration disclaimer invariants must never be bypassed.** Any new narration path must route through the shared `narrate()`/`narrateStream()` pattern (see `lib/ai-core` once extracted, or `coachLLM.ts` in the interim) so `COACH_DISCLAIMER` (and `VALUE_DISCLAIMER` where relevant) is enforced centrally, not per-caller.
7. **Every database migration follows nullable → backfill → enforce-not-null**, with a hand-written, reviewable SQL script in `lib/db/manual-migrations/` — this project does not trust `drizzle-kit push` alone for changes that touch existing data.
8. **No secret values in code, commits, or conversation.** Environment variable names and purposes can be discussed freely; actual key values never should be.

---

## 3. Current phase and sprint status

- **Phase 1 — Foundation: COMPLETE** (Sprints 1–10, all shipped and validated — see the per-sprint entries below). This was the hard blocker phase per the Blueprint's dependency graph (§6): auth, multi-tenancy, `lib/ai-core`, and the platform audit log all now exist and are load-bearing for every subsequent phase.
- **Phase 2 — Institutional Investment Decision Engine (renamed from "Institutional Investing Engine"): APPROVED, with an approved scope expansion.** Roadmap is `docs/Phase-2-Investing-Engine-Execution-Plan.md` (module-by-module mapping, cross-cutting owner decisions, and a 21-sprint breakdown, Sprints 11–31 — renumbered after Sprint 15's insertion, see that sprint's entry below). After Sprint 12 shipped, the project owner directed an expansion of the engine's mandate: an 18th module, a **Tom Nash Investment Engine** (a third named analyst alongside Graham/Buffett — business quality, growth, capital allocation, cash flow quality, balance sheet strength, buybacks, insider ownership, sector/macro, rate sensitivity, AI/tech-cycle analysis, probability-based scoring, conviction score, Buy/Hold/Wait recommendation — split across a Core sprint plus 2 later Enhancement sprints to maximize reuse of data infrastructure other sprints build first), and a 19th, an **AI Investment Committee** (a synthesis layer combining Graham + Buffett + Tom Nash's independent verdicts into one consolidated recommendation + confidence score). See the roadmap's §0.1 for the full rationale and the 4 new owner decisions this introduces. Read that document before starting any Phase 2 sprint — it is the authoritative execution plan for this phase, the same role `docs/Phase-1-Foundation-Execution-Plan.md` played for Phase 1.
- **Sprint 11 — COMPLETE.** Universe Decoupling, Provider Verification & Engine 1 Settings (Phase 2 plan, Sprint 11). The Investing Engine's SIMULATED path no longer depends on `optionsMath.ts` in any way. New `lib/deterministic.ts` holds the pure RNG/date helpers (`hashStr`/`mulberry32`/`makeRng`/`todayStr`) extracted byte-for-byte out of `optionsMath.ts`, which now just imports and re-exports them — every existing options-engine caller is unaffected. New `lib/investingUniverse.ts` gives the Investing Engine its own `INVESTING_UNIVERSE` (the same 10 symbols/base prices `optionsMath.ts`'s `UNIVERSE` used, duplicated not imported), `investingPrice()` (reproduces the exact historical price formula for those 10 symbols), and `isValidTickerShape()` (a ticker-shaped-string guard so "NOTASYMBOL"-style garbage still honestly 404s). `fundamentals.ts`'s `SimulatedFundamentalsProvider` now uses these instead of `optionsMath.getSnapshot()`/`UNIVERSE`, and gained a new private `syntheticProfile()` generator so any valid-shaped ticker outside the original 10 (e.g. `IBM`) now produces a plausible, deterministic, clearly-SIMULATED report instead of `null` — never fabricating a real-sounding company name (falls back to the ticker itself). `routes/stockAnalyst.ts`'s `/value-universe` and `fundamentals-freshness.test.ts` were updated to import `INVESTING_UNIVERSE` instead of `optionsMath`'s `UNIVERSE` (mechanical, same 10 symbols, same behavior). Three new nullable-with-default `settings` columns for future Investing Engine sprints: `investingRiskFreeRate` (default `0.045`, matching `optionsMath.ts`'s existing `RISK_FREE` constant), `investingDefaultDiscountRate` (default `0.09`), `investingFilingsProvider` (default `"edgar"`), via `lib/db/manual-migrations/006_investing_settings.sql` (purely additive, no backfill needed) — `openapi.yaml`'s `Settings`/`SettingsUpdate` schemas and the generated `api-zod`/`api-client-react` packages were regenerated via `pnpm --filter @workspace/api-spec run codegen` to match. **Deliberately, explicitly out of scope for this sprint (discovered during implementation, not the pre-approval plan):** `lib/valueReport.ts` and `lib/valueInvesting.ts` also import from `optionsMath.ts` (`getSnapshot` for the Stock-vs-Options bridge's `ivRank` signal, and the `Snapshot` type) — this is a deliberate, intentional cross-engine read (Engine 1 consulting Engine 3's options data for its own "stock vs. options" verdict), not an accidental coupling, and was left untouched. **Deferred per explicit instruction: live FMP/Alpha Vantage provider verification — awaiting production API credentials.** Neither `FMP_API_KEY` nor `ALPHA_VANTAGE_API_KEY` was available in this session; no live fetch was attempted and none is claimed. New tests: `lib/deterministic.test.ts`, `lib/investingUniverse.test.ts`, `lib/fundamentals.investingUniverse.test.ts` (the last independently re-derives the pre-Sprint-11 price formula from hardcoded legacy base prices and asserts byte-for-byte equality for all 10 legacy symbols, plus proves `IBM` now returns a valid SIMULATED report while `NOTASYMBOL` still correctly 404s). No trading logic, options execution, pricing, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (24 files / 315 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 12 — COMPLETE.** Graham Valuation Engine (Phase 2 plan, Sprint 12). New `lib/grahamValuation.ts` — `analyzeGrahamValuation(f, riskFreeRate?)` — a second, standalone, provider-agnostic valuation model alongside the existing blended `analyzeValuation()` (which is untouched this sprint; splitting it into "Buffett Valuation" is later sprints' job). Computes the classic Graham Number (`sqrt(22.5 × EPS × book value/share)`) and Graham's growth formula (`EPS × (8.5 + 2g) × 4.4/Y`), averaging whichever method(s) are computable, honestly `available: false` when trailing EPS isn't positive — never fabricated. Deliberately uses **trailing EPS only** (`epsTtm`, never `epsFwd`), preserving Graham's own conservative methodology, unlike the blended model's forward-EPS preference. `riskFreeRate` (the formula's "Y") defaults to `investingRiskFreeRate`'s Sprint-11 default (0.045) as a parameter — **per-user Settings threading is explicitly deferred**, not wired into `buildValueResearchReport()` this sprint. Continues generating for ETFs, with the existing "diversified fund" caveat pattern appended to the summary, matching every other analyzer's ETF handling. `valueInvesting.ts` gained two small, reuse-driven changes: `FairValueMethod` is now exported (Graham reuses the exact `{method, fairValue, detail}` shape instead of redefining it) and the margin-of-safety → label/rating classification ladder was extracted into a new exported `classifyMarginOfSafety()` (a pure behavior-preserving refactor — `analyzeValuation()`'s output is unchanged; Graham calls the same function, and DCF/Buffett will too). `valueReport.ts`'s report grew from 15 to 16 sections: a new "9. Graham Valuation" section (id `graham-valuation`) was inserted right after "8. Valuation & Fair Value," renumbering sections 9–15 to 10–16 — every existing section's stable `id` string is unchanged, only the display numbers shifted, confirmed by regression test. `StockResearch.tsx` gained a 5th pillar card ("Graham Valuation," `Calculator` icon) mirroring the existing Valuation card's layout. `openapi.yaml` gained a new `ValueGrahamValuation` schema and a `grahamValuation` field on `ValueResearchReport`; `api-zod`/`api-client-react` regenerated to match. No database migration — the entire report (including the new field) is already persisted via `stockAnalysisHistoryTable.valueResearchJson`, unchanged. One pre-existing frontend test (`StockResearch.test.tsx`) needed a one-line fix: `getByText("Margin of safety")` became `getAllByText(...).toHaveLength(2)` since that label now legitimately appears on two cards (Valuation and Graham) — not a behavior regression, just two real matches instead of one. New tests: `grahamValuation.test.ts` (formula correctness, honest-unavailable path, ETF caveat, trailing-vs-forward EPS discipline, custom risk-free-rate sensitivity) and `valueReport.grahamIntegration.test.ts` (proves every pre-existing section `id` and top-level field is unchanged, the new section/field are correctly wired and shaped, and Graham's own unavailable path is independent of the blended model's). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (26 files / 330 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 13 — COMPLETE.** DCF Valuation Engine (Phase 2 plan, Sprint 13). New `lib/dcfValuation.ts` — `analyzeDcfValuation(f, discountRate?, terminalGrowthRate?, projectionYears?)` — a third, standalone, provider-agnostic valuation model alongside Graham (Sprint 12) and the existing blended model. A genuine multi-year projection: FCF grows from `f.revenueGrowth5y` and decays **linearly** to a terminal growth rate over the projection horizon, discounted at `discountRate`, plus a Gordon-growth terminal value discounted back — honestly `available: false` when FCF isn't positive or the discount rate doesn't exceed the terminal growth rate (no finite terminal value), never fabricated. Approved defaults: `discountRate` 0.09 (matches `investingDefaultDiscountRate`'s Sprint-11 default, same deferred-per-user-Settings-threading pattern as Graham's risk-free rate), `terminalGrowthRate` 0.025, `projectionYears` 5 — all plain function parameters, no new Settings field this sprint. A new deterministic **confidence label + explanation** (`High`/`Moderate`/`Low`) is derived from a ±1% discount-rate / ±0.5% terminal-growth sensitivity band computed by rerunning the same internal projection helper — never a fabricated qualitative claim. Reuses Sprint 12's shared infrastructure directly: `classifyMarginOfSafety()` for the label/rating (zero duplicated classification logic) and the `FairValueMethod` shape for its 2-entry methods breakdown ("Projected Cash Flows" + "Terminal Value"). Continues the established ETF caveat pattern. `valueReport.ts`'s report grew from 16 to 17 sections: a new "10. DCF Valuation" section (id `dcf-valuation`) was inserted right after "9. Graham Valuation," renumbering sections 10–16 to 11–17 — every stable section `id` unchanged, confirmed by regression test, including Sprint 12's own `graham-valuation` id. `StockResearch.tsx` gained a 6th pillar card ("DCF Valuation," `LineChart` icon) showing Intrinsic Value, Discount Rate, Terminal Growth Rate, Margin of Safety, and the Confidence Explanation. `openapi.yaml` gained a new `ValueDcfValuation` schema and a `dcfValuation` field on `ValueResearchReport`; `api-zod`/`api-client-react` regenerated to match. No database migration — persists via the same `stockAnalysisHistoryTable.valueResearchJson` blob. Two pre-existing test files needed mechanical updates, not behavior changes: `StockResearch.test.tsx`'s `getAllByText("Margin of safety")` count went from 2 to 3 (three cards now share that label), and Sprint 12's own `valueReport.grahamIntegration.test.ts` had its hardcoded total-section-count (16→17) and post-Graham section-numbering assertions (10→11 onward) updated to reflect Sprint 13's legitimate insertion — Graham's actual computed output is separately proven byte-identical via a new dedicated regression assertion (`analyzeGrahamValuation()` called standalone vs. via the full report, `toEqual`). New tests: `dcfValuation.test.ts` (hardcoded golden-value projection/terminal-value math, honest-unavailable paths for non-positive FCF and discount-rate-below-terminal-growth, confidence-band derivation, parameter overrides, ETF caveat) and `valueReport.dcfIntegration.test.ts` (proves every pre-existing section `id` including `graham-valuation` is unchanged, **Graham's and the blended model's own outputs are proven unchanged via direct equality against standalone calls**, the new section/field are correctly wired, and DCF's unavailable path is independent of Graham's). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (28 files / 347 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 14 — COMPLETE.** Buffett Valuation & Consolidated Margin of Safety (Phase 2 plan, Sprint 14). New `lib/buffettValuation.ts` — `analyzeBuffettValuation(f, bq, moat, baseRequiredReturn?)` — a fourth, standalone, provider-agnostic valuation model alongside Graham, DCF, and the existing blended model. **The blended `analyzeValuation()` was deliberately NOT retired** — the approved sprint plan resolved the roadmap's own flagged open question in favor of the lower-risk additive path (matching Graham/DCF's precedent), since retiring it would have touched `analyzeStockVsOptions`/`analyzeValueDecision`/`buffettChecklist`/the report's required `valuation` field/the heaviest existing test file for no real benefit. Buffett's model capitalizes owner earnings (approximated as `fcfPerShare`, same honest approximation DCF uses, pending Sprint 18's real statement data) as a **no-growth perpetuity** at a quality/moat-adjusted required return — base rate 9% (matching `investingDefaultDiscountRate`'s Sprint-11 default) minus up to a 4% discount scaled by `businessQuality.score` and `moat.rating`, floored at 3% — a deterministic heuristic, not a real WACC/CAPM model, deliberately distinct in shape from both DCF's multi-year projection and Graham's fixed formulas. New `lib/marginOfSafety.ts` — `consolidateMarginOfSafety(price, entries)` — takes all 4 models' results and produces a fair-value range (min/max/average), an average margin of safety, and an honest agreement signal (`unanimous`/`majority`/`split`/`insufficient-data`, based on how the available models' ratings cluster into cheap/fair/expensive buckets) — never fabricates a value for a model that reports itself unavailable. Reuses Sprint 12's shared infrastructure directly: `classifyMarginOfSafety()` and `FairValueMethod` for Buffett's own output shape; `ConsolidatableResult` is a small new structural interface (not a new dependency on Graham/DCF/Buffett's concrete types) so `marginOfSafety.ts` stays independent. `valueReport.ts`'s report grew from 17 to 18 sections: a new "11. Buffett Valuation" section (id `buffett-valuation`) was inserted right after "10. DCF Valuation," renumbering sections 11–17 to 12–18. **The existing `margin-of-safety` section's body was deliberately upgraded** (id/title/position unchanged) from describing only the blended model's own margin of safety to showing the new consolidated cross-model view — a real, visible content change to existing output, per the roadmap's own framing of what this sprint's Margin of Safety deliverable means, confirmed as an explicit owner decision before implementation. `StockResearch.tsx` gained a 7th pillar card ("Buffett Valuation," `Briefcase` icon) and a new "Margin of Safety (Consolidated)" summary card positioned above the four valuation-model cards, showing the range/average/agreement badge. `openapi.yaml` gained `ValueBuffettValuation` and `ValueConsolidatedMarginOfSafety` schemas (plus a small `ValueModelFairValue` schema) and two new fields on `ValueResearchReport`; `api-zod`/`api-client-react` regenerated to match. No database migration — persists via the same `stockAnalysisHistoryTable.valueResearchJson` blob. Three pre-existing test files needed mechanical updates, not behavior changes: `StockResearch.test.tsx`'s `getAllByText("Margin of safety")` count went from 3 to 4 (four cards now share that label), and both `valueReport.grahamIntegration.test.ts` and `valueReport.dcfIntegration.test.ts` had their hardcoded total-section-counts and post-insertion section-numbering assertions updated to reflect Sprint 14's legitimate insertion — Graham's, DCF's, and the blended model's actual computed outputs are separately proven byte-identical via dedicated regression assertions (`toEqual` against standalone calls). One existing test (`value.test.ts`) asserted the `margin-of-safety` section's body matched `/unavailable|not computable/i` in its honest-unprofitable-fixture case; rather than patch the test, `marginOfSafety.ts`'s own zero-models-available summary wording was adjusted to naturally say "unavailable," keeping it consistent with every other analyzer's honesty vocabulary — the test needed no change. New tests: `buffettValuation.test.ts` (hand-calculated required-return/perpetuity math, quality/moat sensitivity, honest-unavailable path, ETF caveat, required-return floor), `marginOfSafety.test.ts` (unanimous/majority/split/insufficient-data scenarios via constructed fixtures, never fabricating a value for an unavailable model), and `valueReport.buffettIntegration.test.ts` (proves every pre-existing section `id` including `graham-valuation`/`dcf-valuation` is unchanged, **Graham's, DCF's, and the blended model's own outputs are proven unchanged via direct equality against standalone calls**, the new fields/section are correctly wired, and Buffett's unavailable path is independent of the others'). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (31 files / 373 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 15 — COMPLETE.** Investment Quality Engine (Phase 2 plan, Sprint 15 — inserted ahead of Tom Nash Core at the project owner's explicit direction after Sprint 14 shipped; this insertion pushed the roadmap's former Sprint 15 (Tom Nash Core) onward to Sprint 16, and every sprint after it by one, through the former Sprint 30 (Company Research Unification) becoming Sprint 31 — the roadmap document itself was renumbered end-to-end to reflect this). New `lib/investmentQuality.ts` — `analyzeInvestmentQuality(f)` — a reusable, provider-agnostic, metric-by-metric quality scoring engine explicitly designed as **shared infrastructure**: the Buffett Engine, the future Tom Nash Engine, the AI Investment Committee, future valuation models, and Portfolio AI are all meant to consume this rather than each hand-rolling its own quality scoring (this sprint does not yet rewire Buffett/Graham/DCF/the blended model to consume it — that remains future work; this sprint only builds and reports the new engine standalone). Scores all 12 requested metrics — Revenue Growth, EPS Growth, Free Cash Flow Growth (a CAGR derived from the existing `fcfHistory` array — no schema change), ROE, ROIC, Gross Margin, Operating Margin, Net Margin, Debt Levels, Cash Position, Share Dilution/Buybacks, Insider Ownership — against fixed target benchmarks (e.g. 20% revenue growth, 35% ROE), each individually reported with a 0–100 score, weight, and human-readable detail. **Share Dilution/Buybacks and Insider Ownership are always honestly reported `unavailable`** (never fabricated) since `Fundamentals` has no backing data for either, for any provider — this was a known, pre-flagged gap: the roadmap's own Sprint 24 (Tom Nash Enhancement I, formerly Sprint 23) is explicitly sequenced to add exactly this data by reusing filing-ingestion infrastructure that doesn't exist yet. A direct consequence, called out deliberately rather than hidden: with 10 of 12 metrics always available by design, `confidenceLevel` computes to **"Moderate" for every company today** — a self-documenting property, not a bug, that will naturally become "High" once Sprint 24 fills the last two fields, with zero code change to this module. `valueInvesting.ts`'s `analyzeFinancialStrength()` had its leverage/interest-coverage/net-cash formulas extracted into three small exported pure functions (`leverageScore`, `coverageScore`, `cashPositionScore`) — a behavior-preserving refactor, confirmed byte-identical via `analyzeFinancialStrength`'s own existing tests — so Investment Quality's "Debt Levels" (leverage+coverage, same 5:4 relative weighting `analyzeFinancialStrength` already used) and "Cash Position" metrics reuse the exact same math instead of a second, subtly different formula. `valueReport.ts`'s report grew from 18 to 19 sections: a new "4. Investment Quality" section (id `investment-quality`) was inserted immediately after "3. Business Quality," renumbering every section from "Economic Moat" (4→5) through "Disclaimers & Data Source" (18→19) — every existing section's stable `id` is unchanged, only display numbers shifted, confirmed by regression test. `StockResearch.tsx` gained a new "Investment Quality" pillar card (`ListChecks` icon, positioned right after the Business Quality card) showing all 12 metrics with per-metric score bars (unavailable metrics shown as "N/A" with their reason in italics instead of a fabricated bar), plus Strengths/Weaknesses lists. `openapi.yaml` gained new `ValueQualityMetricScore`/`ValueInvestmentQuality` schemas and an `investmentQuality` field on `ValueResearchReport`; `api-zod`/`api-client-react` regenerated to match. No database migration — persists via the same `stockAnalysisHistoryTable.valueResearchJson` blob. Three pre-existing integration test files needed mechanical section-count/numbering updates only (established pattern): `valueReport.grahamIntegration.test.ts`, `valueReport.dcfIntegration.test.ts`, `valueReport.buffettIntegration.test.ts` — Graham's, DCF's, and Buffett's actual computed outputs are separately proven byte-identical via dedicated regression assertions (`toEqual` against standalone calls), and the blended model's own output is likewise proven unchanged. New tests: `investmentQuality.test.ts` (all 12 metrics' scoring math against their benchmarks, the FCF-Growth-CAGR derivation and its honest-unavailable path, the Debt Levels/Cash Position formula-reuse proof, the two permanently-unavailable metrics' honest reasons, confidence-level threshold behavior, strengths/weaknesses derivation, ETF caveat) and `valueReport.investmentQualityIntegration.test.ts` (proves every pre-existing section `id` is unchanged, **Graham's, DCF's, Buffett's, and the blended model's own outputs are proven unchanged via direct equality against standalone calls**, the new section/field are correctly wired, and Investment Quality computes independently of Graham/DCF's own availability). One threshold-tuning fix during implementation, not a design change: the confidence-level thresholds were adjusted from `>=0.9`/`>=0.7` to `>=0.95`/`>=0.8` so a 9-of-12 scenario (FCF Growth also unavailable) correctly falls to "Low" rather than still reading "Moderate," which a test caught before commit. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (33 files / 397 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 16 — COMPLETE.** Tom Nash Investment Engine — Core (Phase 2 plan, Sprint 16, per the roadmap's post-Sprint-15 renumbering). New `lib/tomNashEngine.ts` — `analyzeTomNash(f, iq, fin, blended, graham, dcf, buffett)` — a pure COMPOSITION layer over the reusable modules already shipped, deliberately introducing almost no new scoring logic. Five pillars: **Business Quality** = Investment Quality Engine's own overall score (Sprint 15), reused whole; **Growth** = a renormalized average of Investment Quality's own Revenue/EPS/FCF Growth metric scores (same scored objects, re-aggregated over a subset); **Capital Allocation** = a renormalized average of Cash Position/Debt Levels/ROIC (relabeled "Capital efficiency" for this engine's display), with Share Dilution/Buybacks surfaced as an unavailable line item but excluded from the numeric average — never fabricated; **Financial Strength** = `analyzeFinancialStrength()`'s own score, called through directly; **Valuation** — **the one genuinely new piece of logic** — a `{Cheap:100, Fair:65, Expensive:35, "Very Expensive":0}` bucket mapping applied to whichever of Blended/Graham/DCF/Buffett are `available`, reusing their already-computed `rating` fields (themselves produced by the shared `classifyMarginOfSafety()`), deliberately isolated inside `tomNashEngine.ts` so it can be refined later without touching Graham/DCF/Buffett/Investment Quality. Overall **Conviction Score** = an equal-weighted (20% each, per approved decision) average across the 5 pillars, renormalized over whichever produced a non-null score; the weight constants are named and kept adjustable for future Tom Nash enhancement sprints, not hardcoded inline. **Buy/Hold/Wait verdict**: `>=70` → Buy, `45–69` → Hold, `<45` → Wait (approved thresholds, explicitly recalibratable later with real-world performance data). Output shape (`{businessQuality, growth, capitalAllocation, financialStrength, valuation, convictionScore, verdict, rationale, summary}`) deliberately mirrors the `{verdict, conviction, rationale, summary}` contract every other analyst already produces, so a future AI Investment Committee (Sprint 17) can treat Tom Nash as one more voting member via the same structural pattern. Explicitly, deliberately **not** in this sprint (per the approved scope): macro analysis, interest-rate analysis, sector rotation, AI-cycle analysis, insider-ownership scoring, filing ingestion — Investment Quality's own "Insider Ownership" metric is simply never pulled into any Tom Nash pillar, proven by a dedicated regression test. **Integration decision:** folded directly into `buildValueResearchReport()` (new field, new "14. Tom Nash Analysis" section inserted right after "Margin of Safety") rather than the roadmap's originally-envisioned separate `GET /stock-analyst/tom-nash/:symbol` route — Core's actual composition is 100% in-memory reuse of already-computed values with no new provider calls, so the "keep the core report fast" rationale for a dedicated route doesn't apply yet; that route makes more sense once the later Enhancement sprints (24/26) add genuinely heavier external data. `valueReport.ts`'s report grew from 19 to 20 sections: the new `tom-nash` section sits between `margin-of-safety` (unchanged at "13.") and `risks` (which, along with `decision`/`stock-vs-options`/`checklist`/`metrics`/`disclaimer`, shifted by exactly one) — every existing section's stable `id` is unchanged, confirmed by regression test. `StockResearch.tsx` gained a new full-width "Tom Nash Analysis" card (`Compass` icon, positioned between the pillar-cards grid and the Decision/Stock-vs-Options row) showing the conviction score, Buy/Hold/Wait badge, all 5 pillar scores via the existing `FactorList` component, and the full reasoning list. `openapi.yaml` gained new `ValueTomNashPillarScore`/`ValueTomNashAnalysis` schemas and a `tomNash` field on `ValueResearchReport`; `api-zod`/`api-client-react` regenerated to match. No database migration — persists via the same `stockAnalysisHistoryTable.valueResearchJson` blob. Four pre-existing integration test files needed mechanical section-count/numbering updates only (established pattern): `valueReport.grahamIntegration.test.ts`, `valueReport.dcfIntegration.test.ts`, `valueReport.buffettIntegration.test.ts`, `valueReport.investmentQualityIntegration.test.ts` — Graham's, DCF's, Buffett's, and Investment Quality's actual computed outputs are separately proven byte-identical via dedicated regression assertions (`toEqual` against standalone calls), and the blended model's own output is likewise proven unchanged. New tests: `tomNashEngine.test.ts` (each pillar's reuse/composition math, the bucket-mapping Valuation score and its honest-unavailable path when all 4 models are unavailable, conviction-score weighting and renormalization, Buy/Hold/Wait threshold derivation, proof that Insider Ownership is never surfaced, ETF caveat) and `valueReport.tomNashIntegration.test.ts` (proves every pre-existing section `id` is unchanged, **Graham's, DCF's, Buffett's, the blended model's, and Investment Quality's own outputs are proven unchanged via direct equality against standalone calls**, the new section/field are correctly wired, and Tom Nash's Business Quality/Financial Strength pillars are byte-identical to the report's own `investmentQuality.score`/`financialStrength.score`). One pre-existing test (`fundamentals.investingUniverse.test.ts`) failed once on a 1ms `fetchedAt` timing flake unrelated to this sprint's changes and passed cleanly on re-run — not a regression. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (35 files / 423 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 17 — COMPLETE.** AI Investment Committee — Core (Phase 2 plan, Sprint 17, per the roadmap's post-Sprint-15 renumbering). New `lib/investmentCommittee.ts` — `synthesizeInvestmentCommittee(graham, buffett, tomNash)` — a pure ORCHESTRATION layer, not a fourth analyst: it takes Graham's, Buffett's, and Tom Nash's independently-computed outputs and combines them into one consolidated recommendation, introducing no new business logic beyond what genuinely didn't exist yet. Graham and Buffett are pure valuation models — they only produce a categorical `rating`, not a Buy/Hold/Wait verdict — so per the approved decision each casts its Committee vote via a bucket collapse of that rating (Cheap → Buy, Fair → Hold, Expensive/Very Expensive → Wait), with confidence reusing the **exact same** rating→score table Tom Nash's own Valuation pillar already computes from (`VALUATION_RATING_SCORE`, exported from `tomNashEngine.ts` — zero new scoring logic, one more consumer of an existing table). Tom Nash (Sprint 16) already produces a real `{verdict, convictionScore}` and votes with that directly, completely unmodified. **Agreement classification reuses `marginOfSafety.ts`'s own algorithm**, generalized (this sprint's extraction) into an exported, generic `classifyAgreementSignal<T>(labels: T[])` — the exact same `unanimous/majority/split/insufficient-data` bucket-counting logic already proven for the Consolidated Margin of Safety (Sprint 14), confirmed byte-identical for `consolidateMarginOfSafety()`'s own output via its existing tests, now reused for the Committee's 3 Buy/Hold/Wait votes instead of 4 valuation ratings. Per the approved decision, when all voting analysts genuinely disagree (**split**), the consolidated verdict defaults to **"Hold"** — the safe, neutral call, never a forced coin-flip — and the `agreement` field is always surfaced in the summary so a user can see the actual level of consensus regardless of outcome. A model that's `available: false` (e.g. Graham/Buffett on a deeply unprofitable company) is simply excluded from `votes[]` — never fabricated; if only Tom Nash ends up voting, `agreement` honestly reports `"insufficient-data"` and the consolidated verdict is that lone vote. **Reasoning is deterministic and rule-based only this sprint** (one line per vote plus one consolidated line) — per the approved decision, real `ai-core`-narrated synthesis is explicitly deferred to a later Committee enhancement sprint once the aggregation logic itself is stable; no LLM calls, no new disclaimer-enforcement surface, no `coachLLM.ts` involvement this sprint. **Integration decision, consistent with Sprint 16's own precedent:** folded directly into `buildValueResearchReport()` (new field, new "15. Investment Committee" section inserted right after "Tom Nash Analysis") rather than a separate route, since Core has no new external data dependency. `valueReport.ts`'s report grew from 20 to 21 sections: the new `investment-committee` section sits between `tom-nash` (unchanged at "14.") and `risks` (which, along with `decision`/`stock-vs-options`/`checklist`/`metrics`/`disclaimer`, shifted by exactly one) — every existing section's stable `id` is unchanged, confirmed by regression test. `StockResearch.tsx` gained a new "Investment Committee" card (`Users` icon, positioned right after the Tom Nash Analysis card) showing the consolidated verdict badge, agreement badge, confidence score bar, summary, and each analyst's individual vote. `openapi.yaml` gained new `ValueCommitteeVote`/`ValueInvestmentCommittee` schemas and an `investmentCommittee` field on `ValueResearchReport`; `api-zod`/`api-client-react` regenerated to match. No database migration — persists via the same `stockAnalysisHistoryTable.valueResearchJson` blob. Five pre-existing integration test files needed mechanical section-count/numbering updates only (established pattern): `valueReport.grahamIntegration.test.ts`, `valueReport.dcfIntegration.test.ts`, `valueReport.buffettIntegration.test.ts`, `valueReport.investmentQualityIntegration.test.ts`, `valueReport.tomNashIntegration.test.ts` — Graham's, DCF's, Buffett's, Investment Quality's, and Tom Nash's actual computed outputs are separately proven byte-identical via dedicated regression assertions (`toEqual` against standalone calls), and the blended model's own output is likewise proven unchanged. New tests: `investmentCommittee.test.ts` (the rating→verdict bucket mapping for Graham/Buffett, Tom Nash's direct pass-through vote, unavailable-model exclusion from voting, unanimous/majority/split/insufficient-data scenarios via constructed fixtures, the split→Hold default, confidence-score averaging over only the votes actually cast, the reasoning shape, no LLM narration) and `valueReport.investmentCommitteeIntegration.test.ts` (proves every pre-existing section `id` is unchanged, **Graham's, DCF's, Buffett's, the blended model's, Investment Quality's, and Tom Nash's own outputs are all proven unchanged via direct equality against standalone calls**, the new section/field are correctly wired, and Graham/Buffett are correctly excluded from the Committee's votes — while Tom Nash still votes — when their valuation is unavailable). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (37 files / 447 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 18 — COMPLETE.** Financial Ratio Analysis (Phase 2 plan, Sprint 18, per the roadmap's post-Sprint-15 renumbering). New `lib/financialRatios.ts` — `analyzeFinancialRatios(f)` — promotes the existing flat "Key Metrics" list into a dedicated, grouped ratio-analysis surface, purely additive (the existing "Key Metrics" section/field is completely untouched, despite real overlap in the underlying numbers). Of the 4 ratios requested for this sprint (quick ratio, ROA, asset turnover, payout ratio), **only Payout Ratio (`dividendPerShare / epsTtm`) is honestly computable** from fields that exist today — added. **Quick Ratio, Return on Assets, and Asset Turnover all need balance-sheet detail (inventory, total assets, current-liability breakdown) that doesn't exist anywhere in `Fundamentals` for any provider**, and per the approved decision none were approximated (e.g. via a DuPont-identity shortcut, which would silently distort the number for asset-heavy or highly-levered companies) — all three are honestly reported `available: false` with an explicit reason pointing to the future Financial Statement Analysis sprint, never fabricated. Every other ratio (P/E trailing + forward, PEG, P/S, P/B, FCF Yield, Dividend Yield, ROE, ROIC, Gross/Operating/Net Margin, Current Ratio, Debt/Equity, Interest Coverage) is a direct reuse of already-computed `Fundamentals` fields, grouped into Valuation/Profitability/Liquidity & Leverage categories — zero recomputation, same formulas the existing "Key Metrics" section and other analyzers already use. Trend data (`trends: [{label, history}]`, 3 entries: Revenue/EPS/FCF per share) is a pure pass-through of `Fundamentals`' own `revenueHistory`/`epsHistory`/`fcfHistory` 6-year arrays — no new math. **UI decision, per the approved decision (deviating from the roadmap's original "new Ratios tab" text):** a new **card** in the existing continuous `ReportView` (`Percent` icon, positioned between the Financial Strength and Valuation cards) rather than a separate tab — `ReportView` has no internal tab navigation, and every prior Sprint 12–17 addition followed the same card-based pattern. Trend charts reuse the exact `recharts` `ResponsiveContainer`/`LineChart` styling already established in `Backtest.tsx`'s equity-curve chart (same CSS-variable-driven colors, same `CartesianGrid`/`Tooltip` conventions) — a new small `RatioTrendChart` component, not a new charting approach. `valueReport.ts`'s report grew from 21 to 22 sections: the new `financial-ratios` section sits between `growth` (unchanged at "8.") and `valuation` (shifted from "9." to "10."), with every subsequent section (Graham through Disclaimers) shifting by exactly one further — every existing section's stable `id` is unchanged, confirmed by regression test. **One real test-infrastructure gap caught and fixed during implementation, not anticipated in the pre-approval plan:** jsdom doesn't implement `ResizeObserver`, which `recharts`' `ResponsiveContainer` requires to mount — the first attempt at running the Ravish Trading suite failed with `ReferenceError: ResizeObserver is not defined` across all 3 `StockResearch.test.tsx` tests that render `ReportView`. Fixed with a minimal `ResizeObserverStub` class added to the shared `src/test/setup.ts` (a `globalThis.ResizeObserver ??= ...` polyfill), which benefits any future chart component's tests too, not just this sprint's. Six pre-existing integration test files needed mechanical section-count/numbering updates only (established pattern): `valueReport.grahamIntegration.test.ts`, `valueReport.dcfIntegration.test.ts`, `valueReport.buffettIntegration.test.ts`, `valueReport.investmentQualityIntegration.test.ts`, `valueReport.tomNashIntegration.test.ts`, `valueReport.investmentCommitteeIntegration.test.ts` — every prior sprint's actual computed outputs are separately proven byte-identical via dedicated regression assertions (`toEqual` against standalone calls). New tests: `financialRatios.test.ts` (Payout Ratio's formula, the honest-unavailable path for all 3 uncomputable ratios proven never-approximated, direct-reuse proofs for every existing-field ratio, ETF handling for Current Ratio, trend-array pass-through proof, summary honesty) and `valueReport.financialRatiosIntegration.test.ts` (proves every pre-existing section `id` is unchanged, **Graham's, DCF's, Buffett's, the blended model's, Investment Quality's, Tom Nash's, and the Investment Committee's own outputs are all proven unchanged via direct equality against standalone calls**, the new section/field are correctly wired, and financial ratios compute independently of Graham/DCF's own availability). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (39 files / 468 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 19 — COMPLETE.** Financial Statement Analysis (Phase 2 plan, Sprint 19, per the roadmap's post-Sprint-15 renumbering). `lib/fundamentals.ts` gained a new `getFinancialStatements(symbol, opts?)` method directly on all three existing `FundamentalsProvider` implementations — `SimulatedFundamentalsProvider`, `FmpFundamentalsProvider`, `AlphaVantageFundamentalsProvider` — per the approved decision, reusing existing API-key/caching/error-handling/provider-selection infrastructure rather than a second parallel provider hierarchy. New types `IncomeStatementYear`/`BalanceSheetYear`/`CashFlowYear`/`FinancialStatements` (5-year annual arrays, oldest-to-newest, `dataSource: "SIMULATED" | "LIVE"`). **`SimulatedFundamentalsProvider`'s statements are derived deterministically and internally consistently from the same already-computed `Fundamentals` fields** (`revenueHistory`/`epsHistory`/`fcfHistory`, margins, ratios) that the rest of the report already uses — the only new synthetic input is a deterministically-seeded share count (`makeRng(\`${symbol}|financial-statements-shares\`)`) needed to convert per-share figures into absolute-dollar statement line items — never an independently random source, confirmed by dedicated consistency tests (margin reconciliation against `Fundamentals`' own `grossMargin`/`operatingMargin`, balance-sheet identity assets=liabilities+equity, cash-flow identity FCF=OCF−Capex, byte-identical determinism via `toEqual`, ETF zero-inventory/currentAssets handling matching Financial Ratios' own ETF convention, honest `null` for an invalid ticker shape). `FmpFundamentalsProvider.getFinancialStatements()` fetches `/income-statement`, `/balance-sheet-statement`, `/cash-flow-statement` (period=annual, limit=5) in parallel, detects FMP's documented `{"Error Message"}` rate-limit/error shape and throws (never silently returns empty or fabricated data), reverses FMP's most-recent-first ordering to this codebase's oldest-to-newest convention, and normalizes capex to a positive magnitude. `AlphaVantageFundamentalsProvider.getFinancialStatements()` does the same against AV's `INCOME_STATEMENT`/`BALANCE_SHEET`/`CASH_FLOW` endpoints' `annualReports` arrays, detecting AV's `Note`/`Information`/`Error Message` shapes, and derives free cash flow as `operatingCashflow − capitalExpenditures` since AV doesn't publish FCF directly. A new short-lived in-memory `statementsCache` (15-minute TTL, keyed `${providerId}:${symbol}`, no `asOf` since statements are annual rather than date-scoped) mirrors the existing `liveCache` pattern exactly. **Scope discipline, per the approved decision:** kept fully additive and on-demand — `buildValueResearchReport()`/`GET /stock-analyst/value/:symbol` were never touched, proven by a dedicated regression test asserting that response carries no `financialStatements`/`incomeStatement` field; instead a new standalone route `GET /stock-analyst/financial-statements/:symbol` was added to `routes/stockAnalyst.ts`, returning a clean `502` (never a silent SIMULATED swap the user didn't ask for) if a live provider throws, and `404` for an invalid/unknown ticker. `StockResearch.tsx` gained a new "Statements" tab (disabled until a report is loaded) using Orval's generated `useGetFinancialStatements` hook, fetched only when that tab is actually opened via `enabled: tab === "statements" && !!report?.symbol` — this required explicitly importing and passing the paired `getGetFinancialStatementsQueryKey()` helper alongside `enabled`, since this codebase's generated-hook types require `queryKey` even when overriding `enabled` (the first use of the `enabled` option with a generated hook anywhere in this codebase — no prior precedent existed to follow). Sub-tabs render Income Statement/Balance Sheet/Cash Flow as HTML tables (not charts) with compact-currency formatting (`Intl.NumberFormat`'s `notation: "compact"`). `openapi.yaml` gained `IncomeStatementYear`/`BalanceSheetYear`/`CashFlowYear`/`FinancialStatements` schemas and the new `getFinancialStatements` path; `api-zod`/`api-client-react` regenerated to match. No database migration — statements are fetched on demand and cached only in-process, never persisted. **One real bug caught during test validation, not anticipated in the pre-approval plan:** the new `statementsCache` is keyed by symbol only (no `asOf`), and the initial `financialStatements.test.ts` reused the same symbol (`"ACME"`) across a provider's success-path test and its immediately-following rate-limit test — the second test was silently hitting the first test's cached result instead of exercising the mocked error response, masking the rate-limit-detection code path entirely. Fixed by using a distinct symbol per test case (`"RATELMTD"`), matching the existing test-isolation convention already used elsewhere in `value.test.ts`; the underlying rate-limit-detection logic itself needed no change once properly exercised. New tests: `financialStatements.test.ts` (all cases above) and `financialStatements.route.test.ts` (live end-to-end route test against a real running app instance: 200 + 5 years of SIMULATED statements for AAPL, 404 for an invalid ticker shape, and the scope-discipline proof that `/value/:symbol`'s response gains no new field). One pre-existing, previously-disclosed flake (`fetchedAt` timestamps captured 1ms apart in a determinism test — the same category first noted in Sprint 16's report, this time surfacing once in `value.test.ts` and once in `fundamentals.investingUniverse.test.ts` across repeated runs) reproduced twice during validation and passed cleanly on a third run — not a regression, not touched by this sprint's changes. **Live FMP/Alpha Vantage verification is explicitly deferred** — neither `FMP_API_KEY` nor `ALPHA_VANTAGE_API_KEY` was available in this session; both live providers' parsing/error-handling logic is covered only by mocked-fetch tests, never a live call, and no live verification is claimed. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (41 files / 482 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 20 — COMPLETE.** Industry Comparison Engine (Phase 2 plan, Sprint 20, per the roadmap's post-Sprint-15 renumbering). `Fundamentals` gained `sector`/`industry` fields (required-but-nullable) — FMP's `/profile` and Alpha Vantage's `OVERVIEW` already return this and simply weren't captured until now; both live providers pass it through directly, honest `null` when the provider omits it, never fabricated. New `lib/industryPeers.ts`: an 11-sector taxonomy, a `KNOWN_SECTOR_PROFILES` table of ~63 real, publicly-known companies' real sector/industry classification (categorical metadata, not fabricated financial data — e.g. "Apple → Technology / Consumer Electronics"), a `SECTOR_PEER_UNIVERSE` fixed-order peer list per sector derived directly from that table (single source of truth), `getSectorProfile()` (known-symbol lookup, else a deterministic `makeRng`-seeded synthetic bucket for an unlisted symbol — never a fabricated real-sounding classification, consistent with Sprint 11's `syntheticProfile()` precedent), and `selectPeerSymbols()` (deterministic fixed-order slice, excluding the target itself — no market-cap/liquidity ranking, which would require a live call). New `lib/industryComparison.ts` — `buildIndustryComparison(symbol, provider, asOf?, opts?, peerCount = 5)` — resolves the target's Fundamentals via the existing `resolveFundamentals()` (live-with-simulated-fallback), selects up to 5 sector peers (`DEFAULT_PEER_COUNT`, kept as a named constant for future configurability per the approved decision), resolves each peer the same way (a peer that fails to resolve is simply dropped from the group, never fabricated), and for target + each resolved peer runs the **exact same analyzer composition `valueReport.ts` already uses** — `analyzeBusinessQuality`, `analyzeInvestmentQuality`, `analyzeMoat`, `analyzeFinancialStrength`, `analyzeValuation`, `analyzeGrahamValuation`, `analyzeDcfValuation`, `analyzeBuffettValuation`, `analyzeTomNash` — zero new scoring logic; `investmentQuality.ts`'s previously-private `fcfGrowth5y()` CAGR derivation was `export`ed (not reimplemented) so FCF Growth ranks on the identical number the main report uses. 17 comparison rows: Revenue/EPS/FCF Growth, Gross/Operating/Net Margin, ROE, ROIC, Debt-to-Equity, Investment Quality Score, Graham/DCF/Buffett Margin of Safety, and Tom Nash Conviction Score are all ranked (higher-better, except Debt-to-Equity which is lower-better per the approved decision) — each reports company value, peer median, rank, and percentile, honestly `available: false` with a reason when data is insufficient. **P/E, P/S, and P/B are shown for context only** (company value + peer median) and deliberately **excluded from percentile/rank/strengths/weaknesses**, per the approved decision that "cheaper is better" isn't a defensible convention across sectors with structurally different valuation levels. Rolls up to an `overallPercentile` (mean of available ranked-metric percentiles), a `competitivePosition` (Leader/Above Average/Average/Below Average/Laggard/Insufficient Data, thresholded off the overall percentile), a `confidenceLevel` (High/Moderate/Low, derived from peer count and ranked-metric coverage — the same honesty-driven pattern as Investment Quality's own `confidenceLevel`), and a deterministic rule-based summary sentence (no LLM narration, consistent with Sprint 17's precedent for this kind of synthesis). **Scope discipline, per the approved decision:** kept fully additive and on-demand — `buildValueResearchReport()`/`GET /stock-analyst/value/:symbol` were never touched (beyond the new `sector`/`industry` header fields, see below), proven by a dedicated regression test; instead a new standalone route `GET /stock-analyst/industry-comparison/:symbol` was added to `routes/stockAnalyst.ts`, since each peer needs its own Fundamentals fetch and a full comparison therefore costs several times the provider calls of viewing a single report. A new short-lived in-memory `comparisonCache` (15-minute TTL, keyed `${providerId}:${symbol}:${asOf}:${peerCount}` — **deliberately including `peerCount` in the key**, a correctness fix caught during test validation: an earlier draft omitted it, which would have silently returned a stale peer group's worth of results to any future caller requesting a different peer count while the cache was warm for the same symbol) mirrors Sprint 19's `statementsCache` pattern. Per the approved decision, `StockResearch.tsx` gained a new "Peers" tab (disabled until a report is loaded, fetched only when opened via the same `enabled`-gated generated-hook pattern Sprint 19 established) showing the peer group, a metric-by-metric table, strengths/weaknesses lists, a competitive-position badge, a confidence badge, and the summary; the existing report header also now shows `Sector — Industry` next to the price line (free — sourced from the same Fundamentals fetch the report already makes, no new provider call), and `ValueResearchReport` itself gained `sector`/`industry` fields sourced directly from `Fundamentals`. `openapi.yaml` gained `ComparisonMetric`/`IndustryPeerSummary`/`IndustryComparisonResult` schemas and the new `getIndustryComparison` path, plus `sector`/`industry` on the existing `ValueResearchReport` schema; `api-zod`/`api-client-react` regenerated to match. No database migration — the entire comparison is computed on demand and cached only in-process. **Mechanical fallout from extending `Fundamentals` with two new required fields, caught by typecheck and fixed before commit:** 6 existing test files' local `fixture(overrides)` helpers (`buffettValuation.test.ts`, `dcfValuation.test.ts`, `financialRatios.test.ts`, `grahamValuation.test.ts`, `investmentQuality.test.ts`, `tomNashEngine.test.ts`) and 1 frontend fixture (`src/test/fixtures/valueReport.ts`) each needed a one-line `sector`/`industry` default added — the 8 other test files that build `Fundamentals` objects via `{ ...base, ... }` spreads off a real `getFundamentals()`/`resolveFundamentals()` call needed no changes at all, since the real providers already supply the new fields. **One real bug caught during test validation, not anticipated in the pre-approval plan:** the initial `comparisonCache` key omitted `peerCount` (see above) — caught when a test explicitly overriding `peerCount` got back a cached default-peer-count result instead; fixed by including `peerCount` in the cache key, which also fixed several of `industryComparison.test.ts`'s own FakeProvider-based tests that had been colliding with each other's cached results under the same symbol (same test-isolation trap first documented in Sprint 19, same fix: explicit `forceRefresh: true` in tests that intentionally reuse a symbol across cases). One pre-existing, previously-disclosed flake (`fetchedAt` timestamps captured 1ms apart in a determinism test) reproduced once during validation in `fundamentals.investingUniverse.test.ts` and passed cleanly on a re-run — not a regression, not touched by this sprint's changes; the same category of flake also required this sprint's own new regression test to explicitly exclude `fetchedAt` from a `toEqual` comparison of two separately-computed SIMULATED reports, since that field is stamped fresh (`new Date().toISOString()`) on every call and therefore legitimately differs between any two calls regardless of this sprint's changes. New tests: `industryPeers.test.ts`, `industryComparison.test.ts`, `industryComparison.route.test.ts`, `valueReport.industryComparisonIntegration.test.ts` (proves every pre-existing section id/count/numbering is completely unchanged — no new report section this sprint — and Graham's, DCF's, Buffett's, the blended model's, Investment Quality's, Tom Nash's, and the Investment Committee's own outputs are all proven unchanged via direct equality against standalone calls). **Live FMP/Alpha Vantage sector-field verification is explicitly deferred** — neither `FMP_API_KEY` nor `ALPHA_VANTAGE_API_KEY` was available in this session; only mocked-fetch-covered logic and the SIMULATED path were exercised, and no live verification is claimed. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (45 files / 519 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 21 — COMPLETE.** Competitive Advantage Engine (Phase 2 plan, Sprint 21 — expanded at kickoff from the roadmap's originally narrower "Economic Moat Analysis Enhancement" text into an 11-dimension reusable engine, approved before implementation). New `lib/competitiveAdvantage.ts` — `analyzeCompetitiveAdvantage(f, iq, fin)` — a new, purely additive analysis alongside the existing `analyzeMoat()` (`valueInvesting.ts`), which is **completely untouched behaviorally** — same fields, same thresholds, same `durabilityYears` lookup, confirmed byte-identical by regression test, consistent with this project's unbroken never-replace-only-add precedent. Scores 11 dimensions: **8 are direct, zero-new-logic reuse** of `Fundamentals.qualitative`'s existing 0-100 scored fields — Brand Strength (`qualitative.brand`), Network Effects (`qualitative.networkEffect`), Switching Costs (`qualitative.switchingCost`), Economies of Scale (`qualitative.scale`), Intangible Assets (`qualitative.ipStrength`), Regulatory Advantages (`qualitative.regulatoryAdvantage`), Distribution Advantages (`qualitative.distribution`), Recurring Revenue Quality (`qualitative.recurringRevenue`). **Cost Advantages** blends `qualitative.pricingPower` with Investment Quality's own already-scored "Gross Margin" metric (`iq.metrics`) — a trivial average, not a new formula. **Customer Concentration Risk is always honestly `unavailable`** — no provider (SIMULATED or LIVE) publishes revenue-by-customer data for any symbol today, the same gap category as Investment Quality's Share Dilution/Insider Ownership (Sprint 15), reason points to the same future filing-ingestion sprint, never fabricated. **Competitive Durability** (the one genuinely new formula, per the approved decision) blends Investment Quality's own already-scored "Return on Invested Capital" metric, Financial Strength's own already-scored "FCF reliability" metric, and a new `historyConsistencyScore()` helper measuring how many year-over-year steps in `Fundamentals.revenueHistory`/`epsHistory` (the same 6-year arrays every report already fetches) were non-declining — **deliberately never calls `getFinancialStatements()`** (Sprint 19), so the whole engine stays inside the eager `buildValueResearchReport()` path with zero new provider calls; real multi-year ROIC persistence from actual filed statements is explicitly deferred to a future enhancement sprint. **Industry Comparison (Sprint 20) was deliberately excluded from the core score**, per the approved decision — it's peer-relative and costs ~5 extra provider calls per symbol, so coupling it into this engine's eager computation would quietly undo Sprint 20's own on-demand design; it remains available only via the existing, separate Peers tab. Overall score = renormalized weighted average (1/11 each, named/adjustable constants) over the 10 always-available dimensions, Customer Concentration excluded from the average per Investment Quality's own established renormalization pattern. **One small shared-code extraction, behavior-preserving:** `analyzeMoat()`'s inline Wide/Medium/Narrow/None threshold logic became an exported `classifyMoatRating(score): MoatRating` in `valueInvesting.ts` — the same "extract a shared pure classifier" pattern as Sprint 12's `classifyMarginOfSafety()` and Sprint 15's `leverageScore`/`coverageScore`/`cashPositionScore` — confirmed byte-identical via `analyzeMoat()`'s own existing tests, and reused by the new engine so both speak the same Wide/Medium/Narrow/None vocabulary without duplicating thresholds. Confidence level mirrors Investment Quality's own availability-ratio thresholds (High ≥95%, Moderate ≥80%, else Low) — with 10 of 11 dimensions always available by design, this reads **"Moderate" for virtually every company today**, the same self-documenting property Investment Quality itself disclosed in Sprint 15, that will naturally improve once a future sprint fills the Customer Concentration gap. `valueReport.ts`'s report grew from 22 to 23 sections: a new "6. Competitive Advantage" section (id `competitive-advantage`) was inserted immediately after "5. Economic Moat," renumbering every section from "Financial Strength" (6→7) through "Disclaimers & Data Source" (22→23) — every existing section's stable `id` is unchanged, confirmed by regression test. `StockResearch.tsx` gained a new "Competitive Advantage" card (`Castle` icon, positioned immediately after the existing, visually-unchanged Moat card) showing all 11 dimension score bars (Customer Concentration Risk shown as "N/A" with its reason in italics, matching Investment Quality's unavailable-metric UI convention) plus Strengths/Weaknesses lists. `openapi.yaml` gained `ValueCompetitiveDimensionScore`/`ValueCompetitiveAdvantageAnalysis` schemas and a `competitiveAdvantage` field on `ValueResearchReport`; `api-zod`/`api-client-react` regenerated to match. **No new route** — folded directly into the existing `GET /stock-analyst/value/:symbol`, zero new provider calls, per the approved integration decision. No database migration — persists via the same `stockAnalysisHistoryTable.valueResearchJson` blob. 8 pre-existing integration test files needed mechanical section-numbering updates only (established pattern, this sprint inserting a real new section for the first time since Sprint 20): `valueReport.grahamIntegration.test.ts`, `valueReport.dcfIntegration.test.ts`, `valueReport.buffettIntegration.test.ts`, `valueReport.investmentQualityIntegration.test.ts`, `valueReport.tomNashIntegration.test.ts`, `valueReport.investmentCommitteeIntegration.test.ts`, `valueReport.financialRatiosIntegration.test.ts`, `valueReport.industryComparisonIntegration.test.ts` — every prior sprint's actual computed outputs are separately proven byte-identical via dedicated regression assertions (`toEqual` against standalone calls). New tests: `competitiveAdvantage.test.ts` (all 11 dimensions' derivation order and math, the direct-reuse proofs for the 8 qualitative dimensions, the Cost Advantage blend formula, the honest-always-unavailable Customer Concentration path proven even for a maximally-strong company, the Competitive Durability blend's directional correctness, overall-score renormalization, classification via the shared `classifyMoatRating()`, confidence-level thresholds, strengths/weaknesses derivation, ETF caveat) and `valueReport.competitiveAdvantageIntegration.test.ts` (proves every pre-existing section id/count/numbering is correctly shifted with zero id changes, **`analyzeMoat()`'s own now-refactored output is proven unchanged**, and Graham's/DCF's/Buffett's/the blended model's/Investment Quality's/Tom Nash's/the Investment Committee's/Financial Ratios' own outputs are all proven unchanged via direct equality against standalone calls, plus that Competitive Advantage computes independently of Graham/DCF's own availability). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (47 files / 544 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real, each run twice to confirm stability with zero flakes.
- **Sprint 22 — COMPLETE.** Document Intelligence Engine (Phase 2 plan, Sprint 22 — expanded at kickoff from the roadmap's originally narrower "Annual Report Analysis" text into a generalized, provider-agnostic document-ingestion engine, approved before implementation; this was the roadmap's own designated "long pole" of Phase 2, the highest-complexity sprint so far). Two approved scope decisions shaped the whole sprint: **(1) no SIMULATED document mode** — unlike every other engine, there is no honest way to fabricate synthetic 10-K prose the way `syntheticProfile()` fabricates synthetic numbers, so a document is either fetched live from SEC EDGAR (`documentAvailable: true`) or honestly unavailable (`false`, with a reason) — never a fabricated placeholder; **(2) zero LLM calls this sprint** — Sprint 22 stops at deterministic ingestion, extraction, and structuring, exactly matching the roadmap's own Sprint 23 framing ("build the qualitative management-analysis layer **on top of** Sprint 22's filing text"). New `lib/documentProviders.ts` — a `DocumentProvider` interface mirroring `FundamentalsProvider`'s established seam, with a `DocumentType` union covering all 6 requested document types (10-K, 10-Q, earnings-transcript, investor-presentation, sustainability-report, management-commentary) so future types plug in without touching this interface or any caller. Only `"10-K"` is implemented, via `EdgarDocumentProvider`: resolves ticker→CIK from SEC's own `company_tickers.json` (cached in-process 24h — a ~700KB file covering every ticker, refetched lazily rather than per-request), fetches the filing index from `data.sec.gov/submissions/CIK##########.json`, finds the latest 10-K, and fetches the primary filing document — any other `DocumentType` honestly throws "not yet implemented," never fabricates a result. New `lib/filingExtraction.ts` — pure text utilities with no I/O: `stripHtml()` (regex-based tag/script/style removal + entity decoding, sufficient for SEC's simple filing markup), heuristic Item-heading boundary detection for the Business/Risk Factors/MD&A sections (10-K's standard SEC Regulation S-K numbering) — a "last-match" heuristic deliberately used instead of first-match, since every real 10-K opens with a table of contents that repeats each Item heading and a naive first-match would extract the TOC line instead of the actual section; `excerpt()` (a deterministic extractive preview — first sentence + longest sentence, never an LLM paraphrase); `chunkText()` (paragraph-boundary chunking sized for a future LLM-summarization step, built per the roadmap's own "chunked per-section summarization" text but **unused by any LLM call this sprint**, provided so Sprint 23 can reuse it rather than reimplement it). Every extracted section carries an honest `found` flag and `reason` when a boundary heuristic fails to locate it — never silently guessing. New `lib/filingAnalysis.ts` — `buildFilingAnalysis()` composes two **completely independent** branches so a failure in one never blanks the other: the document branch (EDGAR fetch + extraction, cached 24 hours in-process since 10-Ks are annual documents, not date-scoped quotes) and a financial-highlights branch that calls the **existing, already-tested `buildValueResearchReport()`** exactly once and reads off Graham/DCF/Buffett margin-of-safety + rating, Investment Quality score, Competitive Advantage score + classification, Tom Nash conviction + verdict, and the Investment Committee's consolidated verdict into a flat highlights list — **zero new scoring or extraction logic**, the most literal possible form of "reuse every existing module," and it works even when EDGAR is completely unreachable, proving the "degrade honestly, not silently" requirement live in this session (this environment's proxy denies `data.sec.gov` outright — confirmed via a live connectivity probe before implementation, 403/policy denial, the same situation as this codebase's FMP/Alpha Vantage integrations lacking API keys). The deterministic `executiveSummary` is a template stitching together already-computed facts (filing date, sections found, Investment Committee verdict, Tom Nash conviction) — never LLM-generated, matching the explicit "(deterministic)" qualifier in the approved objectives. `confidenceLevel` mirrors Investment Quality/Competitive Advantage's own established derivation pattern, based on how many sections were located. New table `investing_filing_analysis` (`lib/db/manual-migrations/007_investing_filing_analysis.sql`, `lib/db/src/schema/investingFilingAnalysis.ts`) — `userId` mandatory with `ON DELETE RESTRICT`, matching `stock_analysis_history`'s established per-user-research-history convention (the roadmap's own column design already specified this); `filingType` is free text, not a DB enum, so every future document type reuses this exact table with zero schema change; brand-new table, `NOT NULL` from creation, no nullable→backfill→enforce migration needed (same precedent as `platform_audit_log`, Sprint 10); persistence is deliberately **best-effort** (wrapped in try/catch, a write failure is logged but never breaks the user-facing response, mirroring `recordAuditEvent`'s established philosophy). A separate short-lived in-process cache (mirroring `statementsCache`/`comparisonCache`) sits in front of the EDGAR fetch itself so repeated requests for the same symbol don't hammer EDGAR even before the DB layer is consulted. New route `GET /stock-analyst/filings/:symbol` in `stockAnalyst.ts` — **deliberately collapsed from the roadmap's original two-route design** (`GET` + `POST .../analyze`), since that split existed specifically to separate a cheap fetch from an expensive LLM-triggering step, and decision (2) above removed the LLM step entirely; never `502`s for a missing or unreachable filing (an honest `200` with `documentAvailable:false`), only a genuinely unknown/invalid-shaped symbol `404`s, matching every other stock-analyst route's contract. `StockResearch.tsx` gained a new "Filings" tab (`FileText` icon, disabled until a report is loaded, fetched only when opened via the same `enabled`-gated generated-hook pattern established in Sprints 19–20) showing the executive summary, a confidence badge, a source-filing link, key financial highlights as badges, and the 3 extracted sections with their excerpts (or an honest "not found" reason). `openapi.yaml` gained `FilingDocumentSection`/`FilingFinancialHighlight`/`FilingAnalysis` schemas and the new `getFilingAnalysis` path; `api-zod`/`api-client-react` regenerated to match. **`valueReport.ts` and `valueInvesting.ts` were not modified at all this sprint** — Document Intelligence only *reads* `buildValueResearchReport()`'s output, it never feeds into it, so there is no new report section and no section-renumbering fallout this sprint, confirmed by a lighter-weight regression test than prior sprints required. **One test-authoring mistake caught and fixed during validation, not a production bug:** two new tests initially asserted the API response `not.toHaveProperty("sections")` intending to check for Document Intelligence's own `sections` field — but `ValueResearchReport` already has an unrelated, pre-existing `sections` field (the report's own list of report sections, since Sprint 12), so both assertions were false positives; fixed by removing that specific check while keeping the assertions for genuinely Document-Intelligence-specific fields (`executiveSummary`, `documentAvailable`, `keyFinancialHighlights`). New tests: `documentProviders.test.ts` (mocked-fetch CIK resolution, filing-index parsing, primary-document fetch, honest-null paths for an unknown ticker/no-10-K-filed/invalid-shape, unimplemented-document-type throws, fetch-failure propagation), `filingExtraction.test.ts` (HTML stripping, the TOC-skipping last-match boundary heuristic proven against a synthetic-but-realistic fixture with a repeating table of contents, excerpt/chunking math, honest not-found paths for missing or unrecognizable sections), `filingAnalysis.test.ts` (the two-branch independence proof — highlights populate correctly even when the document branch returns null or throws — 24h caching, forceRefresh bypass, executive-summary determinism), `filingAnalysis.route.test.ts` (live end-to-end against the real app: `200` with an honest `documentAvailable:false` against the genuinely-unreachable real EDGAR, `404` for an invalid ticker shape, a real persistence proof against the test database, and the scope-discipline proof that `/value/:symbol` gains no filing-shaped field), `valueReport.filingAnalysisIntegration.test.ts` (proves `buildValueResearchReport()`'s own output is byte-identical before and after a filing-analysis call, excluding the same legitimately-always-fresh `fetchedAt` field excluded since Sprint 20's precedent). **Live SEC EDGAR verification is explicitly deferred** — confirmed via a live connectivity probe that this session's outbound proxy denies `data.sec.gov` (403, policy denial) before any code was written; `EdgarDocumentProvider` is built and covered by mocked-fetch tests only, never exercised against the real SEC API, and no live verification is claimed. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (52 files / 575 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real, the API server suite run twice to confirm stability with zero flakes.
- **Sprint 23 — COMPLETE.** Management Quality Analysis Engine (Phase 2 plan, Sprint 23). **Approved decision, deviating from the roadmap's original LLM-narrated design: deterministic only, zero LLM calls, no commentary generated about any named individual.** A genuine, disclosed audit of the 9 requested dimensions found only 5 have a defensible non-fabricating deterministic path — the other 4 are honestly `unavailable` rather than backed by a stretched proxy, matching this project's unbroken never-fabricate discipline over shipping a "complete-looking" but debatable output. New `lib/managementAnalysis.ts` — `buildManagementQualityAnalysis()` — pure composition: **Capital Allocation Discipline** = direct reuse of Tom Nash's own "Capital Allocation" pillar score; **Financial Stewardship** = direct reuse of `analyzeFinancialStrength()`'s own score; **Execution Discipline** = direct reuse of Competitive Advantage's own "Competitive Durability" dimension (itself already a blend of ROIC + FCF reliability + growth consistency) — three dimensions, zero new scoring logic. **Risk Acknowledgement** (the one genuinely new formula, structural not opinion-based) derives from Document Intelligence's Risk Factors section's presence + relative word count — measures disclosure magnitude, never sentiment — and is the only dimension carrying a real `sourceSection` (section excerpt + filing URL), satisfying "source references where available" honestly rather than attaching a fake source to a purely quantitative dimension. **Transparency** (the other new formula) composites Investment Quality's own `confidenceLevel`, Competitive Advantage's own `confidenceLevel`, and Document Intelligence's own section-completeness ratio — reused confidence signals translated to a shared 0-100 scale, not a new opinion. **Strategic Consistency, Long-Term Focus, Communication Quality, Shareholder Alignment are always honestly `unavailable`** — each reason names its real blocker (multi-year filing comparison not built, no R&D/reinvestment line item published by any provider, LLM reading comprehension explicitly deferred, and insider/buyback data — the exact same gap already disclosed for Investment Quality's Share Dilution/Buybacks and Insider Ownership metrics, Sprint 15 — respectively), never fabricated. Overall score = renormalized weighted average (1/9 each, named/adjustable constants) over the 5 available dimensions. With 5 of 9 always available by design, `confidenceLevel` computes to **"Low" for virtually every company today** — a self-documenting property, not a bug, disclosed the same way Investment Quality (Sprint 15) and Competitive Advantage (Sprint 21) disclosed their own analogous gaps. `lib/filingAnalysis.ts`'s `buildFilingAnalysis()` gained one small, backward-compatible addition: an optional `persist = true` parameter, so Management Quality Analysis (which also needs the filing sections) can call it with `persist:false` and never write a duplicate `investing_filing_analysis` row — every existing caller (the Sprint 22 `/filings/:symbol` route) is completely unaffected, defaulting to the old behavior. **Deliberately not given its own persistence table this sprint** — simpler than the roadmap's original "extends investing_filing_analysis table usage" text, since the analysis is cheap to recompute from already-cached Filing/Fundamentals data, the same cost class as Competitive Advantage/Industry Comparison (neither of which persists its own output). New route `GET /stock-analyst/management-quality/:symbol` in `stockAnalyst.ts`, on-demand, mirroring the Filings route's honest-degradation contract (never `502`s for a missing/unreachable filing, only a genuinely unknown symbol `404`s). `StockResearch.tsx` folded the new UI into the existing Filings tab as a second card (`Users` icon, not a new top-level tab, since it shares the same underlying document + report) showing all 9 dimension score bars (4 shown as "N/A" with their reason in italics, matching Investment Quality's established unavailable-metric convention) and the Risk Acknowledgement source excerpt with a link back to the filing. `openapi.yaml` gained `ManagementSourceSection`/`ManagementQualityDimension`/`ManagementQualityAnalysis` schemas and the new `getManagementQualityAnalysis` path; `api-zod`/`api-client-react` regenerated to match. **Why this sidesteps the roadmap's flagged compliance risk almost entirely:** the roadmap named this module "the highest reputational/compliance risk in Engine 1 (discussing real executives)" because its original design involved LLM-generated commentary about named individuals — this deterministic implementation never names or characterizes a specific executive, scoring only the *company's* management-process discipline via numbers and structural filing signals, so the disclaimer states plainly this is "not a characterization of any individual executive." No database migration — persists via nothing new. `valueReport.ts` and `valueInvesting.ts` were **not modified at all** this sprint — Management Quality Analysis only *reads* `buildValueResearchReport()`'s and `buildFilingAnalysis()`'s output, it never feeds into either. New tests: `managementAnalysis.test.ts` (all 9 dimensions' derivation order, the 3 direct-reuse proofs, the Risk Acknowledgement word-count formula and its source-reference wiring, the Transparency composite, the 4 honestly-unavailable paths each proven never-fabricated even under a maximally-favorable fixture, overall-score renormalization, confidence-level thresholds, strengths/weaknesses derivation, the disclaimer's no-named-individual guarantee), `managementAnalysis.route.test.ts` (live end-to-end: 200 with 9 dimensions and populated reused-financial dimensions for a known symbol, 404 for an invalid ticker shape, a real proof against the test database that `persist:false` never writes a row, and the scope-discipline proof that `/value/:symbol` gains no management-quality-shaped field), `valueReport.managementAnalysisIntegration.test.ts` (proves both `buildValueResearchReport()`'s and `buildFilingAnalysis()`'s own outputs are byte-identical before and after a management-quality call, and that neither gained a new field this sprint). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (55 files / 593 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real, the API server suite run twice to confirm stability with zero flakes.
- **Sprint 24 — COMPLETE.** Tom Nash Investment Engine — Enhancement I: Capital Allocation, Buybacks & Insider Ownership (Phase 2 plan, Sprint 24, per the roadmap's post-Sprint-15 renumbering). Two owner decisions were asked and approved before implementation: (1) insider ownership % and share-buyback trend are aggregate numbers about capital structure (like ROE or debt/equity), not claims about a specific person, so SIMULATED mode may generate deterministic values for them, consistent with every other financial metric in this app; (2) extending Tom Nash's Capital Allocation pillar to include these once available means the pillar's computed score for a company genuinely changes from Sprint 16's shipped formula (though it stays byte-identical wherever the new fields remain null). On top of those, "recent insider buy/sell activity" was deliberately scoped down (my own judgment, consistent with Document Intelligence's precedent, not re-asked) to an aggregate net-direction signal only (`"buying" | "selling" | "neutral"`) — never per-transaction records, never named individuals. `Fundamentals` gained three new fields — `insiderOwnershipPct`, `sharesOutstandingChange5y`, `netInsiderActivity` — mirroring Sprint 20's sector/industry precedent so the data flows through the eager `buildValueResearchReport()` path for free. `lib/fundamentals.ts`: new `simulatedCapitalAllocation(symbol)` (deterministic, `makeRng`-seeded) wires all three for `SimulatedFundamentalsProvider`; new `fetchFmpCapitalAllocation(base, sym, k)` best-effort helper for `FmpFundamentalsProvider` fetches FMP's `/enterprise-values` (5-year shares-outstanding trend) and `/insider-trading` (aggregated buy/sell volume → net direction at a 60%/40% buy-share threshold) — genuinely unverified endpoints with no prior in-repo precedent and no `FMP_API_KEY` available this session, so the whole helper is wrapped to degrade to honest `null` on any failure or unexpected shape, never blocking the main fetch; `insiderOwnershipPct` is left honestly `null` for FMP entirely (judged more speculative than the other two, no documented free-tier field found, never guessed). `AlphaVantageFundamentalsProvider` implements no new fetch this sprint — all three fields honestly `null`, a disclosed scope reduction. `lib/investmentQuality.ts`'s Share Dilution/Buybacks and Insider Ownership metrics — unconditionally `unavailable` since Sprint 15 — are now scored when the active provider supplies the corresponding field (linear ramps: buybacks/dilution against a -15%/+20% five-year band, insider ownership against a 20% benchmark) and honestly `unavailable` otherwise; a SIMULATED report's `confidenceLevel` now reads "High" (12 of 12) instead of Sprint 15's permanent "Moderate," with zero further code change. `lib/tomNashEngine.ts`'s `capitalAllocationPillar()` extended from Sprint 16's 3-metric average (Cash Position/Debt Levels/ROIC) to a 5-metric average adding Share Dilution/Buybacks and Insider Ownership — still a plain renormalized average over whichever are available, byte-identical to the pre-Sprint-24 formula wherever a provider leaves the two new fields `null` (proven by a dedicated regression test); `netInsiderActivity`, when known, is appended to the pillar's `detail` string as descriptive text only, never scored on its own. Because Tom Nash's `businessQuality` pillar is `= iq.score` directly (Sprint 16's own design) and Investment Quality's own `score`/`confidenceLevel` auto-renormalize over available metrics, both improved for a SIMULATED symbol with zero code change to either field's own logic — the only genuine code change to a prior sprint's computed output is the Capital Allocation pillar extension itself. **No `StockResearch.tsx` change needed** (confirmed before implementation): the existing Tom Nash card already renders each pillar's `detail` string via the shared `FactorList` component, so the new detail text surfaces automatically. **No `openapi.yaml` change needed** either: `Fundamentals` is not directly exposed via the API, and `ValueQualityMetricScore.metric` is already a free `string` with no enum constraint. 8 existing test fixture files got the 3 new `Fundamentals` field defaults (mechanical, Sprint 20's precedent); 2 existing integration/unit test files (`valueReport.investmentQualityIntegration.test.ts`, `valueReport.tomNashIntegration.test.ts`) had their old always-unavailable/never-mentions-Insider-Ownership assertions mechanically updated to match this sprint's approved, disclosed behavior change — not a regression. New tests: `fundamentals.capitalAllocation.test.ts` (mocked-fetch coverage for FMP's `/enterprise-values`/`/insider-trading` — success, majority-buy/majority-sell/neutral classification, network failure, FMP error-shape response, fewer-than-2-data-points path, all degrading to honest `null`; Alpha Vantage's all-three-fields-null path; SIMULATED determinism/range checks) and `valueReport.capitalAllocationIntegration.test.ts` (proves no new report section; Graham/DCF/Buffett/the blended model/Financial Ratios/Competitive Advantage are all unaffected since none read the 3 new fields; Investment Quality/Tom Nash/the Investment Committee are wired consistently between standalone calls and the report). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (57 files / 622 tests, run three times — one run hit the same previously-disclosed `fetchedAt`-timing flake in `fundamentals.investingUniverse.test.ts` first noted in Sprint 16's report, unrelated to this sprint's changes, clean on the other two runs), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests, unmodified — no frontend change this sprint), and `pnpm run build` all pass for real.
- **Sprint 25 — COMPLETE.** Earnings Intelligence Engine (Phase 2 plan, Sprint 25, per the roadmap's post-Sprint-15 renumbering). Three owner decisions were asked and approved before implementation: (1) SIMULATED mode generates deterministic quarterly earnings (actual/estimate/surprise%) derived from the existing annual `revenueHistory`/`epsHistory` arrays, consistent with Sprint 24's "aggregate numbers can be simulated" precedent, rather than LIVE-only like Document Intelligence; (2) Revenue Surprise is honestly `unavailable` for Alpha Vantage specifically (its documented `EARNINGS` endpoint has no revenue-estimate field), never approximated, matching Financial Ratios' Sprint 18 precedent; (3) 8 quarters (2 years) tracked by default, a named constant (`EARNINGS_QUARTERS_TRACKED`). `lib/fundamentals.ts` gained `QuarterlyEarningsRecord`/`EarningsHistory` types and a new `getEarningsHistory(symbol, opts?)` method on `FundamentalsProvider` and all 3 implementations, mirroring Sprint 19's `getFinancialStatements()` seam: `SimulatedFundamentalsProvider` derives 8 quarters from the last 2 annual history entries via seeded quarterly weights and small seeded estimate noise (never an independently random source); `FmpFundamentalsProvider` fetches FMP's documented `/historical/earning_calendar/{symbol}` (both EPS and revenue in one endpoint); `AlphaVantageFundamentalsProvider` fetches the `EARNINGS` function's `quarterlyEarnings` (EPS only, revenue honestly null, preferring AV's own reported `surprisePercentage` over recomputing). A shared `computeSurprisePct()` helper guards a zero-estimate divide (null, never fabricated). New `earningsCache` (24h TTL) mirrors `statementsCache`. **Live FMP/Alpha Vantage verification is explicitly deferred** — no API keys were available this session; both live paths are covered by mocked-fetch tests only. New `lib/earningsAnalysis.ts` — `analyzeEarningsIntelligence(history, kind?)` — pure and provider-agnostic (never touches a `FundamentalsProvider`), computing EPS/revenue beat rates, a consecutive beat/miss/meet streak, a YoY earnings-growth-trend classifier (`accelerating`/`decelerating`/`stable`/`insufficient-data`, the one genuinely new formula this sprint), a consistency score, a confidence level, and a deterministic rule-based summary — zero LLM calls, per the explicit instruction. Consistency Score reuses `competitiveAdvantage.ts`'s `historyConsistencyScore()` (exported this sprint, a one-line behavior-preserving change, confirmed byte-identical via regression test); Financial Statements/Financial Ratios/Graham/DCF/Buffett/Tom Nash/Management Quality/Document Intelligence were each checked during planning and none compute anything at quarterly-earnings granularity, so no other genuine reuse target exists — none was forced. New `buildEarningsIntelligence(symbol, provider, opts?)` orchestrates the earnings fetch plus a `getFundamentals()` call purely for the ETF-caveat `kind` field. **Integration decision, consistent with Sprints 19/20/22/23:** kept fully additive and on-demand — `buildValueResearchReport()`/`GET /stock-analyst/value/:symbol` untouched (proven by regression test); new standalone route `GET /stock-analyst/earnings/:symbol` mirrors Financial Statements' honest-502/404 contract. `StockResearch.tsx` gained a new "Earnings" tab (disabled until a report is loaded, `enabled`-gated hook) showing confidence/streak/growth-trend/consistency badges, the deterministic summary, and a per-quarter EPS/revenue actual-vs-estimate-vs-surprise table. `openapi.yaml` gained `QuarterlyEarningsRecord`/`EarningsGrowthTrend`/`EarningsSurpriseStreak`/`EarningsIntelligenceAnalysis` schemas and the new `getEarningsIntelligence` path; `api-zod`/`api-client-react` regenerated to match. No database migration — computed on demand, cached only in-process. One mechanical fallout: `industryComparison.test.ts`'s `FakeProvider implements FundamentalsProvider` needed a one-line `getEarningsHistory` stub added since the interface gained a new required method — no behavior change. New tests: `earningsAnalysis.test.ts` (24 tests), `fundamentals.earningsHistory.test.ts`, `routes/earningsIntelligence.route.test.ts`, `valueReport.earningsIntelligenceIntegration.test.ts`, plus one added regression assertion in `competitiveAdvantage.test.ts` proving the `historyConsistencyScore` export didn't change Competitive Advantage's own output. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (61 files / 664 tests, run twice to confirm stability with zero flakes), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests, unmodified), and `pnpm run build` all pass for real.
- **Sprint 26 — COMPLETE.** Tom Nash Investment Engine — Enhancement II: Sector & Macro, Rate Sensitivity, AI/Tech Cycle + Committee Confidence Refinement (Phase 2 plan, Sprint 26, per the roadmap's post-Sprint-15 renumbering). `AskUserQuestion` failed at kickoff with the same infrastructure error first seen in Sprint 23; per that sprint's disclosed precedent, 4 genuine decisions were stated as recommendations in chat and implementation proceeded once the project owner replied "Continue from where you left off." All 4 recommended options were adopted: (1) AI/Tech-Cycle stays deterministic-only this sprint (deviating from the roadmap's original "LLM-narrated" sketch) — a structural composite of already-known qualitative/financial signals, never a real LLM claim about a company's actual AI/technology strategy; (2) the 3 new dimensions (Sector & Macro, Interest Rate Sensitivity, AI/Tech-Cycle) are informational only, never entered into `PILLAR_WEIGHTS`/the conviction-score average — `convictionScore`/`verdict` are byte-identical to the pre-Sprint-26 5-pillar formula, confirmed by regression test; (3) macro/rate-regime data (roadmap §2 item 8, flagged "needs owner OK" since Sprint 11, resolved now) comes from a new, self-contained SIMULATED proxy (`lib/investingMacro.ts`) rather than reading the Options Engine's `marketBriefing.ts`/`optionsMath.ts` (a genuinely different signal — options IV isn't a macro rate regime); (4) the Investment Committee's confidence-weighting refinement blends Tom Nash's Committee-vote confidence with a new `dataCompleteness` field (`confidence = convictionScore × dataCompleteness`) — byte-identical to the pre-Sprint-26 formula whenever `dataCompleteness` is 1, with the aggregation methodology itself (vote mapping, agreement classification, split→Hold default, `confidenceScore`'s own averaging) completely untouched. New `lib/investingMacro.ts` — `buildMacroContext(asOf)` — a deterministic, day-seeded regime generator (`rising_rates`/`falling_rates`/`stable_rates`), clearly `dataSource: "SIMULATED"`, never a real forecast. `lib/tomNashEngine.ts` gained `sectorMacroPillar()` (reads `f.sector`/`f.industry` directly, zero new provider calls, combined with a hand-curated per-sector rate-sensitivity note table), `rateSensitivityPillar()` (the one genuinely new quantitative framework this sprint — an illustrative 0-100 "duration score" from growth/forward-P/E/dividend-yield/leverage, classifying Long-Duration Growth/Value-Short-Duration/Blend against the macro regime), and `aiTechCyclePillar()` (the deterministic structural proxy). `analyzeTomNash()` gained an 8th, optional parameter defaulting to `buildMacroContext(f.asOf)`, so every existing call site kept compiling and behaving identically without modification. The 3 new dimensions append informational lines to `rationale` — surfaced automatically via the existing `tom-nash` report section's bullets wiring, requiring **zero `valueReport.ts` changes**. `StockResearch.tsx` gained one small addition: a `{dataCompleteness}% data` badge on the existing Tom Nash card. `openapi.yaml` gained 3 new schemas and 4 new `ValueTomNashAnalysis` fields — no new path (Tom Nash stays folded into the existing `/value/:symbol`). No database migration. Mechanical fallout: `investmentCommittee.test.ts`'s and the frontend's `TomNashAnalysis` fixtures needed the 4 new required fields added; one genuine, disclosed test-shape update in Sprint 16's own `valueReport.tomNashIntegration.test.ts` (`rationale.length` 5→8). New tests: `investingMacro.test.ts`, new cases appended to `tomNashEngine.test.ts` and `investmentCommittee.test.ts`, and a new `valueReport.tomNashEnhancementIIIntegration.test.ts` proving no new report section, every prior sprint's own output unchanged, Tom Nash's conviction/verdict byte-identical to a standalone call, and the Investment Committee's new confidence discount wired correctly. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (63 files / 696 tests, run twice to confirm stability with zero flakes), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests, unmodified assertions), and `pnpm run build` all pass for real.
- **Sprint 27 — COMPLETE.** Watchlist Polish (Phase 2 plan, Sprint 27, per the roadmap's post-Sprint-15 renumbering). No genuinely novel owner decision came up during planning — the one real design choice (compute badge targets on-demand vs. eagerly on every watchlist load) followed the same on-demand-for-heavier-operations precedent already established since Sprint 19, not a fresh decision. `GET /stock-analyst/value-watchlist` gained an optional `?checkTargets=true` query param; default behavior (no param) is byte-identical to pre-Sprint-27. When `checkTargets=true`, a new `computeWatchlistTargets()` helper resolves fresh `Fundamentals` per row (reusing `resolveFundamentals()`) and computes 3 new, always-nullable, never-fabricated fields: `currentPrice`, `priceTargetCrossed` (vs. `desiredBuyPrice`), and `marginOfSafetyTargetCrossed` (vs. `fairValueEstimate`/`marginOfSafetyTarget`) — null, not false, when the relevant target was never set. Kept strictly on-demand via an explicit "Check Targets" button in `StockResearch.tsx`'s Watchlist tab. Bulk-add required no new backend route — the frontend calls the existing `POST /value-watchlist` once per selected symbol not already watched (`Promise.allSettled`, toast reporting success/failure counts): `StockResearch.tsx`'s Peers tab gained an "Add peers to watchlist" button, and `StockScanner.tsx` gained an "Add to Watchlist" button reusing its existing `selected` row-selection state (built for the Compare feature). `openapi.yaml` gained the `checkTargets` param and 3 new `ValueWatchlistItem` fields; `api-zod`/`api-client-react` regenerated to match. No database migration. New `routes/valueWatchlist.route.test.ts` (the Watchlist CRUD endpoints had no dedicated test file before this sprint — covers a baseline smoke test of default-GET behavior plus full `checkTargets` coverage: real price resolution, both crossed-flags true/false against real SIMULATED prices, and honest-null paths when no target was set) and one new `StockScanner.test.tsx` bulk-add case. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (64 files / 702 tests, run three times — two runs hit the same previously-disclosed `fetchedAt`-timing flake in `fundamentals.investingUniverse.test.ts` first noted in Sprint 16's report, unrelated to this sprint's changes, clean on the other runs), `pnpm --filter @workspace/ravish-trading run test` (6 files / 34 tests), and `pnpm run build` all pass for real.
- **Sprint 28 — COMPLETE.** Portfolio Construction (Phase 2 plan, Sprint 28, per the roadmap's post-Sprint-15 renumbering). `AskUserQuestion` failed twice more this sprint with the same "Tool permission stream closed before response received" infrastructure error first seen in Sprint 23 (and again in Sprint 26) — the 3rd/4th occurrences overall this session. Per the project owner's own explicit instruction for this sprint ("If AskUserQuestion fails again, state your recommendation, document the deviation, and proceed only as previously agreed"), 2 genuine owner decisions were stated as recommendations directly in chat and implementation proceeded once the owner replied "Continue from where you left off": (1) **portfolio deletion cascades its own holdings** — `investing_holdings.portfolio_id → investing_portfolios.id` is `ON DELETE CASCADE`, the first FK in the entire codebase to deviate from the otherwise-universal `ON DELETE RESTRICT` convention, justified because deleting your own portfolio is routine self-service cleanup on your own sub-resource, functionally distinct from the whole-user-deletion scenario RESTRICT protects against — the `user_id → users.id` FK stays RESTRICT on both new tables, no exception there; (2) **rebalance drift threshold of ±5 percentage points**, a named constant (`REBALANCE_DRIFT_THRESHOLD_PCT`). New tables `investing_portfolios`/`investing_holdings` via `lib/db/manual-migrations/008_investing_portfolio_construction.sql` — `NOT NULL` from creation, zero existing rows, same precedent as `platform_audit_log`/`investing_filing_analysis`. **`investing_holdings` deliberately stores no price/market-value column** — extends Sprint 27's own never-persist-a-price discipline; every market-value/actual-weight/drift figure is computed fresh on read via `resolveFundamentals()`. New `lib/portfolioConstruction.ts` — `computePortfolioAllocation(holdings, prices)` (pure, provider-agnostic) and `buildPortfolioAllocation(holdings, provider)` (dedupes symbols, resolves each once). Genuinely never-fabricate: target weights are never silently normalized to 100% (only an honest `targetWeightSumWarning` when the real sum deviates by more than 1 point); a holding's `marketValue`/`actualWeightPct`/`driftPct` are `null` — not `0`/an approximation — whenever `shares` is null or the price can't be resolved; `rebalanceAction` is honestly `"unknown"` (never `"hold"`) when drift can't be computed. The roadmap's "(a) equal-weight or (b) target-weight-drift" phrasing was resolved as one coherent design: target-weight-drift is the general engine (every holding stores its own `targetWeightPct`), and equal-weight is a pure client-side convenience (`PortfolioConstruction.tsx`'s "Add from Watchlist" button computes `100 / N` client-side) — no separate backend mode, no new endpoint for the trivial math. New `routes/portfolioConstruction.ts` — full CRUD, mounted in `routes/index.ts` without a path prefix (matching `scannerRouter`/`portfolioRouter`'s pattern). Every query uses `getScopedUserId(req)` and the established `and(eq(id), eq(userId))` ownership-scoping pattern — 404, never a separate 403, for both "doesn't exist" and "isn't yours." `GET .../portfolios/:id` resolves the full allocation **eagerly** — a disclosed design choice matching Industry Comparison's Peers-tab precedent (Sprint 20: a small, bounded item count per portfolio, unlike Watchlist's own list-of-many symbols, which stays opt-in-gated per Sprint 27). Deliberately a separate file/route namespace from `routes/portfolio.ts`/`routes/portfolioAI.ts` (Engine 3's real, trades-backed account tracking) to avoid confusion between the two similarly-named systems, same precedent as Sprint 25's `earningsAnalysis.ts` vs `earnings.ts`. **A real schema-naming collision with pre-existing Options Engine schemas was discovered and fixed during implementation, not anticipated in the pre-approval plan** — the original names (`PortfolioSummary`, `PortfolioCreate`, `Holding`, etc.) silently collided with existing Options Engine schemas in the same `openapi.yaml` (YAML permits duplicate top-level keys, so this surfaced as an opaque Orval "Failed to resolve input" codegen error, not a clear duplicate-key error); fixed by renaming all 9 new schemas with a `Construction` prefix, verified collision-free across the whole file, then `api-zod`/`api-client-react` regenerated successfully. New page `PortfolioConstruction.tsx` at `/stock-analyst/portfolio-construction` (nested to avoid colliding with Engine 3's own `/portfolio` route), wired into `App.tsx`'s router and `AppLayout.tsx`'s nav; portfolio list/create/delete, inline-editable target weight/shares (`onBlur`-triggered `PATCH`), color-coded rebalance-action badges, "Add from Watchlist" equal-weight bulk-add. Required the established `queryKey` + `enabled` fix for the generated `useGetPortfolio` hook (Sprint 19's precedent). **Test-infrastructure refactor, disclosed, not just new Sprint-28-scoped code:** while wiring the new tables into the existing tenant-isolation suite (reusing "the established `assertTenantIsolation` helper" per the roadmap's own instruction), discovered that helper was defined inline inside `lib/tenantIsolation.test.ts` with a closure over that file's own module-level `userA`/`userB` state — unsafe to import directly into another `.test.ts` file, since doing so would re-execute the imported file's own `describe()`/`beforeAll()` blocks as an import side effect. Fixed by extracting it into a new, non-test module `lib/tenantIsolationHelper.ts` with a redesigned signature taking `userA`/`userB` as explicit parameters instead of closing over test state — confirmed to preserve every one of the ~13 existing call sites' behavior unchanged (only the function's location and call-site argument count changed). New tests: `lib/portfolioConstruction.test.ts` (12 tests — market-value/actual-weight math, honest-null paths for missing shares/unresolved price, buy/sell/hold rebalance flagging at the ±5pp threshold, the never-normalize warning, zero-holdings handling, a never-fabricates-execution-language check, SIMULATED-provider dedup, invalid-ticker honest null), `routes/portfolioConstruction.route.test.ts` (6 live end-to-end HTTP tests — full CRUD+cascade-delete flow, honest null market value with no shares entered, honest target-weight-sum warning, single-holding removal leaving others intact, 404/400 error handling, list-view `holdingsCount` accuracy), 2 new cases in `lib/tenantIsolation.test.ts` (`investing_portfolios`, `investing_holdings`) via the extracted helper, and new `PortfolioConstruction.test.tsx` (5 frontend smoke tests). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (66 files / 722 tests, run 6 times total — 4 runs fully clean, 1 run hit the previously-disclosed `fetchedAt`-timing flake in `value.test.ts`'s SIMULATED-determinism check first noted in Sprint 16's report, 1 run hit a pre-existing test-isolation FK-violation flake in `autoScheduler.multiUser.test.ts` — both confirmed unrelated to this sprint's changes via `git status` showing neither file touched), `pnpm --filter @workspace/ravish-trading run test` (7 files / 40 tests, including the 5 new `PortfolioConstruction.test.tsx` cases), and `PORT=5000 BASE_PATH=/ pnpm run build` all pass for real.
- **Sprint 29 — COMPLETE.** Portfolio Risk Analysis (Phase 2 plan, Sprint 29, per the roadmap's post-Sprint-15 renumbering). `AskUserQuestion` failed again at kickoff with the same "Tool permission stream closed before response received" infrastructure error (5th occurrence this session — Sprint 23, 26, twice in 28, now this). Per the established, repeatedly-approved recovery, 3 genuine decisions were stated as recommendations in chat and implementation proceeded once the project owner replied "Continue from where you left off": (1) **beta is a real `Fundamentals` field**, not a derived proxy — FMP's `/profile` and Alpha Vantage's `OVERVIEW`, both already fetched by the existing providers for sector/industry (Sprint 20), each carry a documented `beta` field simply not mapped until now (zero new provider calls); SIMULATED gets a deterministic seeded value in a 0.5-2.0 range; (2) **hard-cap thresholds of 25% single-symbol / 40% single-sector**, named constants (`SINGLE_SYMBOL_CONCENTRATION_CAP_PCT`, `SECTOR_CONCENTRATION_CAP_PCT`), same precedent as Sprint 28's `REBALANCE_DRIFT_THRESHOLD_PCT`; (3) **risk snapshots are written only via an explicit "Save Snapshot" user action**, never automatically on every read — extends Sprint 27/28's never-persist-unless-asked discipline. New `lib/investingRisk.ts` — `computePortfolioRisk(holdings)` — architecture deliberately modeled on the Options Engine's `portfolioHealth.ts` pattern: a pure, I/O-free scorer producing a concentration score (largest single holding vs. the 25% cap), a sector-exposure score (largest single sector vs. the 40% cap, with a full per-sector breakdown and an honest `unclassifiedWeightPct` for holdings with no known sector), a market-value-weighted portfolio beta (renormalized over only the holdings with a known beta — never averaging in an unknown beta as 0), and an overall score blending all three with a **hard-cap override**: breaching either cap caps the overall score at 60 regardless of the blend, the same pattern `portfolioHealth.ts` uses for a breached risk limit. Every component honestly reports `null`/"Insufficient data" (never a fabricated 0 or 100) when there isn't enough resolvable market value or classification data to score it. `lib/portfolioConstruction.ts`'s `PortfolioHoldingAllocation` was extended additively with `sector`/`beta` per holding, sourced from the exact same Fundamentals resolution `buildPortfolioAllocation()` already performs for price — zero new provider calls. **The extension was made via a new *optional* third parameter** (`meta: Map<string, SymbolMeta> = new Map()`) to `computePortfolioAllocation()` rather than changing the existing `prices` map's shape, so every pre-existing 2-argument call site (including all of Sprint 28's own unit tests) kept compiling and behaving identically — confirmed by a dedicated regression test proving the exact pre-existing fields/values are unchanged when no `meta` map is supplied. New table `investing_risk_snapshots` (`lib/db/manual-migrations/009_investing_risk_snapshots.sql`) — `userId` FK RESTRICT (universal convention), `portfolioId` FK **CASCADE** (matching `investing_holdings`' own Sprint 28 precedent — a snapshot of a deleted portfolio is meaningless and should be deleted with it), `analysisJson` holding the full `PortfolioRiskAnalysis` blob plus a promoted, independently-queryable `overallScore` column (same JSON-blob-plus-headline-columns pattern as `stock_analysis_history`) — brand-new table, `NOT NULL` from creation except the honestly-nullable `overallScore`, no backfill needed. New routes on the existing `routes/portfolioConstruction.ts`: `GET /portfolio-construction/portfolios/:id/risk` (eager, reuses the exact same `buildPortfolioAllocation()` call the detail route already makes), `GET`/`POST .../risk/snapshots` (list newest-first / save-on-demand) — all ownership-scoped via `getScopedUserId(req)` and `and(eq(id), eq(userId))`, 404 for both "doesn't exist" and "isn't yours," matching every established route's discipline. `openapi.yaml` gained the `portfolio-construction` tag's 3 new paths and 8 new `Construction`-prefixed schemas (`ConstructionPortfolioRiskAnalysis`, `ConstructionConcentrationRisk`, `ConstructionSectorExposureRisk`/`Row`, `ConstructionBetaEstimateRisk`, `ConstructionRiskComponent`, `ConstructionRiskSnapshot`), following Sprint 28's own collision-avoidance naming convention (verified collision-free before codegen, no repeat of Sprint 28's schema-naming incident this time); `api-zod`/`api-client-react` regenerated cleanly. `PortfolioConstruction.tsx` gained a new "Portfolio Risk" card (`ShieldAlert` icon, shown once a portfolio is selected) with overall/concentration/sector/beta score badges (cap-breach badges highlighted in red), the deterministic overall detail sentence, an explicit "Save Snapshot" button (disabled when risk couldn't be scored), and a compact saved-snapshot history strip. **Mechanical fallout from extending `Fundamentals` with one new required field (`beta`), caught by typecheck and fixed before commit:** 8 existing test files' local `fixture(overrides)` helpers (`buffettValuation.test.ts`, `competitiveAdvantage.test.ts`, `dcfValuation.test.ts`, `financialRatios.test.ts`, `grahamValuation.test.ts`, `industryComparison.test.ts`, `investmentQuality.test.ts`, `tomNashEngine.test.ts`) each needed a one-line `beta: null` default added — the same fallout category and fix as Sprint 20's `sector`/`industry` addition; every other test file that builds `Fundamentals` via a real provider call needed no changes. New tests: `investingRisk.test.ts` (20 tests — concentration/sector-exposure/beta banding math, both hard-cap overrides and their effect on the overall score, every honest-insufficient-data path, the renormalize-over-known-beta-only proof, the adapter's equivalence to calling the core function directly), `fundamentals.beta.test.ts` (SIMULATED determinism/range/per-symbol-variation, FMP's direct `/profile.beta` mapping and its honest-null path, Alpha Vantage's direct `OVERVIEW.Beta` mapping and its honest-null path — all against mocked fetch, live verification explicitly deferred per the established no-API-key-this-session precedent), 2 new cases in `portfolioConstruction.test.ts` (the additive-parameter regression proof, plus `buildPortfolioAllocation()` resolving sector/beta for real via `SimulatedFundamentalsProvider`), 1 new case in `tenantIsolation.test.ts` (`investing_risk_snapshots`, reusing the extracted `assertTenantIsolation` helper), `routes/portfolioRisk.route.test.ts` (6 live end-to-end HTTP tests — a concentrated-portfolio risk computation with a tripped cap, 404/400 error handling, proof that a plain `GET /risk` never auto-persists a snapshot, the full save-then-list snapshot flow, an honest insufficient-data response for an empty portfolio, 404 on saving/listing snapshots for a non-existent portfolio), and 2 new frontend cases in `PortfolioConstruction.test.tsx` (the risk panel's badges/save-snapshot flow, and the disabled-Save-Snapshot state when risk is unscoreable). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (69 files / 759 tests, run 3 times to confirm stability — all 3 fully clean, zero flakes), `pnpm --filter @workspace/ravish-trading run test` (7 files / 42 tests), and `PORT=5000 BASE_PATH=/ pnpm run build` all pass for real.
- **Sprint 30 — COMPLETE.** AI Investment Analyst (Phase 2 plan, Sprint 30, per the roadmap's post-Sprint-15 renumbering). Investigation before implementation found that `coachLLM.ts` already had `narrateValueResearch()`/`narrateValueResearchStream()` (a fixed-prompt thesis narration wired into the existing `/value-research` route, pre-dating Phase 2) and `narrateFreeform()` (options-education only, no value-safety wrapper) — the roadmap's actual novel deliverable, free-form Q&A grounded in the FULL report including Tom Nash and the Investment Committee, didn't exist yet: `narrationContext()` in `stockAnalyst.ts` only ever passed a narrow subset (business quality, moat, financial strength, valuation, decision), excluding Graham/DCF/Buffett, Investment Quality, Competitive Advantage, Tom Nash, and the Investment Committee entirely. **One disclosed deviation from the roadmap's literal text, not a fresh owner decision** (an implementation-detail correction made once the real code was inspected, consistent with this project's precedent of disclosing such corrections rather than following stale planning prose): the roadmap said "New `lib/investingCoachLLM.ts`," but per CLAUDE.md rule 6 ("route through the shared narrate()/narrateStream() pattern... coachLLM.ts in the interim"), the new function was added directly inside `coachLLM.ts` next to `narrateValueResearch`, reusing its already-private `narrate()`/`narrateStream()`/`enforceValueSafety()` machinery rather than forking a second file that would have had to duplicate or re-import that private machinery — the safer, more central-enforcement-preserving choice. New `narrateValueFreeform()`/`narrateValueFreeformStream()` in `coachLLM.ts` — identical safety contract to `narrateValueResearch`: `narrate()`/`narrateStream()` guarantee `COACH_DISCLAIMER`, `enforceValueSafety()` guarantees `VALUE_DISCLAIMER` + the anti-impersonation guard, applied to the authoritative final payload on both the single-shot and streaming paths. New prompt explicitly instructs the model to answer only from the provided DATA and to say so plainly (never fabricate) when the question asks for something the DATA doesn't cover (a future price prediction, a different symbol, a number not present). New `buildFreeformContext(report)` in `stockAnalyst.ts` — extends `narrationContext()`'s fields with Investment Quality, Competitive Advantage, all 3 named valuation models (Graham/DCF/Buffett) + the consolidated margin of safety, the full Tom Nash pillar analysis (all 5 pillar scores, conviction score, verdict, data completeness, rationale), and the Investment Committee's votes/agreement/verdict/summary — everything already computed on `ValueResearchReport` since Sprints 12-17, zero new provider calls. Deliberately excludes Industry Comparison/Earnings/Filings/Management Quality, since those are separate on-demand reports never present on the eager report — an honest scope boundary, not an oversight. New routes `POST /stock-analyst/value-research/ask` (single-shot) and `POST /stock-analyst/value-research/ask/stream` (SSE, matching `/value-research/stream`'s own precedent of staying outside the OpenAPI/orval contract since orval only models single-shot JSON) — both rebuild the report server-side per call (same pattern as `/value-research`; SIMULATED reports are cheap/deterministic and LIVE reports are already short-TTL cached in `fundamentals.ts`, so no additional caching was added), 404 for an unknown symbol, and an honest deterministic `freeformFallback()` when the LLM is unavailable (never attempts to literally answer the open-ended question — states plainly that AI narration isn't available, then surfaces the same deterministic facts `narrationFallback()` already provides). `openapi.yaml` gained the `/stock-analyst/value-research/ask` path and `AskValueResearchInput`/`AskValueResearchResult` schemas (Orval named the generated Zod schemas `AskValueResearchBody`/`AskValueResearchResponse`, matching `GenerateValueResearchBody`/`Response`'s own established POST-endpoint naming convention); `api-zod`/`api-client-react` regenerated cleanly, no schema collisions. `StockResearch.tsx`'s `ReportView` gained a new "Ask the AI Investment Analyst" card (`MessageCircle` icon, positioned directly after the existing "AI Research Thesis" card) — a self-contained chat panel (question input + running Q&A transcript) using local component state keyed only off `report.symbol`, reusing the exact same `streamCoach()` SSE client the AI Research Thesis panel above it already uses, zero new frontend streaming infrastructure. No database migration — this sprint touches no schema. New tests: 2 disclaimer-invariant tests appended to `value.test.ts`'s existing "value narration safety invariants" block (mirroring its established pattern exactly, proving `narrateValueFreeform` carries both `COACH_DISCLAIMER` and `VALUE_DISCLAIMER` on both the LLM and template path), `routes/valueResearchAsk.route.test.ts` (5 tests — a live 200 response for a known symbol, 404 for an unknown symbol, 400 for a missing question, and a mocked-`narrateValueFreeform` test proving the grounding context genuinely includes `tomNash`/`investmentCommittee`/all 3 named valuation models/the consolidated margin of safety/Investment Quality/Competitive Advantage — the sprint's own explicit acceptance-criteria examples), and 2 new `StockResearch.test.tsx` cases (the chat panel submits to the correct SSE endpoint with the correct body, and a streamed answer renders as a Q&A turn in the transcript). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `valueReport.ts` was not modified at all this sprint. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (70 files / 764 tests, run 5 times — 4 runs fully clean, 1 run hit an unreproduced transient flake in an unidentified file, consistent with this suite's established live-Postgres-parallelism flake category from prior sprints and confirmed unrelated to any Sprint-30-touched file via the other 4 clean runs), `pnpm --filter @workspace/ravish-trading run test` (7 files / 44 tests), and `PORT=5000 BASE_PATH=/ pnpm run build` all pass for real.
- **Sprint 31 — COMPLETE.** Company Research Unification (Phase 2 plan, Sprint 31, per the roadmap's post-Sprint-15 renumbering — the final sprint of Phase 2). **Audit finding, made before writing any code:** the roadmap's objective — "wire every module built in Sprints 12–29 into one coherent Company Research experience" — was already substantially satisfied by the time this sprint began, discovered incrementally across those very sprints (the same pattern Sprint 20 found for Tom Nash/Committee already being "folded in" back in Sprints 16/17). `buildValueResearchReport()` already assembled all 23 narrative sections (Business Quality through Disclaimers, including Investment Quality, Competitive Advantage, Financial Ratios, all 4 valuation models, the Consolidated Margin of Safety, the full Tom Nash pillar analysis, and the Investment Committee's consolidated recommendation), and `StockResearch.tsx` already had one page, one symbol context, with tabs for every heavier on-demand module built since: Statements (Sprint 19), Peers/Industry Comparison (Sprint 20), Filings + Management Quality (Sprints 22–23, folded into one tab), Earnings (Sprint 25), plus Sprint 30's free-form Ask panel inside the Research tab itself. **Genuine owner decisions found: none** — no `AskUserQuestion` call was needed this sprint, since there was no binary choice to present, only an implementation-quality audit finding. The on-demand-vs-eager split for Statements/Peers/Filings/Management/Earnings is a deliberate, repeatedly-reaffirmed cost-control design (every one of those sprints explicitly disclosed "kept fully additive and on-demand"); collapsing them into the eager path would have been a **regression** against established architecture, not a fix, so it was explicitly not done, consistent with the user's own "do not introduce regressions" instruction for this sprint. **Actual scope shipped:** (1) one small, additive UI discoverability fix — `StockResearch.tsx`'s "Filings" tab label was renamed to "Filings & Mgmt" (a one-line, zero-logic change) since Sprint 23 folded Management Quality analysis into that tab as a second card without any label hint; (2) the roadmap's own explicitly-required deliverable that didn't exist yet — a genuine "end-to-end regression pass across the whole engine." New `lib/valueReport.fullEngineIntegration.test.ts` (5 tests) — a full-report-shape integration test proving all 23 section ids appear in the documented order and every top-level analytical field a complete institutional decision report needs is present and correctly shaped (company overview, all 4 valuation models + consolidated MoS with `modelsConsidered === 4`, moat/quality/competitive-advantage, the full Tom Nash pillar analysis including the Sprint 26 sector-macro/rate-sensitivity/AI-tech-cycle dimensions, and the Investment Committee's final verdict/agreement) — for a standard SIMULATED stock (MSFT), an ETF (SPY, the diversified-fund caveat path), a symbol outside the original 10-symbol universe (IBM, the `syntheticProfile()` path), and an honest-null proof for a genuinely invalid ticker shape. New `routes/companyResearchUnification.route.test.ts` (9 live end-to-end HTTP tests against the real running app) — proves one symbol lookup (AAPL) resolves successfully across every module's own route (`/value`, `/financial-statements`, `/industry-comparison`, `/filings`, `/management-quality`, `/earnings`, `/value-research/ask`), that all 6 core modules 200 concurrently for the same symbol with zero 404s (the literal "unified single-symbol-lookup guarantee"), and that an unknown symbol 404s consistently across all 6 rather than fabricating a partial report for some modules and not others; the Filings/Management-Quality tests also live-confirmed the already-disclosed EDGAR-unreachable degradation (this environment's proxy denies `data.sec.gov` with a 403, first disclosed in Sprint 22) still degrades honestly to `documentAvailable: false` with a `200`, never a `502`, exactly as designed. **LIVE-mode (FMP/Alpha Vantage) verification is explicitly deferred** — no API key was available this session, the same unbroken disclosure every sprint has made since Sprint 11; every new test in this sprint runs SIMULATED-only. No changes to `buildValueResearchReport()`'s logic, no `openapi.yaml` change, no database migration — the lowest-risk, lowest-schema-impact sprint of the entire phase, exactly as the roadmap's own estimate predicted. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (72 files / 778 tests, run twice — both fully clean, zero flakes), `pnpm --filter @workspace/ravish-trading run test` (7 files / 44 tests, unaffected by the tab-label rename), and `PORT=5000 BASE_PATH=/ pnpm run build` all pass for real. **This completes Phase 2 (Institutional Investment Decision Engine), Sprints 11–31, per `docs/Phase-2-Investing-Engine-Execution-Plan.md`.**
- **Phase 3 — Institutional Trading Engine: APPROVED.** Roadmap is `docs/Phase-3-Trading-Engine-Execution-Plan.md` (fresh architecture review against the actual completed codebase, corrections to 3 of the Blueprint's Engine 2 reuse assumptions, all 26 sections, 8 approved owner decisions, ~18-sprint estimate). Continues the project's single global sprint counter — Phase 1 was Sprints 1–10, Phase 2 was Sprints 11–31, **Phase 3 begins at Sprint 32.** Read that document before starting any Phase 3 sprint — it is the authoritative execution plan for this phase, the same role the Phase 2 plan played for Phase 2. Key finding driving the whole phase: there is no real price-series market data anywhere in this codebase (only options-chain quotes) — every Engine 2 module is genuinely net-new, built SIMULATED-first with live data explicitly deferred (approved Decision 7), not a generalization of existing options-engine code the way Phase 2's modules mostly were.
- **Sprint 32 — COMPLETE.** Market Data Foundation (Phase 3 plan, Sprint 32). No genuinely new owner decisions were found beyond the 8 already approved before this sprint began — the implementation boundary the plan already drew (provider seam + two new tables + settings columns, no routes/UI yet) matched the actual codebase cleanly once inspected. New `lib/tradingMarketData.ts` — the `MarketDataProvider` seam every later Engine 2 module builds on: `getCandles(symbol, interval, lookback, asOf?)` and `getQuote(symbol, asOf?)`, both honestly returning `null` for an invalid ticker shape, never fabricating a candle or quote. `SimulatedMarketDataProvider` (`id: "simulated"`, `isLive: false`) is the only implementation shipped this sprint — deterministic, seeded via the exact `makeRng`/`todayStr` primitives already extracted in `lib/deterministic.ts` (Sprint 11), reused unchanged. `TRADING_MARKET_UNIVERSE` **deliberately duplicates, not imports**, the same 10 real-world tickers/base prices `lib/investingUniverse.ts`'s `INVESTING_UNIVERSE` already duplicates from `optionsMath.ts`'s legacy `UNIVERSE` — engines never depend on each other's internals (Architecture Blueprint §1), so Engine 2's SIMULATED AAPL price is independently seeded from Engine 1's and Engine 3's, not shared, even though all three cover the same real-world ticker for plausibility. Any other valid-shaped ticker gets an honest, deterministic synthetic base price (`syntheticBasePrice()`), mirroring `investingUniverse.ts`'s own `syntheticBasePrice()` precedent exactly — never a fabricated real-sounding series. Daily candles (`1D`) are generated directly from a `dailyClose()` formula independent of `investingUniverse.ts`'s own `investingPrice()` seed namespace (same shape, `base × (1 + bounded daily drift)`, deliberately different seed string) — per the plan's §0 Correction 1 precedent (`investingMacro.ts` not reusing `marketBriefing.ts`'s data), extended here to the market-data layer itself. Intraday candles (`1m`/`5m`/`15m`/`1h`) are generated as an internally-consistent per-session random walk from the previous day's close to the current day's own `dailyClose()` anchor — never an independently-random walk disconnected from the daily series (a dedicated regression test proves the last intraday bar's close matches the day's own 1D close). **Bounded lookback windows** (`MAX_LOOKBACK`, per timeframe: 390 bars for `1m`/`5m`, 130 for `15m`, 35 for `1h`, 180 for `1D`, roughly 5 trading days of intraday / 6 months of daily) — a caller requesting more is honestly capped, never silently expanded, addressing the Phase 3 plan's own flagged scalability concern (§26) before it became a real cost problem. `getMarketDataProvider(userId?)` is the resolution seam future live-provider work (explicitly deferred, Decision 7) will slot into — it always returns the simulated instance today, regardless of the new `tradingDataProvider` setting's stored value, mirroring `getFundamentalsProvider()`'s exact shape (settings-driven selection with an honest fallback) one call away from being real. Two brand-new tables via `lib/db/manual-migrations/010_trading_engine_tables.sql`: `trading_positions` (instrument-agnostic open/closed position ledger — `instrumentType` free text, not a DB enum, so options/futures support later needs no schema change, mirroring `investing_filing_analysis.filing_type`'s precedent) and `trading_journal_entries` (mirrors `journal_entries`' own core shape — title/content/mood/tags/lessonLearned — plus trading-relevant nullable fields per the plan's §16: `entryPrice`/`exitPrice`/`rMultiple`/`setupType`, replacing the options-specific `entryCredit`/`maxProfit`/`pop`/`ev`/`ravishScore` fields that shape doesn't need). **Deliberately new tables, not a retrofit of `trades`/`journal_entries`** (approved Decision 2) — direct inspection during Phase 3 planning found `trades` far more options-coupled (`legs` jsonb, `credit`/`maxProfit`/`pop`/`ev`/`ravishScore` all required) than the original Architecture Blueprint assumed, so retrofitting it in place would have meant either awkward instrument-type branching or a near-total rewrite, at which point it wouldn't be "the existing foundation" anymore. `trading_journal_entries.trading_position_id` deliberately has **no** foreign key constraint, mirroring `journal_entries.trade_id`'s own established precedent exactly (a loose, unenforced reference) — the same choice, made for the same reason. Both tables `NOT NULL` from creation (except the genuinely-optional fields), no backfill needed (zero existing rows, same precedent as `platform_audit_log`/`investing_portfolios`). New `settings` columns via `lib/db/manual-migrations/011_trading_settings.sql`: `tradingDataProvider` (default `"simulated"`) and `tradingDataConnected` (default `false`) — mirrors `fundamentalsProvider`/`fundamentalsConnected`'s exact shape from Sprint 11; `tradingDataConnected` is deliberately **not** included in `SettingsUpdate` (read-only, matching `fundamentalsConnected`'s own precedent — never client-settable, always reflects real state, which today is honestly always `false` since no live provider exists). `openapi.yaml`'s `Settings`/`SettingsUpdate` schemas gained the 2 new fields; `api-zod`/`api-client-react` regenerated cleanly, no collisions. `routes/settings.ts` needed **zero code changes** — its existing `{...settings, ...}` spread pattern (established since Sprint 5) picks up new DB columns automatically. No frontend/UI work this sprint (explicitly out of scope per the plan's own Sprint 32 deliverables — "No UI yet... this is the seam everything else builds on," mirroring Sprint 11's own foundational role in Phase 2). New tests: `tradingMarketData.test.ts` (23 tests — provider identity, ticker-shape validation, byte-identical determinism across repeated calls for every one of the 5 timeframes, OHLCV range validity for every timeframe, oldest-to-newest ordering, lookback clamping at both the upper `MAX_LOOKBACK` bound and the lower 1-bar floor, honest synthetic generation for a symbol outside the default universe, per-symbol seed independence, and the intraday-brackets-daily-close internal-consistency proof), 2 new cases in `tenantIsolation.test.ts` (`trading_positions`, `trading_journal_entries`, reusing the established `assertTenantIsolation` helper unchanged). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`trades`/`journal_entries` were not modified at all this sprint (CLAUDE.md rules 1–3 fully intact). `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (73 files / 803 tests, run twice — both fully clean, zero flakes), `pnpm --filter @workspace/ravish-trading run test` (7 files / 44 tests, unmodified — no frontend change this sprint), and `PORT=5000 BASE_PATH=/ pnpm run build` all pass for real.
- **Sprint 33 — COMPLETE.** Market Structure Engine — Core (Phase 3 plan, Sprint 33). No genuinely new owner decisions surfaced — the plan's own drawn boundary (pure scorer + unit tests only, no route/UI, deliberately deferred to Sprint 34) matched the actual implementation need cleanly. New `lib/tradingMarketStructure.ts` — `analyzeMarketStructure(candles, symbol, interval, isLive)` — a pure, I/O-free scorer over an already-resolved candle series (the exact same "pure function over already-resolved data" discipline as `computePortfolioRisk()`/`analyzeTomNash()`), reusing Sprint 32's `MarketDataProvider`/`Candle`/`Timeframe` types unmodified. Detects swing highs/lows via a symmetric-window local-extrema check (`SWING_WINDOW = 2`), clusters swing touches within 0.5% of each other into support/resistance zones (`MIN_ZONE_TOUCHES = 2` — a single, un-repeated swing is never reported as a "level," proven by a dedicated test), and classifies trend structure (`uptrend`/`downtrend`/`range`) from the most recent 3 swing highs and 3 swing lows' monotonic sequencing (higher-highs+higher-lows → uptrend, lower-highs+lower-lows → downtrend, anything else → an honestly-labeled range, never a forced/fabricated trend read). `MarketStructureConfidenceLevel` deliberately matches the established 3-tier `"High" | "Moderate" | "Low"` convention used throughout this codebase (Investment Quality, Competitive Advantage, Earnings Intelligence, etc.) — no invented 4th "insufficient data" tier; a thin or empty candle sample still produces a well-shaped, honest result (empty zones, a "range" trend with an explicit reason) at `"Low"` confidence, never a crash or a fabricated read, proven directly by a zero-candle test. New `buildMarketStructureAnalysis(symbol, interval, lookback, provider)` orchestrates via any `MarketDataProvider` (today only `SimulatedMarketDataProvider`), returning `null` for an invalid ticker shape exactly like every other honest-null seam in this codebase, never fabricating an analysis for an unresolvable symbol. **Core only this sprint, per the approved plan** — no route, no UI, no database changes; Sprint 34 (Route + UI) is next. New tests: `tradingMarketStructure.test.ts` (17 tests — hand-constructed candle fixtures proving a textbook higher-highs/higher-lows sequence classifies as `uptrend` and its exact mathematical mirror-image (a monotonic transform, `new_high = 19 - old_low` / `new_low = 19 - old_high`, provably preserving each bar's `high > low` invariant while flipping every local extremum's direction) classifies as `downtrend`; a non-monotonic mixed sequence — which doubles as a 4-touch zone-clustering fixture — classifies as `range`; zone strength counting, sort order, and support/resistance-vs-current-price classification; confidence banding across candle-count thresholds; the zero-candle honest-empty-result path; determinism; `dataSource` SIMULATED/LIVE labeling; and the `buildMarketStructureAnalysis()` orchestration seam's invalid-ticker/valid-resolution/determinism proofs). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`trading_positions`/`trading_journal_entries` were not modified at all this sprint. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (74 files / 820 tests — run under normal parallel execution multiple times, hitting 3 distinct **pre-existing, previously-disclosed-category** flakes across unrelated files under this session's elevated resource contention (4 CPUs, load ~2.8): the established `fetchedAt`-timing race in `fundamentals.investingUniverse.test.ts`, a count-mismatch race in `autoScheduler.multiUser.test.ts`, and an `afterAll`-cleanup FK race in `tenantIsolation.test.ts` — all three confirmed via `git status` to be in files untouched this sprint; re-run twice with `--no-file-parallelism` for a controlled, contention-free confirmation and both runs were **fully clean, zero failures**), `pnpm --filter @workspace/ravish-trading run test` (7 files / 44 tests, unmodified — no frontend change this sprint), and `PORT=5000 BASE_PATH=/ pnpm run build` all pass for real.
- **Sprint 34 — COMPLETE.** Multi-Timeframe Trend Engine — Core (Phase 3 plan §13). **Disclosed reordering, directed explicitly by the project owner:** the plan's original roadmap table (§21) had Sprint 34 as Market Structure's own Route+UI sprint and the Multi-Timeframe Engine at Sprint 37; the project owner's Sprint 34 kickoff message explicitly named "Sprint 34 (Multi-Timeframe Trend Engine)," so this sprint builds §13 instead, and Market Structure's Route+UI work remains outstanding for a future sprint — not a silently-dropped deliverable, just resequenced at the owner's direction. No other genuinely new owner decisions surfaced beyond that resequencing — the plan's own §13 design ("runs the Market Structure scorer independently at each configured timeframe, then a thin confluence layer... reusing `classifyAgreementSignal<T>()`... deliberately not its own giant new algorithm") mapped directly onto a Core-only scope (pure scorer + unit tests, no route/UI, no DB), mirroring Sprint 33's own Core/Route+UI split precedent. New `lib/tradingMultiTimeframe.ts` — `analyzeMultiTimeframe(symbol, timeframes, isLive)`, pure and I/O-free over an array of already-computed `TimeframeStructure` (`{interval, structure: MarketStructureAnalysis}`, each produced by Sprint 33's unmodified `analyzeMarketStructure()`) — and `buildMultiTimeframeAnalysis(symbol, provider, timeframes = DEFAULT_MULTI_TIMEFRAMES)`, the orchestration wrapper resolving candles per timeframe via Sprint 32's unmodified `MarketDataProvider` seam and calling Sprint 33's scorer once per timeframe before handing off to the pure confluence layer. `DEFAULT_MULTI_TIMEFRAMES = ["15m", "1h", "1D"]` — a short/medium/long-horizon spread extending §13's own worked example ("is the 1-hour trend aligned with the daily trend"); any caller-supplied subset of `Timeframe` works, reusing `MAX_LOOKBACK` per timeframe from Sprint 32 for the candle fetch. **Confluence classification directly reuses `classifyAgreementSignal<T>()`** (Phase 2 Sprint 17's extraction, already reused once for the Investment Committee's votes) over each timeframe's own `TrendStructure` — a second, disclosed reuse of that exact generic utility, zero new agreement-scoring formula, per the plan's own explicit design intent. The one genuinely new logic in this sprint: **dominant-trend derivation** (`deriveDominantTrend()`) picks the strictly-highest-count trend and returns `null` on any genuine tie — including the all-different "split" case and an even-count tie like 2-uptrend/2-downtrend — never guessing a winner; **confluence score** (`% of considered timeframes sharing the dominant trend`) is likewise honestly `null`, not a fabricated number, whenever there's no single dominant trend or fewer than 2 timeframes were supplied. Confidence level reuses the exact `MarketStructureConfidenceLevel` type from `tradingMarketStructure.ts` (imported, not redefined) and derives `Low` whenever any individual timeframe's own confidence is `Low` or the timeframes genuinely split, `High` only when every timeframe agrees unanimously with `High` confidence each, else `Moderate` — never a 4th tier, matching the established codebase-wide convention. **Core only this sprint** — no route, no UI, no database changes, no `openapi.yaml` change; a future sprint adds Multi-Timeframe's own route/UI (and Market Structure's still-outstanding Route+UI remains available to schedule separately). New tests: `tradingMultiTimeframe.test.ts` (17 tests) — reuses Sprint 33's own proven uptrend/downtrend/range candle fixtures (including the exact mathematical mirror-transform for the downtrend case) run through the real `analyzeMarketStructure()` per timeframe to build `TimeframeStructure` fixtures, covering: unanimous agreement (100% confluence), majority agreement (correct rounded percentage), split agreement (honest null dominant trend/confluence score), a genuine even-count tie across 4 timeframes (never fabricates a winner), single-timeframe and zero-timeframe honest `insufficient-data` paths, confidence-level derivation (thin-sample-forces-Low, split-forces-Low regardless of individual confidences), determinism, `SIMULATED`/`LIVE` dataSource labeling, non-boilerplate summary content, and `buildMultiTimeframeAnalysis()` orchestration against the real `SimulatedMarketDataProvider` (honest null for an invalid ticker, well-shaped resolution using the default timeframe set, a caller-supplied timeframe subset, and determinism across repeated calls). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`trading_positions`/`trading_journal_entries` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 75 files / 835 tests, fully clean on the first (normal parallel) run; a second normal-parallel run hit the same previously-disclosed, previously-documented `tenantIsolation.test.ts` `afterAll`-cleanup FK race first observed in Sprint 33 (every individual test assertion passed; only the cleanup hook raced against a concurrent file's settings insert) — confirmed via `git status` as unrelated to any Sprint-34-touched file, and definitively confirmed clean with a serial re-run (`vitest run --no-file-parallelism`): 75 files / 835 tests, zero failures. `pnpm --filter @workspace/ravish-trading run test` — 7 files / 44 tests, unmodified — no frontend change this sprint. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 35 — COMPLETE.** Order Flow and Liquidity Engine — Core (Phase 3 plan §11). No genuinely new owner decisions surfaced — the plan's own drawn boundary (pure scorer + unit tests, no route/UI/DB) matched cleanly, mirroring Sprint 33's Core/Route+UI split precedent exactly as the roadmap table already scheduled it. New `lib/tradingLiquidity.ts` — `analyzeLiquidity(candles, symbol, interval, isLive)`, a pure, I/O-free scorer over an already-resolved candle series (the same "pure function over already-resolved data" discipline as Sprints 33/34), deliberately independent of Sprint 33/34's Market Structure/Multi-Timeframe modules — a sibling engine per §11, not built on top of Structure. **Volume profile**: buckets each candle's typical price (`(high+low+close)/3`) into `VOLUME_PROFILE_BUCKETS = 10` equal-width price buckets across the sample's own high/low range, summing real volume per bucket, sorted strongest-first and capped at `MAX_PROFILE_LEVELS_RETURNED = 8` — never a level with no real volume behind it; a degenerate zero-price-range sample (every bar at the same price) is handled without a divide-by-zero, collapsing to one honest single-price level. **Liquidity score** adapts `marketData.ts`'s `checkLiquidity()` threshold-gating shape (options open interest/volume/spread) into a single continuous 0-100 equity dollar-volume score (`LIQUIDITY_HIGH_THRESHOLD = $25M` average daily dollar volume reaches the ceiling), banded Low/Moderate/High (`<40`/`<75`/`≥75`) — zero or negative dollar volume honestly floors at score 0, never a fabricated positive read; the score is capped at exactly 100, never overshooting for extreme dollar volume. **Buy/sell-pressure proxy** is derived directly from each candle's own already-seeded up/down close (bullish candle's volume counts toward buying pressure, bearish toward selling, a doji's volume excluded from the directional total rather than guessed either way) — deliberately **not** a separately-fabricated random imbalance number, satisfying §11's "seeded, directionally consistent with the candle's own up/down close" design via an honest read of Sprint 32's already-seeded candle data rather than a second independent RNG call; direction (`buying`/`selling`/`neutral`) trips at a `PRESSURE_THRESHOLD_PCT = 55%` symmetric threshold. `LiquidityConfidenceLevel` is its own local 3-tier `"High" | "Moderate" | "Low"` type (not imported from `tradingMarketStructure.ts`, since Liquidity is a sibling engine, not built on Structure) following the same established codebase-wide convention and the same candle-count thresholds (`≥80`/`≥30`) as Sprint 33's confidence banding — a zero-candle sample still produces a fully honest empty result (empty volume profile, neutral 50/50 pressure, score 0, `Low` confidence) rather than a crash or fabrication. New `buildLiquidityAnalysis(symbol, interval, lookback, provider)` orchestrates via Sprint 32's unmodified `MarketDataProvider` seam, honestly returning `null` for an invalid ticker shape. **Explicitly deferred, per §11:** real Level 2/order-book depth and real trade-print tape reading — both need the live tick-data vendor relationship §25 Decision 7 explicitly defers. **Core only this sprint** — no route, no UI, no database changes, no `openapi.yaml` change; a future sprint adds Liquidity's own route/UI (Sprint 36, per the roadmap table, still applicable). New tests: `tradingLiquidity.test.ts` (19 tests — volume profile bucketing/sorting/percentage math, the zero-candle and zero-volume honest-empty paths, the degenerate zero-range divide-by-zero guard, liquidity score/band thresholds at all 3 bands plus the 100-cap and 0-floor, buy/selling/neutral pressure derivation including the doji-exclusion proof, confidence banding across candle-count thresholds, the zero-candle honest-empty-result path, determinism, `dataSource` SIMULATED/LIVE labeling, non-boilerplate summary content, and `buildLiquidityAnalysis()` orchestration seam's invalid-ticker/valid-resolution/determinism proofs). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`trading_positions`/`trading_journal_entries` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 76 files / 854 tests; the first normal-parallel run hit the previously-disclosed, previously-documented `fetchedAt`-timing race in `fundamentals.investingUniverse.test.ts` first noted in Sprint 16's report (unrelated to any Sprint-35-touched file, confirmed via `git status`); a second normal-parallel run was fully clean, and a serial re-run (`vitest run --no-file-parallelism`) definitively confirmed clean: 76 files / 854 tests, zero failures. `pnpm --filter @workspace/ravish-trading run test` — 7 files / 44 tests, unmodified — no frontend change this sprint. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 36 — COMPLETE.** Market Regime Detection Engine — Core (Phase 3 plan §25 Decision 3, §0 Correction 1). **Disclosed reordering, directed explicitly by the project owner:** the roadmap table had Sprint 36 as Liquidity's own Route+UI sprint and Regime Detection at Sprint 39; the project owner's Sprint 36 kickoff message explicitly named "Sprint 36 (Market Regime Detection Engine)," so this sprint builds Regime Detection instead — Liquidity's Route+UI work remains outstanding and unscheduled, not dropped. No genuinely new owner decisions surfaced beyond that resequencing — §25 Decision 3 ("a new `lib/tradingRegime.ts`, independent of `marketBriefing.ts`, per §0 Correction 1... never derived from options IV") was already approved as part of the original 8 Phase 3 decisions, and the module design followed directly from the explicit instruction to reuse Sprints 32–35 rather than build a parallel implementation. New `lib/tradingRegime.ts` — deliberately a **composition layer**, not a new candle-analysis algorithm, the same discipline as Sprint 34's Multi-Timeframe Engine and Phase 2's Tom Nash Engine: `analyzeMarketRegime(symbol, multiTimeframe, liquidity, dailyCandles, isLive)` combines a **trend axis** (reused directly, unmodified, from Sprint 34's `MultiTimeframeAnalysis.dominantTrend`/`.trendAgreement`), a **liquidity axis** (reused directly, unmodified, from Sprint 35's `LiquidityAnalysis.liquidityBand`), and a **volatility axis** (the one genuinely new formula this sprint: realized annualized volatility from daily log returns' sample standard deviation, banded Low `≤15%` / Normal / High `≥40%` against plausible equity historical-volatility norms) into a single 5-value `TradingRegimeLabel` (`trending-bullish`/`trending-bearish`/`range-bound`/`volatile-choppy`/`quiet-consolidation`) via a small, fully deterministic rule table: a real dominant trend (uptrend/downtrend) always wins regardless of volatility; only when there's no clear direction (a "range" structure or a genuinely split confluence) does volatility become the primary read. `TradingRegimeAnalysis` also carries the full `multiTimeframe`/`liquidity` sub-analyses through unchanged, so a caller never has to re-fetch them. Volatility honestly defaults to a neutral "normal" band with an explicit reason (never a fabricated "high" or "low") whenever there are fewer than 2 usable daily returns to compute a standard deviation from. Confidence level reuses the exact `MarketStructureConfidenceLevel` type (imported from `tradingMarketStructure.ts`, the same reuse precedent Sprint 34 set) and is honestly `Low` whenever any one of the 3 underlying signals (trend confluence, liquidity, or volatility computability) is itself thin, `High` only when trend and liquidity both read `High` and volatility was computable, else `Moderate` — never a 4th tier. New `buildMarketRegimeAnalysis(symbol, provider, timeframes)` orchestrates using **only already-existing abstractions**: Sprint 34's `buildMultiTimeframeAnalysis()`, Sprint 35's `buildLiquidityAnalysis()`, and a direct daily-candle fetch through Sprint 32's own `MarketDataProvider.getCandles()` — zero new candle generation, exactly matching the explicit "prefer extending existing abstractions rather than introducing parallel implementations" instruction; honestly returns `null` for an invalid ticker shape. **§0 Correction 1 / §25 Decision 3 compliance, explicitly verified:** `tradingRegime.ts` never imports `marketBriefing.ts` or `optionsMath.ts` — confirmed via the module's own import list and a dedicated doc comment explaining the deliberate separation. **One small, additive, behavior-free cleanup also shipped this sprint, per the Phase 3 plan's own Technical Debt Review recommendation (§26):** a one-line cross-reference doc comment was added to the top of `marketBriefing.ts` (the only change to that file — confirmed via `git diff` showing a comment-only addition) pointing to `tradingRegime.ts` and explaining why the two `MarketRegime`-shaped reads are deliberately never merged, addressing the exact confusion risk §26 flagged ahead of time ("once Engine 2's own Regime Detection ships... there will be two regime-shaped things in the codebase... easy to confuse"). **Core only this sprint** — no route, no UI, no database changes, no `openapi.yaml` change; Liquidity's own Route+UI work (Sprint 36, per the original roadmap table) remains available to schedule at a future sprint. New tests: `tradingRegime.test.ts` (24 tests) — realized-volatility banding at all 3 tiers via hand-constructed daily candle series with known log-return patterns (low/high/normal), the honest too-thin-sample default-to-normal path, all 7 regime-label derivation combinations (trend-wins-regardless-of-volatility for uptrend/downtrend, volatility-decides for range/null-dominant-trend across high/normal/low), confidence-level derivation across all 4 "any signal is Low" cases plus the High/Moderate paths, determinism, `dataSource` labeling, the sub-analysis pass-through proof, non-boilerplate summary content, and `buildMarketRegimeAnalysis()` orchestration against the real `SimulatedMarketDataProvider` (honest null for an invalid ticker, well-shaped resolution, determinism, and a caller-supplied timeframe subset propagating correctly into the underlying Multi-Timeframe read). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`trading_positions`/`trading_journal_entries` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 77 files / 878 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint — the only warning-level log output observed was an intentional mocked-network-failure test case in `value.test.ts`, not a real failure). `pnpm --filter @workspace/ravish-trading run test` — 7 files / 44 tests, unmodified — no frontend change this sprint. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 37 — COMPLETE.** Probability Engine — Core (Phase 3 plan §14). **Disclosed reordering, directed explicitly by the project owner:** the roadmap table had Sprint 37 as Multi-Timeframe's own Route+UI sprint and the Probability Engine at Sprint 38; the project owner's Sprint 37 kickoff message explicitly named "Sprint 37 (Probability Engine)," so this sprint builds §14 instead — Multi-Timeframe's, Market Structure's, and Liquidity's own still-outstanding Route+UI work all remain unscheduled, not dropped. No genuinely new owner decisions surfaced — §14's own recommendation (a historical-volatility-based probability cone, reusing only the *shape* of `optionsMath.ts`'s POP-math rigor, not its options-specific code) mapped directly onto this sprint's explicit instruction to compose Sprints 32–36 rather than duplicate logic. New `lib/tradingProbability.ts` — deliberately the **thinnest composition layer yet**: `buildProbabilityAnalysis()`/`buildLevelProbability()` each make exactly **one** call, to Sprint 36's `buildMarketRegimeAnalysis()`, and read its already-computed `volatilityAnnualizedPct` and (via its nested `liquidity.currentPrice`) current price — zero new candle fetches, zero new volatility formula, transitively reusing Sprint 34's Multi-Timeframe Engine, Sprint 35's Liquidity Engine, Sprint 33's Market Structure Engine, and Sprint 32's `MarketDataProvider` in that one call, exactly as the kickoff instruction's reuse list specified. The one genuinely new logic this sprint is the **lognormal-diffusion probability math itself** — a driftless geometric Brownian motion assumption (this module never forecasts direction, only dispersion, consistent with the codebase's "never fabricate a directional forecast" discipline): `computeLevelProbability(currentPrice, targetPrice, daysAhead, volatilityAnnualizedPct)` (pure) returns both `probabilityAtHorizon` (P(price is above/below the target exactly at the horizon date), via the standard normal CDF of `ln(target/current)/σ√T`) and `probabilityOfTouch` (P(price reaches the target at any point before the horizon), via the exact reflection-principle formula for driftless Brownian motion, `min(1, 2 × probabilityAtHorizon)` in both directions — proven algebraically identical for both the "above" and "below" cases) — honestly `null` (never a fabricated probability) whenever current price, target price, or day horizon is non-positive, or volatility is `null`/non-positive. `analyzeProbability()` (pure) additionally computes a **±1σ/±2σ probability cone** across `DEFAULT_CONE_HORIZONS_DAYS = [5, 10, 20, 30, 60]` using the same driftless lognormal assumption, and honestly reports `available: false` with an explicit `unavailableReason` (current price unresolved vs. volatility uncomputable, distinguished) and an empty cone — never a fabricated range — whenever the reused regime's own price or volatility isn't usable; `confidenceLevel` directly reuses the underlying regime's own `confidenceLevel` (imported type, same Sprint 34/36 reuse precedent) when available, forced to `Low` only when the cone itself couldn't be computed. The standard normal CDF is computed via the well-documented Abramowitz & Stegun 7.1.26 `erf` approximation (max error ~1.5e-7) — a deterministic numerical technique, never ML/LLM-generated. **Core only this sprint** — no route, no UI, no database changes, no `openapi.yaml` change; **zero existing files were modified** (confirmed via `git status --porcelain` showing only the 2 new lib files) — the first Engine 2 sprint since Sprint 32 needing no changes to any prior file at all, a direct consequence of composing entirely on top of Sprint 36's already-complete regime resolution. New tests: `tradingProbability.test.ts` (30 tests) — honest-null paths for every non-positive/missing input to `computeLevelProbability()`, direction classification, the exact-50%/certain-touch degenerate case when target equals current price, the touch-probability-capped-at-1 proof, the algebraic `probabilityOfTouch ≈ min(1, 2 × probabilityAtHorizon)` proof in both directions (via `toBeCloseTo` to avoid a legitimate double-rounding artifact between the test's own re-derivation and the source's single-rounding-from-the-unrounded-intermediate — caught during validation, a test-construction fix, not a production bug), monotonicity sanity checks (farther target → lower probability; higher volatility → higher probability; longer horizon → higher probability), the full [0,1] probability-range proof across extreme inputs, cone-widening-with-horizon and 2σ-wider-than-1σ proofs, both honest-unavailable paths (price vs. volatility, independently triggered and distinguished by message), confidence-level pass-through/never-inflated proof, determinism, `dataSource` pass-through, the full regime-object pass-through-unchanged proof, non-boilerplate summary content, and `buildProbabilityAnalysis()`/`buildLevelProbability()` orchestration against the real `SimulatedMarketDataProvider` (honest null for an invalid ticker on both entry points, well-shaped resolution for both, and determinism). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`trading_positions`/`trading_journal_entries` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 78 files / 905 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint). `pnpm --filter @workspace/ravish-trading run test` — 7 files / 44 tests, unmodified — no frontend change this sprint. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 38 — COMPLETE.** Risk Management Engine — Core (Phase 3 plan §15, §0 Correction 3). **`AskUserQuestion` failed again at kickoff** with the same "Tool permission stream closed before response received" infrastructure error observed repeatedly earlier this session (Sprints 23, 26, 28×2, 29) — the 6th occurrence overall. Genuinely novel this time: the project owner's kickoff message did not name a specific module (unlike every prior Sprint-34/36/37 kickoff, which each named its target engine explicitly), and the roadmap table itself had gone fully "unscheduled" after three consecutive reorderings left a 4-deep Route+UI backlog (Structure/Liquidity/Multi-Timeframe/Regime) with no clear next module. Per the established, repeatedly-approved recovery, the choice was stated as a recommendation directly in chat — build the next unbuilt **Core** module (Risk Management, Phase 3 plan §15) rather than tackle the Route+UI backlog, since a multi-engine UI sprint is harder to bound and risks the explicit "do not expand scope into Sprint 39" instruction — and implementation proceeded once the project owner replied "Continue from where you left off." New `lib/tradingRisk.ts` — `computeTradingRisk(positions, accountValue)`, pure and I/O-free, built fresh on **Phase 2 Sprint 29's `investingRisk.ts` template** per the already-approved §0 Correction 3 (`risk.ts`/`execution.ts` are far too options-legs-coupled to safely generalize in place, confirmed by direct inspection during Phase 3 planning) — reusing `investingRisk.ts`'s own `ScoreCard`/`band()`/`gradeLabel()`/hard-cap-override shape (a proven, already-tested pattern), reimplemented instrument-agnostically for single-symbol stop/target positions rather than portfolio target-weight holdings. Three components, each scoring independently based on what data is actually available (never fabricating a value the underlying data can't support): **Position Sizing** (largest open position's dollar risk — `|entryPrice − stopPrice| × quantity`, side-agnostic — as a % of account value, banded against a named `MAX_RISK_PER_POSITION_PCT = 2%` cap); **Stop/Target Discipline** (what fraction of open positions have *both* a stop and a target defined, with the missing-stop and missing-target symbol lists reported separately); **Portfolio Risk Budget** (aggregate dollar risk across every stop-defined open position, banded against a named `MAX_PORTFOLIO_RISK_PCT = 6%` cap, with a full per-position dollar/percent/within-limit breakdown mirroring `risk.ts`'s own `computePortfolioRisk()` shape — read for its shape only, never imported, never modified). Both cap constants are the standard, widely-used "risk 1-2% per trade, cap aggregate open risk around 5-6%" discretionary/systematic trading convention, the same "state a reasonable named default, proceed" precedent as `investingRisk.ts`'s own 25%/40% concentration caps. A closed position carries no live risk and is excluded from every component. Overall score = a weighted blend (0.35/0.30/0.35) over whichever components scored, with the same hard-cap-override pattern as `investingRisk.ts`: breaching either dollar-risk cap caps the overall score at 60 regardless of the blend. New `buildTradingRiskAnalysis(positions, accountValue, provider, daysAhead)` is the sprint's actual composition point, satisfying the kickoff's full reuse list in one orchestration: for every open position with a defined stop and/or target, it resolves Sprint 36's `buildMarketRegimeAnalysis()` **once per distinct symbol** (a `Map`-based dedup cache — two positions in the same symbol trigger no more provider calls than one, proven by a dedicated counting-provider test) and calls Sprint 37's pure `computeLevelProbability()` directly against the regime's own already-resolved current price/volatility to attach an honest `stopTouchProbability`/`targetTouchProbability`/`regimeLabel` context per position — transitively reusing Sprint 34's Multi-Timeframe Engine, Sprint 35's Liquidity Engine, Sprint 33's Market Structure Engine, and Sprint 32's `MarketDataProvider`, with zero duplicate candle fetches or probability math. An unresolvable symbol's context is honestly all-`null`, never a fabricated probability. **Core only this sprint** — no route, no UI, no database migration (`trading_positions` already exists since Sprint 32; a future caller/route is responsible for fetching rows and mapping them to `TradingPositionInput`). New tests: `tradingRisk.test.ts` (27 tests) — honest-insufficient-data paths for each of the 3 components independently (no account value, no stop-defined positions, zero open positions), the side-agnostic dollar-risk-magnitude proof (a stop above or below entry produces the same risk figure), cap-breach detection and the hard-cap-override for both the position-sizing and portfolio-budget caps (constructed so each trips independently), the never-fabricates-riskDollars-for-an-unstopped-position proof, closed-position exclusion from every component, determinism, and the orchestration seam's context-attachment/exclusion/honest-null/dedup-call-count/determinism proofs against the real `SimulatedMarketDataProvider`. **One test-construction bug caught and fixed during validation, not a production bug:** the original portfolio-budget-cap-breach fixture accidentally also breached the per-position cap on one leg (a fixture math error), which the test's own `capBreached: false` assertion caught before commit — fixed by reconstructing the fixture with 4 positions each individually under the 2% per-position cap but summing to 7.6%, above the 6% portfolio cap. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`trading_positions`/`trading_journal_entries` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 79 files / 928 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint). `pnpm --filter @workspace/ravish-trading run test` — 7 files / 44 tests, unmodified — no frontend change this sprint. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 39 — COMPLETE.** Trading Journal — Core + basic CRUD (Phase 3 plan §16). **`AskUserQuestion` succeeded this time** — the 7th attempt this session and the first to actually return, after 6 consecutive infrastructure failures across Sprints 23/26/28×2/29/38. Presented with the same "roadmap unscheduled, no module named" ambiguity as Sprint 38, the project owner chose **"Trading Journal (Core + basic CRUD)"** over a combined Route+UI backlog sprint — continuing the established pattern of shipping the next unbuilt module rather than working through the 5-deep outstanding Route+UI backlog (Structure/Liquidity/Multi-Timeframe/Regime/Risk Management), which remains unscheduled, not dropped. **Genuinely novel this sprint: it's the first Engine 2 route** (every Sprint 32-38 module was Core-only, zero routes) — `trading_journal_entries` itself needed no schema change (the table already existed since Sprint 32's migration, with exactly the fields §16 specified: `title`/`content`/`mood`/`tags`/`lessonLearned` core fields, `tradingPositionId` as a loose, unenforced reference mirroring `journal_entries.trade_id`'s own precedent, and `setupType`/`entryPrice`/`exitPrice`/`rMultiple` in place of the options-specific `entryCredit`/`maxProfit`/`pop`/`ev`/`ravishScore` fields). New `routes/tradingJournal.ts` mirrors `routes/journal.ts`'s own list/create/get/update pattern directly, adapted (not rewritten) — every query scoped via `getScopedUserId(req)` and the established `and(eq(id), eq(userId))` ownership pattern, 404 (never a separate 403) for both "doesn't exist" and "isn't yours," matching every route in this codebase since Sprint 7. **One small, disclosed scope addition beyond `journal.ts`'s own shape:** a `DELETE /trading/journal/:id` endpoint — the options-side `journal.ts` has no delete route at all, but "basic CRUD" was the explicit approved scope for this sprint, so delete was added as the natural 4th verb, also ownership-scoped and 404-honest. `openapi.yaml` gained a new `trading-journal` tag, 5 new paths (`GET`/`POST /trading/journal`, `GET`/`PATCH`/`DELETE /trading/journal/{id}`), and 3 new `TradingJournalEntry`/`TradingJournalEntryInput`/`TradingJournalEntryUpdate` schemas — deliberately `Trading`-prefixed to avoid any collision with the existing options-side `JournalEntry`/`JournalEntryInput`/`JournalEntryUpdate` schemas (verified collision-free before codegen, no repeat of Sprint 28's schema-naming incident); `api-zod`/`api-client-react` regenerated cleanly. **No frontend UI this sprint** — the project owner's own chosen option explicitly scoped this to backend CRUD only ("No Route+UI backlog touched"), consistent with every other Engine 2 module's established Core-first, UI-later split; a future sprint can add the `Journal.tsx`-derived UI §16 originally envisioned. **One real test-authoring bug caught and fixed during validation, not a production bug:** the route test's create-entry fixture initially sent `rMultiple: null` explicitly in the request body, but `CreateTradingJournalEntryBody`'s generated Zod schema uses `.optional()` (undefined-only) for nullable-in-the-DB-but-input-optional fields, not `.nullish()` — passing `null` correctly 400'd, and the fix was to omit the field entirely (matching how a real client would send it), not to loosen the schema. New tests: `routes/tradingJournal.route.test.ts` (7 live end-to-end HTTP tests — full create/list/get/update/delete flow, the loose-`tradingPositionId`-reference proof against a nonexistent position id, 400 for a missing required field, 400 for an invalid `mood` enum value, 404 for GET/PATCH/DELETE on a nonexistent id, 400 for a non-numeric id) and 1 new case in `tenantIsolation.test.ts`'s existing IDOR block (`trading_journal_entries`, matching the exact `and(id, userId)`-never-crosses-accounts proof already established for `trades`/`journal_entries`/`value_watchlist`) — DB-level tenant isolation for this table was already covered since Sprint 32's own `assertTenantIsolation` case. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`journal.ts`/`journal_entries` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 80 files / 935 tests; several runs under normal parallel execution hit the previously-disclosed, previously-documented `autoScheduler.multiUser.test.ts` FK-violation race (unrelated to any Sprint-39-touched file, confirmed via `git status`) — a serial re-run (`vitest run --no-file-parallelism`) definitively confirmed clean: 80 files / 935 tests, zero failures. `pnpm --filter @workspace/ravish-trading run test` — 7 files / 44 tests, unmodified — no frontend change this sprint. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 40 — COMPLETE.** Market Structure Route + UI — first bounded slice of the Route+UI backlog reduction (Phase 3 plan, original Sprint 34 scope: "`GET /trading/structure/:symbol`, new Trading Research page skeleton, structure card"). The project owner's own kickoff message set the boundary explicitly ("begin reducing the backlog... if it cannot reasonably fit into one sprint, implement only the first bounded portion") — no `AskUserQuestion` needed this sprint since the scope was self-evident: Market Structure is the earliest-built, most foundational Core engine (Sprint 33), and the plan's own original Sprint 34 text already specifies exactly this bounded unit (one route, one page shell, one card). **This is the first Engine 2 UI work of any kind** — every prior Route+UI-adjacent sprint (39) was backend-only. New `routes/tradingStructure.ts` — `GET /trading/structure/:symbol` — a deliberately thin pass-through with zero business logic of its own: resolves `?interval=`/`?lookback=` query overrides (defaulting to `1D`/90 candles), resolves the provider via `getMarketDataProvider(userId)`, and calls straight through to Sprint 33's already-tested `buildMarketStructureAnalysis()` — 404 for an unresolvable symbol (never a fabricated analysis), 400 for an invalid `interval`/non-positive `lookback`. No ownership/tenant scoping needed on this route (unlike `tradingJournal.ts`'s user-authored rows) — this is read-only market analysis, not a per-user resource. **One real, previously-undiscovered Orval codegen limitation caught and worked around, not a spec error:** documenting both a path parameter (`symbol`) and a query parameter (`interval`) together on the same operation — the first time this exact combination appears anywhere in `openapi.yaml` — causes Orval's zod+split-types generator to emit a genuine duplicate `GetTradingStructureParams` export (one from its inline per-operation Zod object in `api.ts`, one from its extracted TypeScript type file), a `TS2308` ambiguity error confirmed via direct, isolated testing (removing the query param made the error disappear; re-adding it — with or without an enum — reproduced it every time) to be a toolchain quirk, not a mistake in the spec. Resolved by documenting the path parameter only in `openapi.yaml` (the route's actual `?interval=`/`?lookback=` query-override behavior is real and still fully functional, just handled server-side outside the formal typed contract) — the same precedent Sprint 30 already set for `/value-research/stream` being kept outside strict OpenAPI modeling when the generator can't cleanly represent a route's full surface. `openapi.yaml` gained a new `trading-structure` tag, the `/trading/structure/{symbol}` path, and 4 new `Trading`-prefixed schemas (`TradingStructureAnalysis`, `TradingSwingPoint`, `TradingSupportResistanceZone`, `TradingTimeframe` — the last a small reusable enum extraction, since `MarketStructureAnalysis.interval` and future Multi-Timeframe/Liquidity/Regime routes all share the same 5-value `Timeframe` type); `api-zod`/`api-client-react` regenerated cleanly after the fix. New page `TradingResearch.tsx` at `/trading-research` (new top-level nav item, `Activity` icon, positioned between "Adjustments" and "Value Research") — a genuinely minimal skeleton per the explicit "avoid UI scope creep" instruction: one symbol input, one card. The Market Structure card shows the current price, trend badge (with directional icon), confidence badge, trend detail, a support/resistance zone list (or an honest "no zone detected" message — never a fabricated empty-looking list vs. a real empty result), and the deterministic summary sentence — future sprints add further panels (Multi-Timeframe, Liquidity, Regime, Risk, Probability) onto this same shell without touching this card's own logic, per the page's own header comment. Required the established `queryKey` + `enabled` fix for the generated `useGetTradingStructure` hook (Sprint 19's precedent, now needed for the first time in Engine 2). No database migration. New tests: `routes/tradingStructure.route.test.ts` (6 live end-to-end HTTP tests — default-1D/90-candle resolution, `?interval=`/`?lookback=` override behavior, 404 for an invalid ticker shape, 400 for an invalid interval, 400 for a non-positive lookback, determinism across repeated calls) and `TradingResearch.test.tsx` (4 frontend smoke tests, following the established mocked-generated-hook pattern — pre-search advisory copy, the full search-to-card-render flow, the honest empty-zones message, and the not-found message for an unresolvable symbol). **Explicitly deferred, per the "first bounded portion" instruction — six items remain in the Route+UI backlog for Sprint 41+:** Multi-Timeframe's own panel, Liquidity's own tab, Regime Detection's own panel, Risk Management's own panel, Probability's own panel/cone visualization, and Trading Journal's own frontend UI (`Journal.tsx`-derived, per §16) — none dropped, all tracked in the roadmap table. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts` were not modified at all this sprint. `pnpm run typecheck` clean (both the caught-and-fixed Orval collision and the frontend `queryKey` requirement were resolved before this passed). `pnpm --filter @workspace/api-server run test` — 81 files / 941 tests; runs under normal parallel execution intermittently hit the previously-disclosed, previously-documented `autoScheduler.multiUser.test.ts` FK-violation race (unrelated to any Sprint-40-touched file, confirmed via `git status`) — a serial re-run (`vitest run --no-file-parallelism`) definitively confirmed clean: 81 files / 941 tests, zero failures. `pnpm --filter @workspace/ravish-trading run test` — 8 files / 49 tests (4 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 41 — COMPLETE.** Multi-Timeframe Route + UI — second bounded slice of the Route+UI backlog reduction (Phase 3 plan, original Sprint 37 scope: "`GET /trading/multi-timeframe/:symbol`, confluence panel on the Trading Research page"). No `AskUserQuestion` needed — the project owner's own kickoff instruction ("expose the next approved engine(s)... implement only the next bounded portion if it cannot fit") plus the plan's own already-defined scope for this exact module resolved the boundary unambiguously; Multi-Timeframe was chosen as the next slice since it composes directly on top of Structure (Sprint 40's own already-shipped card) and matches Sprint 40's own bounded size (one route, one card). New `routes/tradingMultiTimeframe.ts` — `GET /trading/multi-timeframe/:symbol` — mirrors `tradingStructure.ts`'s own Sprint 40 pattern exactly: a thin pass-through with zero business logic, calling straight through to Sprint 34's already-tested `buildMultiTimeframeAnalysis()` (always using `DEFAULT_MULTI_TIMEFRAMES`, no query-param overrides this sprint — deliberately simpler than Sprint 40's interval/lookback overrides), 404 for an unresolvable symbol, no ownership/tenant scoping needed. **Zero duplicated business logic, fully reused response shape:** the new `TradingMultiTimeframeAnalysis` schema's `timeframes[].structure` field directly `$ref`s the existing `TradingStructureAnalysis` schema from Sprint 40 — the exact same shape Sprint 33's Market Structure Engine already produces per timeframe, reused rather than redefined. `openapi.yaml` gained a new `trading-multi-timeframe` tag, the `/trading/multi-timeframe/{symbol}` path (path parameter only, no query params — the Orval path+query collision Sprint 40 disclosed doesn't apply here since none were needed, but the documented-surface discipline was kept anyway for consistency), and 2 new schemas (`TradingTimeframeStructure`, `TradingMultiTimeframeAnalysis`); `api-zod`/`api-client-react` regenerated cleanly with no collisions this time. `TradingResearch.tsx` gained a second card — "Multi-Timeframe Confluence" (`Layers` icon) — positioned directly below the Market Structure card on the same page shell, exactly as Sprint 40's own header comment anticipated ("future sprints add further panels... without touching this card's own logic," confirmed true: the Market Structure card's own code is byte-for-byte unchanged this sprint). Shows the dominant-trend badge (or an honest "No dominant trend" badge when the timeframes split — never a fabricated winner), trend-agreement badge, confluence-score badge (hidden entirely, not shown as "null%" or "0%", when `confluenceScore` is `null`), confidence badge, a per-timeframe trend breakdown list, and the deterministic summary sentence. Required the same established `queryKey` + `enabled` fix for the new generated hook. No database migration. **One real test-authoring bug caught and fixed during validation, not a production bug:** the frontend split-agreement test case overrode `trendAgreement`/`dominantTrend`/`confluenceScore` but not `summary`, so the mocked fixture's default summary text (still literally containing "67% confluence") leaked into the "never fabricates a winner" assertion and failed it — fixed by also overriding `summary` in that test's fixture to match the split scenario, not by loosening the assertion. New tests: `routes/tradingMultiTimeframe.route.test.ts` (4 live end-to-end HTTP tests — default-timeframe-set resolution shape, the honest null-dominant-trend/null-confluence-score proof on non-agreement, 404 for an invalid ticker shape, determinism) and 2 new `TradingResearch.test.tsx` cases (the full render of both cards together once multi-timeframe data resolves, and the honest "No dominant trend"/no-confluence-badge display on a split scenario). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 82 files / 945 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint). `pnpm --filter @workspace/ravish-trading run test` — 8 files / 51 tests (2 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 42 — COMPLETE.** Market Regime Route + UI — third bounded slice of the Route+UI backlog reduction (Phase 3 plan §148's own eager/on-demand split: "Market Structure + Regime Detection + Probability + Risk are cheap enough to compute eagerly"). **`AskUserQuestion` failed again at kickoff** with the same "Tool permission stream closed before response received" infrastructure error (7th occurrence this session — Sprints 23, 26, 28×2, 29, 38, now this one; Sprint 39's own success remains the sole exception). Per the established, repeatedly-approved recovery, the choice was stated as a recommendation directly in chat — Market Regime over Liquidity — and implementation proceeded once the project owner replied "Continue from where you left off." **Reasoning for Regime over Liquidity, disclosed:** the plan's own §148 text designs Liquidity's UI as an **on-demand tab** (a different UI pattern than the eager cards Sprints 40–41 established, matching Statements/Peers' cost-control precedent), a bigger design decision better suited to its own dedicated sprint; Regime fits the existing eager-card pattern seamlessly and composes directly on Multi-Timeframe (Sprint 41, already shipped) and the Liquidity Engine itself (Sprint 35, its own dedicated panel not required for Regime's route to work). New `routes/tradingRegime.ts` — `GET /trading/regime/:symbol` — mirrors `tradingStructure.ts`'s and `tradingMultiTimeframe.ts`'s own Sprint 40/41 pattern exactly: a thin pass-through with zero business logic, calling straight through to Sprint 36's already-tested `buildMarketRegimeAnalysis()` (which itself transitively resolves Multi-Timeframe, Liquidity, Market Structure, and the `MarketDataProvider`), 404 for an unresolvable symbol, no ownership/tenant scoping needed, eager (no query overrides). **Disclosed scope choice:** the response is a deliberate projection of `TradingRegimeAnalysis`'s own fields — the full nested `multiTimeframe`/`liquidity` sub-analyses are omitted from this endpoint's documented contract (Zod's default non-strict `.parse()` naturally strips them), since Structure/Multi-Timeframe's own already-shipped cards on the same page already surface that detail, and Liquidity's own dedicated schema doesn't exist yet (its Route+UI remains backlogged) — this keeps Sprint 42 bounded to exactly "Regime route + panel" rather than prematurely modeling a schema for a module not in this sprint's scope. `openapi.yaml` gained a new `trading-regime` tag, the `/trading/regime/{symbol}` path (path parameter only, no collision risk since no query params are needed), and 1 new schema (`TradingRegimeAnalysis`, reusing the established `Trading`-prefix and enum-vocabulary conventions — `uptrend`/`downtrend`/`range` for `trendRegime`, `High`/`Moderate`/`Low` for `confidenceLevel`/`liquidityRegime`, matching Sprint 40/41's own schemas exactly); `api-zod`/`api-client-react` regenerated cleanly with no collisions. `TradingResearch.tsx` gained a third card — "Market Regime" (`Gauge` icon) — below the Multi-Timeframe card on the same page shell, exactly as Sprint 41's own header comment anticipated; the Structure and Multi-Timeframe cards' own code is byte-for-byte unchanged this sprint. Shows the regime-label badge (color-coded: bullish=green, bearish=red, choppy=amber, else neutral), volatility badge (with the annualized percentage shown only when computable — honestly omitted, never a fabricated number, when `volatilityAnnualizedPct` is `null`), liquidity-regime badge, confidence badge, and the deterministic summary sentence. Required the same established `queryKey` + `enabled` fix for the new generated hook. No database migration. **Explicitly deferred, per the "next bounded portion" instruction — four items remain in the Route+UI backlog for Sprint 43+:** Liquidity's own on-demand tab, Risk Management's own panel, Probability's own panel, and Trading Journal's own frontend UI — none dropped. New tests: `routes/tradingRegime.route.test.ts` (4 live end-to-end HTTP tests — well-shaped resolution, the disclosed-projection proof that `multiTimeframe`/`liquidity` are never leaked into the response, 404 for an invalid ticker shape, determinism) and 2 new `TradingResearch.test.tsx` cases (the full render of the regime card with a computable volatility percentage, and the honest omission of that percentage — built via a single JS template-string expression rather than adjacent JSX text/expression children, to avoid JSX whitespace-collapsing ambiguity around the conditional — when volatility couldn't be computed). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 83 files / 949 tests; one run under normal parallel execution hit a single unreproduced transient failure (full details not captured before the failing test file scrolled out of view; two subsequent normal-parallel runs were fully clean, and a definitive serial re-run — `vitest run --no-file-parallelism` — confirmed clean: 83 files / 949 tests, zero failures), consistent with this suite's established live-Postgres-parallelism flake category from prior sprints and confirmed unrelated to any Sprint-42-touched file. `pnpm --filter @workspace/ravish-trading run test` — 8 files / 53 tests (2 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 43 — COMPLETE.** Probability Route + UI — fourth bounded slice of the Route+UI backlog reduction (Phase 3 plan, original Sprint 37 backlog note: Probability's own panel on the Trading Research page). **`AskUserQuestion` failed again at kickoff** with the same "Tool permission stream closed before response received" infrastructure error (8th occurrence this session; Sprint 39's own success remains the sole exception). Per the established, repeatedly-approved recovery, the choice was stated as a recommendation directly in chat — Probability over Risk Management/Liquidity/Trading Journal UI — and implementation proceeded once the project owner replied "Continue from where you left off." **Reasoning for Probability, disclosed:** the plan's own §21 explicitly names Probability in the eager-card group ("Market Structure + Regime Detection + Probability + Risk are cheap enough to compute eagerly"), and it composes directly on Sprint 36's Market Regime Engine whose own Route + UI shipped last sprint (Sprint 42) — the most tightly-bounded next slice, following the exact Sprint 40–42 pattern. New `routes/tradingProbability.ts` — `GET /trading/probability/:symbol` — mirrors `tradingStructure.ts`'s, `tradingMultiTimeframe.ts`'s, and `tradingRegime.ts`'s own Sprint 40/41/42 pattern exactly: a thin pass-through with zero business logic, calling straight through to Sprint 37's already-tested `buildProbabilityAnalysis()` (which itself transitively resolves Sprint 36's Market Regime Engine, and through it Multi-Timeframe, Liquidity, Market Structure, and the `MarketDataProvider`), 404 for an unresolvable symbol, no ownership/tenant scoping needed, eager (no query overrides — always the default ±1σ/±2σ cone horizons and timeframe set). **Disclosed scope choice, same pattern as Sprint 42:** the response is a deliberate projection of `ProbabilityAnalysis`'s own fields — the full nested `regime: TradingRegimeAnalysis` sub-analysis is omitted from this endpoint's documented contract (Zod's default non-strict `.parse()` naturally strips it), since the Market Regime card (Sprint 42) already surfaces that detail on the same page; only the probability-specific fields (current price, volatility, availability, the cone, confidence, summary) are exposed here. `openapi.yaml` gained a new `trading-probability` tag, the `/trading/probability/{symbol}` path (path parameter only, avoiding any Orval path+query collision risk per Sprint 40's disclosed precedent), and 2 new `Trading`-prefixed schemas (`TradingProbabilityConeLevel`, `TradingProbabilityAnalysis`); `api-zod`/`api-client-react` regenerated cleanly with no collisions. `TradingResearch.tsx` gained a fourth card — "Probability" (`Target` icon) — below the Market Regime card on the same page shell, exactly as Sprint 42's own header comment anticipated; the Structure, Multi-Timeframe, and Market Regime cards' own code is byte-for-byte unchanged this sprint. Shows an honest unavailable-reason message (never a fabricated cone) when `available` is `false`; otherwise shows the annualized-volatility badge, confidence badge, a per-horizon ±1σ/±2σ price-range list, and the deterministic summary. Required the same established `queryKey` + `enabled` fix for the new generated hook. No database migration. **Explicitly deferred, per the "next bounded portion" instruction — three items remain in the Route+UI backlog for Sprint 44+:** Liquidity's own on-demand tab, Risk Management's own panel, and Trading Journal's own frontend UI — none dropped. New tests: `routes/tradingProbability.route.test.ts` (4 live end-to-end HTTP tests — well-shaped resolution with cone-ordering sanity checks, the disclosed-projection proof that `regime` is never leaked into the response, 404 for an invalid ticker shape, determinism) and 2 new `TradingResearch.test.tsx` cases (the full render of the probability cone once data resolves, and the honest unavailable-reason message with no fabricated cone when probability can't be computed). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 84 files / 953 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint). `pnpm --filter @workspace/ravish-trading run test` — 8 files / 55 tests (2 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 44 — COMPLETE.** Risk Management Route + UI — fifth bounded slice of the Route+UI backlog reduction (Phase 3 plan §15). `AskUserQuestion` succeeded this time (2nd success this session, after Sprint 39). Presented with the 3 remaining backlog items (Liquidity's on-demand tab, Risk Management's panel, Trading Journal's frontend UI), the project owner chose **Risk Management panel**, reasoning: it composes on already-shipped Regime/Probability (Sprints 42/43) and fits the eager-card pattern named in plan §21 alongside Structure/Regime/Probability. **Two further genuine design questions surfaced during planning, both resolved via `AskUserQuestion` before implementation:** (1) `computeTradingRisk()` needs an `accountValue` to size position risk against, and Engine 2 had no such concept — Engine 3's `getAccountValue()` (`serverState.ts`) is derived from options trades' realized P&L, and reusing it would have violated the established engine-independence discipline (Sprint 26 `investingMacro.ts`, Sprint 32 `TRADING_MARKET_UNIVERSE`, Sprint 38's own header comment on `tradingRisk.ts` being "a wholly new, additive module") — the project owner chose a **new nullable `tradingAccountValue` settings column**, independent of Engine 3's; (2) `trading_positions` had no CRUD route at all (only Sprint 39's Trading Journal CRUD shipped, which loosely references position IDs but never creates them) — without one, the Risk panel would always read an empty table and show "no open positions," never useful — the project owner chose to **add basic Trading Positions CRUD this sprint too**, mirroring Sprint 39's `tradingJournal.ts` pattern exactly. New `lib/db/manual-migrations/012_trading_account_value.sql` — one nullable `trading_account_value REAL` column on `settings`, no default, no backfill (honestly `null` until the user sets it — `computeTradingRisk()` correctly reports "insufficient data" for position-sizing/portfolio-budget scoring rather than fabricating a default account size). New `routes/tradingPositions.ts` — full CRUD (`GET`/`POST /trading/positions`, `GET`/`PATCH`/`DELETE /trading/positions/:id`) mirroring `routes/tradingJournal.ts`'s Sprint 39 list/create/get/update/delete pattern directly; every query ownership-scoped via `getScopedUserId(req)` and `and(eq(id), eq(userId))`, 404 for both "doesn't exist" and "isn't yours." **Disclosed simplification:** `entryDate`/`exitDate` are DB-managed (entry defaults on creation, exit isn't settable via this sprint's API surface) — not part of `TradingPositionInput`/`Update`, since risk scoring only needs `status`/stop/target/entry, not exit timing. New `routes/tradingRisk.ts` — `GET /trading/risk` — unlike Sprints 40-43's per-symbol routes, this one is **portfolio-wide** (reads the calling user's own `trading_positions` and `tradingAccountValue`), so it's ownership-scoped via `getScopedUserId(req)` like `tradingPositions.ts`/`tradingJournal.ts`, not the read-only-market-data pattern of the earlier 4 routes; still a thin pass-through with zero risk-scoring logic, calling straight through to Sprint 38's already-tested `buildTradingRiskAnalysis()` (which itself transitively resolves Sprint 36's Market Regime Engine — and through it Multi-Timeframe, Liquidity, Market Structure, and the `MarketDataProvider` — for each open, stop/target-defined position's touch-probability context). `openapi.yaml` gained `tradingAccountValue` on `Settings`/`SettingsUpdate` (settable, unlike the read-only `tradingDataConnected`), 3 new tags (`trading-positions`, `trading-risk` — `trading-probability` already existed), the `/trading/positions`/`/trading/positions/{id}`/`/trading/risk` paths, and 13 new `Trading`-prefixed schemas (`TradingPosition`/`Input`/`Update`, `TradingRiskScoreCard`, `TradingRiskComponent`, `TradingPositionSizingRisk`, `TradingStopDisciplineRisk`, `TradingPerPositionBudget`, `TradingPortfolioRiskBudget`, `TradingPositionProbabilityContext`, `TradingRiskAnalysis`); `api-zod`/`api-client-react` regenerated cleanly with no collisions. `TradingResearch.tsx` gained a new, **always-visible** "Portfolio Risk" section (`ShieldAlert` icon) below the 4 existing symbol-search-gated cards — deliberately not gated by the symbol search box, since risk is portfolio-wide, not per-symbol: an account-value form (reads/writes `settings.tradingAccountValue` via `useGetSettings`/`useUpdateSettings`), an add-position form (symbol/side/quantity/entry/stop/target) and a positions list with delete buttons (via `useListTradingPositions`/`useCreateTradingPosition`/`useDeleteTradingPosition`, invalidating both the positions and risk queries on success), and the risk analysis itself (overall/position-sizing/stop-discipline/portfolio-budget detail sentences plus a per-position touch-probability list) via `useGetTradingRisk`. A small `riskGradeBadgeClass()` helper was added since the risk engine's own grade vocabulary (Excellent/Strong/Moderate/Elevated/Poor) differs from the existing `confidenceBadgeClass()`'s High/Moderate/Low vocabulary. **One pre-existing gap fixed in passing:** Sprint 43's own header-comment update to `TradingResearch.tsx` had silently failed mid-sprint (an `Edit` tool encoding mismatch that went unnoticed since the import/icon edits it was bundled with succeeded) — the file's top comment still read "Sprint 42... the third bounded slice" with no mention of Sprint 43's Probability card; fixed as part of this sprint's own header update, not a new regression. New tests: `routes/tradingPositions.route.test.ts` (7 live end-to-end HTTP tests — full create/list/get/update/delete flow, 400 for a missing required field, 400 for an invalid `side` enum value, 404 for GET/PATCH/DELETE on a nonexistent id, 400 for a non-numeric id), `routes/tradingRisk.route.test.ts` (3 live end-to-end HTTP tests — deliberately **per-position assertions rather than aggregate/overall assertions**, since `GET /trading/risk` reads the legacy-owner's entire shared `trading_positions` table across this file's own sibling test file; proves the route's own per-position risk math, the honest-null probability context for an invalid-shaped symbol, and `openPositionsCount` reflecting a created position), and 1 new case in `tenantIsolation.test.ts`'s existing IDOR block (`trading_positions`, matching the exact `and(id, userId)`-never-crosses-accounts proof already established for `trades`/`journal_entries`/`value_watchlist`/`trading_journal_entries`) — DB-level tenant isolation for this table was already covered since Sprint 32's own `assertTenantIsolation` case. New frontend tests: 6 new `TradingResearch.test.tsx` cases (the Portfolio Risk section rendering without a symbol search, the honest empty-positions message, the add-position form submission payload, the delete-position submission, the account-value form submission payload, and the per-position touch-probability display). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`routes/settings.ts`/`routes/journal.ts`/`routes/trades.ts` were not modified at all this sprint — `routes/settings.ts` needed zero code changes since its existing `{...settings, ...}` spread pattern (established since Sprint 5) picks up the new `tradingAccountValue` column automatically. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 86 files / 962 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint). `pnpm --filter @workspace/ravish-trading run test` — 8 files / 61 tests (6 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 45 — COMPLETE.** Liquidity Route + UI — sixth and final bounded slice of the Route+UI backlog reduction for the eager/on-demand modules (Phase 3 plan §11/§21). The project owner's own kickoff message named the target module explicitly ("implement the remaining Liquidity Route + UI backlog item... as the planned on-demand Trading Research tab, not an eager card"), so no `AskUserQuestion` was needed this sprint. New `routes/tradingLiquidity.ts` — `GET /trading/liquidity/:symbol` — mirrors `routes/tradingStructure.ts`'s own Sprint 40 pattern exactly, including its `?interval=&lookback=` query-override support (defaulting to `1D`/90, undocumented in `openapi.yaml`'s formal parameter list per Sprint 40's own disclosed Orval path+query collision precedent): a thin pass-through with zero business logic, calling straight through to Sprint 35's already-tested `buildLiquidityAnalysis()` via Sprint 32's `MarketDataProvider` seam, 404 for an unresolvable symbol, no ownership/tenant scoping needed. **Zero changes to `lib/tradingLiquidity.ts` itself** — the Liquidity Engine's own volume-profile/liquidity-score/buy-sell-pressure math is reused completely unmodified, confirmed via `git diff --stat` showing no change to that file. `openapi.yaml` gained a new `trading-liquidity` tag, the `/trading/liquidity/{symbol}` path (path parameter only), and 3 new `Trading`-prefixed schemas (`TradingVolumeProfileLevel`, `TradingBuySellPressure`, `TradingLiquidityAnalysis` — the last reusing the existing `TradingTimeframe` enum extraction from Sprint 40 for its `interval` field); `api-zod`/`api-client-react` regenerated cleanly with no collisions. **On-demand tab, not an eager card, per the explicit instruction and Phase 3 plan §21's own on-demand-vs-eager split** ("Liquidity + Order Flow... are the heavier, on-demand-tab pattern," matching Phase 2's Statements/Peers cost-control precedent, Sprints 19–20): `TradingResearch.tsx` was restructured to wrap the four existing eager cards (Structure/Multi-Timeframe/Regime/Probability) in a new "Research" tab — their own content and logic are byte-for-byte unchanged, only relocated inside a `<TabsContent>` wrapper — and a new "Liquidity" tab was added alongside it, disabled until a symbol is searched (mirroring `StockResearch.tsx`'s own `disabled={!report}` precedent for its Statements/Peers/Filings/Earnings tabs) and fetched only when that tab is actually opened for a searched symbol, via the established `queryKey` + `enabled: tab === "liquidity" && !!symbol` gated-hook pattern (Sprint 19's precedent, reused for the first time in Engine 2). The Liquidity tab shows the liquidity-band badge, avg-daily-dollar-volume badge, buy/sell-pressure badge, confidence badge, a volume-profile price-level list (or an honest "no volume data" message — never a fabricated empty-looking list vs. a real empty result, matching the engine's own zero-candle honesty contract), and the deterministic summary. The always-visible Portfolio Risk section (Sprint 44) sits below the tabs, completely untouched. New tests: `routes/tradingLiquidity.route.test.ts` (6 live end-to-end HTTP tests — default-1D/90-candle resolution, `?interval=`/`?lookback=` override behavior, 404 for an invalid ticker shape, 400 for an invalid interval, 400 for a non-positive lookback, determinism across repeated calls) and 4 new `TradingResearch.test.tsx` cases (the Liquidity tab trigger disabled with no symbol searched, the full tab-open-to-card-render flow with volume-profile/pressure/liquidity-band assertions, the honest empty-volume-profile message, and the not-found message for an unresolvable symbol on the Liquidity tab). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`lib/tradingLiquidity.ts` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 87 files / 968 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint — the only warning-level log output observed was an intentional mocked-network-failure test case in `value.test.ts`, not a real failure). `pnpm --filter @workspace/ravish-trading run test` — 8 files / 65 tests (4 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully. **This closes out the eager/on-demand Route+UI backlog for Structure/Multi-Timeframe/Regime/Probability/Risk/Liquidity — only Trading Journal's own frontend UI (`Journal.tsx`-derived, §16) remains outstanding and unscheduled.**
- **Sprint 46 — COMPLETE.** Trading Journal Frontend UI — the final item in the Route+UI backlog first disclosed at Sprint 40's own kickoff. The project owner's own kickoff message named the target module explicitly, so no `AskUserQuestion` was needed. **Zero backend changes** — Sprint 39's own `routes/tradingJournal.ts` CRUD (`GET`/`POST /trading/journal`, `GET`/`PATCH`/`DELETE /trading/journal/:id`) and its already-generated `useListTradingJournalEntries`/`useCreateTradingJournalEntry`/`useGetTradingJournalEntry`/`useUpdateTradingJournalEntry`/`useDeleteTradingJournalEntry` hooks were reused exactly as they were, confirmed by `git diff --stat -- artifacts/api-server/` showing an empty diff — no genuine defect was found in the existing API, so per the explicit instruction none was touched. New `pages/TradingJournal.tsx` — per Phase 3 plan §16's own design note ("UI reuses `Journal.tsx`'s established list/detail/mood-tag pattern, adapted, not rewritten from scratch"), adapts the options-side `Journal.tsx`'s two-column list-plus-sticky-new-entry-form layout for `trading_journal_entries`' own field set (`setupType`/`entryPrice`/`exitPrice`/`rMultiple`/`tradingPositionId` in place of the options-specific `strategy`/`entryCredit`/`maxProfit`/`pop`/`ev`/`ravishScore` fields `Journal.tsx` shows) — **unlike the options-side page** (which only has list+create, no update/delete route to build on), this page exposes full CRUD since Sprint 39 shipped all four verbs: each entry card has inline Edit (toggles a pre-filled edit form in place, Save/Cancel) and Delete buttons. A shared `EntryFields` component is reused for both the always-visible "New Entry" sidebar form and any in-progress inline edit, avoiding duplicated field-rendering logic between the two. The optional "Related Position" dropdown reuses Sprint 44's own `useListTradingPositions()` (already shipped, zero new logic) to let a user associate an entry with one of their own `trading_positions` rows by symbol, mirroring the backend's own loose, unenforced `tradingPositionId` reference (same precedent as `journal_entries.trade_id`). **No search/filter** — Phase 3 plan §16 never defines one for Trading Journal (unlike, say, a scanner), so none was added, per the explicit "if already defined in the execution plan" scoping instruction. **No SIMULATED labelling on this page** — trading journal entries are user-authored text and user-entered prices, never derived from SIMULATED market data (the same honest distinction Sprint 39's own route-header comment already drew), so there is no `dataSource` field here to label, unlike `TradingResearch.tsx`'s cards. `App.tsx` gained a new `/trading-journal` route; `AppLayout.tsx` gained a new "Trading Journal" nav item (`NotebookPen` icon, positioned directly after "Trading Research"). No database migration. New tests: `TradingJournal.test.tsx` (10 tests — loading skeletons, an honest load-error message, the empty-state message, a full entry render with its mood/setup/entry-exit-price/R-multiple badges and lesson-learned callout, a negative R-multiple rendering without a fabricated leading plus sign, the new-entry form's submitted payload, the required-field guard rejecting an empty title/content submission, the edit-form's pre-fill-then-submit round trip — scoped via `within()` to disambiguate the always-present sidebar form's own identical `data-testid`s from the in-progress edit card's, a genuine test-authoring fix caught during validation, not a production bug — the cancel-edit path submitting nothing, and the delete submission). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`routes/tradingJournal.ts` were not modified at all this sprint — confirmed the entire `artifacts/api-server/` tree has a zero-line diff. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 87 files / 968 tests; one run hit the previously-disclosed, previously-documented `fetchedAt`-timing race in `value.test.ts`'s SIMULATED-determinism check (unrelated to any Sprint-46-touched file, confirmed via `git status` showing `value.test.ts` untouched) — a third run confirmed clean: 87 files / 968 tests, zero failures. `pnpm --filter @workspace/ravish-trading run test` — 9 files / 76 tests (10 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully. **This closes the entire Route+UI backlog first disclosed at Sprint 40 — Structure/Multi-Timeframe/Regime/Probability/Risk/Liquidity/Trading-Journal all now have a working frontend.** It does **not**, however, complete every module the Phase 3 execution plan originally scoped: AI Trade Coach (Core + Route/UI, §17), Institutional Dashboard, Backtesting (a genuine Engine-2-native price-action backtester, §18, distinct from the existing options-strategy `routes/backtest.ts`), an optional Live Market-Data Provider (§25 Decision 7, explicitly conditional), and the closing Sprint-31-style "Trading Engine Unification" integration sprint were never started this session — see the Phase 3 Completion Report delivered alongside this sprint's own report for the full disclosure and Phase 4 recommendation.
- **Sprint 47 — COMPLETE.** AI Trade Coach — Core + Route (approved Phase 3 plan §17; the first of Phase 3's remaining un-started modules per the Phase 3 Completion Report delivered alongside Sprint 46). The project owner's own kickoff message named the target module explicitly and scoped it deliberately narrow ("reuse the existing outputs... build as composition layer... no new trading calculations"), so no `AskUserQuestion` was needed. **Pure composition, zero new trading calculations, confirmed by `git diff --stat` showing every prior Engine 2 module (`tradingMarketStructure.ts`/`tradingMultiTimeframe.ts`/`tradingLiquidity.ts`/`tradingRegime.ts`/`tradingProbability.ts`/`tradingRisk.ts`/`routes/tradingJournal.ts`) untouched.** New `narrateTradeFreeform()`/`narrateTradeFreeformStream()` in `coachLLM.ts` — following Sprint 30's exact precedent (added directly inside the file, not a new one, reusing its already-private `narrate()`/`narrateStream()` machinery so `COACH_DISCLAIMER` is enforced centrally). Unlike the Value coach's "in the spirit of Warren Buffett" framing, no persona is invoked here, so only the baseline `COACH_DISCLAIMER` invariant applies — no `VALUE_DISCLAIMER`-equivalent or anti-impersonation guard was needed. New `routes/tradingCoach.ts` — `POST /trading/coach/ask` (+ `/ask/stream`, SSE, same `meta`→`delta`→`done` contract as `/value-research/ask/stream`, deliberately outside the OpenAPI contract per that route's own precedent). `buildTradeCoachContext()` is a genuine, near-zero-new-logic composition: **one call**, `buildProbabilityAnalysis(symbol, provider)`, transitively resolves the Probability Engine (Sprint 37), the Market Regime Engine (Sprint 36, via `.regime`), the Multi-Timeframe Trend Engine (Sprint 34, via `.regime.multiTimeframe`), the Liquidity Engine (Sprint 35, via `.regime.liquidity`), and the Market Structure Engine (Sprint 33, via each `multiTimeframe.timeframes[].structure`) — zero duplicate candle fetches, mirroring Sprint 38's own reuse of this exact regime-resolution chain. A second call, `buildTradingRiskAnalysis()`, reuses the Risk Management Engine (Sprint 38) over the calling user's own `trading_positions` + `settings.tradingAccountValue`. A direct, `eq(userId)`-scoped DB read (mirroring `routes/tradingJournal.ts`'s own established query pattern, unmodified) surfaces the user's 5 most recent Trading Journal reflections (Sprint 39/46). The prompt explicitly instructs the model to say plainly when a question asks for something the DATA doesn't cover (a real-time quote, a future price prediction, live order-flow/Level 2 data) rather than inventing an answer — the exact discipline `valueFreeformPrompt` already established (Sprint 30). 404 for an unresolvable symbol, matching every other per-symbol Engine 2 route. `openapi.yaml` gained a new `trading-coach` tag, the `/trading/coach/ask` path, and `TradingCoachAskInput`/`TradingCoachAskResult` schemas; `api-zod`/`api-client-react` regenerated cleanly with no collisions (Orval named the generated Zod symbols `AskTradingCoachBody`/`AskTradingCoachResponse`, matching `AskValueResearchBody`/`Response`'s own established POST-endpoint naming convention). **Deliberately Core + Route only this sprint, no UI** — the original Phase 3 roadmap table itself split this into two sprints ("AI Trade Coach (Core)" then "AI Trade Coach (Route + UI)"), and the project owner's own kickoff instructions for this sprint were entirely backend-focused (reuse engines, composition layer, no mention of a chat panel) — a bounded, disclosed deferral, not an oversight; a future sprint can add the chat-panel UI reusing the existing `streamCoach()` SSE client exactly as Sprint 30 did for the Value coach. No database migration. New tests: `coachLLM.tradeCoach.test.ts` (3 tests — the disclaimer-invariant proof on both the single-shot and streaming paths, mirroring `value.test.ts`'s own safety-invariant block, plus a never-fabricates-when-unavailable proof) and `routes/tradingCoach.route.test.ts` (5 live end-to-end HTTP tests — a 200 answer for a known symbol, 404 for an invalid ticker shape, 400 for a missing question, 400 for a missing symbol, and a mocked-`narrateTradeFreeform` test proving the grounding context genuinely includes `structure`/`multiTimeframe`/`liquidity`/`regime`/`probability`/`portfolioRisk`/`recentJournalReflections` — this sprint's own explicit acceptance criteria, mirroring `valueResearchAsk.route.test.ts`'s own Sprint 30 pattern exactly). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 89 files / 976 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint). `pnpm --filter @workspace/ravish-trading run test` — 9 files / 76 tests, unmodified — no frontend change this sprint. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 48 — COMPLETE.** AI Trade Coach — Frontend UI (approved Phase 3 plan §17, completing the module Sprint 47's Core + Route began). The project owner's own kickoff message named the target module explicitly and scoped it deliberately narrow ("reuse the Sprint 47 backend exactly... do not modify backend behaviour unless a genuine defect is discovered"), so no `AskUserQuestion` was needed — no defect was found, so none was fixed. **Zero backend changes** — confirmed via `git diff --stat -- artifacts/api-server/` showing an empty diff; Sprint 47's `POST /trading/coach/ask/stream` route and its SSE `meta`→`delta`→`done` contract were reused exactly as shipped. New chat panel embedded directly in `TradingResearch.tsx`'s existing "Research" tab (not a new page/route) — positioned as a fifth card, immediately after the Probability card, gated on `symbol` alone (visible as soon as a symbol is searched, independent of whether Structure/Multi-Timeframe/Regime/Probability have themselves finished loading, since the coach's own backend composition is a fully independent request). Reuses the **exact same `streamCoach()` SSE client** `StockResearch.tsx`'s own "Ask the AI Investment Analyst" panel already established (Phase 2, Sprint 30) — imported unmodified from `@/lib/coach-stream`, zero new streaming infrastructure. Local component state only (`coachQuestion`/`coachHistory`/`coachStreamingAnswer`/`coachAsking`), matching Sprint 30's own "conversation history within the current session, not persisted" precedent exactly — a page refresh clears it, by design, same as the Value coach's panel. **Loading state**: streamed tokens render inline as they arrive, falling back to a "thinking…" placeholder before the first token; **empty state**: an honest "No questions yet — ask something like..." prompt shown before any Q&A, distinct from the history view (never rendered simultaneously); **error state**: `onError` pushes a plain "Failed to get an answer — please try again." turn into the conversation, mirroring Sprint 30's own error-handling precedent exactly — never a fabricated answer. **No "Clear conversation" button** — Phase 3 plan §17 never defines one (confirmed by re-reading the full section before implementation), and Sprint 30's own precedent for the Value coach's identical panel shape also has none, so per the explicit "if already defined in the execution plan" scoping instruction, none was added; a page refresh is the de facto clear action, consistent with the non-persisted local-state design. All responses stay honestly grounded in Sprint 47's already-composed engine outputs — the frontend adds no new grounding logic, only renders whatever `narrateTradeFreeform()`/`narrateTradeFreeformStream()` returns, and `COACH_DISCLAIMER` continues to be enforced entirely server-side (Sprint 47's own invariant, untouched). No database migration, no `openapi.yaml` change (the streaming endpoint was already deliberately kept outside the OpenAPI/orval contract since Sprint 47, matching `/value-research/ask/stream`'s own precedent). New tests: 5 new `TradingResearch.test.tsx` cases (the empty-state message shown once a symbol is searched, the panel correctly absent before any symbol is searched, the submitted request payload sent to `/trading/coach/ask/stream`, a streamed answer rendering as a Q&A turn once the stream completes, and the honest error-turn path when the stream fails) — following the exact `streamCoach` mocking pattern `StockResearch.test.tsx` already established for its own Ask panel. No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; the entire `artifacts/api-server/` tree has a zero-line diff this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 89 files / 976 tests; one run hit the previously-disclosed, previously-documented `autoScheduler.multiUser.test.ts` FK-violation race under live-DB parallelism (unrelated to any Sprint-48-touched file, confirmed via `git status` and via the entire backend tree's zero-line diff this sprint) — two subsequent runs confirmed clean: 89 files / 976 tests, zero failures each. `pnpm --filter @workspace/ravish-trading run test` — 9 files / 81 tests (5 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 49 — COMPLETE.** Backtesting, Engine-2-native (approved Phase 3 plan §18). Presented with a bare "yes approve" and no module named, `AskUserQuestion` succeeded (3rd success this session, after Sprints 39/44) and the project owner selected **"Backtesting, Engine-2-native (Recommended)"** over Institutional Dashboard and Trading Engine Unification. **Genuine architectural finding made before writing any code:** direct inspection of the existing options-side `routes/backtest.ts` confirmed it does not replay real trades at all — it fabricates an entire equity curve directly from `makeRng()`-seeded win-rate/avg-win/avg-loss statistics, with no actual price-path simulation. Per §18's own framing ("a genuine price-action backtest engine... distinct from `routes/backtest.ts`'s options-strategy-equity-curve generator"), Engine 2's own backtest was deliberately built as a REAL walk-forward simulation: it replays an already-resolved SIMULATED candle series bar-by-bar, evaluates a genuine entry/exit rule at each bar, and produces the equity curve and trade log FROM those real simulated trades. New `lib/tradingBacktest.ts` — `runTradingBacktest(candles, symbol, strategy, interval, isLive, opts?)` (pure, I/O-free) and `buildTradingBacktest(symbol, strategy, interval, lookback, provider, opts?)` (the orchestration wrapper, resolving candles via Sprint 32's unmodified `MarketDataProvider` seam). **The only genuinely new logic is the walk-forward harness and 3 named entry/exit rule sets** — the trend/zone SIGNAL itself is 100% reused, unmodified, from Sprint 33's `analyzeMarketStructure()`, called repeatedly over an expanding window of already-resolved candles (`candles.slice(0, i+1)` at each bar) to read that day's trend/zones. Three strategies: `trend-following` (enter/hold while `trend === "uptrend"`), `structure-breakout` (enter only on a fresh flip INTO uptrend, never mid-trend — proven by a dedicated test showing it produces zero trades on a series that is already uptrend from the first evaluated bar), `mean-reversion` (enter when a candle's low pierces a real, already-detected support zone from Sprint 33's own zone-clustering, exit on a time limit). A fixed-risk-per-trade convention (every trade risks the same % of entry price, named constants `DEFAULT_STOP_LOSS_PCT`=3%/`DEFAULT_TARGET_PCT`=6%) is the simplest honest convention for a rule-based backtest, never varied per-trade without a documented reason. `MIN_STRUCTURE_WINDOW`=30 candles are required before the first signal is evaluated, giving `analyzeMarketStructure()` enough of a window for a meaningful swing-based read; too few candles honestly reports `available:false` with zero trades, never a fabricated one. A strategy that never triggers a signal honestly reports zero trades and null KPIs (winRate/avgR/etc.), never an invented win rate. An open position at the end of the sample is honestly closed at the final candle's close (`"end-of-period"` exit reason), never left dangling or silently dropped. New table `trading_backtest_results` (`lib/db/manual-migrations/013_trading_backtest_results.sql`) — reuses the options-side `backtest_results` table's own persisted-results SHAPE (promoted headline columns + jsonb detail), not that table itself and not its options-specific simulation logic, since Engine 2's own KPI vocabulary (`winRate`/`avgR`/`maxDrawdownPct`/`totalReturnPct`/`sharpeRatio`) deliberately differs from the options table's own (`avgWin`/`avgLoss`/`ev*`/`sortinoRatio`), and this table's `tradeLog` is a REAL trade-by-trade log, not a fabricated curve; `userId` mandatory + `ON DELETE RESTRICT` (universal convention); brand-new table, `NOT NULL` from creation except the honestly-nullable KPI fields, no backfill needed. A `data_source` column (`SIMULATED`/`LIVE`) was added to the table during implementation — not in the original design sketch — once it became clear the route needed to honestly persist which data source produced each backtest rather than reconstructing it after the fact from unrelated columns; `routes/backtest.ts` and its own table are not modified at all. New `routes/tradingBacktest.ts` — `POST /trading/backtest/run` (ownership-scoped via `getScopedUserId(req)`, resolves the provider, calls `buildTradingBacktest()`, persists the result to the calling user's own history, 404 for an unresolvable symbol) and `GET /trading/backtest/results` (lists only the calling user's own rows, newest first, bounded to 50). `openapi.yaml` gained a new `trading-backtest` tag, the `/trading/backtest/run`/`/trading/backtest/results` paths, and 5 new `Trading`-prefixed schemas (`TradingBacktestStrategy` enum, `TradingRunBacktestInput`, `TradingBacktestTrade`, `TradingBacktestEquityPoint`, `TradingBacktestResult`); `api-zod`/`api-client-react` regenerated cleanly with no collisions. New page `pages/TradingBacktest.tsx` at `/trading-backtest` (new top-level nav item, `History` icon, positioned directly after "Trading Journal") — reuses the options-side `Backtest.tsx`'s own rendering pattern (recharts `LineChart` equity curve, KPI tile grid, a clickable results-history table) but deliberately **not** its options-specific "Explain This Trade" `TradeExplanationSheet` feature, since this engine has no options legs to explain; honest-empty handling throughout (`unavailableReason` shown instead of a fabricated chart when too few candles were available, a "triggered no trade signals" message instead of a fabricated empty chart when a strategy never fires). **Two genuine test-authoring corrections caught and fixed during validation, neither a production bug:** (1) a mean-reversion test initially assumed a specific custom-appended "dip bar" would be the trade's entry candle, but the fixture's own repeated range-cycle candles already formed a real support zone that triggered entry earlier — fixed by asserting the general invariant (every triggered trade's own entry candle low is at or below the known support price) rather than hardcoding which bar fires first; (2) a mean-reversion test assumed a pure monotonic uptrend would produce zero support zones, but Sprint 33's zone-clustering considers the FULL swing-point history (not limited to the `RECENT_SWINGS=3` window trend classification uses), so even a monotonic uptrend produced several proximity-clustered zones — fixed by replacing the negative ("zero zones") assertion with a stronger positive-existence proof (every mean-reversion entry is independently re-verified against a real support zone that existed in that day's own structure read at that exact point in time). New tests: `lib/tradingBacktest.test.ts` (16 tests — honest-unavailable/zero-trade-signal paths, trend-following vs. structure-breakout's fresh-flip-only entry discipline, mean-reversion's support-zone grounding, equity-curve/KPI bookkeeping including the open-position-at-end-of-sample honest close, determinism, the orchestration seam's honest-null path for an unresolvable symbol), `routes/tradingBacktest.route.test.ts` (8 live end-to-end HTTP tests — a full run+persist+list flow, the honest-unavailable path for too few candles, 404 for an unresolvable symbol, 400 for a missing required field and an invalid strategy enum, default interval/lookback behavior, determinism, newest-first list ordering), 1 new case in `tenantIsolation.test.ts` (`trading_backtest_results`, reusing the established `assertTenantIsolation` helper), and `pages/TradingBacktest.test.tsx` (5 frontend smoke tests — the empty-history message, the run-form's submitted payload, a triggered-trade result's KPI tiles and trade log, the honest unavailable-reason display, and the honest zero-trades display). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts`/`routes/backtest.ts` were not modified at all this sprint, confirmed via `git diff --stat`. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 91 files / 1001 tests; the first run was fully clean, a second run hit the previously-disclosed, previously-documented `fetchedAt`-timing race in `fundamentals.investingUniverse.test.ts` first noted in Sprint 16's report (unrelated to any Sprint-49-touched file, confirmed via `git status`), a third run confirmed clean: 91 files / 1001 tests, zero failures. `pnpm --filter @workspace/ravish-trading run test` — 10 files / 87 tests (5 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Sprint 50 — COMPLETE.** Institutional Dashboard (Phase 3 plan §3/§4/§21, the module named "Institutional Dashboard — NEW page" in the plan's own architecture diagram). The project owner's own kickoff message named the target module explicitly and scoped it deliberately narrow ("build the dashboard as a composition layer over the existing engines... do not introduce duplicate engine calculations"), so no `AskUserQuestion` was needed. **Pure composition, zero new engine calculations, confirmed by `git diff --stat` showing every prior Engine 2 backend module (`tradingMarketStructure.ts`/`tradingMultiTimeframe.ts`/`tradingLiquidity.ts`/`tradingRegime.ts`/`tradingProbability.ts`/`tradingRisk.ts`, and every existing `routes/trading*.ts` file) completely untouched — this sprint shipped zero new backend production code, only a new test file.** New page `pages/InstitutionalDashboard.tsx` at `/institutional-dashboard` (new top-level nav item, `LayoutGrid` icon, positioned first among the Engine 2 nav items as the natural landing page) satisfies the literal acceptance bar the plan's own §22 draws for this module: "one symbol lookup surfaces Structure + Liquidity + Multi-Timeframe + Probability + Regime + Risk without any additional navigation." Unlike `TradingResearch.tsx` (Sprints 40–48), which deliberately puts Liquidity behind its own on-demand tab (a disclosed cost-control choice, Sprint 45), this page fetches all five per-symbol signals (`useGetTradingStructure`/`useGetTradingMultiTimeframe`/`useGetTradingRegime`/`useGetTradingProbability`/`useGetTradingLiquidity`) concurrently and renders them together in one grid — no tabs, no additional clicks — because "no additional navigation" is this page's entire reason to exist. Portfolio Risk (Sprint 38/44) is portfolio-wide, not per-symbol, so it's shown as an always-visible read-only summary card (`useGetTradingRisk`) alongside the per-symbol grid, satisfying the bar's own "...and Risk" clause without requiring a symbol to be searched first. Two further always-visible summary cards were added, reusing the explicitly-named-for-reuse Sprint 39 Trading Journal and Sprint 49 Backtesting hooks (`useListTradingJournalEntries`/`useListTradingBacktestResults`, each sliced client-side to the 3 most recent rows) — a disclosed, bounded scope choice: every card on this page is a condensed **read-only** summary, never a re-implementation of `TradingResearch.tsx`'s/`TradingJournal.tsx`'s/`TradingBacktest.tsx`'s own full detail views (support/resistance list, full per-timeframe list, full probability cone table, full volume-profile list, the position-management form, the full journal/backtest CRUD) — duplicating those would mean duplicating their own state/mutation logic, the exact thing this sprint's "do not duplicate calculations or business logic" instruction rules out. Each summary card links out (via `wouter`'s `Link`) to its own full, already-built page for management actions — Trading Research, Trading Journal, Trading Backtest — reusing that page's own UI rather than re-implementing it here, including a direct link to Trading Research's own already-shipped AI Trade Coach panel (Sprint 47/48) rather than embedding a second, duplicate chat/streaming implementation. **One small, disclosed, behavior-preserving refactor to `TradingResearch.tsx` itself:** its 10 small presentational badge-class/icon helpers (`fmtUsd`, `trendBadgeClass`, `TrendIcon`, `confidenceBadgeClass`, `agreementBadgeClass`, `regimeBadgeClass`, `volatilityBadgeClass`, `riskGradeBadgeClass`, `liquidityBandBadgeClass`, `pressureBadgeClass`) were extracted, unmodified, into a new shared `src/lib/trading-format.tsx` module so the new Dashboard page could reuse them instead of redefining its own copies — confirmed byte-identical behavior via `TradingResearch.test.tsx`'s own full 25-test suite passing unmodified after the extraction. No database migration, no `openapi.yaml` change — every hook this page uses was already generated by a prior sprint's codegen. New tests: `pages/InstitutionalDashboard.test.tsx` (6 frontend smoke tests — advisory copy and no signal grid before a symbol is searched, all five per-symbol cards rendering together with zero tab/navigation elements once a symbol is searched — the literal "no additional navigation" proof, the honest probability-unavailable-reason display, the three always-visible summary cards showing their own honest-empty messages with no symbol searched, the same three cards rendering real data when available, and the "links out rather than duplicates" proof for all 4 outbound links) and `routes/institutionalDashboard.route.test.ts` (8 live end-to-end HTTP tests, mirroring Phase 2 Sprint 31's `companyResearchUnification.route.test.ts` pattern exactly — each of the 5 per-symbol routes plus the portfolio-wide risk route resolves individually for the dashboard's own symbol, all 5 per-symbol routes resolve **concurrently** for the same symbol with zero 404s — the literal acceptance-bar proof — and an unknown symbol 404s consistently across all 5, never a partial/fabricated dashboard). No trading logic, options execution, scheduler behavior, guardrails, kill switches, authentication, tenant isolation, or audit logging were touched; `execution.ts`/`optionsMath.ts`/`risk.ts` were not modified at all this sprint. `pnpm run typecheck` clean. `pnpm --filter @workspace/api-server run test` — 92 files / 1009 tests, fully clean on both the first and a repeated normal-parallel run (zero flakes this sprint). `pnpm --filter @workspace/ravish-trading run test` — 11 files / 94 tests (6 new), all passing. `PORT=5000 BASE_PATH=/ pnpm run build` — all 3 packages build successfully.
- **Current Sprint: 51** — **not yet started.** Per the established process, Sprint 51 requires its own explicit pre-implementation plan presentation and approval before any code is written. Remaining un-started Phase 3 modules: the closing Trading Engine Unification integration sprint, and the optional/conditional Live Market-Data Provider. Per the project owner's own repeated instruction, Phase 4 must not begin until these are explicitly addressed or deliberately deferred by the project owner.

### Phase 1 sprint history (of 7 total phases; see the Blueprint doc for the full phase list)

- **Sprint 1 — COMPLETE.** Users table schema (`lib/db/src/schema/users.ts`), manual migration scripts, and a CI pipeline (`.github/workflows/ci.yml`) were added. Commit `968b8c7` on branch `sprint-1/foundation-users-table-ci`, off baseline `35d2c42`. Zero existing lines were modified or deleted — 5 files added, 1 file gained a single export line.
- **Sprint 1 verification status:** the real `pnpm typecheck`/`pnpm build`/`pnpm test` have been run for real, with dependencies installed and a live Postgres database — all green.
- **Sprint 2 — COMPLETE.** `.env.example` added at the repo root; `coachLLM.ts`'s `init()` now checks `ANTHROPIC_API_KEY` first, falls back to `OPENAI_API_KEY` (OpenAI-shaped or, for backward compatibility, `sk-ant-`-prefixed with a logged deprecation warning), preserving identical behavior for existing deployments. Zero existing lines of application logic outside `init()` were touched. New test: `coachLLM.envMigration.test.ts`; existing `phase7.coach.test.ts`, `coach-level.test.ts`, `coach-slowload.test.ts` pass unmodified.
- **Sprint 3 — COMPLETE.** Nullable `user_id` (uuid) added to all 13 user-scoped schema files, via `lib/db/manual-migrations/001_add_user_id_nullable.sql` (renumbered the Sprint 1 backfill draft to `002_...` to keep run-order numbering correct). No FK, no `NOT NULL`, no application code reads or writes it yet — `auto_execution_log` untouched, per scope. Full existing test suite passes unmodified.
- **Sprint 4 — COMPLETE.** Backfilled all 12 non-`settings` tables to a single legacy-owner user (`dunnikarp@gmail.com`, Owner Decision #4 resolved), then enforced `NOT NULL` + FK (`ON DELETE RESTRICT`) + index on `user_id` for all 12, via `lib/db/manual-migrations/002_backfill_and_enforce.sql`. `settings` deliberately untouched (still nullable — ships with Sprint 5's code change, per §2.3). Enforcing `NOT NULL` made every existing `.insert()` into these tables require `userId` at the TypeScript level (a plan assumption gap, not anticipated by "no application code reads user_id yet") — 24 call sites across 10 files (including `execution.ts` and `tradeClose.ts`) now resolve a temporary `getLegacyOwnerUserId()` stand-in (`lib/legacyOwner.ts`), explicitly approved for this exact scope. No execution/order-routing/risk logic changed — only the added `userId` field. Full existing test suite passes unmodified in assertions (two DB-mock test files gained a `usersTable`/`legacyOwner` stub to support the new dependency).
- **Sprint 5 — COMPLETE.** `settings` is now per-user, not a singleton (§2.3) — `user_id` backfilled (to the same legacy-owner user), enforced `NOT NULL` + FK + **unique** constraint, via `lib/db/manual-migrations/003_settings_user_scoped.sql`. `routes/settings.ts`'s `getOrCreateSettings()` now takes `userId` and scopes every select/update by it — fixing the literal leak the plan flagged as highest-priority (the old `PATCH` had no `WHERE` clause at all). `serverState.ts`'s `getSettingsRow()` and `fundamentals.ts`'s `getFundamentalsProvider()` both gained an optional `userId` param that defaults to the legacy-owner stand-in when omitted, so every existing caller — **including `autoExecution.ts`/`autoAdjustment.ts`, deliberately left untouched per §4.4's pending owner decision** — keeps working identically without modification. New test `routes/settings.perUser.test.ts` proves two different `userId`s get fully independent settings, including independent `autoExecuteEnabled`/`autoAdjustEnabled` kill switches. Full existing test suite passes unmodified.
- **Sprint 6 — COMPLETE.** Real authentication, per Owner Decision #1 (Better-Auth, self-hosted, official Drizzle adapter). New `lib/auth` package points Better-Auth at the *existing* `users` table (modelName "users", `name`→`displayName`, `role` exposed as a read-only `additionalField`) rather than letting it create its own — every one of the 13 user-scoped tables' FK to `users.id` stays the single source of truth. Three new tables (`sessions`, `accounts`, `verifications`) added via `lib/db/manual-migrations/004_better_auth_tables.sql`, plus `users.email_verified`/`users.image` (Better-Auth's required fields) and `users.display_name` tightened to `NOT NULL` (Better-Auth requires it; the sole existing row already had one). `authProvider`/`externalId`/`passwordHash` on `users` are now vestigial — Better-Auth stores credentials on `accounts.password` instead — left in place, unused, not dropped. `middlewares/auth.ts` (`loadSession`, non-blocking) and `middlewares/requireAuth.ts` (the actual gate) are new; `routes/auth.ts` mounts every Better-Auth endpoint at `/api/auth/*` plus one first-party `/api/auth/me` proving `req.user` reflects a real session. Per the plan's rollback note, none of the existing 16 business routes are gated yet — they still resolve `getLegacyOwnerUserId()` (Sprint 7's job). CORS hardened additively: `CORS_ALLOWED_ORIGINS` (unset preserves today's open `cors()` behavior exactly). Frontend: `src/lib/auth-client.ts` (Better-Auth's own React client, not the Orval/OpenAPI pipeline — deliberately not regenerating `lib/api-client-react` for Better-Auth's ~15 endpoints), `src/pages/Login.tsx`, a session-status widget in `AppLayout`, and a `/login` route — none of the other routes redirect unauthenticated users yet, matching the backend's same not-yet-enforced scope. New test `routes/auth.test.ts` (11 tests: unauthenticated/authenticated/cross-user isolation, wrong-password rejection, role-spoofing rejection, sign-out revocation) runs against a real Better-Auth instance backed by its memory adapter (`better-auth/adapters/memory`) — no live Postgres needed, matching this project's test convention, while still exercising Better-Auth's genuine logic rather than a hand-rolled mock of it. Full existing test suite passes unmodified. Verified live end-to-end (signup/sign-in/session/sign-out/CSRF/cross-user isolation) against the real dev database before the automated tests were written. Cross-checked the hand-written schema against `@better-auth/cli generate`'s own output — caught and fixed two real gaps (`display_name` NOT NULL, a missing index on `verifications.identifier`) before committing. `autoExecution.ts`/`autoAdjustment.ts`/`execution.ts`/`optionsMath.ts`/`risk.ts` are untouched.
- **Sprint 7 — COMPLETE ("the leak-closing sprint").** Applied real, mandatory `userId` scoping to every query across all business route files identified in the approved plan's §4.5 leak table (`trades.ts`, `portfolio.ts`, `portfolioAI.ts`, `journal.ts`, `backtest.ts`, `scanner.ts`, `ai.ts`, `coach.ts`, `stockAnalyst.ts`, `settings.ts`, `marketDataHealth.ts`, plus `execution.ts`'s route, which transitively needed it), and their shared `lib/*.ts` dependencies (`execution.ts`, `tradeClose.ts`, `dailyReport.ts`, `serverState.ts`, `valueReport.ts`/`fundamentals.ts`'s provider selection). New `lib/tenantScope.ts` (`getScopedUserId(req)` — a real session wins, else falls back to the legacy-owner stand-in; `authRequired()` — the `REQUIRE_AUTH` env-var gate) is the one, consistent per-request resolution point every route now calls, replacing direct `getLegacyOwnerUserId()` calls throughout. Every fetch-by-ID endpoint now filters ownership in the query itself (`and(eq(id), eq(userId))`), returning 404 for both "doesn't exist" and "exists but isn't yours" — no separate 403 branch, per the plan's explicit anti-existence-leak requirement. `app.ts` now mounts `healthRouter` separately (always auth-exempt, even when `REQUIRE_AUTH=true`) and conditionally mounts `requireAuth` on the business router only when `REQUIRE_AUTH=true` — still off by default, the plan's required rollback switch. The automation scheduler (`autoExecution.ts`/`autoAdjustment.ts`) call sites were updated to pass an explicit, resolved legacy-owner id at each Sprint-7-changed function signature — its real per-user redesign remains Sprint 8's job per §4.4, untouched here; `autoExecutionLog` writes and all cycle/guardrail logic are byte-for-byte unchanged. New `lib/tenantIsolation.test.ts` implements the plan's §8.3 shared `assertTenantIsolation` helper against a live Postgres connection, applied across all 13 user-scoped tables, plus dedicated IDOR tests (trades/journal/watchlist) and a live settings-kill-switch-independence check. The four flagged non-regression files (`adjustmentTicket.test.ts`, `tradeClose.test.ts`, `phase7.coach.test.ts`, `autoAdjustment.cycle.test.ts`) were updated only for the new call signatures; three of the four needed zero assertion changes. **One narrow, disclosed exception to §8.4's word-for-word bar:** `autoAdjustment.cycle.test.ts`'s `closeSpy.mock.calls[0][1]` became `[0][2]` — a mechanical index shift from `closeTradePosition` gaining its (already-approved, mid-sprint) `userId` positional parameter, not a cycle/guardrail behavior change; the regex assertion itself is unchanged. Verified live end-to-end (not just unit-tested): two real signed-up users, `REQUIRE_AUTH=true`, confirmed User B's trade list excludes User A's trade and `GET /trades/:id` for User A's trade returns 404 (not 403) for User B. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (19 files / 274 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all run and pass for real, against a disposable local Postgres database with the full schema pushed and the legacy-owner user seeded.
- **Sprint 8 — COMPLETE.** Automation scheduler multi-tenancy, per Owner Decision #2 (per-user iteration loop within the existing scheduler, not a replatform onto per-user scheduled jobs). `runAutoExecutionCycle`/`runAutoAdjustmentCycle` keep their exact pre-Sprint-8 single-user signature and behavior — `userId` is now an *optional* parameter defaulting to the legacy-owner stand-in when omitted, so every existing caller/test is unaffected. New `runAutoExecutionCycleForAllUsers()`/`runAutoAdjustmentCycleForAllUsers()` query `settings` for users who are actually armed (`executionMode='full_auto'` AND the relevant kill switch(es), master before subordinate) and run the existing single-user cycle once per armed user; `index.ts`'s scheduler tick calls these instead of the bare functions, same 60s interval, no new infrastructure. The single-flight guard in both engines changed from one global boolean to a `Set<userId>`, so two different users' cycles never block each other while the same user still can't double-run. `evaluateAutoGuardrails`, `decideCandidate`, `autoAdjustAllowed`, and every `autoExecutionLog` write are byte-for-byte unchanged — Sprint 8 only added the per-user iteration layer on top of logic Sprint 7 had already scoped correctly underneath. Also fixed two related gaps missed in Sprint 7 (same "doesn't import `@workspace/db` directly" blind spot as `execution.ts`): `routes/settings.ts` was still resolving `getLegacyOwnerUserId()` directly instead of `getScopedUserId(req)`, and `routes/marketDataHealth.ts` read per-user settings unscoped — both now route through the real authenticated user; `routes/autoExecution.ts`'s status/manual-run endpoints now resolve the calling user too (its log-read endpoints stay global/unscoped, since `autoExecutionLog` has no `userId` column and is not to be modified, per CLAUDE.md rule 3). New `lib/autoScheduler.multiUser.test.ts` proves the sprint's own acceptance criteria directly against a live Postgres connection: a fully-armed user's trade closes while a switch-off user's and a wrong-mode user's identical candidates are never touched (even when their cycle is invoked directly), two different users' cycles run concurrently without blocking each other, and the same user invoked twice concurrently still single-flights. The four flagged hard-gate non-regression files (`phase6.test.ts`, `phase9.eventRisk.test.ts`, `autoAdjustment.cycle.test.ts`, plus `adjustmentTicket.test.ts`/`tradeClose.test.ts` from Sprint 7) needed **zero changes this sprint** — none were touched. Verified live end-to-end: two real signed-up users, one armed (`executionMode=full_auto`, both switches on) via a real `PATCH /settings` call, the other left at manual defaults — `GET /execution/auto/status` returns `armed:true`/`armed:false` respectively, over real HTTP with real sessions. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (20 files / 281 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Sprint 9 — COMPLETE.** `lib/ai-core` extraction (plan §5), the shared AI narration core for all three future engines. New `lib/ai-core` workspace package holds every provider-agnostic, engine-agnostic piece moved as-is from `coachLLM.ts`: `init()`/`llmAvailable()` (key-prefix provider detection), `complete()`/`completeStream()`, the narration cache + LRU eviction + TTL + single-flight `inflight` map, `withTimeout()`, `extractJsonObject()`, and the types/helpers `Provider`, `Narration`, `NarrationSource`, `TokenSink`, `CoachLevel`, `levelInstruction()`, `levelKey()`. The one real design change (not just a lift-and-shift): `narrate()`/`narrateStream()`/`complete()`/`completeStream()` now take `systemPrompt`/`disclaimer` as explicit parameters instead of hardcoding `SYSTEM_PROMPT`/`COACH_DISCLAIMER`, so the disclaimer-enforcement guarantee generalizes to the Trading/Investing coaches Phase 2/3 will build. A `WarnHandler` injection point (`onWarn: (meta, message) => void`) lets `coachLLM.ts` bridge lib/ai-core's warnings back into the app's real pino logger without lib/ai-core taking a hard dependency on it. `coachLLM.ts` is now a thin, options-specific domain layer on top of `@workspace/ai-core`: `SYSTEM_PROMPT`'s wording, `COACH_DISCLAIMER`, `enforceDisclaimer()`, `violatesAntiImpersonation()`/`enforceValueDisclaimer()`/`enforceValueSafety()` (value-module safety), every template/prompt, and every exported function name/signature are byte-for-byte unchanged — confirmed by grepping every real importer of `coachLLM.js` across the codebase (5 route files, 6 test files) before touching anything. New `lib/ai-core/src/index.test.ts` (14 tests) proves the package is independently unit-testable with generic, non-options fixtures — provider detection, cache/single-flight/timeout behavior, streaming, mid-stream-error recovery, `extractJsonObject`. **One real bug caught and fixed during validation, not anticipated in the pre-code plan:** removing `openai`/`@anthropic-ai/sdk` as direct dependencies of `api-server` (since `coachLLM.ts` no longer imports them directly) broke three existing test files (`coach-level.test.ts`, `coach-slowload.test.ts`, `coachLLM.envMigration.test.ts`) that `vi.mock("openai", ...)`/`vi.mock("@anthropic-ai/sdk", ...)` — pnpm's strict per-package `node_modules` means Vitest's mock interception requires the SDK to be independently resolvable from `api-server`'s own dependency tree, even though only `lib/ai-core` imports it at runtime now. Fixed by re-adding both as `devDependencies` (not `dependencies` — genuinely test-only from `api-server`'s side now) of `api-server`. All four hard-gate test files (`phase7.coach.test.ts`, `coach-level.test.ts`, `coach-slowload.test.ts`, `coachLLM.envMigration.test.ts`) plus two more real importers of `coachLLM.js`'s exports (`phase8.adjustment.test.ts`, `value.test.ts`) pass with **zero assertion changes**. Verified live end-to-end: a built production server with no LLM key configured correctly serves `POST /coach/teach-greek` through the full route → `coachLLM.ts` → `@workspace/ai-core` path, returning the deterministic template with the disclaimer intact. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (20 files / 281 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), `pnpm --filter @workspace/ai-core run test` (14 tests, new), and `pnpm run build` all pass for real.
- **Sprint 10 — COMPLETE.** Reusable `platform_audit_log` system (plan §6), separate from and never touching `autoExecutionLog`. New table `platform_audit_log` (`lib/db/src/schema/auditLog.ts`, migration `lib/db/manual-migrations/005_platform_audit_log.sql`): `id`, nullable `user_id` (FK to `users.id`, **`ON DELETE SET NULL`** — deliberately different from the 13 business tables' `RESTRICT`, since an audit trail should outlive the user it audits), `engine` (`"platform" | "trading" | "investing" | "options_income"`, forward-looking for Phase 2/3), `event_type`, `action`, `result`, optional `resource_type`/`resource_id`/`reason`/`run_id`, a `metadata` jsonb column, `created_at`. New `lib/db/src/auditLog.ts` exports `recordAuditEvent()` — a best-effort writer (mirrors `autoExecutionLog`'s established philosophy) that never throws back to its caller; write failures are swallowed and reported via an injected `onError` callback (defaults to `console.error`). `lib/db/src/client.ts` is a new extraction of the `db`/`pool` construction out of `index.ts`, purely to avoid a circular import (`auditLog.ts` needs `db`; `index.ts` needs to re-export `auditLog.ts`) — `index.ts` is now just three re-export lines. Two real event types wired for this sprint: `auth.login`/`auth.login_failed`, via a `hooks.after` callback on the `betterAuth({...})` config in `lib/auth/src/index.ts` (the only correct integration point — `routes/auth.ts` only mounts Better-Auth's own request handler, so no route body of ours ever runs for `/sign-in/email`); and `settings.updated`, added to `routes/settings.ts`'s `PATCH` handler, whose `metadata.changedFields` carries only the changed field **names**, never values — critical since `settings` includes `alpacaApiKey`. No password, token, cookie, API key, or request body is ever logged, confirmed by both automated tests and a live smoke test. **One disclosed deviation from the pre-approval plan:** the plan's discussion said sign-up's automatic post-registration login "probably" should also emit `auth.login`; the hook as built filters strictly on `ctx.path === "/sign-in/email"`, so **a fresh sign-up does NOT emit an `auth.login` row** — only an explicit, subsequent sign-in does. This was a deliberate implementation choice once the exact Better-Auth hook context shape was verified empirically (see below), not an oversight, but it does narrow the plan's stated scope and is called out here per the plan's explicit deviation-disclosure requirement. Better-Auth's hook context shape (`ctx.path` has the `/api/auth` basePath already stripped; `ctx.context.newSession` is populated only on success; `ctx.context.returned` is the thrown `APIError` on failure, whose `.body.message` is Better-Auth's own already-client-visible generic `"Invalid email or password"`) was confirmed empirically against the real installed `better-auth@1.6.23` via a throwaway probe script, then deleted. New `artifacts/api-server/src/lib/auditLog.test.ts` (8 tests): direct unit coverage of `recordAuditEvent` (full field round-trip, `userId: null`, and a forced FK-violation proving the write is genuinely best-effort and never throws), plus a live end-to-end suite against a real Postgres-backed app instance proving sign-up does not create a login row, a real sign-in does, a real failed sign-in does (with `userId: null`, never guessing), a real `PATCH /settings` with a fake secret value creates a row whose serialized form never contains that secret, and that `autoExecutionLog`'s row count is provably unaffected by any of the above. One pre-existing test (`routes/settings.perUser.test.ts`, Sprint 5) needed a one-line no-op `recordAuditEvent` stub added to its hand-rolled `@workspace/db` mock, since it predates this sprint's new dependency — its own per-user-isolation assertions are unchanged. Verified live end-to-end against a built production server (not just vitest): real sign-up, real successful sign-in, real failed sign-in, and a real `PATCH /settings` carrying a fake `alpacaApiKey` value, with the resulting `platform_audit_log` rows inspected directly via `psql` and grepped for the literal secret/password/cookie/token strings (none found) — and `auto_execution_log`'s row count confirmed unchanged across the whole sequence. `pnpm run typecheck`, `pnpm --filter @workspace/api-server run test` (21 files / 289 tests), `pnpm --filter @workspace/ravish-trading run test` (6 files / 33 tests), and `pnpm run build` all pass for real.
- **Outstanding owner decisions blocking later sprints** (see Phase 1 plan §11 for full detail — do not resolve these unilaterally):
  1. ~~Authentication provider~~ — **RESOLVED (Sprint 6): Better-Auth.**
  2. ~~Automation scheduler multi-tenancy model~~ — **RESOLVED (Sprint 8): per-user iteration loop within the existing scheduler.**
  3. `stock_analysis_history` — per-user vs. shared cache
  4. ~~Legacy data ownership~~ — **RESOLVED (Sprint 4): `dunnikarp@gmail.com`.**
  5. `uuid` vs `serial` precedent for `users.id` (currently proceeding with `uuid`, per the plan's default recommendation)
  6. CORS allowed-origin list
  7. Deprecation window for the legacy `OPENAI_API_KEY` overload

---

## 4. Running tests, type checking, and builds — do this honestly

- Run the actual commands: `pnpm run typecheck`, `pnpm run build`, `pnpm --filter @workspace/api-server run test`, `pnpm --filter @workspace/ravish-trading run test`, `pnpm --filter @workspace/ai-core run test` (added Sprint 9).
- **Report the real output.** If a command fails, say so and show the failure — do not summarize a failure as a pass, and do not skip a command and imply it was run.
- If a command cannot be run (missing dependency, no database available, etc.), say exactly that, plainly, rather than substituting a partial check and presenting it as equivalent. A partial verification (e.g., a standalone syntax check without full type resolution) is only acceptable when clearly labeled as partial.
- The CI workflow added in Sprint 1 (`.github/workflows/ci.yml`) is the real gate for this project going forward — treat a local claim of "it passes" as provisional until CI confirms it.
- Several existing tests (`adjustmentTicket.test.ts`, `autoAdjustment.cycle.test.ts`, `tradeClose.test.ts`, `phase7.coach.test.ts`, and others) require a live Postgres connection via `DATABASE_URL` — a disposable local or CI database is required for the full suite to run at all, not just for new Phase 1 tests.

---

## 5. Quick orientation for a fresh Claude Code session

- Monorepo: pnpm workspaces, TypeScript, Node 24 target. Backend: `artifacts/api-server` (Express 5). Frontend: `artifacts/ravish-trading` (React 19 + Vite). Shared: `lib/db` (Drizzle/Postgres), `lib/api-spec` + `lib/api-zod` + `lib/api-client-react` (OpenAPI contract + codegen — don't hand-edit generated files).
- `replit.md` at the repo root is the original, still-accurate product/architecture doc for the Options Income engine as it exists today — read it alongside the three docs in `/docs/`, not instead of them.
- `.agents/memory/*.md` contains prior engineering "gotcha" notes on the trickiest subsystems (auto-execution safety, coach disclaimer invariants, signed-gap threat detection). Read the relevant one before touching adjacent code.
